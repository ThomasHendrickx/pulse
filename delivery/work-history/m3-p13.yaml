kind: work-history
phase: M3-P13
prompt: >-
  IMPLEMENTER for Pulse phase M3-P13 (identity on the review screen: the screen
  says what it is grouping on and how far a naming will reach). Worktree
  /home/user/wt-m3p13, branch claude/m3-p13-identity-on-review cut from
  origin/main 975d0ce. Read CLAUDE.md and all three skills (pulse-domain,
  pulse-frontend, pulse-typescript). Compose the implementer brief with tiphys
  and follow its contract. The phase section is M3-P13 in
  delivery/plan/pulse-v02.yaml; its criteria are the spec and its steps are the
  order, verification-first before any edit. Checkpoint protocol: commit and
  push after each meaningful unit of work. The slow gate RUNS here: docker plus
  the repository's own Supabase CLI plus prisma migrate deploy; never prisma
  migrate reset; pin DATABASE_URL, DIRECT_URL and E2E_DATABASE_URL to loopback
  and assert the parsed host is 127.0.0.1 before running. Gates with exit codes:
  typecheck, lint, npm test, gate:privacy, gate:decisions, gate:tokens, npm run
  build, and npm run test:e2e for real. Write delivery/work-history/m3-p13.yaml
  per the schema and validate it. No pull request, no merge, no plan edits.
files-touched: []
per-step-commits: []
key-decisions: []
verification-first: []
deviations: []
gate-evidence: []
claims: []
environment-warnings: []
