kind: work-history
phase: M3-P14
prompt: |
  Implementer for phases M3-P14 (account registry) and M3-P15 (ring
  correction), dispatched together on one branch,
  claude/m3-p14-account-registry, in worktree /home/user/wt-m3p14, cut from
  main at 999d378.

  Both phases answer one owner question: "I have 4 bank accounts, 4 saving
  accounts and 2 credit cards. When I upload 1 bank account first, that one
  contains transfers to other accounts. Now Pulse asks me to name them as
  merchants, but they are not merchants. What do I need to do to get to that
  final state?"

  Two owner decisions bind and are not open: DR-0030 (Pulse ACCEPTS a reserve
  account's own statement; its rows are HELD, in no month total and not an
  uninterpreted gap) and DR-0028 (identity is deterministic: mod-97 plus a
  pinned country-length table; a model names, it never decides identity).

started: 2026-08-24

verification-first:
  - id: baseline-FOUR-account-shape
    what: >-
      M3-P14 step 1 baseline FOUR, RE-MEASURED rather than copied, by the
      method the step records: the file set is git ls-files with the privacy
      gate's own skip list, the pattern is that gate's IBAN alternation in
      BOTH arms, each hit is normalised by that gate's own norm(), the
      distinct values are then run through ISO 7064 mod-97.
    command: bash scratch/measure-accounts.sh | node scratch/mod97.mjs -
    output: |
      FAIL BE00000000000000
      FAIL BE02233445566778
      FAIL BE02979245566602
      FAIL BE10987654321001
      FAIL BE20539007547099
      PASS BE39103123456719
      PASS BE43068999999501
      FAIL BE45678901234515
      FAIL BE54540123456789
      PASS BE68539007547034
      FAIL BE68539075417034
      PASS BE71096123456769
      FAIL BE77123456789012
      FAIL BE90012345678944
      PASS DE89370400440532013000
      PASS FR1420041010050500013M02606
      PASS NL91ABNA0417164300
      distinct=17 pass=7 fail=10
    conclusion: >-
      17 distinct account-shaped values, 7 passing, 10 failing. My own run
      agrees with the figure the plan records. Recorded as a reproduction of
      the measurement, not as a citation of it.
    note: >-
      A TRAP FOUND WHILE DOING THIS, worth writing down because it is exactly
      the shape of the two earlier wrong figures the plan warns about. My
      FIRST generator for the new fixture numbers used a mod-97 loop with NO
      four-character rotation, and it reported eleven of twelve generated
      numbers invalid. The measurement script above does carry the rotation,
      so the 17/7/10 figure was never affected; but a mod-97 without the
      rotation is a check that looks like a check and answers a different
      question, and it took a direct comparison against the shipped module to
      see it. Every number this phase invents was regenerated against
      src/platform/account-number.ts itself and asserted valid by a fast-gate
      test (test/domain/account-number.test.ts).

  - id: claim-single-caller-of-declareAccount
    command: grep -rn "declareAccount" src/
    output: |
      src/modules/import/ui/profile-confirmation.tsx:169:            <legend>{t("declareAccountTitle")}</legend>
      src/modules/import/ui/profile-confirmation.tsx:170:            <p className="import-note">{t("declareAccountBody")}</p>
      src/modules/import/application/index.ts:8:  declareAccount,
      src/modules/import/application/index.ts:59:  accounts: { findAccountByIban, getAccountById, declareAccount },
      src/modules/import/application/confirm-import.ts:96:    const created = await deps.accounts.declareAccount(context, {
      src/modules/import/application/ports.ts:247:  readonly declareAccount: (
      src/modules/accounts/application/index.ts:27:export const declareAccount = (
    conclusion: >-
      Confirmed. One CALL SITE in application code, confirm-import.ts:96. The
      other hits are the definition, the port declaration, the composition
      root's binding and two i18n message keys whose names merely contain the
      word.

  - id: claim-no-accounts-route
    command: ls -d "src/app/(app)/accounts"; find src/app -ipath '*account*' -print
    output: |
      ls: cannot access 'src/app/(app)/accounts': No such file or directory
      (find printed nothing)
    conclusion: Confirmed. No accounts route exists at the phase base.

  - id: claim-null-flow-reads
    command: grep -rn --include=*.ts -i 'flow" IS NULL' src/
    output: |
      src/modules/overview/adapters/overview-repository.ts:230:      COUNT(*) FILTER (WHERE t."flow" IS NULL)::bigint                                  AS "uninterpretedCount",
      src/modules/overview/adapters/overview-repository.ts:280:        WHEN t."flow" IS NULL THEN 'uninterpreted'
      src/modules/overview/adapters/overview-repository.ts:295:        t."flow" IS NULL
    conclusion: >-
      Confirmed, and confirmed at the shape criterion 14.14 states: THREE
      occurrences in TWO reads, the uninterpreted count and the gap listing,
      the listing carrying two of its own.
  - id: claim-no-prisma-client-null-flow
    command: grep -rn --include=*.ts 'flow: null' src/
    output: "(no output)"
    conclusion: >-
      At the phase base the Prisma-client style of the absent-flow condition
      appears nowhere under src/. Criterion 14.14 case FIVE's enumeration must
      still cover it, because this phase and M3-P15 add reads.

open-questions:
  - id: OQ-1-gate-privacy-is-flaky
    severity: blocks-a-clean-gate-reading-not-this-phase
    statement: >-
      `npm run gate:privacy` is INTERMITTENTLY RED, and it names a different
      already-listed identifier in a different untouched file each time. It
      is deterministic-green when run alone and unhealthy under concurrent
      load. I did NOT find the mechanism and I did not change the script,
      which is not in this phase's files-to-touch.
    evidence: |
      Sweep of six consecutive runs on an otherwise idle container:
        run 1 exit=0 0 hits
        run 2 exit=0 0 hits
        run 3 exit=1 1 hits      <-- named test/fixtures/mv-transit-a.csv
        run 4 exit=0 0 hits
        run 5 exit=0 0 hits
        run 6 exit=0 0 hits
      With a vitest run in parallel, two of two runs failed.
      Instrumented copy (scratch only, the tracked script untouched), one
      failure in fifteen:
        DBG f=test/domain/kbc-fix-round-3.test.ts hit=[541788XXXXXX7654] knownlen=799 allowexists=yes
      541788XXXXXX7654 IS on the allow list, and knownlen=799 is the same
      length the healthy runs produce, so at the moment of the false report
      BOTH sides of the comparison were correct.
      The comparison itself was then stressed in isolation, 3000 iterations
      of the exact `printf '%s\n' "$known" | grep -qxF "541788XXXXXX7654"`:
        misses=0 non-one-exits=0
      So the isolated comparison is stable and the fault is upstream of it,
      somewhere in the per-file hit pipeline. I did not isolate it further.
    not-caused-by-this-phase: |
      git log --oneline -1 -- test/domain/kbc-fix-round-3.test.ts scripts/gate-privacy.sh
        ac37a63 gate:privacy: fold case on the shape that reaches a letters-bearing body
      git diff --name-only origin/main...HEAD | grep -E 'gate-privacy|kbc-fix-round-3|mv-transit-a'
        (no output: none of the three is touched by this branch)
      Two different runs named two different files, both untouched, both
      carrying only allow-listed values. A defect in this phase's own
      identifiers could not select an untouched file at random.
    what-a-reviewer-should-do: >-
      Re-run it. A lone red gate:privacy naming an identifier that IS on the
      allow list is this flake until someone shows otherwise, and treating it
      as a real finding will send a round chasing a value that is listed. The
      honest reading of my green gate line below is therefore "green on the
      run recorded, on a quiet container", not "green deterministically".
      This is a real hole in the project's most safety-critical gate and it
      deserves a phase of its own; I am recording it rather than fixing it
      because the script is not in this phase's files-to-touch.

  - id: live-captures-against-a-real-database
    what: >-
      The remaining verification-first observations and baselines, run against
      a REAL Postgres and not by reading. The database is the LOCAL supabase
      stack, started in this container, with all five connection values
      pinned explicitly in the invoking shell. The ambient DATABASE_URL was
      never used: it resolves to a different Supabase project with a working
      password (fleet warning 16), and nothing that writes was allowed near
      it.
    method: >-
      The PHASE BASE's overview repository was materialised verbatim with
      `git show 999d378:src/modules/overview/adapters/overview-repository.ts`
      into an untracked file beside the current one, so that BOTH versions
      run against the SAME seeded rows in the same process and the comparison
      is a measurement rather than a recollection. That file was deleted
      after the capture and is not part of the diff. Four households were
      seeded directly through Prisma, because these are READ criteria and the
      import pipeline is not what is under test here.
    command: npx tsx scratch-capture/capture.mts   # with the local stack pinned
    output: |
      [pulse:db] DATABASE_URL present, scheme ok
      
      ================ BASELINE ONE: every account is a POT account ================
      --- PHASE BASE 999d378 ---
      MonthFigures: {"incomeSignedCents":320000,"spendSignedCents":-16550,"reserveSignedCents":0,"changeInPotCents":303450,"unresolvedCents":0,"unresolvedCount":0,"unmatchedInternalCents":0,"unmatchedInternalCount":2,"inTransitCents":0,"inTransitCount":0,"uninterpretedCount":0,"rowCount":5}
      listGapRows: [{"gap":"unmatched-internal","text":"OVERSCHRIJVING EIGEN REKENING","accountLabel":"Current account","amountCents":-10000},{"gap":"unmatched-internal","text":"OVERSCHRIJVING EIGEN REKENING","accountLabel":"Joint account","amountCents":10000}]
      listReserveMovements: []
      --- THIS PHASE ---
      MonthFigures: {"incomeSignedCents":320000,"spendSignedCents":-16550,"reserveSignedCents":0,"changeInPotCents":303450,"unresolvedCents":0,"unresolvedCount":0,"unmatchedInternalCents":0,"unmatchedInternalCount":2,"inTransitCents":0,"inTransitCount":0,"uninterpretedCount":0,"rowCount":5}
      listGapRows: [{"gap":"unmatched-internal","text":"OVERSCHRIJVING EIGEN REKENING","accountLabel":"Current account","amountCents":-10000},{"gap":"unmatched-internal","text":"OVERSCHRIJVING EIGEN REKENING","accountLabel":"Joint account","amountCents":10000}]
      listReserveMovements: []
      --- THIS PHASE, the two new reads ---
      listCountedAccountRows: [{"accountId":"00000000-0000-4000-8000-000000002020","label":"Current account","rowCount":4},{"accountId":"00000000-0000-4000-8000-000000002021","label":"Joint account","rowCount":1}]
      listHeldAccountRows: []
      
      ================ BASELINE THREE / ROUTE ONE: a statement imported for a RESERVE account ================
      --- PHASE BASE 999d378 ---
      MonthFigures: {"incomeSignedCents":320000,"spendSignedCents":0,"reserveSignedCents":-25000,"changeInPotCents":295000,"unresolvedCents":0,"unresolvedCount":0,"unmatchedInternalCents":0,"unmatchedInternalCount":0,"inTransitCents":0,"inTransitCount":0,"uninterpretedCount":2,"rowCount":2}
      listGapRows: [{"gap":"uninterpreted","text":"SPAREN AUGUSTUS","accountLabel":"Buffer","amountCents":25000},{"gap":"uninterpreted","text":"INTEREST","accountLabel":"Buffer","amountCents":137}]
      listReserveMovements: [{"counterpartyIban":"BE24902200001138","label":"Buffer","parkedCents":25000,"rowCount":1}]
      --- THIS PHASE ---
      MonthFigures: {"incomeSignedCents":320000,"spendSignedCents":0,"reserveSignedCents":-25000,"changeInPotCents":295000,"unresolvedCents":0,"unresolvedCount":0,"unmatchedInternalCents":0,"unmatchedInternalCount":0,"inTransitCents":0,"inTransitCount":0,"uninterpretedCount":0,"rowCount":2}
      listGapRows: []
      listReserveMovements: [{"counterpartyIban":"BE24902200001138","label":"Buffer","parkedCents":25000,"rowCount":1}]
      --- THIS PHASE, the two new reads ---
      listCountedAccountRows: [{"accountId":"00000000-0000-4000-8000-000000002040","label":"Current account","rowCount":2}]
      listHeldAccountRows: [{"accountId":"00000000-0000-4000-8000-000000002041","label":"Buffer","rowCount":2}]
      
      ================ ROUTE TWO: rows INTERPRETED on a pot account whose account then LEFT the pot (stale flow, no clearing) ================
      --- PHASE BASE 999d378 ---
      MonthFigures: {"incomeSignedCents":320000,"spendSignedCents":-16550,"reserveSignedCents":0,"changeInPotCents":303450,"unresolvedCents":0,"unresolvedCount":0,"unmatchedInternalCents":0,"unmatchedInternalCount":0,"inTransitCents":0,"inTransitCount":0,"uninterpretedCount":0,"rowCount":3}
      listGapRows: []
      listReserveMovements: []
      --- THIS PHASE ---
      MonthFigures: {"incomeSignedCents":320000,"spendSignedCents":0,"reserveSignedCents":0,"changeInPotCents":320000,"unresolvedCents":0,"unresolvedCount":0,"unmatchedInternalCents":0,"unmatchedInternalCount":0,"inTransitCents":0,"inTransitCount":0,"uninterpretedCount":0,"rowCount":1}
      listGapRows: []
      listReserveMovements: []
      --- THIS PHASE, the two new reads ---
      listCountedAccountRows: [{"accountId":"00000000-0000-4000-8000-000000002060","label":"Current account","rowCount":1}]
      listHeldAccountRows: []
      
      ================ ROUTE TWO, AFTER THE CLEARING THIS ROUND ADDS ================
      --- PHASE BASE 999d378 ---
      MonthFigures: {"incomeSignedCents":320000,"spendSignedCents":0,"reserveSignedCents":0,"changeInPotCents":320000,"unresolvedCents":0,"unresolvedCount":0,"unmatchedInternalCents":0,"unmatchedInternalCount":0,"inTransitCents":0,"inTransitCount":0,"uninterpretedCount":2,"rowCount":1}
      listGapRows: [{"gap":"uninterpreted","text":"Bakkerij Ochtend","accountLabel":"Was a spending account","amountCents":-4500},{"gap":"uninterpreted","text":"Warenhuis Zuid","accountLabel":"Was a spending account","amountCents":-12050}]
      listReserveMovements: []
      --- THIS PHASE ---
      MonthFigures: {"incomeSignedCents":320000,"spendSignedCents":0,"reserveSignedCents":0,"changeInPotCents":320000,"unresolvedCents":0,"unresolvedCount":0,"unmatchedInternalCents":0,"unmatchedInternalCount":0,"inTransitCents":0,"inTransitCount":0,"uninterpretedCount":0,"rowCount":1}
      listGapRows: []
      listReserveMovements: []
      --- THIS PHASE, the two new reads ---
      listCountedAccountRows: [{"accountId":"00000000-0000-4000-8000-000000002060","label":"Current account","rowCount":1}]
      listHeldAccountRows: [{"accountId":"00000000-0000-4000-8000-000000002061","label":"Was a spending account","rowCount":2}]
    conclusions:
      - >-
        BASELINE ONE (criterion 14.14 case TWO, criterion 15.4 second
        witness): over a household whose accounts are ALL pot accounts, the
        phase base and this phase return BYTE IDENTICAL MonthFigures and
        BYTE IDENTICAL listGapRows. Scoping the reads moves nothing that
        exists today, measured rather than asserted.
      - >-
        BASELINE THREE and M3-P15 ROUTE ONE, the shipped route (a statement
        imported for an account declared RESERVE). AT THE PHASE BASE its rows
        carry NO flow, uninterpretedCount is 2, listGapRows returns both of
        them labelled "uninterpreted", and a non-zero uninterpreted count
        holds the verdict open, so the household's books never close again.
        UNDER THIS PHASE uninterpretedCount is 0, listGapRows is empty, the
        held read names the account with its row count, and every counted
        figure is unchanged: income 320000 both, reserveSigned -25000 both,
        changeInPot 295000 both, rowCount 2 both. That is criterion 14.16
        arm ONE, measured.
      - >-
        M3-P15 ROUTE TWO, which no shipped path reaches at the phase base:
        rows INTERPRETED on a pot account whose account then LEFT the pot,
        reached by a direct declaration write against the scratch database
        and never through shipped code. AT THE PHASE BASE the stale rows GO
        ON COUNTING: spendSigned -16550 and rowCount 3, with no gap listed
        and the books closing. That is hazard H15.2 observed live rather
        than argued. Under this phase's scoped reads they are already
        invisible to the totals (spendSigned 0, rowCount 1) even before the
        clearing, which is what "defence in depth" means here and is why the
        phase does not claim the verdict depends on the scoping.
      - >-
        AND THE CASE THE COUNTED READ'S RING RESTRICTION EXISTS FOR is
        visible in that same block: in ROUTE TWO before the clearing, the
        held read is EMPTY (those rows still carry a flow) and the counted
        read reports only the current account. A counted read written
        without its ring restriction would have reported the stale-flow rows
        to the household as counted money on the one screen state built to
        tell counted from held. That is hazard H14.21, and the restriction
        is not made redundant by the flow condition.
      - >-
        ROUTE TWO AFTER THE CLEARING: the phase base would report
        uninterpretedCount 2 and list both rows as gaps, which is hazard
        H15.3, the books never closing again. Under this phase the count is
        0, the listing is empty, and the held read names the account with 2
        rows. The two halves criterion 15.3 asks to be separated are
        separated, and both were observed.

  - id: baseline-TWO-navigation-row
    what: >-
      M3-P14 step 1 baseline TWO: for every navigation link the shell renders
      TODAY, the rendered line count of its label and its border box in both
      dimensions, at 390 and at 360 and at the 150 and 200 percent text
      scales the existing spec already applies. Captured with the FOURTH LINK
      REMOVED, so it really is the phase base's geometry, then restored.
    method: >-
      A throwaway Playwright spec against the local dev server, using the
      SAME applyTextScale helper test/e2e/month-view.spec.ts uses.
    a-wrong-measurement-made-first-and-corrected: >-
      My first version of the helper scaled text with
      `html { font-size: Npx }`. It reported IDENTICAL numbers at 1x, 1.5x
      and 2x, which is what a helper doing nothing looks like: every type
      token in this design system is declared in px, so a root font-size
      never reaches them. The real helper multiplies each element's own
      computed size. Recorded because the useless baseline was internally
      consistent and would have been believed.
    output: |
      THREE LINKS (the phase base):
        390@1    overview {lines:1,w:100,h:44}  import {1,100,44}  merchants {1,100,44}   doc 390/390
        390@1.5  overview {lines:2,w:92,h:69}   import {1,92,69}   merchants {2,92,69}    doc 390/390
        390@2    overview {lines:2,w:84,h:88}   import {2,84,88}   merchants {2,84,88}    doc 390/390
        360@1    overview {lines:1,w:90,h:44}   import {1,90,44}   merchants {1,90,44}    doc 360/360
        360@1.5  overview {lines:2,w:82,h:69}   import {1,82,69}   merchants {2,82,69}    doc 360/360
        360@2    overview {lines:3,w:74,h:127}  import {2,74,127}  merchants {3,74,127}   doc 360/360

      FOUR LINKS, ROW UNCHANGED (the state criterion 14.7 exists to catch):
        390@1    overview {lines:2,w:74,h:49}   import {1,74,49}   merchants {2,74,49}    accounts {1,74,49}
        390@1.5  overview {lines:2,w:68,h:69}   import {2,68,69}   merchants {2,68,69}    accounts {2,68,69}
        390@2    overview {lines:3,w:62,h:127}  import {2,62,127}  merchants {3,62,127}   accounts {3,62,127}
        360@1    overview {lines:2,w:67,h:49}   import {1,67,49}   merchants {2,67,49}    accounts {2,67,49}
        360@1.5  overview {lines:2,w:60,h:98}   import {2,60,98}   merchants {3,60,98}    accounts {2,60,98}
        360@2    overview {lines:4,w:54,h:166}  import {3,54,166}  merchants {4,54,166}   accounts {3,54,166}

      FOUR LINKS, ROW WRAPPED TWO PER LINE (what ships):
        390@1    overview {lines:1,w:152,h:44}  import {1,152,44}  merchants {1,152,44}   accounts {1,152,44}
        390@1.5  overview {lines:1,w:140,h:44}  import {1,140,44}  merchants {1,140,44}   accounts {1,140,44}
        390@2    overview {lines:2,w:127,h:88}  import {1,127,88}  merchants {2,127,88}   accounts {1,127,88}
        360@1    overview {lines:1,w:137,h:44}  import {1,137,44}  merchants {1,137,44}   accounts {1,137,44}
        360@1.5  overview {lines:1,w:125,h:44}  import {1,125,44}  merchants {1,125,44}   accounts {1,125,44}
        360@2    overview {lines:2,w:112,h:88}  import {1,112,88}  merchants {2,112,88}   accounts {2,112,88}
      Document scrollWidth never exceeded clientWidth in any of the eighteen
      measurements above.
    conclusion: >-
      HAZARD H14.8 WAS REAL AND THIS PHASE HIT IT. With the row left as it
      was, the fourth link pushed Overview from one line to two at 390 and
      from two to FOUR at 360 at a 200 percent text size, and Merchants with
      it. Wrapping two per line puts every existing target at or below its
      three-link baseline at every width and every scale, and the added link
      never exceeds three lines, which is the largest count the baseline
      records for any existing link. The witness is in
      test/e2e/month-view.spec.ts with those numbers written in by hand, and
      it was shown RED against the un-wrapped row before being trusted.

files-touched:
  - src/platform/account-number.ts (new: the one canonical form, the pinned
    country-length table, the ISO 7064 check, four named refusals)
  - src/modules/accounts/domain/account-role.ts (unchanged)
  - src/modules/accounts/application/ports.ts
  - src/modules/accounts/application/register-account.ts (new)
  - src/modules/accounts/application/correct-account-ring.ts (new)
  - src/modules/accounts/application/index.ts
  - src/modules/accounts/adapters/account-repository.ts
  - src/modules/accounts/ui/ (new: the screen, its actions, its status keys)
  - src/app/(app)/accounts/page.tsx (new route)
  - src/app/(app)/layout.tsx (the fourth nav link)
  - src/app/globals.css (the wrapped nav row, the new element's styles)
  - src/modules/overview/adapters/overview-repository.ts
  - src/modules/overview/application/ports.ts
  - src/modules/overview/application/month-overview.ts
  - src/modules/overview/application/index.ts
  - src/modules/overview/ui/month-view.tsx
  - src/modules/ledger/application/preview-declaration-change.ts (new)
  - src/modules/ledger/application/interpret-window.ts
  - src/modules/ledger/application/ports.ts
  - src/modules/ledger/application/index.ts
  - src/modules/ledger/adapters/ledger-repository.ts
  - src/modules/ledger/domain/classify-flow.ts
  - src/modules/ledger/domain/corrections.ts
  - src/modules/ledger/domain/ledger-transaction.ts
  - src/modules/ledger/domain/pair-transfers.ts
  - src/modules/merchants/adapters/merchant-repository.ts
  - src/modules/import/application/confirm-import.ts
  - src/modules/import/application/index.ts
  - src/modules/import/domain/belfius-current-account-template.ts
  - src/modules/import/ui/actions.ts
  - src/modules/import/ui/profile-confirmation.tsx
  - src/modules/import/ui/status-keys.ts
  - .claude/skills/pulse-domain/SKILL.md
  - messages/en.json, messages/nl.json, messages/fr.json
  - scripts/gate-diff-tokens.sh (new)
  - test/ (fixtures, domain, application and e2e; see per-commit history)
  - test/fixtures/allowed-identifiers.txt

scope-note-outside-files-to-touch: |
  THREE PATHS THIS PHASE TOUCHED THAT ITS files-to-touch DOES NOT NAME, said
  the moment they were found rather than left to a red gate.

  1. src/modules/ledger/domain/corrections.ts and
     src/modules/ledger/domain/pair-transfers.ts. Criterion 14.4 requires
     that no file outside the platform module contains a second copy of the
     uppercase-plus-whitespace-removal applied to an account-bearing field,
     and names "one written inline inside another module's identity
     derivation" as exactly the shape to catch. corrections.ts carried one,
     in counterpartyKey. pair-transfers.ts compares a stored counterparty
     column against the declared-set map and had to canonicalise its side
     too or the canonicalisation would have been half applied. Both are
     inside src/modules/ledger/domain/, which the declaration does not list
     separately although it lists the ledger's other layers.
  2. src/modules/merchants/adapters/merchant-repository.ts IS listed by
     M3-P15's declaration and not by M3-P14's; the two phases ship together
     on one branch, so it is in scope for the pair.
  3. scripts/gate-diff-tokens.sh is new. Criterion 14.10 requires the phase
     to assert by its own diff-scoped grep that no line it adds carries a
     colour literal or a length, and requires that grep's output in this
     work history. Writing it as a script rather than as a shell one-liner
     is what let it be shown RED against four offenders before being
     trusted; a one-liner would have been recorded and never executed
     again.

mechanism-lookups: |
  tuition/mechanism-index.yaml does not exist in this repository and is not
  reachable from this worktree: the tiphys kernel documents live in the
  fleet, and the composed brief's mandated-reading list resolves against
  that tree rather than against the project checkout. The index had no
  entry I could open, and that is recorded rather than silently skipped, as
  the clause requires.

  THE MECHANISMS THIS PHASE DID ESTABLISH RULES ABOUT are recorded at their
  definitions in the source, with their sibling implementations NAMED there
  so the next reader learns the rule is not local:

  - THE CANONICAL ACCOUNT-NUMBER FORM. Rule recorded at
    src/platform/account-number.ts: a DECLARATION is normalised on the way
    in; a FACT is never rewritten, so every comparison canonicalises BOTH
    sides at comparison time. The five sibling implementations that share
    the mechanism are listed by path at that definition.
  - THE NULL-FLOW WRITE. Rule recorded at
    src/modules/ledger/application/ports.ts beside the flow write: null
    CLEARS an interpretation, exactly as a null merchantId clears an
    assignment, and held is derived from the account's ring rather than
    from a new enum member.
  - THE RING PREDICATE ON EVERY MONTH READ. Rule recorded once at the top
    of src/modules/overview/adapters/overview-repository.ts, with the
    reason the two null-flow reads must carry the SAME predicate and why
    the held read carries its inverse.
  - A REPOSITORY READ ASKED FOR AN IDENTIFIER THAT WAS NEVER ISSUED. Rule
    recorded at the fake world's own boundary in
    test/application/fake-import-world.ts, because that is where the
    difference between the fake and the database lives, together with the
    measurement that produced it.
