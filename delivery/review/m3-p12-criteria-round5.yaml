# M3-P12 clean-room review, CRITERIA contract, round five (judging fix round four)
# Reviewer lane: criteria. Worktree /home/user/wt-m3p12-r5crit, branch claude/m3-p12-rev5-crit, head 606e3ee.
# WRITTEN INCREMENTALLY. Entries appended as they are witnessed.
# Sibling hazard lane document NOT read: see decorrelation note at the end.

phase: M3-P12
round: 5
contract: criteria
head: 606e3ee30ccfa44c1bbd110fbbc03b243d1025fd
status: in-progress

log:
  - at: start
    note: >-
      Worktree confirmed at 606e3ee on claude/m3-p12-rev5-crit. Machine load
      3.78 3.89 4.72 on four cores at start; disk 3.5G free of 252G (91% used),
      which fleet warning 10 marks as the shape that produces moving chromium
      page crashes in e2e.
  - at: setup
    note: >-
      Ambient DATABASE_URL in this container resolves to a deployed pooler on
      aws-1-eu-north-1 (a foreign project, fleet warnings 1 and 16). It was
      never used. This lane created its own database on the shared local
      supabase Postgres at 127.0.0.1:54322 and pinned every invocation to it;
      the name is recorded in the closing section. The worktree carries no
      .env and no node_modules at check-out, so npm install was needed before
      any gate could run.

gates-run-by-this-lane:
  note: >-
    Every invocation pinned to this lane's own local database. Node in this
    container defaults to v26.7.0 while package.json engines declares 22.x;
    that mismatch is pre-existing and not this round's, and every gate below
    was green on v26.7.0.
  - command: npm run typecheck
    exit: 0
    summary: "tsc --noEmit, no output"
  - command: npm run lint
    exit: 0
    summary: "eslint ., no output"
  - command: npm test
    exit: 0
    summary: "Test Files 41 passed (41); Tests 624 passed (624); Duration 21.52s"
  - command: npm run gate:privacy
    exit: 0
    summary: "gate:privacy clean"
  - command: npm run gate:tokens
    exit: 0
    summary: "grep negation over src/modules/*/ui, no match"

directed-claims:
  - id: 1
    claim: >-
      playwright.config.ts resolves its target at module scope through
      src/platform/db/gate-target.ts, refuses anything not a local stack, and
      ASSIGNS the approved values into process.env.
    verdict: pending-e2e-witness
    read-so-far: >-
      Structure confirmed by reading. The import is playwright.config.ts:2 and
      the call is playwright.config.ts:28 to :29, above every other read of
      process.env in that file except PLAYWRIGHT_BASE_URL at :7. prodEnv is
      built at playwright.config.ts:43 to :47 from process.env AFTER the
      assignment, so the production web server inherits the pin even before
      the explicit spread. There is no globalSetup and no globalTeardown in
      either playwright.config.ts or playwright.deploy.config.ts, so nothing
      runs ahead of the config module. The assignment itself is
      src/platform/db/gate-target.ts:209 to :211.
  - id: 3
    claim: >-
      applyRuleWrites checks every distinct inserted merchantId against a
      household-filtered findMany inside the same transaction before any
      create.
    verdict: met-in-code
    witness: >-
      The check is src/modules/merchants/adapters/merchant-repository.ts:180
      to :196 and the first create is at :198, both inside the
      prisma.$transaction callback opened at :148. The ids are deduped through
      a Set at :181 so the length comparison at :191 is sound. The
      insert-submitted-ALONE case is
      test/e2e/merchant-rule-write.spec.ts:192 to :207, with a positive
      control at :211 to :227 proving the refusal is a refusal. Execution
      witness owed by the e2e run below.
  - id: 4
    claim: >-
      EXACT collisions supersede; a same-merchant PREFIX or PATTERN collision
      skips; a different-merchant one blocks as an ordinary conflict, and
      every superseded id is EXACT.
    verdict: yes
    witness: >-
      The split is src/modules/merchants/application/rederive-rules.ts:505 to
      :540. Both writes into supersededRuleIds are inside a rule.kind ===
      "EXACT" guard, at :509 to :510 for the same-merchant branch and at :521
      to :523 for the different-merchant branch, and grep finds no third
      write. The split sits exactly where the argument splits: the argument is
      that a bare pattern can never be applied again, which holds for equality
      and fails for a glob or a namespace-prefix, and the comment at
      src/modules/merchants/application/rederive-rules.ts:481 to :491 states
      the false half it used to assert and why.
  - id: 5
    claim: >-
      An unknown port refused, --expect-port comparing exactly, the two ports
      this product uses admitted when neither is named; schema pinned to
      absent or public and EVERY occurrence checked.
    verdict: yes
    witness: >-
      Executed against the shipped predicate, importing
      src/platform/db/target-guard.ts directly through tsx. Captured, one line
      per case: ALLOW for port 5432 with no --expect-port, ALLOW for 6543 with
      no --expect-port, REFUSE for 9999 with no --expect-port, ALLOW for 9999
      with --expect-port 9999, REFUSE for a 5432 string against --expect-port
      6543. Schema, same run: ALLOW absent, ALLOW schema=public, REFUSE
      schema=other, REFUSE schema=public&schema=other, REFUSE
      schema=other&schema=public, REFUSE schema=PUBLIC, REFUSE an empty
      schema value, ALLOW schema=%70ublic which percent-decodes to the
      approved value exactly as the connector would decode it. Both orderings
      of the duplicated parameter refuse, which is the assertion that
      distinguishes getAll from get; the implementation is
      src/platform/db/target-guard.ts:259 to :270.
  - id: 2
    claim: >-
      The wholesale before-set exclusion is gone; a superseded rule's claim is
      dismissed at the comparison only when the row is still covered after the
      run, and otherwise it is a reported, blocking, acknowledgeable loss,
      with both directions in one run.
    verdict: yes
    witness: >-
      The before-set is whole at
      src/modules/merchants/application/rederive-rules.ts:707 and the
      dismissal is at :738, guarded on the row being covered afterwards. Both
      directions in ONE run are
      test/application/rederive-merchant-rules.test.ts:1686 to :1705, whose
      seed carries one descriptor-basis row the claimant covers and two
      account-basis rows on different trusted accounts that nothing covers:
      it asserts lostAssignments is exactly the two account rows and the exit
      code is 1, so the covered row is silent and the uncovered ones block in
      the same report. The whole file passed in this lane's own npm test run
      (41 files, 624 tests, exit 0).

criteria-walk:
  note: >-
    Every criterion of M3-P12 walked in order. The count is not inherited from
    round four. Evidence is a path with a line number, a captured command, or
    a count this lane produced. Where a criterion could only be evaluated from
    the work history because its subject is a document this repository must
    not contain, that is said rather than counted silently as met.

  - id: "12.1"
    criterion: CONVERGENCE, COUNTED.
    verdict: met
    evidence: >-
      test/domain/counterparty-identity.test.ts:72 asserts identity keys 12
      and baseline keys 24 over the fixture parsed through the shipped path,
      and prints both beside the row count. Run by this lane: the captured
      stdout line is "fixture rows 24, identity keys 12, baseline keys 24".
      The composition assertion is at test/domain/counterparty-identity.test.ts:89
      and printed "account-basis rows 16 over 4 accounts, descriptor-basis
      rows 8". The committed harness reproduces the same figures
      independently: this lane ran the harness over the fixture and captured
      rows 24, rows-with-account 18, baseline-distinct-keys 24,
      identity-distinct-keys 12, identity-account-keys 4,
      identity-descriptor-keys 8, identity-rows-account-basis 16,
      identity-rows-descriptor-basis 8.
    plan-defect: >-
      THE COMPOSITION CLAUSE'S ARITHMETIC IS FALSE, and it is the plan's
      defect and not the implementation's. The criterion asserts that "the 24
      rows and 12 counterparties this criterion also pins force the
      descriptor-basis count to exactly 8". They do not. With 24 rows, 12
      counterparties, at least 8 account-basis rows over at least 4 accounts
      and at least 8 descriptor-basis rows, a fixture of 4 account
      counterparties at 2 rows each and 8 descriptor counterparties at 2 rows
      each satisfies every pinned number and has 16 descriptor-basis rows.
      The shipped fixture is 16 account-basis rows and 8 descriptor-basis
      rows, which also satisfies every pinned number. So the sentence claiming
      the threshold and the arithmetic are one requirement replaced an earlier
      wrong clause with a wrong derivation. The implementation is not affected
      either way; the criterion should drop the derivation or pin the split
      directly. Attributed to the plan.

  - id: "12.2"
    criterion: CONVERGENCE ON THE OWNER'S OWN MONTH.
    verdict: met-on-recorded-evidence-not-independently-reproducible
    evidence: >-
      The work history records, at delivery/work-history/m3-p12.yaml:608
      onward, the command naming the COMMITTED harness path
      (test/fixtures/measure-identity-convergence.ts) and every figure the
      criterion pins: rows 39, baseline-distinct-keys 25,
      identity-distinct-keys 20, identity-account-keys 6 against a baseline of
      11, identity-descriptor-keys 14 against a baseline of 14; and for the
      card document rows 22 with 16 identity keys unchanged from a baseline
      of 16. The numbers are internally consistent (11 account-basis rows plus
      28 descriptor-basis rows is 39) and none is below its floor.
    what-this-lane-could-not-reach: >-
      The two real documents are deliberately not in the repository, so this
      lane cannot re-run the measurement against them and does not claim to
      have. What it CAN and did check is that the harness the command names is
      committed and runnable: run over test/fixtures/belfius-counterparty-identity.pdf
      and test/fixtures/kbc-statement-a.pdf it exits 0 and prints counts only,
      and its fixture figures reproduce the work history's recorded fixture
      figures exactly. Hazard H12.23, that the headline numbers are recorded
      against a harness nobody else can run, is therefore closed even though
      the numbers themselves rest on the implementer's run.

  - id: "12.3"
    criterion: NAMED ONCE, MATCHED AGAIN.
    verdict: met
    evidence: >-
      test/application/counterparty-identity-journey.test.ts:82. The call
      counter is installed at :86 to :98 and asserted to read 1 at :171. The
      single unresolved group of count 3 is asserted at :155 and :162. The two
      transactions the naming was NOT made from are asserted by id at :185 and
      :186, and :115 to :127 asserts that the designated rows differ in
      description, booking date and amount while sharing one counterparty
      account, which is what makes the second match a real one.

  - id: "12.4"
    criterion: DIFFERENT COUNTERPARTIES STAY DIFFERENT.
    verdict: met
    evidence: >-
      test/domain/counterparty-identity.test.ts:109 asserts no two rows on
      different trusted accounts share a key and no account key equals a
      descriptor key; :140 asserts the row-by-row partition against the
      GENERATOR'S record; :178 asserts regeneration reproduces both files byte
      for byte, so the record cannot have been hand-edited. The record's
      independence is structural: test/fixtures/generate-pdf-fixtures.ts
      imports only node:fs, node:path and node:url, so the derivation under
      test cannot reach it.

  - id: "12.5"
    criterion: THIS PHASE STRIPS NOTHING NEW.
    verdict: met
    evidence: >-
      The git-diff half is witnessed directly by this lane rather than through
      a test. git diff --stat between the branch's merge-base with origin/main
      and HEAD lists NO change at all to
      src/modules/merchants/domain/normalise-counterparty.ts or to
      test/domain/normalise-counterparty.test.ts, so none of the nine named
      constants can have moved and the pinned regression table is unchanged.
      The byte-identity half is test/domain/counterparty-identity.test.ts:217
      against the committed table test/fixtures/baseline-descriptor-keys.json,
      and the no-lowercase assertion is at :187.

  - id: "12.6"
    criterion: A ROW MATCHING NO FAMILY IS NOT GUESSED AT.
    verdict: met
    evidence: >-
      test/domain/counterparty-identity.test.ts:256 for the unrecognised
      descriptor keeping exactly its baseline key, :268 for the empty or
      whitespace-only stored account taking the descriptor basis, and :382 for
      the no-truncation, no-bucket assertion. The bare-namespace floor is
      covered three ways at :288, :303, :316 and :344, including the matcher
      refusing a bare PATTERN, which is hazard H12.26.

  - id: "12.7"
    criterion: NO NAMING THE OWNER MADE IS DISCARDED, MEASURED AS EFFECT.
    verdict: met-with-an-uncontracted-exception
    evidence: >-
      The superset test is
      src/modules/merchants/application/rederive-rules.ts:707 to :746 and its
      tests are test/application/rederive-merchant-rules.test.ts:305 (the
      superset itself), :345 (no UPDATE or DELETE against an account-namespaced
      pattern and no descriptor pattern rewritten into an account one), :401
      (the promotion adds beside the naming and the broadening is a printed
      number), :487 and :524 (the two blocking conditions and the acknowledge
      path clearing only the ids it names).
    finding-ref: CR5-M3P12-01

  - id: "12.8"
    criterion: THE RE-DERIVATION IS IDEMPOTENT.
    verdict: met
    evidence: >-
      test/application/rederive-merchant-rules.test.ts:566 asserts zero
      rewrites, zero adds, the same decision report byte for byte and the same
      exit code on a second run; :591 asserts the seed really contains all
      three shapes the criterion requires, which is the assertion that stops
      the dataset-choosing hazard H12.27; :629 asserts the second run does not
      have to exit 0 and that this seed proves it.

  - id: "12.9"
    criterion: FACTS ARE NOT REWRITTEN.
    verdict: met
    evidence: >-
      test/application/rederive-merchant-rules.test.ts:640 asserts no write
      against the transactions table at all, and :669 is the pulse-domain
      section 2 test: clearing every merchant id and recomputing returns the
      identical assignment set.

  - id: "12.10"
    criterion: PREFIX AND PATTERN ARE SETTLED.
    verdict: met
    evidence: >-
      test/domain/counterparty-identity-contracts.test.ts:26, :35 and :42 for
      the three matcher assertions, :62 for the unchanged MerchantRuleKind
      enum asserted member by member and in order, and :76 for the stated
      disposition appearing in BOTH prisma/schema/merchants.prisma and
      src/modules/merchants/domain/merchant-rule.ts.

  - id: "12.11"
    criterion: THE TWO SCREENS AGREE WHERE THEY ARE MEANT TO.
    verdict: met
    evidence: >-
      test/application/counterparty-identity-surfaces.test.ts:71 over one
      dataset, :119 asserting each of the three named divergences at its own
      site rather than choosing a dataset without them, and :159 asserting the
      SQL fragment appears exactly once and both reads use it.

  - id: "12.12"
    criterion: THE MODULE BOUNDARY HOLDS.
    verdict: met
    evidence: >-
      The port is one member at src/modules/ledger/application/ports.ts:40 to
      :57, asserted both at type level and at runtime in
      test/application/resolve-merchants.test.ts:272 to :276. The
      corrections.ts half is witnessed directly: git diff --stat from the
      merge-base to HEAD lists no change to
      src/modules/ledger/domain/corrections.ts at all.

  - id: "12.13"
    criterion: NOTHING OF THE OWNER'S DATA REACHES THE REPOSITORY.
    verdict: met
    evidence: >-
      npm run gate:privacy run by this lane exits 0 and prints "gate:privacy
      clean". test/fixtures/allowed-identifiers.txt carries 212 lines with
      provenance prose. The work history sections this lane read report counts
      only. The standing limit is recorded rather than glossed: fleet warning
      12 says the gate cannot see a merchant name, a place name, a date or an
      amount inside a file, so a green gate is not a proof of this criterion
      and this lane's reading of the fixture and work history is what stands
      behind it.

  - id: "12.14"
    criterion: THE STANDING GATE.
    verdict: pending
    evidence: "the five fast gates are recorded above at exit 0; the e2e run follows"

  - id: "12.15"
    criterion: THE WHOLE-CORPUS EFFECT IS RECORDED.
    verdict: met
    evidence: >-
      delivery/work-history/m3-p12.yaml:718 carries one line per committed
      fixture with the baseline and the new distinct-key count, 37 parsable
      fixtures plus four deliberately unparsable ones named with their
      reasons. No fixture's new count is HIGHER than its baseline, so the list
      the criterion asks for is empty, and the recoverability test is
      therefore the unconditional one at
      test/application/counterparty-identity-surfaces.test.ts:177. This lane
      reproduced two of the recorded lines with the committed harness:
      belfius-counterparty-identity.pdf baseline 24 then 12, and
      kbc-statement-a.pdf baseline 8, both matching the record.

  - id: "12.16"
    criterion: THE ACCOUNT BASIS FAILS CLOSED, AND THIS IS THE GUARD ON H12.1.
    verdict: met
    evidence: >-
      test/domain/counterparty-identity.test.ts:400 is the positive control
      that makes the refusals refusals; :407 empty or whitespace after
      compaction; :416 a country code the table does not carry; :430 a wrong
      length in BOTH directions; :439 a mod-97 failure at the right country
      and length; :459 the truncation case END TO END through the shipped
      importer, asserting the stored value is a strict PREFIX of the invented
      source and that the row takes the descriptor basis; :492 the
      deterministic-closure argument; :510 the residual pinned honestly, that
      a BE-prefixed over-long spaced token truncates to a TRUSTED value so
      mod-97 alone stands in that direction; and :568 the first-wins pin,
      end to end, naming which token the generator wrote first.

  - id: "12.17"
    criterion: ONE NAMED INVOCATION POINT AND ONE STATED ORDER.
    verdict: met
    evidence: >-
      package.json carries "rederive:merchant-rules": "tsx
      scripts/rederive-merchant-rules.ts", and the test that reads package.json
      and finds it is test/domain/counterparty-identity-contracts.test.ts:121.
      The script's own header states the order and the reason at
      scripts/rederive-merchant-rules.ts:7 to :10 and the window's cost at
      :12 to :17, asserted at test/domain/counterparty-identity-contracts.test.ts:133.
    plan-defect: >-
      THE DEPLOYED HALF IS THE PLAN'S, and it is recorded here factually. The
      criterion once required this phase's work history to record the routine
      run AGAINST THE DEPLOYED DATABASE with its exit code and its printed
      before-and-after sizes, while decision D-46 forbids the routine running
      before the code deploys and this branch is neither merged nor deployed
      while the phase is open. Those two halves cannot both be satisfied by
      any implementation, so the criterion was unsatisfiable as written. The
      plan text now carries the split explicitly and moves the deployed run to
      criterion 16.1, and the work history records it as owed at
      delivery/work-history/m3-p12.yaml:1111. The implementation owes nothing
      further here. Attributed to the plan; no implementer omission.

  - id: "12.18"
    criterion: THE RULE SUBJECT IS VALIDATED AT THE WRITE BOUNDARY.
    verdict: met
    evidence: >-
      test/application/counterparty-identity-surfaces.test.ts:223 for the
      pre-migration un-namespaced key refused with a typed error and writing
      nothing, :242 for an account subject empty after compaction, :262 for a
      descriptor subject with nothing after the namespace, :278 for an account
      subject failing the trust gate, :293 for the positive control on both
      bases, and :317 asserting the guard this replaces is gone and the
      replacement is in its place. The e2e half the criterion names is
      test/e2e/merchants.spec.ts:276, which submits a pre-migration
      un-namespaced subject and asserts the refusal reaches the reader.

  - id: "12.19"
    criterion: THE GOVERNING DOCUMENT SAYS WHAT THE CODE DOES.
    verdict: met
    evidence: >-
      .claude/skills/pulse-domain/SKILL.md:129 opens the amended chain, :133
      states the account basis, :136 states the three trust tests including
      the refused country code, and :147 states that PREFIX and PATTERN never
      apply to an account-basis key. The asserting test is
      test/domain/counterparty-identity-contracts.test.ts:143 to :167.
    note: >-
      The literal phrase the criterion asks be gone still occurs once, at
      .claude/skills/pulse-domain/SKILL.md:129, inside an explicit quotation
      of what the document USED TO SAY and immediately negated ("it used to be
      wrong here"). A reader is not misled and the criterion's intent is met.
      Recorded because a bare grep for the phrase does return a hit, and
      because the two regexes at
      test/domain/counterparty-identity-contracts.test.ts:161 and :164 pin two
      exact wordings rather than the claim, so a reintroduction phrased
      slightly differently would not redden. Low, and it is
      finding CR5-M3P12-04.

  - id: "12.20"
    criterion: THE RESIDUE IS COUNTED AND NAMED.
    verdict: met
    evidence: >-
      delivery/work-history/m3-p12.yaml:751 records, counts only: real
      current-account document baseline 21 singleton keys over 21 rows against
      12 over 12 after the change; real card document 12 over 12 both before
      and after, with the reason stated; the new fixture 24 over 24 against 8
      over 8. The committed corpus is recorded too, 176 singleton keys over
      176 rows before against 160 over 160 after, with the command that
      produced it. Both documents are named separately and the record states
      plainly that a row in that set still needs naming per row. This lane
      reproduced the fixture's own figure with the committed harness:
      baseline-singleton-keys 24, identity-singleton-keys 8.

  - id: "12.21"
    criterion: PASS ONE CANNOT LOSE A RULE'S MEANING, FOR ANY KIND.
    verdict: met
    evidence: >-
      test/property/pass-one-preserves-meaning.test.ts:59 is the property over
      every kind, :83 asserts the generator really produces the two skipped
      shapes so the guards are reached rather than assumed, :99 witnesses
      guard one (the bare namespace IS a prefix of every descriptor key, which
      is hazard H12.26), :129 witnesses guard two (namespacing an
      already-namespaced pattern would kill a correct rule on the FIRST run,
      which is hazard H12.25), and :142 carries the case the property test
      cannot generate, D-40's refusal.

  - id: "12.22"
    criterion: THE TRUST TABLE IS PINNED AND SOURCED.
    verdict: met
    evidence: >-
      test/domain/counterparty-identity.test.ts:594 pins the FULL contents so
      any edit is red, and :619 asserts the entry for the country of the
      committed fixtures resolves to the length those fixtures use, which is
      the assertion that puts hazard H12.28 on the FAST gate rather than only
      in the work-history measurement.

  - id: "12.23"
    criterion: THE ROUTINE REFUSES A DATABASE IT WAS NOT TOLD TO WRITE TO.
    verdict: met
    evidence: >-
      The interlock is src/platform/db/target-guard.ts and the routine's
      wiring is scripts/rederive-merchant-rules.ts. Every refusal the
      criterion enumerates is a case in the table at
      test/db/target-guard.test.ts:95 to :140: no target given, host given but
      empty, ref given but empty, connection absent, connection empty,
      connection unparseable, host mismatched on each shape, ref mismatched
      against the USERNAME, ref mismatched against the HOST, and no ref in
      either field. The per-shape matching cases are at :75 and :80, the
      differing-ref extraction assertions one per shape at :37 and :47, the
      same-region different-project case at :152, the no-override assertions
      at :198 and :230, the refusal-before-any-repository-call assertions at
      :282 and :303, the guard-wired-first source assertion at :332, and the
      no-value-printed assertions at :163 and :460. The accepted
      percent-encoded-username gap is asserted fail closed at :186.
      This lane executed the predicate independently: see claim 5 above.
    note: >-
      The criterion's closing paragraph, that it postdates the implementation
      and is marked NOT MET AND NOT YET OWED, applied to the round at which it
      was relocated. Four fix rounds later the interlock is shipped, so the
      criterion is evaluated normally here and is met.
