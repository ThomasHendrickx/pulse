# Clean-room review, HAZARD contract, phase M3-P13 (identity on the review
# screen), ROUND ONE of at most two.
#
# Head reviewed: 936fcda on claude/m3-p13-identity-on-review, in an isolated
# worktree at /home/user/wt-m3p13-haz on branch claude/m3-p13-rev-haz.
#
# HEADER DISCLOSURES. Claude-family verdict, review-contract hazard, framing
# destructive-paths. The implementation session was not read. The criteria
# lane's verdict was not read until this lane's probing was complete.
#
# CONTRACT ORDER. Started from the eight declared hazard classes (H13.1
# through H13.8) and the dispatch attack directions. The acceptance criteria
# were walked LAST, after the probes.
#
# DATABASE SAFETY. The ambient DATABASE_URL points at a deployed pooler for a
# DIFFERENT project of the owner's. Every command that could reach a database
# ran with DATABASE_URL, DIRECT_URL and E2E_DATABASE_URL pinned explicitly to
# 127.0.0.1 in the invoking shell, and the parsed host was printed and read
# 127.0.0.1 before the first such command. prisma migrate reset was never run.
#
# NO DATA. No account number, no counterparty, no amount and no date from any
# real document appears in this file.
#
# ==========================================================
# ATTACK DIRECTIONS (checkpoint state; each is updated in place as it runs)
# ==========================================================
# A1. Rendered markup, compact and spaced shapes           NOT-YET-PROBED
# A2. data-group-key on the row                            NOT-YET-PROBED
# A3. Hidden form fields                                   NOT-YET-PROBED
# A4. Serialised RSC payload                               NOT-YET-PROBED
# A5. aria/title/data attributes                           NOT-YET-PROBED
# A6. Page JSON payloads and anything logged               NOT-YET-PROBED
# A7. Mask identifies by pinned country+length registry?   PROBED: yes, the
#     mask reads ACCOUNT_NUMBER_LENGTH_BY_COUNTRY from src/platform/
#     account-number.ts and consumes exactly the registry length. Shape
#     alone never masks.
# A8. Mask on separators other than ASCII space            PROBED: LEAKS.
#     See finding HZ-M3P13-01.
# A8b. Unknown country / malformed / empty                 PROBED: an
#     unregistered country code is passed through IN FULL rather than
#     redacted. See finding HZ-M3P13-02.
# A9. Mask collision: two accounts, one displayed string   NOT-YET-PROBED
# A10. Inversion by combining screen with naming submit    NOT-YET-PROBED
# A11. Interaction with M3-P11 optimistic naming           NOT-YET-PROBED
# A12. Interaction with M3-P18 held rows                   NOT-YET-PROBED
# A13. New e2e spec vacuity (project membership, locator)  NOT-YET-PROBED
# A14. 13.2 resolution: is each excluded channel necessary NOT-YET-PROBED
# A15. Fourth channel the implementer missed               NOT-YET-PROBED
#
# ==========================================================
# GATES RUN BY THIS LANE
# ==========================================================
#   (not yet run)
#
kind: verdict
phase: M3-P13
verdict: FIX-ROUND-NEEDED
produced-by: claude
framing: destructive-paths
review-contract: hazard
findings: []
criteria:
  - id: "13.1"
    quote: PLACEHOLDER, checkpoint state, not yet walked.
    evidence:
      - checkpoint
    met: false
deviations-judged: []
hazard-classes-addressed:
  - class-id: H13.1
    probed: PLACEHOLDER, checkpoint state, not yet probed.
    cleared-because: PLACEHOLDER
