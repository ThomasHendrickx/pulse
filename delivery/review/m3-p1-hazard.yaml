# Clean-room HAZARD review of phase M3-P1 (navigation).
# HEAD UNDER REVIEW: bba9132 (defect round, criterion 1.5; code head
# a5d37c0, verified by this review: git diff a5d37c0 bba9132 touches
# only delivery/work-history/m3-p1.yaml and m3-p1-notes.md, so the
# top commit is work-history and notes only). The branch was REBUILT
# from main 0a49ef7, which carries the merged M3-P1 (7796fc9) plus
# the owner-feedback plan amendment adding criterion 1.5; the
# defect-round diff is 0a49ef7..bba9132 (4 files: globals.css, the
# navigation spec, and the two work-history docs). History: round 0
# reviewed 5c23aa4, APPROVE with lows CR-601 and CR-602; round 1
# reviewed the fix round at 2c30eae, APPROVE with both lows resolved
# by re-executed construction; full texts in this file's git history
# (commits edd9912 and 17ac6b6 in the reviewer clone; the fleet's
# copies at 0b6f7c6 and cbf78be on main). THIS REVISION is the
# defect-round micro-scope pass: the owner-reported mobile break,
# fixed as criterion 1.5, swept in the hazard lane. Reviewer clone:
# worktrees/m3-p1-review-hazard (rebuilt head merged, theirs taken
# verbatim for every phase file: diff of src, test, messages and
# work-history against bba9132 is 0 lines). No implementation
# session seen in any round. Framing: destructive-paths, contract:
# hazard. DATABASE OFF-LIMITS throughout (the criteria reviewer owns
# the stack): static analysis, grep, tsc, eslint, vitest and git
# archaeology only; the e2e results (17 passed at a5d37c0, including
# the 390x844 and 1280x720 pair) are relied on from the work
# history's gate-evidence and named where used.
#
# DR-0003 DECORRELATION ESCALATION, unchanged from rounds 0 and 1:
# this verdict is produced by the Claude family, as is the paired
# criteria verdict. DR-0012 condition one is NOT met by family; it
# is discharged for the pilot under DR-0003 (same family, varied
# lenses): review-contract and framing are distinct across the pair.
#
# THE DEFECT-ROUND DELTA, swept probe by probe (all executed in this
# clone at the merged head unless marked relied-on).
# - ELLIPSIZED IDENTITY KEEPS ITS FULL TEXT: the ellipsis is pure
#   CSS painting (.app-household min-width 0, overflow hidden,
#   text-overflow ellipsis, white-space nowrap, globals.css:158-165);
#   the delta touches NO markup (git diff 0a49ef7..bba9132 over
#   src/app/(app)/layout.tsx, src/platform, src/modules and
#   messages/ is 0 lines), so the span's DOM text is the complete
#   household name and assistive tech reads it whole. The e2e's
#   identity assertion (signUp's toHaveText(unique) on
#   household-context) runs inside BOTH new viewport tests and the
#   phone run is green in the gate-evidence, so the full-identity
#   read at 390px is witnessed, not assumed.
# - SIGN-OUT REMAINS A REAL POST: the form markup is untouched
#   (layout.tsx:42, form action={signOutAction}, zero delta) and
#   src/platform/auth has a 0-line diff; the only change is
#   .app-header form { flex-shrink: 0 }, layout-only. The phone test
#   clicks the button and asserts arrival at the Sign in heading,
#   relied on from the 17-passed gate-evidence: behaviour witnessed
#   end to end at the new viewport.
# - DESKTOP MEASUREMENT UNCHANGED BY THE CLAMP, judged statically
#   and backed by the desktop witness: min(var(--layout-rail), 100%)
#   resolves the percentage against the containing block
#   (.import-form's content box), which at 1280 is far wider than
#   the 372px rail token (styles/tokens.css:128), so min() returns
#   exactly the pre-round value and the input's resolved min-width
#   is byte-for-byte the old measurement; max-width 100% binds only
#   when the container is narrower than the element would be, which
#   at 1280 it is not. The header wrap engages only when content
#   exceeds the row: every pre-round e2e ran at the 1280x720 default
#   with no horizontal overflow, so the row content fits at 1280 and
#   flex-wrap changes nothing there. The kept desktop test (1280x720,
#   scrollWidth <= 1280 plus all controls visible on all three
#   routes) is the executable witness, green in the gate-evidence.
# - NO NEW LITERAL SURFACE: grep @media over globals.css at the head
#   exits 1 (the no-media-query claim verified, not trusted); the
#   colour grep over all of src/ exits 1; no px, rem or em literal
#   in the delta's added lines (grep over the + lines, exit 1). The
#   new values are 0, 100% and a min() of an existing token, which
#   are structural, not scale values; the two-value gap shorthands
#   pair existing space tokens.
# - CLIENT-BOUNDARY CENSUS UNCHANGED: grep -rl '"use client"' src/
#   still prints exactly src/platform/ui/nav-link.tsx; the delta
#   contains no tsx change at all.
# - GATES RE-EXECUTED at the merged head in this clone: npm run
#   typecheck exit 0, npm run lint exit 0, npx vitest run 21 files,
#   252 tests, 0 failed (the expected 252 confirmed: the defect
#   round adds e2e assertions only).
# - HYGIENE: no package or lockfile change (diff 0 lines), no
#   deleted files (--diff-filter=D empty), zero em dashes in the
#   delta, golden-journey.spec.ts not in the delta's file list.
# - R-087 CLAIM AUDIT of the defect-round work-history additions,
#   re-executed where reachable: the pre-round absence of any
#   explicit viewport reproduces (git grep viewport at 0a49ef7 over
#   playwright.config.ts and test/e2e/ exits 1, the escaped-defect
#   mechanism in one line); the five pre-round min-width sites
#   reproduce exactly as listed (globals.css:261 the culprit rail
#   floor, :692 and :697 the space-14 amount columns, :454 and :723
#   the zero resets); the honesty notes (derivation captured after
#   the fix; the unprobed CSS-spec fact taken by reading; the
#   not-covered list naming the /import/[id] confirm step, seeded
#   month view, non-EN labels and non-844 heights as unasserted at
#   390) are accurate and correctly scoped. The two-red witness
#   story in claim M3P1-C8 (476>390 on / for the header, then
#   396>390 on /import for the field, then green) names two
#   different overflowing elements and matches the two shipped
#   fixes; the runs are e2e and relied on as recorded.
# LOOKED FOR AND NOT FOUND in the defect round: a DOM or JS
#   truncation of the identity (none; CSS-only); a sign-out
#   behaviour change (none; zero auth diff); a desktop layout change
#   from the clamp (none by resolution analysis; desktop witness
#   green); a media query or breakpoint literal (none); a new colour
#   or px value (none); a second client component (none); a weakened
#   or deleted assertion (none; the spec strictly gains two tests
#   and the two-red witness reproduces the criterion's intent).
# SCHEMA VALIDATION, executed at this revision: tiphys validate
#   --type verdict (Node 26 via nvm) against a freshly created
#   private context directory carrying plan.yaml (copy of the
#   AMENDED delivery/plan/pulse-v02.yaml at the merged head, five
#   acceptance criteria) and work-history.yaml (copy of the
#   defect-round delivery/work-history/m3-p1.yaml): exit 0. The
#   dual-review-decorrelation regime report is expected there (the
#   private context carries no charter deliberately); the real
#   dual-verdict gate runs over the fleet's own directory once both
#   verdicts of this head are committed.
# NOTE ON A SHARED SCRATCH HAZARD, carried forward: the session
#   scratchpad is REUSED across fleet sessions (this round found it
#   fully repopulated by the implementer session's probe files) and
#   old context dirs there can hold SYMLINKS into other review
#   worktrees; a cp onto such a plan.yaml writes THROUGH the link
#   onto another worktree's committed plan file. Build private
#   contexts with freshly created uniquely named directories and cat
#   copies only.

kind: verdict
phase: M3-P1
verdict: APPROVE
produced-by: claude
framing: destructive-paths
review-contract: hazard

findings: []

criteria:
  - id: "1.1"
    quote: >-
      npm run test:e2e exits 0 with test/e2e/navigation.spec.ts
      passing: it asserts the three header nav links are present (by
      testid) on /, /import and /merchants, that clicking each
      navigates to its route, that the active marker matches the
      current route, and that the empty state for a household with no
      imports contains a link whose click lands on /import. And the
      nav is rendered by the shell, not by pages: grep -rl for the
      nav's data-testid over src/ matches exactly one component file,
      src/app/(app)/layout.tsx (finding PR2-006: per-page copies on
      the three current routes would pass the route assertions and
      ship the next route bare).
    evidence:
      - "e2e half RELIED ON, not re-executed (database off-limits to this review): defect-round gate-evidence, npm run test:e2e 17 passed 0 skipped exit 0 at a5d37c0, every earlier navigation test green untouched; the delta modifies no earlier assertion (the spec strictly gains the two viewport tests)"
      - "grep half RE-EXECUTED at the merged head: grep -rl over src/ for each of nav-overview, nav-import, nav-merchants and main-nav prints exactly src/app/(app)/layout.tsx; the defect round touches no tsx file"
    met: true
  - id: "1.2"
    quote: >-
      npm test exits 0 including the new catalog parity test, which
      fails red when any key exists in one of messages/en.json,
      messages/nl.json, messages/fr.json and not the others (witnessed
      red once in the work history by removing a key locally, then
      green).
    evidence:
      - "RE-EXECUTED at the merged head: npx vitest run, 21 files, 252 tests, 0 failed, exit 0, the deep leaf-path parity test among them; the defect round touches no catalog (diff over messages/ is 0 lines), so round 1's three-way red reconstruction stands unchanged"
    met: true
  - id: "1.3"
    quote: >-
      npm run typecheck && npm run lint && npm test exit 0 at the
      phase head, and grep -rE "oklch\(|#[0-9a-fA-F]{3,8}" over src/
      excluding the token and theme files exits 1.
    evidence:
      - "RE-EXECUTED at the merged head: npm run typecheck exit 0; npm run lint exit 0; npx vitest run 252/252 exit 0; the criterion's colour grep over all of src/ exits 1"
      - "defect-round CSS additionally swept: no @media in globals.css (grep exit 1), no px/rem/em literal in the delta's added lines (grep exit 1); the new 0 and 100% values and min() over the existing --layout-rail token introduce no scale literal"
    met: true
  - id: "1.4"
    quote: >-
      The pre-existing test/e2e/golden-journey.spec.ts passes
      unmodified except for selectors the new header markup forces;
      git diff --name-only against the phase base shows no deleted
      test files.
    evidence:
      - "RE-EXECUTED over the defect-round range 0a49ef7..bba9132: git diff --name-only --diff-filter=D prints nothing and golden-journey.spec.ts is not in the 4-file delta; its pass at a5d37c0 is relied on from the gate-evidence (17 passed includes it)"
    met: true
  - id: "1.5"
    quote: >-
      Owner-feedback amendment (2026-08-21, orchestrator): at a phone
      viewport (390x844) on every authenticated route, the navigation
      links, the account identity and the sign-out control are all
      visible and clickable without horizontal scrolling
      (document.documentElement.scrollWidth less than or equal to the
      viewport width), asserted in test/e2e/navigation.spec.ts with an
      explicit viewport; the same spec keeps a desktop-viewport
      assertion so neither presentation regresses the other.
    evidence:
      - "spec half RE-READ against the criterion's letter: test.use viewport 390x844 (navigation.spec.ts:181) and 1280x720 (:196); expectFullHeaderWithoutHorizontalScroll walks all three authenticated routes asserting main-nav, each nav link, household-context and the Sign out button visible plus documentElement.scrollWidth <= the viewport width (:157-176); clickable is witnessed by the phone test's sign-out click landing on the Sign in heading (:190-192)"
      - "execution RELIED ON (database off-limits): gate-evidence npm run test:e2e 17 passed exit 0 at a5d37c0; the two-red witness in claim M3P1-C8 (scrollWidth 476>390 on / pre-fix, then 396>390 on /import after the header-only fix, then green) reddens under two different overflowing elements, matching the two shipped fixes"
      - "mechanism RE-EXECUTED where reachable: the CSS fix is wrap-and-yield with no media query (grep exit 1 at the head), the identity ellipsis is paint-only with DOM text intact (no markup diff), and the clamp's desktop resolution equals the pre-round value by containing-block analysis, so the kept desktop assertion guards a presentation the fix provably does not move"
    met: true

deviations-judged:
  - deviation: >-
      files-to-touch (phase M3-P1): the list does not name
      src/platform/ui/nav-link.tsx or src/app/globals.css, both of
      which the phase in fact requires.
    serves-plan-intent: true
    reasoning: >-
      Rounds 0 and 1 judgement stands and the defect round lands
      inside it: the criterion 1.5 fix is pure CSS in globals.css,
      exactly the file the deviation declared for shell chrome, with
      no second styling site invented; nav-link.tsx is untouched this
      round and remains the one client island keeping criterion 1.1's
      grep pin intact.
  - deviation: >-
      files-to-touch (phase M3-P1): styles/tokens.css is listed but
      was not modified.
    serves-plan-intent: true
    reasoning: >-
      Rounds 0 and 1 judgement stands: the defect round adds no
      token (the clamp reuses --layout-rail, the gaps reuse space
      tokens, and 0 and 100% are structural values, not scale
      entries), so the conditional listing remains correctly
      untriggered at bba9132.

hazard-classes-addressed:
  - class-id: H1.1
    probed: >-
      Re-checked at the merged defect-round head: grep -rl over src/
      for each of the four nav testids still prints exactly
      src/app/(app)/layout.tsx; grep -rl '"use client"' over src/
      still prints exactly src/platform/ui/nav-link.tsx; the
      defect-round delta contains no tsx change and a 0-line diff
      over src/app/(app)/layout.tsx, src/platform, src/modules and
      messages/, so the nav still exists only inside the guarded
      shell (requireHouseholdContext redirects unauthenticated
      requests before render) and the new phone presentation is the
      SAME shell nav wrapping, not a second mobile nav.
    cleared-because: >-
      The nav is rendered by the shell layout and nowhere else at
      bba9132; the mobile fix is CSS on the existing shell classes,
      so a route added later still inherits the nav, its active
      marker, and now its phone-width behaviour, from one place.
  - class-id: H1.2
    probed: >-
      Delta swept for copy: the defect round adds no user-facing
      string (0-line diff over messages/ and no tsx change; the new
      e2e literals are test-side selectors and the pre-existing Sign
      out and Sign in role names). Round 1's re-executed attack on
      the deep leaf-path parity guard stands (reds on dropped array
      entry, array-replaced-by-string, deleted top-level key; green
      on the real catalogs), re-confirmed green in this round's
      252-test run.
    cleared-because: >-
      No new chrome copy exists to bypass the catalogs, the parity
      guard's strengthened reach is unchanged, and the ellipsized
      identity keeps its complete text in the DOM, so nothing
      user-facing degraded across EN, NL, FR at any viewport.
  - class-id: H1.3
    probed: >-
      Executed at the merged head: the criterion grep over all of
      src/ exits 1; grep @media over globals.css exits 1 (the fix is
      wrap-and-yield, no breakpoint literal, exactly as the work
      history claims); no px, rem or em value in the delta's added
      lines (grep exit 1); the new declarations reuse existing
      tokens (--space-2/-4/-7/-8 gap pairs, --layout-rail in the
      min() clamp) plus structural 0 and 100%.
    cleared-because: >-
      The defect-round chrome is tokens-only with no media query and
      no new literal of any kind; every value either is an existing
      token or is a structural 0/100%, so the class has no instance
      in this delta.
