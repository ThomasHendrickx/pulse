# Clean-room HAZARD review of phase M3-P1 (navigation).
# HEAD UNDER REVIEW: 2c30eae (fix round 1; code head 1a0e0d8,
# verified by this review: git diff 1a0e0d8 2c30eae touches only
# delivery/work-history/m3-p1.yaml and m3-p1-notes.md, so the top
# commit is work-history and notes only). History: round 0 reviewed
# 5c23aa4 (code head c2aceec), APPROVE with two lows, CR-601 (active
# marker dark on /import/[id], exact-match pathname test) and CR-602
# (catalog parity guard blind to array-entry drift, top-level keys
# only); round 0's full text lives in this file's git history
# (commit edd9912). THIS REVISION is the round 1 micro-scope delta
# pass over 5c23aa4..2c30eae: both lows re-verified resolved by
# re-executed construction, the tiny delta swept for fresh hazards,
# the fast gates re-run at the head. Phase base: c518181. Reviewer
# clone: worktrees/m3-p1-review-hazard, branch
# claude/m3-p1-review-hazard (delta merged as d41e73e). No
# implementation session seen in any round. Framing:
# destructive-paths, contract: hazard. DATABASE OFF-LIMITS
# throughout (the criteria reviewer owns the stack): static
# analysis, grep, tsc, eslint, vitest and node reconstruction probes
# only; the e2e results (14 passed at c2aceec, 15 passed at 1a0e0d8
# including the new sub-route witness) are relied on from the work
# history's gate-evidence and named where used.
#
# DR-0003 DECORRELATION ESCALATION, unchanged from round 0: this
# verdict is produced by the Claude family, as is the paired
# criteria verdict. DR-0012 condition one is NOT met by family; it
# is discharged for the pilot under DR-0003 (same family, varied
# lenses): review-contract and framing are distinct across the pair.
#
# RESOLUTION LEDGER, round 1 delta (both probes EXECUTED in this
# clone at the delta head; captured in the work log commits).
# - CR-601 (low, round 0): RESOLVED at 1a0e0d8 and verified by
#   execution. isCurrentRoute (nav-link.tsx:28-33) makes the root
#   href exact-only and a non-root href current for its exact path
#   or any href-plus-slash sub-path. This review re-ran the
#   committed matcher logic verbatim in node over every real route:
#   /, /import, /import/abc123, /merchants each yield EXACTLY ONE
#   active href, with /import/abc123 -> /import (the round 0 gap,
#   closed). Edge cases probed: root on /import is false (the
#   mark-everything failure mode the new e2e also guards, asserting
#   nav-overview and nav-merchants NOT current on the sub-route);
#   a hypothetical /imports sibling would NOT match /import
#   (startsWith is on href plus "/"); the real route census (find
#   over src/app/(app): /, /import, /import/[id], /merchants) holds
#   no href that prefixes a sibling href, so the residue the work
#   history records (a future prefix pair) has no instance today,
#   which this review confirms rather than assumes. The claimed red
#   witness (exit 1 at navigation.spec.ts:110 against pre-fix src at
#   bb6ea80, then 15 passed at 1a0e0d8) is consistent with the
#   committed test, which asserts both the positive and the converse;
#   the e2e execution itself is relied on from the gate-evidence.
# - CR-602 (low, round 0): RESOLVED at 1a0e0d8 and verified by
#   execution. The parity test now flattens to deep leaf paths
#   (collectLeafPaths: object keys join with dots, array entries
#   append [index]). This review ran the committed flattener logic
#   verbatim in node against in-memory mutations (the tree never
#   edited): real catalogs GREEN; round 0's exact construction (last
#   steps entry dropped from nl) now RED; the fix round's second
#   member (array replaced by a string) RED; the original top-level
#   key deletion still RED. So the guard reddens under three
#   structurally different members and the round 0 blindness is
#   closed. The R-037a discipline in the work history (lying test
#   shown green against the dangerous state FIRST, then the repaired
#   test red twice, then green) is the correct order and its
#   reproduce-the-reviewer's-construction step matches this review's
#   own round 0 capture. The stated residue is judged honest and
#   correctly scoped: value-level translation quality and ICU
#   placeholder parity are a different check with legitimate
#   per-language asymmetries, and the comment overclaim flagged in
#   CR-602 is gone (the test's header now states the leaf-path
#   contract it actually checks, and names the round 0 defect).
#
# DELTA SWEEP, everything else in 5c23aa4..2c30eae, executed:
# - Scope: 5 files, 238 insertions, 15 deletions; no message
#   catalog, layout, globals.css, month-view, package or lockfile
#   change (git diff over messages/, src/app, src/modules,
#   package.json is 0 lines); no deleted files (--diff-filter=D
#   empty); zero em dashes in the delta.
# - Client boundary unchanged: grep -rl '"use client"' src/ still
#   prints exactly src/platform/ui/nav-link.tsx; isCurrentRoute is a
#   module-local pure function of two strings, no new import, no new
#   prop, nothing server-only crossing.
# - Gates re-executed at the delta head in this clone: npm run
#   typecheck exit 0, npm run lint exit 0, npx vitest run 21 files,
#   252 tests, 0 failed (the coordinator's expected 252 confirmed:
#   the parity test kept its 3-test shape, deepened rather than
#   widened).
# - R-087 claim audit of the fix-round work-history additions: the
#   cited upload redirect exists at src/modules/import/ui/actions.ts:30
#   (redirect to /import/ plus the import id); the quoted middleware
#   lines match the file and src/middleware.ts is byte-identical to
#   base (diff 0 lines), as the derivation's not-covered note
#   claims; the derivation honestly discloses it was captured
#   after the fix. No false claim found.
# LOOKED FOR AND NOT FOUND in the delta: a matcher change that marks
#   multiple links current on any real route (node re-execution:
#   exactly one everywhere); a sibling href prefix collision (route
#   census clean); a parity flattener that no longer reds on the
#   round 0 constructions (all three red); a weakened or deleted
#   test (none; the parity assertions strictly deepened, the e2e
#   gained one test and lost none); an em dash (zero).
# SCHEMA VALIDATION, executed at this revision: tiphys validate
#   --type verdict (Node 26 via nvm) against the private context
#   directory carrying plan.yaml (copy of delivery/plan/pulse-v02.yaml)
#   and work-history.yaml (copy of the fix-round
#   delivery/work-history/m3-p1.yaml): exit 0. The
#   dual-review-decorrelation regime report is expected there (the
#   private context carries no charter deliberately); the real
#   dual-verdict gate runs over the fleet's own directory once both
#   verdicts of this head are committed.
# NOTE ON A SHARED SCRATCH HAZARD, carried from round 0 because a
#   later reviewer will hit it: the session scratchpad is REUSED
#   across fleet sessions and old context dirs there hold SYMLINKS
#   into other review worktrees; a cp onto such a plan.yaml writes
#   THROUGH the link onto another worktree's committed plan file.
#   Round 0 tripped it twice, restored both targets from git
#   immediately (verified clean status), and rebuilt its context
#   from cat into a freshly created uniquely named directory. Build
#   private contexts with fresh directories only.

kind: verdict
phase: M3-P1
verdict: APPROVE
produced-by: claude
framing: destructive-paths
review-contract: hazard

findings: []

criteria:
  - id: "1.1"
    quote: >-
      npm run test:e2e exits 0 with test/e2e/navigation.spec.ts
      passing: it asserts the three header nav links are present (by
      testid) on /, /import and /merchants, that clicking each
      navigates to its route, that the active marker matches the
      current route, and that the empty state for a household with no
      imports contains a link whose click lands on /import. And the
      nav is rendered by the shell, not by pages: grep -rl for the
      nav's data-testid over src/ matches exactly one component file,
      src/app/(app)/layout.tsx (finding PR2-006: per-page copies on
      the three current routes would pass the route assertions and
      ship the next route bare).
    evidence:
      - "e2e half RELIED ON, not re-executed (database off-limits to this review): fix-round gate-evidence, npm run test:e2e 15 passed 0 skipped exit 0 at 1a0e0d8, which includes every round 0 assertion plus the new CR-601 sub-route witness; round 0's static read of the spec's coverage stands and the delta only adds a test"
      - "grep half RE-EXECUTED at the delta head: grep -rl over src/ for each of nav-overview, nav-import, nav-merchants and main-nav prints exactly src/app/(app)/layout.tsx; the delta touches no layout or page file"
      - "active-marker mechanism RE-EXECUTED: the committed isCurrentRoute logic run verbatim in node yields exactly one active href on every real route including /import/abc123 (round 0's CR-601 closed)"
    met: true
  - id: "1.2"
    quote: >-
      npm test exits 0 including the new catalog parity test, which
      fails red when any key exists in one of messages/en.json,
      messages/nl.json, messages/fr.json and not the others (witnessed
      red once in the work history by removing a key locally, then
      green).
    evidence:
      - "RE-EXECUTED at the delta head: npx vitest run, 21 files, 252 tests, 0 failed, exit 0, the deepened parity test among them"
      - "guard reach RE-EXECUTED by reconstruction (tree never edited): the committed leaf-path flattener reds on a deleted top-level key, on a dropped array entry (round 0's CR-602 construction) and on an array-replaced-by-string shape drift, and is green on the real catalogs; strictly stronger than the criterion's letter"
    met: true
  - id: "1.3"
    quote: >-
      npm run typecheck && npm run lint && npm test exit 0 at the
      phase head, and grep -rE "oklch\(|#[0-9a-fA-F]{3,8}" over src/
      excluding the token and theme files exits 1.
    evidence:
      - "RE-EXECUTED at the delta head: npm run typecheck exit 0; npm run lint exit 0; npx vitest run 252/252 exit 0"
      - "colour grep unchanged by construction: the delta touches no CSS and no colour value (git diff over src/app and messages is 0 lines; the only src change is the pure isCurrentRoute function); round 0's executed grep over all of src/ (exit 1) stands, re-checked against a delta that adds no literal"
    met: true
  - id: "1.4"
    quote: >-
      The pre-existing test/e2e/golden-journey.spec.ts passes
      unmodified except for selectors the new header markup forces;
      git diff --name-only against the phase base shows no deleted
      test files.
    evidence:
      - "RE-EXECUTED over the full range c518181..2c30eae: git diff --name-only --diff-filter=D prints nothing; golden-journey.spec.ts is untouched in the delta (not in the 5-file list) and its pass at 1a0e0d8 is relied on from the gate-evidence (15 passed includes it)"
    met: true

deviations-judged:
  - deviation: >-
      files-to-touch (phase M3-P1): the list does not name
      src/platform/ui/nav-link.tsx or src/app/globals.css, both of
      which the phase in fact requires.
    serves-plan-intent: true
    reasoning: >-
      Round 0 judgement stands and the delta reinforces it: the client
      island is forced by the platform (the layout resolves the
      household through next/headers, verified at
      src/platform/auth/supabase-server.ts:2, and the implementer's
      build probe M3P1-C5 reddened the alternative), it keeps every
      testid literal and label in the layout preserving criterion
      1.1's grep pin, and the CR-601 fix landed exactly where that
      structure says route logic lives, one pure function in the
      island, with no second decision site to drift. globals.css is
      where the existing shell classes live; the fix round did not
      touch it, consistent with the round 0 judgement that no token
      was missing.
  - deviation: >-
      files-to-touch (phase M3-P1): styles/tokens.css is listed but
      was not modified.
    serves-plan-intent: true
    reasoning: >-
      Round 0 judgement stands: the listing is conditional on a
      missing token and none was missing (every var(--*) referenced in
      src/app/globals.css resolves in styles/tokens.css, verified by
      set comparison in round 0). The delta adds no CSS, so the fact
      is unchanged at 2c30eae.

hazard-classes-addressed:
  - class-id: H1.1
    probed: >-
      Round 0's executed construction re-checked at the delta head:
      grep -rl over src/ for each of the four nav testids still
      prints exactly src/app/(app)/layout.tsx; grep -rl '"use
      client"' over src/ still prints exactly
      src/platform/ui/nav-link.tsx; the delta's only src change is a
      module-local pure function inside that island, and the diff
      over src/app, src/modules and messages/ is 0 lines, so the nav
      still exists only inside the guarded shell
      (requireHouseholdContext redirects unauthenticated requests
      before render, src/platform/auth/context.ts:16-18).
    cleared-because: >-
      The nav is rendered by the shell layout and nowhere else at
      2c30eae; the fix round moved nothing across the boundary, and a
      route added later under (app) inherits both the nav and, by the
      new isCurrentRoute rule, a correct active marker on its
      sub-routes.
  - class-id: H1.2
    probed: >-
      Round 0's attack on the parity guard re-executed against the
      repaired guard: the committed collectLeafPaths logic run
      verbatim in node over in-memory mutations (tree never edited).
      Real catalogs green; dropped array entry RED (round 0's
      blindness, now caught, naming the drifted leaf path);
      array-replaced-by-string RED; deleted top-level key RED. The
      delta touches no catalog (diff over messages/ is 0 lines), all
      copy remains t()-borne, and the test's header comment now
      states the leaf-path contract it actually checks, closing the
      round 0 overclaim.
    cleared-because: >-
      Round 0's CR-602 is resolved by execution: the guard now
      reddens under three structurally different drift shapes
      including the exact construction that evaded it, so untranslated
      or missing chrome copy cannot ship with a green fast gate; the
      declared residue (ICU placeholder and value-level parity) is a
      different check, honestly recorded, and no instance of the
      hazard exists in the diff.
  - class-id: H1.3
    probed: >-
      Delta swept for new literals: the fix-round diff contains no
      CSS change and no colour, spacing or font value (the only src
      change is string logic in nav-link.tsx); round 0's executed
      probes (criterion grep over all of src/ exit 1, no literal
      px/rem/em/% in the phase's added CSS, every var(--*) in
      globals.css resolving in tokens.css) stand for the unchanged
      files.
    cleared-because: >-
      The chrome's styling is byte-identical to the round 0 state
      already verified tokens-only; the fix round introduced no
      styling surface at all, so the class has no new instance to
      produce.
