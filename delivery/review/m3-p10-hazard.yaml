# Clean-room review, HAZARD contract, phase M3-P10 (busy state), ROUND ONE.
#
# RETROSPECTIVE REVIEW. The phase was merged and DEPLOYED on 2026-08-26 with
# no clean-room review of any kind on record. Everything found here is LIVE
# IN PRODUCTION today unless the finding says otherwise, and severity is set
# on that footing.
#
# Head reviewed: e499d64 (origin/main, which is what is deployed), in an
# isolated worktree at /home/user/wt-m3p10-haz on branch
# claude/m3-p10-rev-haz. The repository at /home/user/pulse was never
# written to.
#
# HEADER DISCLOSURES. Claude-family verdict, review-contract hazard, framing
# destructive-paths. The implementation session was not read. No criteria
# lane verdict for this phase exists on this branch at the time of writing
# and none was read. delivery/work-history/m3-p10.yaml and
# delivery/work-history/m3-p10-notes.md are treated as CLAIMS TO REFUTE:
# nothing below rests on them, and every statement carries a command this
# lane ran in this container.
#
# CONTRACT ORDER. Started from the twelve declared hazard classes H10.1
# through H10.12 and from the dispatch attack directions, BEFORE reading the
# acceptance criteria. The criteria were walked LAST.
#
# DATABASE SAFETY. The ambient DATABASE_URL in this container points at a
# deployed pooler for a DIFFERENT project of the owner's. Every command
# below that could reach a database ran with DATABASE_URL and DIRECT_URL
# pinned in the invoking shell to an invented localhost string, and the
# parsed host was printed and read 127.0.0.1 before the first gate ran.
#
# NO DATA. No account number, no counterparty, no amount and no date from
# any real document appears in this file.
#
# EDIT-NOTHING. Nothing of the head's code, tests or documents was changed.
# git status carries exactly one untracked file, this document.
#
# ==========================================================
# ATTACK DIRECTIONS (progress board, updated as the review runs)
# ==========================================================
# A1  busy state that NEVER CLEARS                                PROBED, see P5/P6
# A2  busy state that NEVER APPEARS: hydration delay, listener
#     attaching after the press, a submit control outside the
#     mechanism                                                  not-yet-probed
# A3  double submission while busy (H10.5)                       not-yet-probed
# A4  the busy signal LYING: shown for an action that already
#     failed or already completed                                not-yet-probed
# A5  interaction with the merged M3-P9 pressed/disabled
#     machinery: same controls, same stylesheet, aria-disabled
#     with pointer-events, keyboard reachability of a busy
#     control                                                    not-yet-probed
# A6  reduced-motion behaviour                                   PROBED, see P8
# A7  client boundary width and import closure (H10.2)           PROBED, see P1/P2
# A8  the phase's own e2e spec going VACUOUS through project
#     membership, testIgnore or prod-only projects               not-yet-probed
# A9  the hidden counterpartyText field (H10.7) and the copy
#     gate glob (H10.10)                                         not-yet-probed
# A10 scope discipline: new dependency, new stylesheet rule
#     (H10.6)                                                    not-yet-probed
# A11 gates and build run by this lane with DB env pinned        not-yet-probed
#
# ==========================================================
# PROBES, in the order run (output captured at run time)
# ==========================================================
# P1. THE CLIENT FILE COUNT ON THE DEPLOYED HEAD.
#       cd /home/user/wt-m3p10-haz && grep -rln "use client" src/
#     returns FIVE files:
#       src/modules/accounts/ui/account-setup-form.tsx
#       src/platform/ui/submit-button.tsx
#       src/platform/ui/link-pending.tsx
#       src/platform/ui/nav-link.tsx
#       src/app/layout.tsx
#     Criterion 10.6(a) asks for AT MOST THREE, all under src/platform/ui/,
#     and 10.6(b) forbids the directive under src/app/, in any layout.tsx,
#     and in any module ui/ directory. Both are false on the deployed head.
#
# P2. THE SAME GREP AT THE PHASE BASE.
#       git grep -ln "use client" 785e611^ -- src/
#     returns THREE files: src/app/layout.tsx (M3-P9's press listener,
#     commit 274a858), src/modules/accounts/ui/account-setup-form.tsx
#     (M3-P14's setup form) and src/platform/ui/nav-link.tsx. So 10.6(a)'s
#     "all of them under src/platform/ui/" and the whole of 10.6(b) were
#     ALREADY FALSE BEFORE THIS PHASE TOUCHED ANYTHING. The criterion was
#     written against 89ed187 and two phases landed under it in between.
#     This is a stale criterion rather than a regression this phase caused,
#     and it is recorded as such: what the phase itself ADDED is two files,
#     both under src/platform/ui/.
#
# P3. THE COMMIT AND ITS FILE SET.
#       git show --stat --oneline 785e611
#     M3-P10 is commit 785e611 (2026-08-26), sixteen files. FOUR are
#     outside the plan's files-to-touch:
#       src/modules/accounts/ui/account-setup-form.tsx
#       src/modules/accounts/ui/accounts-screen.tsx
#       src/modules/import/ui/import-result.tsx
#       src/modules/import/ui/import-status-line.tsx
#     d7db0ff follows it with the attribution note.
#
# P4. THE SUBMIT CONTROL INVENTORY ON THE DEPLOYED HEAD.
#       grep -rn "<SubmitButton\|type=\"submit\"" src/
#     NINE submit controls, not the seven the plan enumerates: the seven
#     plus src/modules/accounts/ui/accounts-screen.tsx:137 (through the
#     leaf) and src/modules/accounts/ui/account-setup-form.tsx:291 (a raw
#     button, NOT through the leaf, carrying its own transition pending
#     flag). The plan's set is stale for the same reason P2 is.
#
# P5. THE ONLY ASSERTION THAT THE BUSY STATE EVER CLEARS IS STRUCTURALLY
#     VACUOUS.
#       grep -n 'aria-busy' test/e2e/busy-state.spec.ts
#     returns four lines. Line 302 asserts aria-busy IS present at 1000ms.
#     Line 444 is the ONLY assertion that it is gone:
#       await expect(page.locator('[aria-busy="true"]')).toHaveCount(0);
#     commented "Criterion 10.3, second half: the busy state does not
#     survive the response." It sits at the END of the nine-press sequence
#     in test one, immediately after the SIGN-IN submit (spec lines 412
#     through 419), whose server action redirects into the application
#     shell. By the time the locator is evaluated the document has been
#     REPLACED and contains no submit control that was ever pressed, so the
#     assertion cannot fail whatever the leaf does. Criterion 10.3 asks for
#     this "for each of the seven submit controls"; it is checked for none
#     of them. The one case it nominally covers is the one where a full
#     navigation guarantees the answer.
#
# P6. THE ONE SUBMIT CONTROL OUTSIDE THE LEAF HAS AN UNCAUGHT ASYNC
#     TRANSITION.
#       sed -n '145,155p' src/modules/accounts/ui/account-setup-form.tsx
#     is
#       onSubmit={(event) => { event.preventDefault();
#         const data = new FormData(event.currentTarget);
#         startTransition(async () => {
#           setState(await registerAccountsAction(INITIAL, data));
#         }); }}
#     with disabled={pending} and aria-busy={pending ? "true" : undefined}
#     at :291 through :295. There is no catch and no finally. The busy and
#     disabled state of this control is therefore tied to a promise whose
#     rejection path is unwritten, and NOTHING in the suite presses it and
#     then fails the action: busy-state.spec.ts presses it once on the happy
#     path (spec line 353) and the only clears-assertion is P5's vacuous one.
#
# P7. E2E PROJECT MEMBERSHIP IS SOUND, which is recorded because it is the
#     defect class this project has hit twice.
#       cat playwright.config.ts
#     busy-state.spec.ts is matched by chromium (testIgnore /prod-smoke/,
#     so not excluded), by chromium-phone (testMatch names busy-state
#     explicitly, config line 109) and by chromium-phone-prod (testMatch
#     /busy-state\.spec\.ts/, config line 136). It is NOT prod-only and
#     carries no test.skip, no project-conditional branch and no
#     fixme:
#       grep -n "test.skip\|test.fixme\|test.fail" test/e2e/busy-state.spec.ts
#     returns nothing. The spec is three tests, all unconditional. This
#     attack direction produced no finding.
#
# P8. REDUCED MOTION IS HANDLED AT THE TOKEN.
#       grep -rn "prefers-reduced-motion" src/ styles/
#     one block, styles/tokens.css:273, redefining --duration-state: 0s and
#     --duration-busy-cycle: 0s at :root. src/app/globals.css:1706 spends
#     --duration-busy-cycle as the animation duration of the busy mark and
#     :1716 spends --duration-state as the unconfirmed marking's transition,
#     so both stand still under the preference with no variant of their own.
#     M3-P9 already measures this (test/e2e/pressed-and-disabled.spec.ts
#     :1937 and :1950 run the same control at both settings). This attack
#     direction produced no finding against M3-P10.
#
kind: verdict
phase: M3-P10
verdict: IN-PROGRESS
produced-by: claude-family
framing: destructive-paths
review-contract: hazard
findings: []
criteria: []
deviations-judged: []
hazard-classes-addressed: []
