# Clean-room review, HAZARD contract, phase M3-P10 (busy state), ROUND ONE.
#
# RETROSPECTIVE REVIEW. The phase was merged and DEPLOYED on 2026-08-26 with
# no clean-room review of any kind on record. Everything found here is LIVE
# IN PRODUCTION today unless the finding says otherwise, and severity is set
# on that footing.
#
# Head reviewed: e499d64 (origin/main, which is what is deployed), in an
# isolated worktree at /home/user/wt-m3p10-haz on branch
# claude/m3-p10-rev-haz. The repository at /home/user/pulse was never
# written to.
#
# HEADER DISCLOSURES. Claude-family verdict, review-contract hazard, framing
# destructive-paths. The implementation session was not read. No criteria
# lane verdict for this phase exists on this branch at the time of writing
# and none was read. delivery/work-history/m3-p10.yaml and
# delivery/work-history/m3-p10-notes.md are treated as CLAIMS TO REFUTE:
# nothing below rests on them, and every statement carries a command this
# lane ran in this container.
#
# CONTRACT ORDER. Started from the twelve declared hazard classes H10.1
# through H10.12 and from the dispatch attack directions, BEFORE reading the
# acceptance criteria. The criteria were walked LAST.
#
# DATABASE SAFETY. The ambient DATABASE_URL in this container points at a
# deployed pooler for a DIFFERENT project of the owner's. Every command
# below that could reach a database ran with DATABASE_URL and DIRECT_URL
# pinned in the invoking shell to an invented localhost string, and the
# parsed host was printed and read 127.0.0.1 before the first gate ran.
#
# NO DATA. No account number, no counterparty, no amount and no date from
# any real document appears in this file.
#
# EDIT-NOTHING. Nothing of the head's code, tests or documents was changed.
# git status carries exactly one untracked file, this document.
#
# ==========================================================
# ATTACK DIRECTIONS (progress board, updated as the review runs)
# ==========================================================
# A1  busy state that NEVER CLEARS                                PROBED, see P5/P6
# A2  busy state that NEVER APPEARS: hydration delay, listener
#     attaching after the press, a submit control outside the
#     mechanism                                                  not-yet-probed
# A3  double submission while busy (H10.5)                       not-yet-probed
# A4  the busy signal LYING: shown for an action that already
#     failed or already completed                                not-yet-probed
# A5  interaction with the M3-P9 machinery                        PROBED, see P9/P10/P11
# A6  reduced-motion behaviour                                   PROBED, see P8
# A7  client boundary width and import closure (H10.2)           PROBED, see P1/P2
# A8  the phase's own e2e spec going VACUOUS through project
#     membership, testIgnore or prod-only projects               not-yet-probed
# A9  the hidden counterpartyText field (H10.7) and the copy
#     gate glob (H10.10)                                         not-yet-probed
# A10 scope discipline: new dependency, new stylesheet rule
#     (H10.6)                                                    not-yet-probed
# A11 gates and build run by this lane with DB env pinned        not-yet-probed
#
# ==========================================================
# PROBES, in the order run (output captured at run time)
# ==========================================================
# P1. THE CLIENT FILE COUNT ON THE DEPLOYED HEAD.
#       grep -rln "use client" src/
#     returns FIVE paths, but ONE OF THEM IS A COMMENT AND NOT A DIRECTIVE.
#     CORRECTED IN PLACE rather than rewritten (clause R-087): this lane's
#     first pass read the grep as five client components and was WRONG.
#     src/app/layout.tsx carries the phrase at line 25, inside M3-P9's
#     comment "WHY A SCRIPT AND NOT A CLIENT COMPONENT ... a \"use client\"
#     file would open ...", with six non-comment lines above it. The
#     re-run that settles it:
#       for f in $(grep -rl "use client" src/); do head -1 "$f" |
#         grep -q '"use client"' && echo DIRECTIVE: $f || echo MENTION: $f;
#       done
#         DIRECTIVE: src/modules/accounts/ui/account-setup-form.tsx
#         DIRECTIVE: src/platform/ui/submit-button.tsx
#         DIRECTIVE: src/platform/ui/link-pending.tsx
#         DIRECTIVE: src/platform/ui/nav-link.tsx
#         MENTION:   src/app/layout.tsx
#     So FOUR files open the boundary, which is exactly what the work
#     history's third deviation claims, and the claim survives the check.
#     Criterion 10.6(b)'s src/app/ and layout.tsx clauses are MET. What is
#     not met is "all of them under src/platform/ui/" in 10.6(a) and the
#     module-ui clause of 10.6(b), both on account-setup-form.tsx alone.
#     SIDE OBSERVATION, recorded because a later phase will re-run it: the
#     criterion names `grep -rln` as its own instrument, and that
#     instrument returns FIVE on this tree because it cannot tell a
#     directive from a comment about one. The phase replaced it with a
#     directive-aware closure walk in the fast gate, which is the better
#     instrument; the criterion's text was never updated to say so.
#
# P2. THE SAME QUESTION AT THE PHASE BASE.
#       git grep -ln "use client" 785e611^ -- src/
#     returns src/app/layout.tsx (the comment again),
#     src/modules/accounts/ui/account-setup-form.tsx and
#     src/platform/ui/nav-link.tsx, so TWO real client files existed before
#     this phase and one of them was already under a module's ui/
#     directory. 10.6(a)'s "all of them under src/platform/ui/" and the
#     module-ui half of 10.6(b) were ALREADY FALSE before this phase
#     touched anything: the criterion was written against 89ed187 and
#     M3-P14 landed under it. What the phase itself ADDED is two files,
#     both under src/platform/ui/.
#
# P3. THE COMMIT AND ITS FILE SET.
#       git show --stat --oneline 785e611
#     M3-P10 is commit 785e611 (2026-08-26), sixteen files. FOUR are
#     outside the plan's files-to-touch:
#       src/modules/accounts/ui/account-setup-form.tsx
#       src/modules/accounts/ui/accounts-screen.tsx
#       src/modules/import/ui/import-result.tsx
#       src/modules/import/ui/import-status-line.tsx
#     d7db0ff follows it with the attribution note.
#
# P4. THE SUBMIT CONTROL INVENTORY ON THE DEPLOYED HEAD.
#       grep -rn "<SubmitButton\|type=\"submit\"" src/
#     NINE submit controls, not the seven the plan enumerates: the seven
#     plus src/modules/accounts/ui/accounts-screen.tsx:137 (through the
#     leaf) and src/modules/accounts/ui/account-setup-form.tsx:291 (a raw
#     button, NOT through the leaf, carrying its own transition pending
#     flag). The plan's set is stale for the same reason P2 is.
#
# P5. THE ONLY ASSERTION THAT THE BUSY STATE EVER CLEARS IS STRUCTURALLY
#     VACUOUS.
#       grep -n 'aria-busy' test/e2e/busy-state.spec.ts
#     returns four lines. Line 302 asserts aria-busy IS present at 1000ms.
#     Line 444 is the ONLY assertion that it is gone:
#       await expect(page.locator('[aria-busy="true"]')).toHaveCount(0);
#     commented "Criterion 10.3, second half: the busy state does not
#     survive the response." It sits at the END of the nine-press sequence
#     in test one, immediately after the SIGN-IN submit (spec lines 412
#     through 419), whose server action redirects into the application
#     shell. By the time the locator is evaluated the document has been
#     REPLACED and contains no submit control that was ever pressed, so the
#     assertion cannot fail whatever the leaf does. Criterion 10.3 asks for
#     this "for each of the seven submit controls"; it is checked for none
#     of them. The one case it nominally covers is the one where a full
#     navigation guarantees the answer.
#
# P6. THE ONE SUBMIT CONTROL OUTSIDE THE LEAF HAS AN UNCAUGHT ASYNC
#     TRANSITION.
#       sed -n '145,155p' src/modules/accounts/ui/account-setup-form.tsx
#     is
#       onSubmit={(event) => { event.preventDefault();
#         const data = new FormData(event.currentTarget);
#         startTransition(async () => {
#           setState(await registerAccountsAction(INITIAL, data));
#         }); }}
#     with disabled={pending} and aria-busy={pending ? "true" : undefined}
#     at :291 through :295. There is no catch and no finally. The busy and
#     disabled state of this control is therefore tied to a promise whose
#     rejection path is unwritten, and NOTHING in the suite presses it and
#     then fails the action: busy-state.spec.ts presses it once on the happy
#     path (spec line 353) and the only clears-assertion is P5's vacuous one.
#
# P7. E2E PROJECT MEMBERSHIP IS SOUND, which is recorded because it is the
#     defect class this project has hit twice.
#       cat playwright.config.ts
#     busy-state.spec.ts is matched by chromium (testIgnore /prod-smoke/,
#     so not excluded), by chromium-phone (testMatch names busy-state
#     explicitly, config line 109) and by chromium-phone-prod (testMatch
#     /busy-state\.spec\.ts/, config line 136). It is NOT prod-only and
#     carries no test.skip, no project-conditional branch and no
#     fixme:
#       grep -n "test.skip\|test.fixme\|test.fail" test/e2e/busy-state.spec.ts
#     returns nothing. The spec is three tests, all unconditional. This
#     attack direction produced no finding.
#
# P8. REDUCED MOTION IS HANDLED AT THE TOKEN.
#       grep -rn "prefers-reduced-motion" src/ styles/
#     one block, styles/tokens.css:273, redefining --duration-state: 0s and
#     --duration-busy-cycle: 0s at :root. src/app/globals.css:1706 spends
#     --duration-busy-cycle as the animation duration of the busy mark and
#     :1716 spends --duration-state as the unconfirmed marking's transition,
#     so both stand still under the preference with no variant of their own.
#     M3-P9 already measures this (test/e2e/pressed-and-disabled.spec.ts
#     :1937 and :1950 run the same control at both settings). This attack
#     direction produced no finding against M3-P10.
#
# ==========================================================
# BROWSER PROBES. Playwright chromium 1194 driven by playwright-core over
# page.setContent, with the head's REAL stylesheet concatenated in its own
# import order (styles/tokens.css, styles/pulse-theme.css,
# src/app/globals.css, the @import lines stripped). No server, no database:
# these answer DOM and computed-style questions only. Scripts .probe.mjs,
# .probe2.mjs, .probe3.mjs, run from the worktree and NOT committed.
# CAVEAT recorded honestly: Tailwind's preflight is not in the
# concatenation, so a property the app's own rules do not set is the UA
# default rather than the preflight default. Every property read below IS
# set by the app's own rules.
# ==========================================================
#
# P9. THE M3-P9 DISABLED RULE BEATS THE M3-P9 BUSY RULE ON EVERY PROPERTY
#     THEY SHARE, AND M3-P10 IS WHAT MAKES THEM COLLIDE.
#     src/platform/ui/submit-button.tsx:98 and :99 set BOTH
#     disabled={pending} and aria-busy="true" on the same element. In
#     src/app/globals.css the disabled rule (:1590 through :1601) is
#     `button:disabled` (specificity 0,1,1) and the busy rule (:1687
#     through :1693) is `[aria-busy="true"]` (0,1,0), so the disabled rule
#     wins. Measured on a real button carrying the shipped
#     .merchant-name-button class, node .probe.mjs:
#       at rest        opacity 1     cursor pointer  bg oklch(0.23 0.008 265)
#                                                    color oklch(0.986 0.004 85)
#       busy+disabled  opacity 0.55  cursor DEFAULT  bg oklch(0.91 0.005 265)
#                                                    color oklch(0.7 0.007 265)
#       aria-busy only opacity 0.7   cursor progress bg oklch(0.23 0.008 265)
#     So NO submit control in the product ever renders the busy appearance
#     M3-P9 designed. Every one of them renders the DISABLED appearance:
#     --color-surface-disabled, --color-ink-disabled, cursor default. The
#     "aria-busy only" row is what the stylesheet's author expected and it
#     is unreachable through the shipped leaf.
#
# P10. THE BUSY MARK IS BELOW ANY VISIBILITY THRESHOLD ONCE THE DISABLED
#      RULE HAS WON. The mark is drawn by [aria-busy="true"]::after with
#      background-color: currentColor (globals.css:1695 through :1708), so
#      its colour is whatever `color` resolved to, which P9 shows is
#      --color-ink-disabled. It then sits inside an element painted at
#      opacity 0.55, and its own opacity is animated 0.25 to 1 by
#      @keyframes pulse-busy-cycle (globals.css:1737).
#      node .probe3.mjs, resolving every colour through a canvas 2D context
#      so the numbers are real sRGB rather than a parse of an oklch string,
#      and compositing in the order the browser paints:
#        page background            rgb(252,250,247)
#        button background          rgb(223,225,229)   element opacity 0.55
#        mark background            rgb(156,158,163)
#        button as painted          rgb(236,236,237)
#        mark at animation low  0.25 rgb(227,227,228)  contrast 1.09:1
#        mark at animation mid  0.5  rgb(218,218,219)  contrast 1.19:1
#        mark at animation peak 1.0  rgb(199,199,201)  contrast 1.43:1
#      Against the 3:1 floor for a meaningful non-text graphic the mark
#      never gets close, at any point in its cycle. What the owner sees on
#      a press is the control going flat pale grey, which is the product's
#      own vocabulary for UNAVAILABLE, with no perceptible motion in it.
#
# P11. A PRESS DESTROYS KEYBOARD FOCUS. Measured in chromium,
#      node .probe.mjs "focus" block: focus a button, then set
#      disabled = true, which is exactly what
#      src/platform/ui/submit-button.tsx:98 does on every press:
#        activeElement before  focusme
#        activeElement after   BODY
#        still tabbable        false
#      Nothing in the leaf, in any caller, or in the spec restores focus.
#      The merchant naming submit is the worst case because it is the one
#      the owner presses most AND the one whose action revalidates in place
#      rather than navigating: after each name, focus is on document.body
#      and the next row must be reached by tabbing from the top of the
#      document again. aria-busy on an unfocused, disabled control is
#      announced to nobody, so the phase delivers no acknowledgement at all
#      on the assistive-technology path.
#
# P12. THE aria-busy ROUTE TAKES NOTHING AWAY, AS M3-P9's COMMENT WARNED,
#      and the leaf is right not to rely on it. node .probe.mjs
#      "ariaBusyOnlyStillActivates": an element carrying only
#      aria-busy="true" computes pointer-events: auto and delivers a click.
#      The leaf uses the NATIVE disabled attribute instead, which refuses
#      activation from pointer and keyboard alike, so hazard H10.5 is
#      genuinely closed for the eight leaf-rendered submits. No finding on
#      the double-submission direction.
#
# P13. THE NAVIGATION MARKER HAS A REAL BOX AND DOES NOT ESCAPE ITS LINK.
#      node .probe2.mjs at 390 by 844: the .pulse-busy span inside a
#      .month-nav Link measures 18 by 10 CSS pixels, its ::after 10 by 10,
#      and markRight 35.59 against linkRight 46, so criterion 10.5's
#      "bounding rect whose width and height are both greater than zero" is
#      satisfiable and the mark stays inside the 46 by 46 tap target. Its
#      colour there is oklch(0.46 0.008 265) on the page surface, which is
#      the link's own ink and is legible. No finding.
#      Recorded as a side observation rather than a finding: a busy submit
#      control grows from 64.36 to 82.36 CSS pixels wide when the mark
#      appears, so a press reflows the row it sits in.
#
kind: verdict
phase: M3-P10
verdict: IN-PROGRESS
produced-by: claude-family
framing: destructive-paths
review-contract: hazard
findings: []
criteria: []
deviations-judged: []
hazard-classes-addressed: []
