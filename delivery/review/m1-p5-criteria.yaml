kind: verdict
phase: M1-P5
verdict: APPROVE
# DR-0003 family escalation, stated in the header as the pilot record
# requires: this review and its hazard-contract sibling are BOTH
# Claude-family. DR-0012 condition 1 (two model families) is discharged
# for this pilot under DR-0003's "same family, varied lenses" decision:
# the pair must differ in review-contract (this one is criteria) and in
# framing (this one is criteria-contract). Any merge decision must check
# the sibling verdict differs on both fields.
produced-by: claude
framing: criteria-contract
review-contract: criteria

# Clean-room CRITERIA review of phase M1-P5 (the two-sided month view),
# THREE ROUNDS, all by execution in this fresh clone. Round 1 approved
# head d21ec71 (code head af759b3); round 2 the fix-round delta
# d21ec71..0254277 (code head 2323645); round 3 the micro-round delta
# 0254277..c7b6507, and this verdict now stands at head c7b6507 (code
# head 8789bf9). The implementation session was never seen in any
# round.
#
# ROUND 3 (micro) DELTA SHAPE, verified myself: git log
# 0254277..c7b6507 is 8789bf9 then c7b6507; git diff --stat
# 8789bf9..c7b6507 is the two delivery/work-history files only (168
# insertions), so the round's code head is 8789bf9; the merge into
# this review branch differs from c7b6507 only by delivery/review
# files. The code delta is 5 files, 48 insertions, 4 deletions: ONE
# ON-clause change in src/modules/overview/adapters/overview-repository.ts:62-64
# (the in-transit partner join now carries p."householdId" =
# t."householdId", closing CR-507), the uninterpretedCause remedy copy
# reworded truthfully in EN, NL and FR (CR-508: the old copy advised
# re-uploading the file, which dedup makes a no-op; the new copy says
# Pulse counts the rows when interpretation next covers the period),
# and two SQL-shape tests in test/application/month-overview.test.ts:197-235
# (the household filter pinned BY NAME on the partner join, and every
# JOIN in the repository required to carry a householdId predicate).
#
# ROUND 3 HONESTY NOTE, stated plainly per this contract's rules: the
# household-unfiltered partner read that CR-507 fixes was INTRODUCED
# in fix round 1 and sat inside code my round 2 walk approved; my
# round 2 record repeated round 1's "householdId filter present in
# every overview query" verification without re-checking the NEW join.
# The hazard sibling caught it. No harm was constructible today (links
# are written only within a household), but the letter of
# non-negotiable 6 was broken and my round 2 did not see it. The new
# by-name test now guards exactly that spot.
#
# ROUND 3 SCOPE, audited over the WHOLE phase range: git diff
# --name-only b54783b..c7b6507 is 41 files and a scripted audit found
# ZERO outside the declared envelope (files-to-touch, the extras at
# cf8d772, the fix-round extras amendment 95c7241 on main, and the
# standing work-history extras). 95c7241 declares exactly the four
# paths finding CR-506 named, so CR-506 is RESOLVED (see the finding).
#
# ROUND 3 EXECUTED CHAIN at the merged head (code-identical to
# 8789bf9), on a FRESH from-zero database (my call, stated: db:reset
# EXIT=0, 5 migrations from scratch, seeded; the round 2 database was
# discarded), five env values pinned local: npm run typecheck EXIT=0;
# npm run lint EXIT=0; npm run gate:tokens EXIT=0; npm test EXIT=0 (20
# files, 249 passed, 0 failed, 0 skipped: the two new SQL-shape tests
# over round 2's 247); npm run test:e2e EXIT=0, 12 passed (2.5m),
# fresh webServer. Matches the re-recorded DR-0001 evidence at 8789bf9
# in delivery/work-history/m1-p5.yaml. No criterion regressed: the
# golden journey, partial month, gapped, unresolved, in-transit,
# cancelling-gaps and locale specs all green unmodified.
#
# ROUND 3 CATALOGS (criterion 4.4): parity re-measured 101/101/101
# with identical key sets after the reword. The reworded
# uninterpretedCause is surfaced by NO e2e fixture (nothing creates a
# flow-NULL row through the UI), so beyond the locale e2e (green, 12
# passed) I verified the new ICU strings directly: IntlMessageFormat
# parses AND formats uninterpretedCause in en, nl and fr for both
# plural branches (count 1 and 3), so the copy cannot be a render-time
# ICU error waiting on the first uninterpreted row. Em-dash sweep over
# messages, src, test: EXIT=1, zero hits.
#
# ROUND 2 DELTA SHAPE, verified myself: origin/claude/m1-p5-month-view
# fetched; git log d21ec71..0254277 is 180581c, 554c7d6, 776c21d,
# 2323645, 0254277; git diff --stat 2323645..0254277 is the two
# delivery/work-history files only (404 insertions), so the round's
# code head is 2323645; the merge of 0254277 into this review branch
# (99524b4) differs from 0254277 only by delivery/review files. The
# code delta (19 files, 918 insertions, 43 deletions) rewrites the
# reconciliation verdict semantics (reconciles now requires difference
# zero AND zero unmatched, unresolved, in-transit and uninterpreted
# counts, src/modules/overview/domain/month-projection.ts:208-213),
# adds the in-transit cause (matched legs whose partner books outside
# the period, the inTransitExists predicate in
# src/modules/overview/adapters/overview-repository.ts:59-70, resolving
# the partner THROUGH the link so a mirror-less settlement debit stays
# unmatched, never in transit) and the uninterpreted cause (the figures
# WHERE drops its flow filter; every named sum, changeInPot and
# rowCount now filter on flow themselves, so interpreted figures are
# unchanged, which my unchanged round 1 totals below re-witness), guards
# the fixed clock against production (fixedNowOverride throws under
# isProduction, src/platform/config.ts:73-80, pinned by
# test/app/fixed-clock.test.ts), sets reuseExistingServer false
# (playwright.config.ts:37, closing this review's round 1 finding,
# register id CR-505), adds 28 fast-gate tests and two e2e specs with
# four fixtures, corrects two R-087 false comments loudly in place, and
# records the CR-501 sibling rule at the ledger reconcile definition
# (verified comment-only: the reconciliation.ts hunk is 14 comment
# lines, no behaviour change).
#
# ROUND 2 INDEPENDENT ARITHMETIC on the new fixtures, from bytes:
# mv-transit August income 1.500,00, spend 0,00, pot 1.200,00,
# difference -300,00 carried entirely by the straddling leg (booked
# 30/08, partner 02/09, 3 days apart, inside the ledger's 4-day pairing
# window so the pair MATCHES); September difference +300,00. mv-cancel:
# legs 05/08 and 25/08 are 20 days apart, outside the window, so two
# genuine unmatched legs whose amounts cancel to a zero difference over
# real gaps. Both agree with the spec's claimed values. The partition
# claim (difference = unresolved + unmatched + in-transit cents) is
# sound by construction: fully-inside matched pairs net zero, so the
# in-transit sum equals the whole matched-INTERNAL sum.
#
# ROUND 2 EXECUTED CHAIN at the merged head (code-identical to
# 2323645), from a from-zero database, five env values pinned local:
# npm run typecheck EXIT=0; npm run lint EXIT=0; npm run gate:tokens
# EXIT=0; npm test EXIT=0 (20 files, 247 passed, 0 failed, 0 skipped);
# npm run db:reset EXIT=0 (5 migrations from scratch, seeded); npm run
# test:e2e EXIT=0, 12 passed (2.5m) on a fresh webServer
# (reuseExistingServer now false), including the two new specs and all
# prior-phase suites. Catalogs re-measured 101/101/101 keys (the 98
# plus reconNoteGaps, inTransitCause, uninterpretedCause) with
# identical key sets; em-dash sweep over messages, src, test EXIT=1.
# This matches the re-recorded DR-0001 evidence in
# delivery/work-history/m1-p5.yaml (247 and 12 at head 2323645) figure
# for figure.
#
# ROUND 2 RED SPOT-CHECK on the NEW verdict logic, executed fresh:
# reconciles weakened back to difference === 0 alone in
# month-projection.ts reddened BOTH layers, the cancelling-gaps e2e
# (data-state received ok where broken expected, the exact dangerous
# state, EXIT=1) and 3 fast-gate tests in
# test/domain/month-projection.test.ts (EXIT=1); restored, tree clean.
#
# ROUND 2 CRITERIA RE-WALKED where the delta touches them: 4.1, 4.3 and
# 4.6 carry round 2 evidence inside their entries below; 4.2, 4.4 and
# 4.5 were re-executed inside the same round 2 chain (partial-month
# test green under the fixed clock, three locales green with the new
# longer copy, lint and gate:tokens EXIT=0 at the new head) with no
# semantic change to their subject matter.
#
# ROUND 1 RECORD BELOW, kept as written at d21ec71. Round 1's
# release-verification code head was af759b3; verified then that git
# diff --stat af759b3..d21ec71 adds only
# delivery/work-history/m1-p5-notes.md (+48) and
# delivery/work-history/m1-p5.yaml (+629), no code.
#
# INDEPENDENT ARITHMETIC, from the committed fixture bytes by hand,
# before reading any implementation. gj-current.csv, gj-pot-b.csv,
# gj-card.csv give August 2026: income 2.500,00 (row 0401 alone); spend
# 12,50 + 86,47 + 950,00 + 20,00 + 450,00 + 250,00 + 150,00 = 1.918,97;
# the 300,00 transfer pair (rows 0405 and 0210) internal on both sides;
# the 850,00 settlement pair (row 0406 and the card credit) excluded on
# both sides, with the card debits summing exactly 850,00; reserves
# 0,00 (no reserve row exists in any fixture); pot change (+301,03
# current) + (+280,00 pot B) + (0 card) = 581,03; identity 2.500,00
# minus 1.918,97 minus 0,00 = 581,03, difference zero. July baseline:
# income 2.400,00 (delta +100,00) and Supermarkt 95,20 against 86,47
# (delta -8,73). Every one of the implementer's claimed totals
# reconfirmed independently; no disagreement to flag. The month-view
# fixtures likewise: mv-partial 2.500,00 / 157,00 / 2.343,00;
# mv-gapped 2.000,00 / 150,00 / 1.450,00 with difference -400,00;
# mv-unresolved 1.000,00 / 200,00 / 800,00 plus one 0,00 row.
#
# EXECUTED CHAIN at the head, from a from-zero database, the five env
# values pinned to the local stack per fleet warnings 1 and 6 (the
# Prisma consent guard satisfied per the m1-p1 precedent after the repo
# guard itself printed "both connection strings target the local
# stack", target 127.0.0.1:54322 local docker):
#   npx prisma generate EXIT=0; npm run typecheck EXIT=0;
#   npm run lint EXIT=0; npm run gate:tokens EXIT=0;
#   npm test EXIT=0 (17 files, 219 passed, 0 failed, 0 skipped);
#   npm run db:reset EXIT=0 (all 5 migrations applied from scratch,
#   seed green with the paginated auth check per warning 7);
#   npm run test:e2e EXIT=0, 10 passed in 2.0m: auth x3 (M1-P1),
#   import (M1-P2), merchants (M1-P4), golden-journey, month-view x4.
# That one run IS the DR-0001 release verification re-executed by this
# reviewer: fast gate plus the Playwright golden journey, green on one
# commit, and it also carries the four prior phases' e2e suites green
# (regression). The 219-test fast gate carries every prior phase's unit
# and property suite.
#
# RED SPOT-CHECKS, three fresh mutations on load-bearing view logic,
# each restored and the tree left clean:
#   A total folded wrong: sumGroups dropping its first group reddened
#     the golden journey (income-total 0,00 vs 2.500,00,
#     golden-journey.spec.ts:135, EXIT=1).
#   The partial-month guard: partial forced false in month-overview.ts
#     reddened the partial test (in-progress-badge absent,
#     month-view.spec.ts:68, EXIT=1).
#   The recon difference hidden: the recon-difference row removed from
#     month-view.tsx reddened the gapped test (month-view.spec.ts:112,
#     EXIT=1).
#
# WHAT THIS CONTRACT DID NOT REACH, said per the contract's own clause:
# this review walked and executed the six acceptance criteria plus the
# two carry-ins and three mutations; it is one input and never a
# terminal green. Not reached here: the hazard-contract probes (the
# sibling review's job); the historical red witnesses at intermediate
# commits (0ec6235's view-less red run is the implementer's captured
# claim, corroborated but not re-executed; my three mutations establish
# the same assertions' teeth independently at the head); any
# deploy-verify against the deployed project (out of this phase's
# criteria); and a non-zero reserves month end to end, which the
# implementer's own open question M1P5-C10 records as impossible in the
# current product surface.

# VALIDATION RECORD, measured: npx --prefix /home/user/pulse-fleet
# tiphys validate --type verdict (Node v26.7.0) with a PRIVATE context
# dir carrying plan.yaml (delivery/plan/pulse-v1.yaml), the M1-P5
# work-history.yaml, charter.yaml and assurance-modes.yaml, run against
# this file present under the context's delivery/review, reddens on
# EXACTLY ONE check, dual-review-decorrelation ("only 1 verdict
# document(s) exist under delivery/review for phase M1-P5, and a
# delegated grant requires two independent clean-room reviews of the
# exact head"): the hazard verdict is produced in a parallel worktree
# and is not visible from this clone, and once both exist the
# produced-by dimension still cannot
# pass truthfully because both M1-P5 clean-room reviews are
# Claude-family (DR-0003). Exactly the m1-p2 and m1-p4 precedent: this
# verdict records produced-by: claude honestly and ESCALATES the
# family-decorrelation failure to the orchestrator, which discharges it
# under DR-0003 by checking review-contract and framing differ across
# the pair. Schema, verdict-criteria-complete and
# verdict-deviations-judged all pass (no other violation is reported).

criteria:
  - id: "4.1"
    quote: >-
      npm run test:e2e exits 0 with test/e2e/golden-journey.spec.ts
      passing end to end as a three-file journey: sign in, upload the
      current-account fixture (profile 1), declare and confirm, upload
      the second pot account fixture containing the other transfer leg,
      upload a card fixture (profile 2: no counterparty-account column,
      no sequence numbers, line items plus the settlement mirror row),
      open the month view, assert card line items are counted as spend
      exactly once, assert the settlement pair is excluded from both
      sides, and assert income, spend, reserves and pot-change totals
      against the fixtures' known values with the reconciliation line
      reading zero difference.
    evidence:
      - >-
        EXECUTED: npm run test:e2e EXIT=0, 10 passed (2.0m), on head
        d21ec71 (code-identical to af759b3), after npm run db:reset
        EXIT=0 applied all 5 migrations from scratch; the golden journey
        passed in 21.6s as test 4 of the run.
      - >-
        The spec is the journey the criterion names: sign-up at
        test/e2e/golden-journey.spec.ts:87-91, three uploads (8, 2, 4
        rows) at lines 94-126 with gj-card.csv carrying no
        counterparty-account column and no sequence numbers plus the
        850,00 C mirror row, month view at line 129.
      - >-
        Counted exactly once: test/e2e/golden-journey.spec.ts:143-148
        asserts PIZZA NAPOLI and ELEKTRO CITY each appear as exactly ONE
        spend group at the line-item amount. Excluded from both sides:
        lines 154-156 assert the whole main region contains no 850,00,
        no MASTERCARD and no 300,00, and line 159 pins income to the one
        salary group.
      - >-
        Totals against fixture-derived values: lines 135-139 assert
        2.500,00 / 1.918,97 / 0,00 / 581,03, which I re-derived by hand
        from the fixture bytes before reading any implementation and
        they agree digit for digit; lines 177-185 assert the recon panel
        in state ok with NO difference figure rendered.
      - >-
        Teeth verified fresh: mutation A (sumGroups dropping one group in
        src/modules/overview/domain/month-projection.ts:134-135) made
        this spec fail at golden-journey.spec.ts:135 with EXIT=1;
        restored, green.
      - >-
        ROUND 2, delta head 2323645: the spec is byte-unchanged in the
        delta and passed again (test 4 of the 12-pass EXIT=0 run from a
        from-zero database). Its month contains no straddling pair
        (transfer legs booked 10/08 and 11/08, settlement pair both
        20/08, all inside August) and no flow-NULL row, so under the
        TIGHTENED verdict (ok now requires zero unmatched, unresolved,
        in-transit and uninterpreted counts) the golden journey still
        reads data-state ok with zero cause blocks, which its own
        assertions at test/e2e/golden-journey.spec.ts:177-185 pin. The
        totals are untouched by the delta: every named sum already
        filtered on flow, verified in the round 2 run by the identical
        2.500,00 / 1.918,97 / 0,00 / 581,03 pass.
    met: true
  - id: "4.2"
    quote: >-
      The partial current month renders the in-progress state and no
      comparison, asserted in test/e2e/month-view.spec.ts with a fixed
      clock mid-month.
    evidence:
      - >-
        EXECUTED green inside the same EXIT=0 e2e run (test 7, 9.2s).
        The fixed clock is real and in-repo: playwright.config.ts sets
        PULSE_FIXED_NOW=2026-09-15T12:00:00Z on the webServer, parsed
        only by fixedNowOverride in src/platform/config.ts:59-69,
        consumed only by appClock in src/platform/clock.ts:24-27,
        resolved per request.
      - >-
        The assertions cover both halves: in-progress badge and 15 / 30
        days meta at test/e2e/month-view.spec.ts:68-70, and NO
        comparison as compare-na visible with ZERO group-delta and ZERO
        spend-delta elements at test/e2e/month-view.spec.ts:74-76.
        Structurally the previous month is not even fetched for a
        partial month (src/modules/overview/application/month-overview.ts:106).
      - >-
        Teeth verified fresh: mutation B (const partial = false in
        src/modules/overview/application/month-overview.ts:79) made the
        test fail at month-view.spec.ts:68 with EXIT=1; restored, green.
    met: true
  - id: "4.3"
    quote: >-
      With a deliberately gapped fixture, the view shows the unexplained
      difference in the alarm treatment and names unmatched legs,
      asserted in test/e2e/month-view.spec.ts; and with a fixture
      containing a transaction no rule can classify, the view renders it
      as a visible UNRESOLVED gap, its amount appears in no income,
      spend or reserves total, and the reconciliation difference names
      it, asserted in the same file.
    evidence:
      - >-
        EXECUTED green inside the EXIT=0 run (tests 8 and 9). Gapped
        half: recon-panel data-state broken with difference -400,00 at
        test/e2e/month-view.spec.ts:110-112, and the alarm treatment
        asserted as the COMPUTED colour of the verdict equalling the
        resolved --color-alarm token via a live probe element at
        test/e2e/month-view.spec.ts:117-131, not a class-name check.
        The cause names 1 transfer leg with amount, counterparty
        Demobank Plus and account at lines 135-142, and the totals
        exclude the leg (2.000,00 / 150,00 / 1.450,00, lines 145-147),
        matching my hand derivation from mv-gapped-a.csv and
        mv-gapped-b.csv.
      - >-
        Unresolvable half: the 0,00 KAARTVERGOEDING row from
        mv-unresolved.csv renders as a visible unresolved-gap row named
        by the panel's cause at test/e2e/month-view.spec.ts:166-172,
        appears in no group (lines 175-180), and the totals hold
        1.000,00 / 200,00 / 0,00 / 800,00 (lines 160-163). The
        zero-amount witness is the only UNRESOLVED shape the shipped
        classifier can produce, which the work history states openly;
        the non-vacuous excluded-from-totals witness for the same
        mechanism is the gapped test's 400,00 leg.
      - >-
        Teeth verified fresh: mutation C (the recon-difference row
        removed from src/modules/overview/ui/month-view.tsx:284-286)
        made the gapped test fail at month-view.spec.ts:112 with EXIT=1;
        restored, green.
      - >-
        ROUND 2, delta head 2323645: both halves re-executed green
        under the TIGHTENED semantics (tests 8 and 9 of the 12-pass
        EXIT=0 run; spec lines shifted to test/e2e/month-view.spec.ts:113
        and :165). The gapped case is unchanged in meaning (non-zero
        difference, alarm colour probe, named leg, excluded totals).
        The unresolved case is STRONGER than round 1: the spec now also
        asserts data-state broken with NO difference figure
        (test/e2e/month-view.spec.ts:179-180), so "Books close" can no
        longer render above the listed 0,00 gap; executed green, and my
        round 2 mutation (reconciles weakened to the bare residual)
        reddened exactly this family (cancelling-gaps e2e received ok
        where broken expected, EXIT=1, plus 3 unit tests), then
        restored. The two new sibling specs (in-transit named cause at
        :204, cancelling gaps at :245) both executed green with
        fixture arithmetic I re-derived from bytes.
    met: true
  - id: "4.4"
    quote: >-
      All three locales render the month view without truncation or
      layout overflow, asserted per locale in
      test/e2e/month-view.spec.ts; message catalogs for en, nl, fr load
      via next-intl; and the ESLint rule react/jsx-no-literals (or an
      equivalent named lint rule, the chosen name recorded in the phase
      work history) is enabled for src/modules/**/ui with npm run lint
      exiting 0.
    evidence:
      - >-
        EXECUTED green inside the EXIT=0 run (test 10, 16.0s): per
        locale, translated title and verdict asserted, zero horizontal
        document overflow and zero clipped elements across the view's
        non-wrapping selectors at test/e2e/month-view.spec.ts:202-243.
      - >-
        Catalogs load via next-intl: src/i18n/request.ts:20-27 resolves
        the cookie locale and imports messages/<locale>.json; parity
        measured myself with a key-flattening script: 98 keys per
        catalog counting arrays as one leaf (103 counting array items),
        identical key SETS across en, nl, fr, zero missing, zero extra.
      - >-
        The named rule is enabled where the criterion says:
        eslint.config.mjs:34-36 sets react/jsx-no-literals to error for
        src/modules/**/ui/**/*.tsx, and the chosen name is recorded in
        the work history (delivery/work-history/m1-p5.yaml:160-172).
        npm run lint EXIT=0 at the head.
      - >-
        Both directions: a bare text node added to
        src/modules/overview/ui/month-view.tsx made npm run lint EXIT=1
        with the error naming react/jsx-no-literals; restored, EXIT=0.
      - >-
        ROUND 2, delta head 2323645: catalogs grew to 101/101/101 (the
        three new cause keys in every locale), identical key sets; the
        locale e2e re-ran green over the longer copy inside the 12-pass
        EXIT=0 run; lint EXIT=0 at the new head.
      - >-
        ROUND 3, micro head 8789bf9: parity still 101/101/101 with
        identical key sets after the CR-508 reword of
        uninterpretedCause in all three locales. The reworded key is
        surfaced by no e2e fixture (no UI path creates a flow-NULL
        row), so beyond the green locale e2e I parsed and formatted the
        new ICU strings directly with IntlMessageFormat in en, nl and
        fr for both plural branches (count 1 and 3): all render, so no
        ICU error waits on the first uninterpreted row. lint EXIT=0.
    met: true
  - id: "4.5"
    quote: >-
      grep -rE "oklch\(|#[0-9a-fA-F]{3,8}|font-size:\s*[0-9.]+px" over
      src/modules/**/ui exits 1 (tokens only), and the npm script that
      wraps it as a gate exits 0.
    evidence:
      - >-
        EXECUTED both halves: the raw grep over src/modules/*/ui EXIT=1
        (zero hits) and npm run gate:tokens EXIT=0 at the head. The
        script's src/modules/*/ui path and the criterion's
        src/modules/**/ui expand identically under the npm sh (globstar
        off), and find shows every ui directory sits exactly at
        src/modules/<module>/ui, so the executed surface is the
        criterion's whole surface.
      - >-
        Teeth verified fresh: a #ff0000 style literal appended to
        src/modules/overview/ui/index.ts made npm run gate:tokens EXIT=1
        printing the hit; restored, EXIT=0.
    met: true
  - id: "4.6"
    quote: >-
      npm run typecheck && npm run lint && npm test exit 0 at the phase
      head, and the release-verification candidate (npm test plus npm
      run test:e2e) is green on the same commit, recorded as evidence
      for DR-0001.
    evidence:
      - >-
        EXECUTED by this reviewer on one tree: at d21ec71, whose code is
        byte-identical to release head af759b3 (git diff --stat
        af759b3..d21ec71 is two work-history files only), npm run
        typecheck EXIT=0, npm run lint EXIT=0, npm test EXIT=0 with 219
        passed 0 failed 0 skipped, npm run gate:tokens EXIT=0, and npm
        run test:e2e EXIT=0 with 10 passed 0 skipped, the database reset
        from zero first. This record is itself DR-0001 release-
        verification evidence: golden journey plus fast gate, green on
        the same commit, independently re-executed.
      - >-
        The declared verification was not weakened anywhere: both e2e
        suites ran in full, no test skipped, no filter applied in the
        recorded head run.
      - >-
        ROUND 2, delta head 2323645: the full chain re-executed by this
        reviewer on the merged tree (code-identical to 2323645), from a
        from-zero db:reset (EXIT=0, 5 migrations): npm run typecheck
        EXIT=0, npm run lint EXIT=0, npm run gate:tokens EXIT=0, npm
        test EXIT=0 (247 passed, 0 failed, 0 skipped), npm run test:e2e
        EXIT=0 (12 passed, 0 skipped, fresh webServer). The re-recorded
        DR-0001 evidence in delivery/work-history/m1-p5.yaml (247 fast,
        12 e2e, typecheck/lint/gate:tokens 0, all at head 2323645)
        matches my executions figure for figure. The declared
        verification was strengthened, not weakened: two e2e specs and
        28 fast-gate tests were added, none removed, none skipped.
      - >-
        ROUND 3, micro head 8789bf9: the full chain re-executed on the
        merged tree (code-identical to 8789bf9), on a FRESH from-zero
        database (db:reset EXIT=0, 5 migrations, seeded): npm run
        typecheck EXIT=0, npm run lint EXIT=0, npm run gate:tokens
        EXIT=0, npm test EXIT=0 (249 passed, 0 failed, 0 skipped: the
        two new SQL-shape tests over round 2's 247), npm run test:e2e
        EXIT=0 (12 passed, 0 skipped, fresh webServer, 2.5m). Matches
        the DR-0001 evidence re-recorded at 8789bf9 in
        delivery/work-history/m1-p5.yaml. No criterion regressed:
        every round 1 and round 2 spec passed unmodified.
    met: true

findings:
  - id: CR-505
    severity: low
    evidence:
      - >-
        ROUND 1 (raised in this verdict as local id CR-501, renamed here
        to the fleet register id CR-505 after the fix-round disposition
        assigned that number; same finding, no content change):
        playwright.config.ts at d21ec71 set reuseExistingServer: true.
        In this fleet's shared container, several worktrees can run npm
        run dev on port 3000; a stale server started WITHOUT
        PULSE_FIXED_NOW=2026-09-15T12:00:00Z (or from another worktree's
        code) would be silently reused by the whole e2e gate, and every
        month assertion would then depend on the wall clock and on
        foreign code. Verified port 3000 was free before my round 1
        runs, so those executions were unaffected.
      - >-
        ROUND 2 RESOLUTION, verified: playwright.config.ts:37 now sets
        reuseExistingServer: false with the reason recorded at the
        setting, and my round 2 e2e run booted a fresh webServer (12
        passed, EXIT=0). RESOLVED.
    concrete-fix: >-
      Applied in fix round 1 (commit 776c21d): reuseExistingServer
      false, so the release-verification gate always boots the server
      whose env and code it pinned. Nothing further needed.
    analysis: >-
      Round 1 analysis kept for the record: a stale real-clock server
      failed loudly in August 2026 but would fail slowly and confusingly
      from 2026-10-01. Low because the gate could not silently pass end
      to end either way, and no criterion text was violated.
  - id: CR-506
    severity: low
    evidence:
      - >-
        Fix round 1 changed four paths that are on neither the plan's
        files-to-touch nor its declared extras as of origin/main
        cf8d772: src/modules/ledger/domain/reconciliation.ts (14
        comment-only lines recording the CR-501 sibling rule),
        test/app/fixed-clock.test.ts, test/application/month-overview.test.ts
        and test/domain/month-projection.test.ts (the 28 new fast-gate
        tests). The work history's files-touched list at
        delivery/work-history/m1-p5.yaml names all four honestly, but
        its deviations array still carries exactly ONE entry (the round
        1 six-file deviation, delivery/work-history/m1-p5.yaml:283-307,
        whose text does not mention these paths), and no plan amendment
        on main declares them, unlike the round 1 precedent commit
        cf8d772. Measured: git show origin/main:delivery/plan/pulse-v1.yaml
        lists eight extras for M1-P5, none of them these four; grep -c
        plan-clause on the work history returns 1.
      - >-
        Content assessed: all four changes strengthen verification or
        record a mandated sibling rule; nothing behavioural shipped
        outside the declared surface. The gap is the missing
        declaration, not the work.
      - >-
        ROUND 3 RESOLUTION, verified: commit 95c7241 on main ("plan:
        declare M1-P5 fix-round extras before the scope gate") adds
        exactly the four named paths to the plan's M1-P5 extras, and my
        round 3 scripted scope audit over the whole phase range (git
        diff --name-only b54783b..c7b6507, 41 files) found zero files
        outside the declared envelope. RESOLVED.
    concrete-fix: >-
      Applied as this finding asked: the plan's M1-P5 extras amended on
      main at 95c7241, mirroring cf8d772, so DR-0012 condition 5 is
      satisfiable truthfully. Nothing further needed.
    analysis: >-
      Round 2 analysis kept for the record: low and mergeable as a
      tracked item; the escalate-then-amend mechanism existed and cost
      one commit, which is exactly what happened.

deviations-judged:
  - deviation: >-
      M1-P5 files-to-touch (src/modules/overview/, src/app/,
      test/e2e/golden-journey.spec.ts, test/e2e/month-view.spec.ts,
      test/fixtures/, messages/en.json, messages/nl.json,
      messages/fr.json; extras test/application/resolve-merchants.test.ts)
    serves-plan-intent: true
    reasoning: >-
      Six files outside the declared list were touched and each is
      criterion-forced, which I verified independently: criterion 4.4's
      lint rule can only live in eslint.config.mjs, criterion 4.5's gate
      script only in package.json, criterion 4.2's deterministic clock
      needs playwright.config.ts plus src/platform/config.ts and
      src/platform/clock.ts (env reads are confined to the config module
      by the repo's own convention), and the cash-destination rule
      (M1P4-C7) needs the ledger's published pattern list
      (src/modules/ledger/application/index.ts,
      src/modules/ledger/domain/constants.ts) precisely so the overview
      does not duplicate it. The escalation landed before the scope
      gate: commit cf8d772 on main ("plan: declare M1-P5 deviation
      extras before the scope gate") adds exactly those seven lines to
      delivery/plan/pulse-v1.yaml, and git diff --name-only
      b54783b..d21ec71 shows every changed file inside files-to-touch
      plus the declared extras plus the standing work-history extras.
      The deviation widens the declaration, weakens no verification, and
      is what the plan's intent (the criteria themselves) requires.
      ROUND 2 note: the fix round's four additional undeclared paths are
      NOT covered by this declared deviation and no new deviation was
      declared for them; that gap is finding CR-506 above, judged low,
      and this entry's judgement is unchanged by it.
