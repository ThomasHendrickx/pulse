# IN PROGRESS. Clean-room criteria review, M3-P12, round 6.
# Beacon file: appended as work proceeds, per clause incremental-output.
type: verdict
phase: M3-P12
contract: criteria
round: 6
head: f57a048
worktree: /home/user/wt-m3p12-c6
branch: claude/m3-p12-rev6-crit
status: in-progress
started: 2026-08-24
lane-sequence: |
  THIS LANE RAN AFTER ITS SIBLING, NOT BESIDE IT. The orchestrator composed this
  brief and prepared this worktree but did not dispatch the lane, so the hazard
  lane reviewed head f57a048 alone and its verdict had already reached the
  implementer before this lane started. The dual-review contract for this round
  was therefore executed as a single lane and then repaired by running this lane
  late against the same head. A reader must not read the two documents as two
  concurrent independent reviews.
  Because the ordering had already spent the independence that concealment
  protects, this lane was permitted to read the sibling verdict, but only AFTER
  forming and writing its own view of each criterion. Every place this lane
  agrees with the sibling is marked explicitly so agreement can be told apart
  from inheritance.
progress-log:
  - step: read the brief, the phase contract and all 23 acceptance criteria
  - step: worktree had no node_modules; npm ci started
gates:
  - command: npm run typecheck
    exit: 0
    summary: "tsc --noEmit, no output"
  - command: npm run lint
    exit: 0
    summary: "eslint ., no output"
  - command: npm test
    exit: 0
    summary: "Test Files 42 passed (42), Tests 648 passed (648), Duration 14.55s"
  - command: npm run gate:privacy
    exit: 0
    summary: "gate:privacy clean"
  - command: npm run gate:tokens
    exit: 0
    summary: "grep negation over src/modules/*/ui, no match"
  - command: npm run test:mutants
    exit: 0
    summary: |
      M1 red on FIRST biconditional; M2 red on FIRST; M3 red on FIRST;
      M5 red on FIRST and SECOND; M4 red on FIRST and SECOND.
      restored: original 33f7f7fee0ddf710, on disk 33f7f7fee0ddf710.
      git status clean after the run.
  - command: npm run test:e2e
    exit: NOT RUN
    summary: "not witnessed; see criterion 12.14"
  - note: |
      Every invocation was run with DATABASE_URL and DIRECT_URL PINNED to an
      unreachable loopback value, never the ambient environment, which points
      at a foreign Supabase project. No database was created and none was used:
      the fast gate is in-process with no network, and the mutant harness runs
      only that one property file. Nothing was dropped because nothing was made.
control-experiment:
  claim: |
    The implementer claims M5, deleting the same-kind conjunct from the
    claimant lookup, reddens two properties WITH the new cross-kind generator
    dimension and is not caught at all without it.
  method: |
    A reviewer probe applied M5's exact edit to
    src/modules/merchants/application/rederive-rules.ts and, for half B, also
    removed the string "cross-kind-pattern-collision" from the scenarios list
    in test/property/rederive-loss-reporting.test.ts, then ran the property
    file and the 59-test example file. Both files were restored and both
    checksums matched (src 33f7f7fee0ddf710, property 13d4bca44a4659ab), and
    git status was clean afterwards.
  half-a-with-the-dimension: |
    Tests 2 failed (2). RED ON BOTH.
    FIRST: AssertionError: expected { row: 'descriptorRow', lost: false } to
    deeply equal { row: 'descriptorRow', lost: true }
    SECOND: AssertionError: expected 'PREFIX' to be 'EXACT'
    The 59 example tests: 59 passed (59). They do not catch it either way.
  half-b-without-the-dimension: |
    Property file: Tests 2 passed (2). GREEN.
    Example file: Tests 59 passed (59). GREEN.
    M5 is NOT CAUGHT AT ALL, by anything.
  half-c-control-for-the-control: |
    With no mutant and the dimension still removed the property file is
    Tests 2 passed (2), so half B's green is the mutant surviving and not the
    generator having been broken by the edit.
  verdict: |
    REPRODUCED IN BOTH HALVES, exactly as claimed. This is the argument the
    round wanted and it holds: a missing generator DIMENSION is a defect in
    the instrument, not a missing count. Six shape counts were all greater
    than zero in half B while a wrong predicate walked through, which is the
    clearest available demonstration that a count is not a discriminator.
findings: []
