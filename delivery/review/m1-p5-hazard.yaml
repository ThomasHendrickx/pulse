# Clean-room HAZARD review of phase M1-P5 (the two-sided month view).
# HEAD UNDER REVIEW: c7b6507 (micro round for the round 1 lows; last
# code commit 8789bf9, verified by this review: git diff 8789bf9
# c7b6507 over src, test, prisma, package.json and messages is empty,
# the top commit is work-history and notes only). History: round 0
# reviewed d21ec71 (code head af759b3), FIX-ROUND-NEEDED with CR-501
# high, CR-502 medium, CR-503 medium, CR-504 low (CR-505 was the
# criteria reviewer's low, consolidated). Round 1 reviewed 0254277
# (code head 2323645), APPROVE: all five dispositions verified by
# re-executed construction (the straddling pair named in transit and
# summing into the difference, cancelling gaps refusing ok, the
# flow-NULL row counted and verdict-blocking, the production clock
# refusing loudly, 28 new fast-gate tests, reuse off), with two fresh
# lows filed: CR-507 (the in-transit partner join read the
# transactions table with no householdId filter of its own) and the
# false uninterpreted-remedy copy (filed by this review as CR-506,
# renumbered CR-508 in the fleet's consolidated findings; both ids
# name the same defect and the mapping is recorded here honestly).
# THIS REVISION is the round 2 micro-scope delta pass over
# 0254277..c7b6507: both lows re-verified by execution, the tiny
# delta swept for fresh hazards, the fast gates re-run at the head.
# Round 1's full text lives in this file's git history (commit
# 45b1936); round 0's earlier still (commit 322a2ef and prior).
# Reviewer clone: worktrees/m1-p5-review-hazard (review branch
# rebased onto c7b6507); no implementation session seen in any round.
# Framing: destructive-paths, contract: hazard. Database off-limits
# throughout; the e2e run (12 passed at 8789bf9) is relied on from
# the micro-round gate-evidence, named where used.
#
# DR-0003 DECORRELATION ESCALATION, unchanged from rounds 0 and 1:
# this verdict is produced by the Claude family, as is the paired
# criteria verdict. DR-0012 condition one is NOT met by family; it is
# discharged for the pilot under DR-0003 (same family, varied
# lenses): review-contract and framing are distinct across the pair.
#
# RESOLUTION LEDGER, micro round.
# - CR-507 (low, round 1): RESOLVED at 8789bf9 and verified by
#   execution. The in-transit partner join now carries
#   p."householdId" = t."householdId" in its ON clause AHEAD of the
#   id match (overview-repository.ts:62-65), restoring the letter of
#   non-negotiable 6. EXECUTED PROBES (R2, captured in the work log):
#   the committed source matches the test's by-name pin; the PRE-FIX
#   shape reconstructed in memory (filter removed, the tree never
#   edited) REDDENS the pin, so the witness is real. THE HONESTY NOTE
#   JUDGED, as the dispatch asked: the implementer states the generic
#   every-JOIN sweep was green even pre-fix because its 300-character
#   window reaches the link's own l."householdId" predicate, so the
#   by-name pin is the actual CR-507 witness and the sweep only a
#   backstop for future joins. VERIFIED TRUE BY EXECUTION: this
#   review ran the sweep's exact logic against the reconstructed
#   pre-fix shape and it stays green (6 joins, zero offenders), while
#   the by-name pin reds. The note is accurate, correctly identifies
#   the weak instrument, and keeps the red-first claim honest: the
#   recorded red (1 failed | 7 passed pre-fix, notes m1) is
#   consistent with exactly the by-name pin failing. This is the
#   R-087 discipline applied by the implementer to their own test
#   suite, and it is the right call.
# - CR-508 (low, round 1, this review's CR-506): RESOLVED at 8789bf9
#   by making the copy true rather than making the false copy work.
#   EXECUTED PROBES: the false remedy phrasing ("Upload the file
#   again" / "Laad het bestand opnieuw" / "Déposez à nouveau le
#   fichier") is ABSENT from all three catalogs; the new
#   covers-the-period promise is present in all three; key parity
#   101/101/101 (all captured). THE NEW COPY'S TRUTH judged against
#   the path this review traced in round 1: "Pulse includes them when
#   interpretation next covers the period" is exactly true, since
#   window interpretation loads pot transactions by household,
#   account and date with NO flow filter
#   (ledger-repository.ts:28-48, re-verified this round), so a
#   flow-NULL row inside any later window is re-stamped by
#   replaceInterpretation; the trailing "for example after the next
#   import touching this account" is a hedged example, true at
#   monthly cadence (the next import's span padded 49 days back
#   covers the prior month) with an inherent residue when the next
#   covering import lags beyond the padding, which the wording's
#   "for example" does not overpromise. No R-087 violation remains.
#   THE SCOPING CALL JUDGED, as the dispatch asked: declining the
#   re-upload-heals behaviour change and handing it on as open
#   question M1P5-C21 (work-history m1-p5.yaml:1060-1072) is sound.
#   The heal would edit the import module's upload path, outside this
#   phase's files-to-touch and beyond a micro round's mandate; the
#   defect this review filed was the FALSE CLAIM, which is gone; the
#   defect state itself stays visible and verdict-blocking either
#   way (round 1 probes D-4), recovery exists organically, and the
#   option is recorded for the orchestrator rather than dropped.
#   Concur.
#
# FRESH-HAZARD SWEEP OF THE MICRO DELTA, what was pressed and what
# came back. The whole code delta is four SQL lines, two test blocks
# and three catalog sentences (git diff 0254277 8789bf9, read in
# full). The SQL change is purely additive filtering: for
# same-household data, which is all data the committed write paths
# can produce, the in-transit predicate's result set is unchanged, so
# every round 1 probe conclusion (partition exactness, disjoint
# buckets, clean months staying ok) carries forward unmodified; no
# behaviour surface beyond the join filter and the copy was touched;
# the new SQL-shape tests read the adapter source read-only and
# introduce no runtime path. Nothing new found.
#
# WORK LOG, ROUND 2.
# - Fetched the branch; verified 0254277..c7b6507 shape and that
#   8789bf9..c7b6507 is code-empty; rebased the review branch onto
#   c7b6507.
# - Read the whole micro delta (repository ON clause, the SQL-shape
#   test suite, three catalogs, notes m1-m3, the work-history
#   micro-round records including M1P5-C21).
# - Executed probe suite R2 (node, over the committed source and an
#   in-memory pre-fix reconstruction; ALL R2 PROBES PASS). Captured:
#   R2-1/R2-1b the committed join carries the filter ahead of the id
#   match and satisfies the by-name pin; R2-2b the pin REDDENS
#   against the pre-fix shape; R2-3 the generic sweep stays green on
#   the pre-fix shape (joins=6, offenders=[]), confirming the honesty
#   note; R2-4 false phrasing absent, new promise present, parity
#   101/101/101 across en, nl, fr.
# - Re-verified the copy's truth anchor: listPotTransactions filters
#   household, accounts and dates only, no flow filter, so covering
#   windows re-stamp flow-NULL rows.
# - Fast gates executed at the head in this clone: npm test exit 0
#   (249 passed, 20 files: the two SQL-shape tests over the round 1
#   count of 247), npm run typecheck exit 0, npm run lint exit 0,
#   npm run gate:tokens exit 0, matching the micro-round
#   gate-evidence. npm run test:e2e relied on: 12 passed exit 0 at
#   8789bf9, same commit as the fast gate, DR-0001 evidence
#   re-recorded there; the declared verification was not weakened.
# - Assembled this round 2 verdict; validated schema plus Kind B
#   checks in the fresh private context dir as in prior rounds (the
#   dual-review pairing red against a single committed verdict is
#   DR-0003's narrowing recorded honestly; produced-by stays honest).
#
# WHAT WAS LOOKED FOR AND NOT FOUND in the micro delta, so the next
# reader can spend the budget elsewhere: a semantic change to the
# in-transit predicate for reachable data (none: additive filter,
# round 1 probes carry forward); a catalog regression (none: parity
# held, plural ICU shapes intact in all three locales, no em dash);
# any new runtime surface (none: the new tests are read-only over
# source text); any weakening of the declared release verification
# (none: fast gate and e2e re-recorded green on the same commit
# 8789bf9).

kind: verdict
phase: M1-P5
verdict: APPROVE
produced-by: claude
framing: destructive-paths
review-contract: hazard

findings: []

criteria:
  - id: "4.1"
    quote: >-
      npm run test:e2e exits 0 with test/e2e/golden-journey.spec.ts
      passing end to end as a three-file journey: sign in, upload the
      current-account fixture (profile 1), declare and confirm,
      upload the second pot account fixture containing the other
      transfer leg, upload a card fixture (profile 2: no
      counterparty-account column, no sequence numbers, line items
      plus the settlement mirror row), open the month view, assert
      card line items are counted as spend exactly once, assert the
      settlement pair is excluded from both sides, and assert income,
      spend, reserves and pot-change totals against the fixtures'
      known values with the reconciliation line reading zero
      difference.
    evidence:
      - >-
        Established in round 0 (spec read in full, fixture arithmetic
        re-derived independently) and untouched by both fix deltas:
        neither diff touches golden-journey.spec.ts or its fixtures,
        and the golden month still satisfies the tightened reconciles
        (its pairs sit fully inside August). Relied on for execution:
        the micro-round gate-evidence at 8789bf9 (npm run test:e2e 12
        passed, exit 0, env pinned, reuseExistingServer false).
    met: true
  - id: "4.2"
    quote: >-
      The partial current month renders the in-progress state and no
      comparison, asserted in test/e2e/month-view.spec.ts with a
      fixed clock mid-month.
    evidence:
      - >-
        Round 0 structural finding stands (no previous-month read for
        a partial month, month-overview.ts untouched since); pinned
        in the fast gate by test/application/month-overview.test.ts
        (executed green in this clone inside the 249-test run);
        hardened by the round 1 production refusal (re-executed
        there) and reuse-off. E2e execution relied on (12 passed at
        8789bf9).
    met: true
  - id: "4.3"
    quote: >-
      With a deliberately gapped fixture, the view shows the
      unexplained difference in the alarm treatment and names
      unmatched legs, asserted in test/e2e/month-view.spec.ts; and
      with a fixture containing a transaction no rule can classify,
      the view renders it as a visible UNRESOLVED gap, its amount
      appears in no income, spend or reserves total, and the
      reconciliation difference names it, asserted in the same file.
    evidence:
      - >-
        Round 1 standing and untouched by the micro delta: the
        unresolved test asserts the broken state, the in-transit and
        cancelling-gap constructions are pinned in e2e
        (test/e2e/month-view.spec.ts:204-262), and this review's
        round 1 probes D-1, D-2, D-3 executed the derivation and
        view logic. E2e execution relied on (12 passed at 8789bf9).
    met: true
  - id: "4.4"
    quote: >-
      All three locales render the month view without truncation or
      layout overflow, asserted per locale in
      test/e2e/month-view.spec.ts; message catalogs for en, nl, fr
      load via next-intl; and the ESLint rule react/jsx-no-literals
      (or an equivalent named lint rule, the chosen name recorded in
      the phase work history) is enabled for src/modules/**/ui with
      npm run lint exiting 0.
    evidence:
      - >-
        Round 1 standing; the micro delta touches one key per
        catalog, verified this round by executed probe R2-4: parity
        101/101/101, ICU plural shapes intact, the false remedy gone
        and the true promise present in all three locales. npm run
        lint executed by this review at the micro-round head: exit
        0. Locale rendering execution relied on (the locale test runs
        inside the 12-passed suite).
    met: true
  - id: "4.5"
    quote: >-
      grep -rE "oklch\(|#[0-9a-fA-F]{3,8}|font-size:\s*[0-9.]+px"
      over src/modules/**/ui exits 1 (tokens only), and the npm
      script that wraps it as a gate exits 0.
    evidence:
      - >-
        Executed by this review at the micro-round head: npm run
        gate:tokens exit 0. The micro delta touches no styled
        surface.
    met: true
  - id: "4.6"
    quote: >-
      npm run typecheck && npm run lint && npm test exit 0 at the
      phase head, and the release-verification candidate (npm test
      plus npm run test:e2e) is green on the same commit, recorded as
      evidence for DR-0001.
    evidence:
      - >-
        Executed by this review in this clone at the micro-round
        head: npm run typecheck exit 0, npm run lint exit 0, npm test
        exit 0 (249 passed, 20 files), npm run gate:tokens exit 0,
        matching the micro-round gate-evidence. The e2e half relied
        on: DR-0001 evidence re-recorded at 8789bf9 with npm test 249
        and npm run test:e2e 12 passed on the same commit (notes m3);
        the declared verification was not weakened.
    met: true

deviations-judged:
  - deviation: >-
      M1-P5 files-to-touch (src/modules/overview/, src/app/,
      test/e2e/golden-journey.spec.ts, test/e2e/month-view.spec.ts,
      test/fixtures/, messages/en.json, messages/nl.json,
      messages/fr.json; extras test/application/resolve-merchants.test.ts)
    serves-plan-intent: true
    reasoning: >-
      Judged in rounds 0 and 1 and unchanged as a declaration: the
      six files beyond the declared list are each the minimal
      enabling change for a declared criterion and all appear in the
      plan phase's own extras list. The fix rounds' two files beyond
      that (the comment-only sibling rule in
      src/modules/ledger/domain/reconciliation.ts and the test/app/
      directory) were demanded by the consolidated findings'
      dispositions and remain noted for the orchestrator's scope
      audit; the micro round itself stays entirely inside the
      declared surface (the overview adapter, one declared test
      file, the three catalogs, work-history files). The plan's
      intent is served.

hazard-classes-addressed:
  - class-id: H4.1
    probed: >-
      The partial current month compared against a full month.
      Rounds 0 and 1 established the structural skip, the executed
      Brussels-day and year-boundary probes, and the executed
      production-clock refusal; the micro delta touches none of
      those paths (verified by reading the whole diff), and the
      fast-gate pins covering them ran green inside this round's
      executed 249-test gate.
    cleared-because: >-
      No path at this head can compare a partial month or freeze the
      current-month clock silently; the micro delta does not reach
      the mechanism and the pins covering it stayed green under
      execution.
  - class-id: H4.2
    probed: >-
      Hidden unknowns making totals lie. Round 1 re-executed every
      round 0 construction against the fixed tree (straddle named in
      transit and summing into the difference, cancelling gaps
      refusing ok, the flow-NULL row counted, listed and
      verdict-blocking, the combined-shape partition exact); the
      micro round's two residues from that pass are now closed and
      re-verified by execution: the partner join carries the tenancy
      filter with a by-name red-able pin (probes R2-1 to R2-3, the
      pre-fix shape reconstructed in memory reddens the pin), and
      the uninterpreted cause's remedy copy is true against the
      committed interpretation path in all three locales (probe
      R2-4; the no-flow-filter window read re-verified at
      ledger-repository.ts:28-48). The declined heal behaviour is
      recorded as open question M1P5-C21 rather than dropped, and
      the defect state remains visible and verdict-blocking with or
      without it.
    cleared-because: >-
      Every construction this class produced across three rounds now
      comes back with the unknown NAMED, counted in no total, and
      blocking the ok verdict; the two low residues are fixed and
      witnessed red-first, and the micro delta introduces no new
      member of the class (additive filter, truthful copy, read-only
      tests).
  - class-id: H4.3
    probed: >-
      Hardcoded user-facing strings. The micro delta's only copy
      change is the uninterpretedCause rewording, verified present
      through the catalogs in all three locales with parity
      101/101/101 (executed probe R2-4); npm run lint
      (react/jsx-no-literals active over module UI) executed exit 0
      at the micro-round head; no view file changed.
    cleared-because: >-
      The reworded copy resolves through the catalogs in all three
      locales, the lint gate is green under execution, and the round
      1 truth defect in that copy is resolved, leaving no known
      false or hardcoded user-facing string.
  - class-id: H4.4
    probed: >-
      Literal colours or sizes. Executed npm run gate:tokens at the
      micro-round head (exit 0); the micro delta contains no style,
      class or view change.
    cleared-because: >-
      The executed token gate is clean at the micro-round head and
      the delta touches no styled surface.
