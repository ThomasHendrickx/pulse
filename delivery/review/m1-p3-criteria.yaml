kind: verdict
phase: M1-P3
verdict: APPROVE
produced-by: claude
framing: criteria-contract
review-contract: criteria

# Clean-room criteria review, TWO ROUNDS, both by execution in this fresh
# clone, never adopting the implementer's claims. All database commands
# pinned to the local supabase stack at 127.0.0.1:54322 and 127.0.0.1:54321
# per fleet environment warning 1; the ambient foreign (Hemma) credentials
# were verified present in the shell and never used. Warning 6 honored in
# both rounds: all five values (DATABASE_URL, DIRECT_URL,
# NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY,
# SUPABASE_SERVICE_ROLE_KEY) pinned in the shell that invoked the e2e gate.
#
# ROUND 1 (original review): base main 6fc43c9 to head f274d30. Verdict at
# that head: APPROVE, zero findings. Gate record, all executed by this
# reviewer: npm ci EXIT=0; the literal chain npm run typecheck && npm run
# lint && npm test EXIT=0 (15 files, 141 passed, 0 failed, 0 skipped);
# npm run db:reset is blocked for an AI agent by the Prisma 6.19 consent
# guard, so the equivalent fresh-database route was taken (CREATE DATABASE
# pulse_review_p3 on the local stack, npx prisma migrate deploy EXIT=0
# applying all three migrations from scratch including
# 20260819152307_transfer_links, migrate status clean, seed EXIT=0), then
# npm run test:e2e EXIT=0 (4 passed, 34.3s). Live RLS verified by psql on
# that from-zero database: relrowsecurity=t for all eight application
# tables including transfer_links. Round 1 red spot-checks, eight reviewer
# mutations, each applied to committed code, run, captured, restored,
# restore re-run green: RM1 declared-set precedence swapped (1 failed,
# the exact both-sets test); RM2 pairing content sort removed (EXIT=1, 3
# failed); RM3 unmatched internal legs dropped from surfacing (EXIT=1, 2
# failed: the 500-case property AND the criterion 2.5 exclusion test); RM4
# re-parse persisting the old spec (EXIT=1, 1 failed); RM5 CR-208 guard
# removed (EXIT=1, 3 failed); RM6 CR-209 collision guard removed (EXIT=1,
# 2 failed); RM7 interpretation window padding removed (EXIT=1, healing
# and recompute failed); RM8 no-match settlement debit left UNRESOLVED
# (EXIT=1, 2 failed). Charter stop-for conformance verified by reading the
# shipped functions beside the plan steps: classification order,
# pairing rules, the four corrections' meanings and the reconciliation
# identity all match the plan's specification exactly; no escalatable
# semantic deviation found. Regression from M1-P2 re-executed green
# (import suites 78 passed, catalogs 93/93/93 identical key sets, zero em
# dashes in added lines, no literal colour or px, integer cents
# everywhere, householdId plus RLS on the one new table, facts tables
# untouched by the migration).
#
# ROUND 2 (fix round 1 delta, regression-focused): f274d30..c3f566a, six
# commits; verified myself that the delta's last code commit is be49a82
# and the two commits above it touch only delivery/work-history/ (git
# diff be49a82..c3f566a --stat: two work-history files). The fix round
# was driven by the sibling hazard review (CR-301..CR-306); this
# criteria-contract round confirms no approved criterion regressed.
# DELTA GATES, all executed by this reviewer at c3f566a: npm run
# typecheck EXIT=0, npm run lint EXIT=0, npm test EXIT=0 (15 files, 152
# passed, 0 failed, 0 skipped), the literal chain npm run typecheck &&
# npm run lint && npm test EXIT=0; fresh database rebuilt from zero a
# second time (CREATE DATABASE pulse_review_p3r2, npx prisma migrate
# deploy EXIT=0 all three migrations, seed EXIT=0, pinned local), npm run
# test:e2e EXIT=0 (4 passed, 31.3s, all five values pinned). NOTHING
# WEAKENED: the delta is purely additive on the test side; git diff shows
# ZERO deleted lines in any criterion test file, and classify-flow,
# pair-transfers and the reconciliation property test are byte-identical
# to the approved round 1 files (numRuns 500 intact). Every round 1 test
# is still present and green inside the 152 (per-suite re-runs:
# classify-flow 13, pair-transfers 9, corrections 16, property 1,
# interpret 5, reparse 6, profile-detection 39, each EXIT=0).
# DELTA RED SPOT-CHECKS on the NEW code, five reviewer mutations, each
# applied, run, captured, restored, restore green:
#   D1 settlement exclusivity removed (an allocated card import can be
#      allocated again): EXIT=1, 3 failed (all three CR-306 tests);
#      restored 16 passed.
#   D2 card accounts loaded through the window bounds again (CR-301's
#      dangerous state): EXIT=1, 1 failed (the settled debit flips);
#      restored 5 passed.
#   D3 refund history window-scoped again (CR-303's dangerous state):
#      EXIT=1, 1 failed (January refund classifies INCOME under the June
#      window); restored green.
#   D4 CR-305 guard removed and Math.abs restored in the indicator arm:
#      EXIT=1, 2 failed; restored 39 passed.
#   D5 cross-import key accumulation removed from the re-parse re-key
#      (CR-302's mechanism defanged): EXIT=1, both CR-302 witnesses
#      failed; restored 6 passed.
# CR-304 (re-parse status downgrade) verified by read plus the committed
# fake-based witness: the real adapter moves affected imports INTERPRETED
# to INGESTED inside the SAME prisma transaction as the facts rewrite
# (src/modules/import/adapters/import-repository.ts:278-291), the fake
# mirrors exactly that semantics (test/application/fake-import-world.ts:205-213),
# and the dying-interpret test asserts the visible marker; forcing a real
# database death mid-repair is the hazard contract's lane.
# DELTA SEMANTICS AGAINST THE PLAN, stop-for list re-checked: the
# exclusive settlement allocation strengthens D-11's "paired to that card
# import" reading (a statement is settled once; the loser of an
# exact-cent coincidence stays honest aggregate SPEND, the same arm
# criterion 2.8 mandates, never INTERNAL, never UNRESOLVED); the
# scope-free refund history makes a window run and a recompute agree on
# the plan's refund rule rather than changing it; CR-305 extends CR-208's
# fail-loud discipline to the third directional shape; the corrected
# padding comment at src/modules/ledger/domain/constants.ts:33-42 states
# loudly (R-087-conformant) that the old claim about what the padding
# protects was wrong. No correction's meaning, no flow semantics and no
# part of the reconciliation identity changed; nothing on the stop-for
# list was touched.
# DELTA HYGIENE re-executed: zero em dashes in the delta's added lines
# (grep count 0 over git diff f274d30..c3f566a), no literal colour, px,
# rgb or hsl in added src lines, catalogs untouched by the delta, no new
# table and no migration in the delta, integer cents preserved (the new
# unbounded history read selects counterparty text fields only).
# WORK-HISTORY DELTA judged: the declared deviation list is UNCHANGED
# (still exactly one, judged below; every delta file falls inside
# files-to-touch plus the extras main ea5fc03 declares). New claims
# M1P3-C10 and M1P3-C11 cross-checked against my own executions: the six
# fixes' teeth were reproduced by D1..D5 today (guard removal reddens the
# committed witnesses), and C11's fake unique-index enforcement is what
# D5's reds abort through. Gate evidence in the work history (152 fast, 4
# e2e at be49a82) matches this reviewer's runs at c3f566a exactly, and
# the code trees of be49a82 and c3f566a are identical.
#
# DECORRELATION RECORD PER DR-0003, stated rather than faked: this verdict
# truthfully records produced-by claude. Both M1-P3 clean-room reviews are
# dispatched on the Claude model family, so the model-family dimension of
# the dual-review decorrelation check CANNOT pass truthfully; that failure
# is ESCALATED to the orchestrator here, per DR-0003's pilot rule, which
# discharges condition one through distinct review-contract and framing
# values instead (criteria-contract/criteria here; the hazard brief's
# framing and contract in the sibling worktree). Recording any other
# family would be a false claim.
#
# WHAT THIS CONTRACT DID NOT REACH, stated rather than summarised as
# completeness: no deployed-environment behaviour (deploy-verify is a
# later stage); the real PC-banking CSV layouts remain unverified
# (M1P2-C1 still open); the settlement window value 45 and the pattern
# lists are grounded in two observed formats only (M1P3-C7, honestly
# open); deep concurrency and crash probing (forced database races, kill
# mid-transaction against the real adapter) is the hazard contract's
# lane, and CR-304's death-window behaviour was verified here against the
# mirrored fake plus a read of the real adapter's single transaction, not
# a forced real crash; the criterion 2.7 clause "merchant rules ...
# intact" is VACUOUS on this base (grep for MerchantRule over src/ and
# prisma/ finds nothing; the tables arrive with M1-P4), so its
# merchant-rules half was not evaluable by execution and is covered by
# the whole-declaration-layer assertions the tests do make, exactly as
# the work history discloses. "All acceptance criteria met" below is a
# statement about this phase's contract, not a completeness claim about
# the artifact.
#
# VALIDATION RECORD, measured at round 2: npx tiphys validate --type
# verdict (Node v26.7.0) against a reviewer-built context directory
# (never a shared scratch dir) carrying plan.yaml
# (delivery/plan/pulse-v1.yaml at this head), the M1-P3 work-history.yaml
# at c3f566a, charter.yaml, assurance-modes.yaml and this file under
# delivery/review. The schema, verdict-criteria-complete and
# verdict-deviations-judged all pass; the run reddens on EXACTLY ONE
# check, dual-review-decorrelation, because only one M1-P3 verdict is
# visible from this clone (the hazard verdict is produced in a parallel
# worktree). Two further measurements, both captured: (1) with a second
# M1-P3 verdict present in a scratch copy of the context, this document
# validates at exit 0, so the single-verdict residue masks nothing; (2)
# a shared scratch directory collision put the sibling's REAL hazard
# verdict beside this one during an earlier validation attempt, and that
# accidental full-pair run reddened on EXACTLY the produced-by dimension
# (claude in 2 of 2 verdicts) while framing and review-contract
# decorrelated, which is precisely the DR-0003 escalation recorded above,
# never a family faked to make a check green. The final validation was
# re-run in a fresh PRIVATE context directory per the brief's
# no-shared-scratch rule.

findings: []

criteria:
  - id: "2.1"
    quote: >-
      npm test exits 0 with test/domain/classify-flow.test.ts covering all
      five flow values and the declared-set precedence (reserve set before
      pot set before sign).
    evidence:
      - "Round 1 at f274d30: npm test EXIT=0 (141 passed) and npx vitest run test/domain/classify-flow.test.ts EXIT=0 (13 passed). All five values each reachable and asserted by name (test/domain/classify-flow.test.ts:68-96, UNRESOLVED for the zero-amount row at test/domain/classify-flow.test.ts:92-95, never dropped). Precedence pinned in the code order (src/modules/ledger/domain/classify-flow.ts:57-115) and in dedicated tests including the degenerate both-sets IBAN (test/domain/classify-flow.test.ts:98-125) and the settlement-between tests (test/domain/classify-flow.test.ts:127-183). Red direction: reviewer mutations RM1 (precedence swap, the exact both-sets test failed) and RM8 (no-match settlement debit to UNRESOLVED, 2 failed), each restored green."
      - "Round 2 at c3f566a: the suite is byte-identical to the approved round 1 file (git diff empty) and re-runs green (13 passed, EXIT=0) inside npm test EXIT=0 (152 passed). The refund correction is now history-fed through an optional outgoingHistoryKeys input (src/modules/ledger/domain/interpret.ts:56-69); when absent it derives from the given transactions, so every round 1 classification assertion holds unchanged. Delta red spot-check D3 (refund history window-scoped again): the CR-303 test failed, EXIT=1, restored green, so the history feed is load-bearing and tested."
    met: true
  - id: "2.2"
    quote: >-
      test/domain/pair-transfers.test.ts asserts pairing is deterministic
      and order-independent, shuffling insertion order and asserting an
      identical pair set, including the tie-break by date distance then id.
    evidence:
      - "Round 1 at f274d30: npx vitest run EXIT=0 (9 passed); five-seed shuffle with identical pair and unmatched sets (test/domain/pair-transfers.test.ts:92-104), idempotence (test/domain/pair-transfers.test.ts:106-110), tie-break both halves (test/domain/pair-transfers.test.ts:114-134), 4-day named constant edge-tested (test/domain/pair-transfers.test.ts:140-147, src/modules/ledger/domain/constants.ts:7). Red direction: mutation RM2 (content sort removed) EXIT=1, 3 failed, restored 9 passed."
      - "Round 2 at c3f566a: the delta does not touch src/modules/ledger/domain/pair-transfers.ts and the test file is byte-identical; re-run EXIT=0 (9 passed). No regression."
    met: true
  - id: "2.3"
    quote: >-
      The four corrections each have a named passing test in
      test/domain/corrections.test.ts, one describe block per correction
      (card settlement, reserve drawdown, refund, cash withdrawal). The card
      settlement fixtures mirror the observed real shapes: the account-side
      debit carries no counterparty IBAN, names the card statement number in
      its settlement-pattern description, and its amount equals the card
      import's settlement total within the date window; the card-side
      fixture carries a positive settlement row with no counterparty account
      column; the test asserts the debit classifies INTERNAL paired to the
      card import, the card-side settlement row classifies INTERNAL as the
      mirror leg, and the card line items are the only counted spend.
    evidence:
      - "Round 1 at f274d30: npx vitest run EXIT=0 (13 passed), four describe blocks named card settlement, reserve drawdown, refund, cash withdrawal (test/domain/corrections.test.ts:63, :190, :229, :282); every fixture clause verified in the file (no counterparty IBAN on the debit, statement number in MASTERCARD AFREKENING NUMMER 42, exact line-item total, positive DOMICILIERING VIA JE BANK mirror with no counterparty field; INTERNAL both legs, line items the only counted spend with the books closing at test/domain/corrections.test.ts:128-135; window bound tested). Correction meanings match the plan exactly (drawdown negative reserve movement never income, refund SPEND with positive amount, cash its own destination never split)."
      - "Round 2 at c3f566a: settlement allocation rewritten to be exclusive per card import (finding CR-306, src/modules/ledger/domain/interpret.ts:83-129 with the extracted candidate enumeration at src/modules/ledger/domain/corrections.ts:78-104). Every round 1 assertion retained verbatim (zero deleted lines in the test file) and re-run green; three new tests pin exclusivity, the id tie-break and the two-imports allocation, suite now 16 passed EXIT=0. Delta red spot-check D1 (exclusivity removed so one card import can absorb two debits): EXIT=1, exactly the 3 CR-306 tests failed, restored 16 passed. The loser of the tie-break stays honest SPEND, the criterion 2.8 arm, so H2.1's double-count and its inverse (spend vanishing onto a settled statement) are both guarded now."
    met: true
  - id: "2.4"
    quote: >-
      test/property/reconciliation.test.ts passes: a fast-check property
      test where change in pot is computed from the facts side only, as the
      sum of raw transaction amounts over pot accounts, while income, spend
      and net-to-reserves come from the interpretation output; the
      generators produce datasets covering all five flow values, unmatched
      internal legs, and all four correction shapes; the property asserts
      the identity holds exactly when no UNRESOLVED rows or unmatched legs
      exist, and that otherwise the computed difference equals the sum of
      the surfaced gaps, in integer cents, across at least 500 generated
      cases.
    evidence:
      - "Round 1 at f274d30: npx vitest run EXIT=0 (1 passed), numRuns 500 (test/property/reconciliation.test.ts:362), facts-side changeInPot (src/modules/ledger/domain/reconciliation.ts:46-61), both identity directions asserted (test/property/reconciliation.test.ts:342-359), integer cents on all seven report figures (test/property/reconciliation.test.ts:327-337), coverage TALLIED FROM EXECUTION for all five flows and all shapes (test/property/reconciliation.test.ts:369-389). Red direction: mutation RM3 (unmatched legs unsurfaced) failed the property within the 500 runs, restored green."
      - "Round 2 at c3f566a: the property file is byte-identical (git diff empty) and re-runs green at 500 runs EXIT=0 against the rewritten exclusive-allocation engine, so the identity and its gap accounting survived the CR-306 rewrite on every generated dataset, not on chosen examples. reconciliation.ts is untouched by the delta."
    met: true
  - id: "2.5"
    quote: >-
      An unmatched internal leg is excluded from both sides and surfaced,
      asserted in test/domain/pair-transfers.test.ts; interpretation re-run
      over the period window after a second file heals it, asserted in
      test/application/interpret.test.ts.
    evidence:
      - "Round 1 at f274d30: first half at test/domain/pair-transfers.test.ts:161-186 (leg stays INTERNAL, enters neither side, difference EQUALS the leg), reddened under RM3; second half at test/application/interpret.test.ts:93-145 (two uploads, two imports, legs pair ACROSS imports with one persisted link carrying householdId), reddened under RM7 (padding removed), both restored green."
      - "Round 2 at c3f566a: both assertions retained verbatim and re-run green (interpret suite 5 passed EXIT=0). The window loading changed for CR-301: card accounts are loaded UNBOUNDED while other pot accounts stay windowed (src/modules/ledger/application/interpret-window.ts:38-56), so a later import's window can no longer truncate a card import's settlement total; the new test pins that a settled debit survives a later unrelated import's window. Delta red spot-check D2 (card load re-windowed): EXIT=1, exactly that test failed, restored 5 passed. The healing path itself is unchanged and still green; the corrected padding comment (src/modules/ledger/domain/constants.ts:33-42) now states the padding protects transfer pairing only, a loud R-087-conformant correction of the round 1 comment rather than a silent rewrite."
    met: true
  - id: "2.6"
    quote: >-
      npm run typecheck && npm run lint && npm test exit 0 and npm run
      test:e2e exits 0 at the phase head.
    evidence:
      - "Round 1 at f274d30: the literal chained command EXIT=0 (141 passed); npm run test:e2e EXIT=0 (4 passed, 34.3s) against a database built from zero (pulse_review_p3), all five env values pinned local."
      - "Round 2 at c3f566a: the literal chained command EXIT=0 in this clone (tsc clean, eslint clean, 15 files, 152 passed, 0 failed, 0 skipped); npm run test:e2e EXIT=0 (4 passed, 31.3s) against a SECOND database built from zero (pulse_review_p3r2: CREATE DATABASE, npx prisma migrate deploy EXIT=0 all three migrations, seed EXIT=0), all five env values pinned local per warnings 1 and 6. The head's two commits above be49a82 are work-history only (verified: git diff be49a82..c3f566a --stat), so the gates bear on the exact code tree at the phase head."
    met: true
  - id: "2.7"
    quote: >-
      test/application/reparse.test.ts asserts that after correcting a
      SourceProfile with a wrong amount representation, the re-parse action
      rebuilds the affected import's fact rows from stored rawLine with
      corrected amounts, without re-upload, and with account declarations,
      merchant rules and dedup behaviour intact.
    evidence:
      - "Round 1 at f274d30: npx vitest run EXIT=0 (3 passed); corrected amounts rebuilt in place with row ids and rawLines identical before and after (test/application/reparse.test.ts:84-123), declarations intact (accounts deep-equal, profile name and binding unchanged), dedup behaviour intact (re-upload adds 0 knows 5), unparseable correction rewrites nothing. Red direction: mutation RM4 (old spec persisted) EXIT=1, restored green."
      - "Round 2 at c3f566a: every round 1 assertion retained verbatim and green; the re-key now derives from the FULL re-parse of each import's rawContent with cross-import insert-ignore simulation (finding CR-302, src/modules/import/application/fix-profile.ts:85-99 and the allocation at src/modules/import/application/fix-profile.ts:150-215), fixing a mechanism whose old in-file claim the fix round names as FALSE for subset-storing imports, stated loudly in the file header per R-087 (src/modules/import/application/fix-profile.ts:12-25). New tests pin the overlap no-op and the corrected-spec convergence, both riding the fake's newly enforced per-household unique (householdId, dedupKey) index; the status discipline is new too (finding CR-304): the facts rewrite downgrades affected imports INTERPRETED to INGESTED inside the SAME transaction (src/modules/import/adapters/import-repository.ts:278-291, mirrored at test/application/fake-import-world.ts:205-213), and a dying interpret leaves the visible marker, asserted in the new death test. Suite 6 passed EXIT=0. Delta red spot-check D5 (cross-import key accumulation removed): EXIT=1, both CR-302 witnesses failed through the unique-index abort, restored 6 passed."
      - "The merchant-rules clause remains VACUOUS on this base and is reported as such rather than counted met by execution: grep for MerchantRule over src/ and prisma/ returns nothing (the tables arrive with M1-P4); every clause that exists on this base is met by execution."
    met: true
  - id: "2.8"
    quote: >-
      With a settlement-pattern debit and no matching card import inside the
      date window, test/domain/corrections.test.ts asserts the debit
      classifies SPEND, never INTERNAL and never UNRESOLVED, and enters the
      spend total exactly once; its grouping under the card issuer as
      merchant rides the normal resolver chain once M1-P4 lands.
    evidence:
      - "Round 1 at f274d30: test/domain/corrections.test.ts:137-169 asserts flow SPEND, not INTERNAL, not UNRESOLVED, spendCents exactly 85000 with the books reconciling; the outside-window variant returns the unitemised-card-spend arm. Red direction: mutation RM8 (that arm changed to UNRESOLVED, hazard H2.6's exact dangerous state) EXIT=1, 2 failed, restored green."
      - "Round 2 at c3f566a: the assertion is retained verbatim and green under the exclusive allocation, which STRENGTHENS this criterion: a debit that loses the exclusivity tie-break (an exact-cent coincidence on an already-settled statement) now falls to the SAME honest-SPEND arm, asserted in the new CR-306 tests and reddened by delta spot-check D1. The no-match path itself is unchanged (src/modules/ledger/domain/classify-flow.ts:92-95); no merchant code exists on this base (grep-verified), so nothing pre-empts the M1-P4 resolver chain."
    met: true

deviations-judged:
  - deviation: >-
      M1-P3 files-to-touch (src/modules/ledger/, src/modules/import/,
      prisma/, test/domain/classify-flow.test.ts,
      test/domain/pair-transfers.test.ts, test/domain/corrections.test.ts,
      test/property/reconciliation.test.ts,
      test/application/interpret.test.ts, test/application/reparse.test.ts)
    serves-plan-intent: true
    reasoning: >-
      Judged item by item against the diff in round 1 (git diff
      6fc43c9..f274d30, 41 files) and re-checked over the round 2 delta
      (git diff f274d30..c3f566a, 19 files), not adopted from the
      declaration. All four extra groups are necessary and minimal: the
      accounts module's listAccounts addition is forced by the
      module-boundary rule (the ledger engine reads declared accounts
      through the accounts module's published interface, never its
      tables); test/application/fake-import-world.ts is the sanctioned
      port-fake seam extended so the REAL interpret and re-parse use cases
      run in the fast gate, which criteria 2.5 and 2.7 need, and whose fix
      round addition (the enforced per-household unique dedup index)
      makes the fake STRICTER than before, never looser; the CR-208 and
      CR-209 witnesses land in test/domain/profile-detection.test.ts
      because the assigned backlog items name src/modules/import as the
      scope hook and their witnesses belong beside the existing parser
      tests (the fix round's CR-305 witnesses land there for the same
      reason); package.json and package-lock.json carry only the
      fast-check devDependency that criterion 2.4 names (no runtime
      dependency, so the charter's stop-for is untouched). The deviation
      was escalated in the notes the moment it was found, and the
      orchestrator amended the declaration on the base branch (main commit
      ea5fc03 adds exactly these five entries to the phase's extras), so
      the scope gate sees a declaration matching the tree. The fix round
      declares NO new deviation and its diff stays entirely inside
      files-to-touch plus those declared extras plus the standing
      work-history files (verified over the 19 delta files).
