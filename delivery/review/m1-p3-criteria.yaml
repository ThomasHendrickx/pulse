kind: verdict
phase: M1-P3
verdict: APPROVE
produced-by: claude
framing: criteria-contract
review-contract: criteria

# Clean-room criteria review of head f274d30 (branch claude/m1-p3-one-pot,
# base main 6fc43c9), executed entirely in this fresh clone. Every criterion
# walked by EXECUTION, never adopted from the implementer's claims. All
# database commands pinned to the local supabase stack at 127.0.0.1:54322
# and 127.0.0.1:54321 per fleet environment warning 1; the ambient foreign
# (Hemma) credentials were verified present in the shell and never used.
# Warning 6 honored: all five values (DATABASE_URL, DIRECT_URL,
# NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY,
# SUPABASE_SERVICE_ROLE_KEY) pinned in the shell that invoked the e2e gate.
#
# GATE RECORD, all executed by this reviewer at f274d30: npm ci EXIT=0;
# npm run typecheck EXIT=0; npm run lint EXIT=0; npm test EXIT=0 (15 files,
# 141 passed, 0 failed, 0 skipped); the literal chain npm run typecheck &&
# npm run lint && npm test EXIT=0. E2E against a database built from zero:
# npm run db:reset is blocked for an AI agent by the Prisma 6.19 consent
# guard (no user in this review loop to consent), so the equivalent
# fresh-database route was taken: CREATE DATABASE pulse_review_p3 on the
# local stack, npx prisma migrate deploy EXIT=0 applying all three
# migrations from scratch including 20260819152307_transfer_links, npx
# prisma migrate status "Database schema is up to date!", npx tsx
# prisma/seed.ts EXIT=0, then npm run test:e2e EXIT=0 (4 passed, 0 skipped,
# 34.3s) with all five env values pinned. Live RLS verified by psql on that
# from-zero database: relrowsecurity=t for all eight application tables
# including transfer_links (_prisma_migrations stays f, outside scope as
# recorded since M1-P2).
#
# RED SPOT-CHECKS, eight reviewer mutations, each applied to committed
# code, run, captured, restored, restore re-run green:
#   RM1 declared-set precedence swapped (pot check moved before reserve):
#       classify-flow 1 failed (the exact both-sets test), restore 13 passed.
#   RM2 pairing content sort removed (insertion order decides): vitest
#       EXIT=1, 3 failed (order-independence plus both tie-break tests),
#       restore 9 passed.
#   RM3 unmatched internal legs dropped from surfacing: EXIT=1, 2 failed
#       (the 500-case reconciliation property AND the criterion 2.5
#       exclusion test), restore 10 passed. The identity's exclusion
#       accounting is property-guarded, not example-guarded.
#   RM4 re-parse persists the OLD spec (declaration correction lost):
#       EXIT=1, reparse 1 failed, restore 3 passed.
#   RM5 CR-208 guard removed (pre-fix sign inversion restored): EXIT=1,
#       profile-detection 3 failed (all three signed-cell witnesses),
#       restore 36 passed.
#   RM6 CR-209 collision guard removed: EXIT=1, 2 failed (case-colliding
#       and identical token pairs accepted again), restore 36 passed.
#   RM7 interpretation window padding removed (import's own dates only):
#       EXIT=1, interpret 2 failed (healing and recompute), restore 3
#       passed. The cross-import healing of criterion 2.5 is load-bearing
#       on the padded window.
#   RM8 no-match settlement debit left UNRESOLVED (hazard H2.6's exact
#       dangerous state): EXIT=1, 2 failed across classify-flow and
#       corrections, restore 13 passed.
#
# CHARTER STOP-FOR CONFORMANCE, verified against the PLAN's specification
# by reading the shipped functions next to the plan steps: the
# classification order in src/modules/ledger/domain/classify-flow.ts:57-115
# is exactly reserve set, pot set, settlement step, sign fallback with
# corrections, UNRESOLVED only for the zero-amount remainder, matching plan
# step 1; the four corrections in
# src/modules/ledger/domain/corrections.ts:38-126 match plan step 2 in
# meaning (settlement pattern plus exact amount plus named 45-day window
# constant with the line-item fallback arm for CSV statements; drawdown
# RESERVE never INCOME; refund SPEND with positive amount keyed on
# outgoing history; cash its own destination, never split); pairing in
# src/modules/ledger/domain/pair-transfers.ts:39-127 requires all four plan
# conditions with the 4-day named constant and the date-then-id tie-break,
# and reserve movements never reach pairing (they classify RESERVE,
# asserted at test/domain/pair-transfers.test.ts:188-200); the identity in
# src/modules/ledger/domain/reconciliation.ts:38-87 is income minus spend
# minus net-to-reserves equals change in pot, exact, integer cents, with
# the difference computed and surfaced. No semantic deviation from the plan
# was found, so nothing on the stop-for list was touched without escalation.
#
# REGRESSION FROM M1-P2, re-executed: ingest, dedup, profile-detection and
# schema suites 78 passed EXIT=0 inside the 141; the e2e import journey
# passed against the from-zero database; catalog key parity 93/93/93 with
# identical key sets across en, nl, fr (node flatten-and-diff, executed);
# zero em dashes in the added lines of git diff 6fc43c9..HEAD (grep count
# 0); no literal colour, px, rgb or hsl in the diff's added src lines; the
# one monetary column stays integer (prisma/schema/import.prisma:97
# amountCents Int) and the property test asserts Number.isInteger on all
# seven report figures; the new table carries householdId with index and
# foreign key (prisma/schema/migrations/20260819152307_transfer_links/migration.sql:4)
# and ENABLE ROW LEVEL SECURITY (same file line 38); facts tables are
# untouched by the migration (CREATE TABLE plus indexes and foreign keys
# only, no ALTER on transactions or imports); every new repository function
# filters on householdId (src/modules/ledger/adapters/ledger-repository.ts:31-37
# and the replaceInterpretation transaction).
#
# WORK-HISTORY CLAIMS, checked against this reviewer's own executions
# rather than adopted: M1P3-C1 reproduced in family (RM1, RM2, RM8);
# M1P3-C2 reproduced (RM3 reddened the property exactly as claimed for P1);
# M1P3-C3 reproduced (RM7 is W1's construction, same 2-failed shape);
# M1P3-C4 reproduced (RM4 is R2's construction); M1P3-C5's fixes verified
# to have teeth today by guard removal (RM5, RM6; the historical red-first
# ordering itself is not re-executable and is taken as the notes record
# it); M1P3-C6 reproduced live (psql relrowsecurity on a from-zero
# database). Gate evidence matches this reviewer's runs exactly (141 fast,
# 4 e2e). The open questions M1P3-C7, C8, C9 are honest limits, each
# recorded at the definition it concerns. The verification-first
# escalation (main missing the approved M1-P2 fix round, the 12de8f5
# commit message false about its tree) was independently confirmed
# plausible from this clone's history (base 6fc43c9 carries creditValue
# and dedup.test.ts; the escalation and resolution are recorded in
# delivery/work-history/m1-p3-notes.md and kept rather than deleted, the
# R-087-conformant handling of a claim that became false).
#
# DECORRELATION RECORD PER DR-0003, stated rather than faked: this verdict
# truthfully records produced-by claude. Both M1-P3 clean-room reviews are
# dispatched on the Claude model family, so the model-family dimension of
# the dual-review decorrelation check CANNOT pass truthfully; that failure
# is ESCALATED to the orchestrator here, per DR-0003's pilot rule, which
# discharges condition one through distinct review-contract and framing
# values instead (criteria-contract/criteria here; the hazard brief's
# framing and contract in the sibling worktree). Recording any other
# family would be a false claim.
#
# WHAT THIS CONTRACT DID NOT REACH, stated rather than summarised as
# completeness: no deployed-environment behaviour (deploy-verify is a
# later stage; warning 5's session pooler was never needed); the real
# PC-banking CSV layouts remain unverified (M1P2-C1 still open); the
# settlement window value 45 and the pattern lists are grounded in two
# observed formats only (M1P3-C7, honestly open); deep concurrency and
# crash probing of replaceInterpretation and applyReparse (forced database
# races, kill mid-transaction) is the hazard contract's lane, and this
# review verified those paths against the port fakes plus a read of the
# real adapter's single-transaction shape, not against a forced race; the
# criterion 2.7 clause "merchant rules ... intact" is VACUOUS on this base
# (grep for MerchantRule over src/ and prisma/ finds nothing; the tables
# arrive with M1-P4), so its merchant-rules half was not evaluable by
# execution and is covered instead by the whole-declaration-layer
# assertions the test does make, exactly as the work history discloses.
# "All acceptance criteria met" below is a statement about this phase's
# contract, not a completeness claim about the artifact.
#
# VALIDATION RECORD, measured: npx tiphys validate --type verdict (Node
# v26.7.0) against a reviewer-built context directory (never a shared
# scratch dir) carrying plan.yaml (delivery/plan/pulse-v1.yaml at this
# head), the M1-P3 work-history.yaml, charter.yaml, assurance-modes.yaml
# and this file under delivery/review. Result: the schema,
# verdict-criteria-complete and verdict-deviations-judged all pass; the
# run reddens on EXACTLY ONE check, dual-review-decorrelation, because
# only one M1-P3 verdict is visible from this clone (the hazard verdict is
# produced in a parallel worktree). Verified not to mask anything: with a
# second M1-P3 verdict present in a scratch copy of the context, this
# document validates at exit 0. Once the real sibling exists, the check's
# produced-by dimension still cannot pass truthfully, because both M1-P3
# clean-room reviews are Claude-family; that is the DR-0003 escalation
# recorded above, never a family faked to make a check green.

findings: []

criteria:
  - id: "2.1"
    quote: >-
      npm test exits 0 with test/domain/classify-flow.test.ts covering all
      five flow values and the declared-set precedence (reserve set before
      pot set before sign).
    evidence:
      - "npm test EXIT=0 (141 passed) and npx vitest run test/domain/classify-flow.test.ts EXIT=0 (13 passed), both executed in this clone at f274d30."
      - "All five values each reachable and asserted by name: INCOME, SPEND, INTERNAL, RESERVE at test/domain/classify-flow.test.ts:68-96, UNRESOLVED for the zero-amount row at test/domain/classify-flow.test.ts:92-95 (never dropped: surfaced via unresolvedIds, src/modules/ledger/domain/interpret.ts:152-155)."
      - "Precedence pinned in both the code order (src/modules/ledger/domain/classify-flow.ts:57-115: reserve, pot, settlement, sign) and dedicated tests (test/domain/classify-flow.test.ts:98-125 including the degenerate both-sets IBAN, and the settlement-between tests at test/domain/classify-flow.test.ts:127-183)."
      - "Red direction, this reviewer's mutation RM1 (pot check moved before reserve check): exactly the both-sets test failed; restored, 13 passed. Mutation RM8 (no-match settlement debit to UNRESOLVED): 2 failed; restored green."
    met: true
  - id: "2.2"
    quote: >-
      test/domain/pair-transfers.test.ts asserts pairing is deterministic
      and order-independent, shuffling insertion order and asserting an
      identical pair set, including the tie-break by date distance then id.
    evidence:
      - "npx vitest run test/domain/pair-transfers.test.ts EXIT=0 (9 passed). The order-independence test shuffles the legs under five seeds and asserts the identical pair set and unmatched set each time (test/domain/pair-transfers.test.ts:92-104); idempotence asserted at test/domain/pair-transfers.test.ts:106-110."
      - "Tie-break asserted in both halves: nearer date wins (test/domain/pair-transfers.test.ts:114-123), lowest transaction id at equal distance (test/domain/pair-transfers.test.ts:125-134). The 4-day window is the named constant TRANSFER_DATE_TOLERANCE_DAYS at src/modules/ledger/domain/constants.ts:7, edge-and-beyond tested at test/domain/pair-transfers.test.ts:140-147."
      - "Red direction, mutation RM2 (content sort of candidate edges removed so insertion order decides): vitest EXIT=1, 3 failed (order-independence plus both tie-break tests); restored, 9 passed. Hazard H2.3's construction reddens."
    met: true
  - id: "2.3"
    quote: >-
      The four corrections each have a named passing test in
      test/domain/corrections.test.ts, one describe block per correction
      (card settlement, reserve drawdown, refund, cash withdrawal). The card
      settlement fixtures mirror the observed real shapes: the account-side
      debit carries no counterparty IBAN, names the card statement number in
      its settlement-pattern description, and its amount equals the card
      import's settlement total within the date window; the card-side
      fixture carries a positive settlement row with no counterparty account
      column; the test asserts the debit classifies INTERNAL paired to the
      card import, the card-side settlement row classifies INTERNAL as the
      mirror leg, and the card line items are the only counted spend.
    evidence:
      - "npx vitest run test/domain/corrections.test.ts EXIT=0 (13 passed) with exactly four describe blocks named card settlement (test/domain/corrections.test.ts:63), reserve drawdown (test/domain/corrections.test.ts:190), refund (test/domain/corrections.test.ts:229), cash withdrawal (test/domain/corrections.test.ts:282)."
      - "Fixture shapes verified in the file: the settlement debit carries no counterpartyIban and names statement 42 in MASTERCARD AFREKENING NUMMER 42 with amount equal to the statement's line-item total (test/domain/corrections.test.ts:77-84); the card-side mirror is a positive DOMICILIERING VIA JE BANK row with no counterparty field (test/domain/corrections.test.ts:88-95); debit INTERNAL paired to card-42 asserted at test/domain/corrections.test.ts:99-117, mirror INTERNAL at test/domain/corrections.test.ts:119-126, line items the only counted spend with the books closing at test/domain/corrections.test.ts:128-135; the window binds at test/domain/corrections.test.ts:171-187."
      - "Correction meanings match the plan exactly: drawdown lands as a negative reserve movement, never income (test/domain/corrections.test.ts:207-213 asserts netToReservesCents -300000 and incomeCents 0); refund is SPEND with positive amount netting spend (test/domain/corrections.test.ts:251-265); cash is SPEND marked cash, one row never split (test/domain/corrections.test.ts:292-305)."
      - "Red direction: mutation RM8 reddened the card-settlement describe; the implementer's recorded mutation battery (M1 drawdown-as-INCOME, M6, M7) is consistent with what this reviewer reproduced in family."
    met: true
  - id: "2.4"
    quote: >-
      test/property/reconciliation.test.ts passes: a fast-check property
      test where change in pot is computed from the facts side only, as the
      sum of raw transaction amounts over pot accounts, while income, spend
      and net-to-reserves come from the interpretation output; the
      generators produce datasets covering all five flow values, unmatched
      internal legs, and all four correction shapes; the property asserts
      the identity holds exactly when no UNRESOLVED rows or unmatched legs
      exist, and that otherwise the computed difference equals the sum of
      the surfaced gaps, in integer cents, across at least 500 generated
      cases.
    evidence:
      - "npx vitest run test/property/reconciliation.test.ts EXIT=0 (1 passed). fast-check property with numRuns 500 at test/property/reconciliation.test.ts:362; fast-check 4.9.0 is a devDependency only."
      - "Facts-side changeInPot: reconcile sums raw amountCents over interpreted pot transactions (src/modules/ledger/domain/reconciliation.ts:46-61); income, spend, net-to-reserves come from interpretation.flows. Both identity directions asserted: exact zero difference when no gaps (test/property/reconciliation.test.ts:350-359), difference equals surfaced gaps otherwise (test/property/reconciliation.test.ts:342-349); integer cents asserted on all seven report figures (test/property/reconciliation.test.ts:327-337)."
      - "Coverage is TALLIED FROM EXECUTION, not assumed: the run asserts all five flow values occurred and that card-settlement, reserve-drawdown, refund, cash-withdrawal, unmatched-internal, unitemised-settlement-debit, zero-row and transfer shapes were all generated, plus both a gapped and a clean-books case (test/property/reconciliation.test.ts:369-389)."
      - "Red direction, mutation RM3 (unmatched legs dropped from surfacing): the property failed within the 500 runs, vitest EXIT=1; restored green. Hazard H2.4's shape (identity holding only on chosen cases) is what this construction probes."
    met: true
  - id: "2.5"
    quote: >-
      An unmatched internal leg is excluded from both sides and surfaced,
      asserted in test/domain/pair-transfers.test.ts; interpretation re-run
      over the period window after a second file heals it, asserted in
      test/application/interpret.test.ts.
    evidence:
      - "First half: test/domain/pair-transfers.test.ts:161-186 asserts the partnerless leg stays INTERNAL, enters neither income nor spend, and the reconciliation difference EQUALS the leg (differenceCents -50000 = unmatchedInternalGapCents). Executed green; reddened under RM3."
      - "Second half: test/application/interpret.test.ts:93-145 uploads account A's file (leg unmatched, zero links, import INTERPRETED), then account B's file in a separate import, and asserts the two legs pair ACROSS imports with one persisted link carrying householdId. Executed green (3 passed)."
      - "The healing is genuinely window-borne: mutation RM7 (padding removed so interpretation runs over the import's own dates only) failed the healing and recompute tests, vitest EXIT=1; restored green. The padded window is INTERPRETATION_WINDOW_PADDING_DAYS at src/modules/ledger/domain/constants.ts:37-38, applied at src/modules/ledger/application/interpret-window.ts:91-94."
    met: true
  - id: "2.6"
    quote: >-
      npm run typecheck && npm run lint && npm test exit 0 and npm run
      test:e2e exits 0 at the phase head.
    evidence:
      - "The literal chained command npm run typecheck && npm run lint && npm test EXIT=0 at f274d30 in this clone (tsc clean, eslint clean, 15 files, 141 passed, 0 failed, 0 skipped)."
      - "npm run test:e2e EXIT=0 (4 passed, 0 skipped, 34.3s) at f274d30, against a database this reviewer built from zero on the local stack (CREATE DATABASE pulse_review_p3, npx prisma migrate deploy EXIT=0 applying all three migrations, seed EXIT=0), with all five environment values pinned local in the invoking shell per fleet warnings 1 and 6. The import journey completes at status INTERPRETED, exercising the new pipeline interpret stage in the real app."
    met: true
  - id: "2.7"
    quote: >-
      test/application/reparse.test.ts asserts that after correcting a
      SourceProfile with a wrong amount representation, the re-parse action
      rebuilds the affected import's fact rows from stored rawLine with
      corrected amounts, without re-upload, and with account declarations,
      merchant rules and dedup behaviour intact.
    evidence:
      - "npx vitest run test/application/reparse.test.ts EXIT=0 (3 passed). The scenario is the H1.1 shape: an indicator-column card file confirmed under a wrong signed spec writes all-positive facts, then fixSourceProfile with the corrected spec rebuilds amounts to [85000, -480, -480, -2303, -1850] from stored rawLine with row ids and rawLines identical before and after (test/application/reparse.test.ts:84-123), no re-upload anywhere in the flow."
      - "Declarations intact asserted: accounts deep-equal, profile name and account binding unchanged, only the spec updated (test/application/reparse.test.ts:133-137); dedup behaviour intact asserted by keys recomputed distinct and a re-upload of the same file adding 0 and knowing 5 (test/application/reparse.test.ts:127-157). The failure direction is also pinned: an unparseable correction rewrites NOTHING (test/application/reparse.test.ts:160-183)."
      - "Red direction, mutation RM4 (applyReparse handed the old spec, the declaration correction silently lost): reparse EXIT=1, 1 failed; restored, 3 passed."
      - "The merchant-rules clause is VACUOUS on this base and reported as such rather than counted met by execution: grep for MerchantRule over src/ and prisma/ returns nothing (the tables arrive with M1-P4), so there is no merchant rule to preserve; the work history discloses exactly this (verification-first finding 3 in delivery/work-history/m1-p3.yaml). Every clause that exists on this base is met by execution."
    met: true
  - id: "2.8"
    quote: >-
      With a settlement-pattern debit and no matching card import inside the
      date window, test/domain/corrections.test.ts asserts the debit
      classifies SPEND, never INTERNAL and never UNRESOLVED, and enters the
      spend total exactly once; its grouping under the card issuer as
      merchant rides the normal resolver chain once M1-P4 lands.
    evidence:
      - "test/domain/corrections.test.ts:137-169 asserts flow SPEND, not INTERNAL, not UNRESOLVED, and spendCents exactly 85000 with the books reconciling (entered exactly once); the outside-window variant returns the unitemised-card-spend arm (test/domain/corrections.test.ts:171-187). The code path is the explicit third arm of the settlement step at src/modules/ledger/domain/classify-flow.ts:92-95."
      - "Red direction, mutation RM8 (that arm changed to UNRESOLVED, hazard H2.6's exact dangerous state): vitest EXIT=1, 2 failed across corrections and classify-flow; restored green."
      - "The merchant-grouping sentence is a forward reference to M1-P4 by the plan's own text; nothing here pre-empts the resolver chain (no merchant code exists on this base, grep-verified), which is what riding the normal chain later requires."
    met: true

deviations-judged:
  - deviation: >-
      M1-P3 files-to-touch (src/modules/ledger/, src/modules/import/,
      prisma/, test/domain/classify-flow.test.ts,
      test/domain/pair-transfers.test.ts, test/domain/corrections.test.ts,
      test/property/reconciliation.test.ts,
      test/application/interpret.test.ts, test/application/reparse.test.ts)
    serves-plan-intent: true
    reasoning: >-
      Judged item by item against the diff (git diff 6fc43c9..HEAD, 41
      files), not adopted from the declaration. All four extra groups are
      necessary and minimal: the accounts module's listAccounts addition is
      forced by the module-boundary rule (the ledger engine must read
      declared accounts through the accounts module's published interface,
      never its tables), and the three touched files are exactly that
      seam; test/application/fake-import-world.ts is the sanctioned
      port-fake seam extended so the REAL interpret use case runs in the
      fast gate, which criteria 2.5 and 2.7 need; the CR-208 and CR-209
      witnesses land in test/domain/profile-detection.test.ts because the
      assigned backlog items name src/modules/import as the scope hook and
      their witnesses belong beside the existing parser tests; package.json
      and package-lock.json carry only the fast-check devDependency that
      criterion 2.4 names (no runtime dependency, so the charter's
      stop-for is untouched). The deviation was escalated in the notes the
      moment it was found, and the orchestrator has since amended the
      declaration on the base branch (main commit ea5fc03 adds exactly
      these five entries to the phase's extras), so the scope gate will see
      a declaration matching the tree. Nothing in the diff falls outside
      files-to-touch plus these declared extras plus the standing
      work-history files.
