# Clean-room review, HAZARD contract, phase M3-P11 (optimistic naming,
# DR-0025 and DR-0026), ROUND ONE of at most two.
#
# Head reviewed: 8fb0f58, read in an isolated worktree at
# /home/user/wt-m3p11-haz on branch claude/m3-p11-rev-haz. 8fb0f58 and
# 45bdf01 add work history only; the code under review is the code at
# b278d87.
#
# CONTRACT ORDER: the attack directions below come from the phase's
# declared hazard classes and were fixed BEFORE the acceptance criteria
# were read as a frame. Each direction is probed and its command and
# captured output recorded here as the probe completes, not from memory
# at the end.
#
# STATUS: IN PROGRESS. The verdict field is not a verdict until the
# probe list below carries no not-yet-probed entry.
kind: verdict
phase: M3-P11
verdict: IN-PROGRESS
produced-by: claude-fable
framing: destructive-paths
review-contract: hazard
probes:
  - id: A
    direction: >-
      An optimistic prediction that LIES: the predicted label differs from
      what the server persists. Trimming, normalisation, identity-key
      versus descriptor grouping. The implementer left a module-scope
      claim about the trimmed-name difference; confirm or refute it.
    status: in-progress
    read: >-
      src/modules/merchants/ui/merchant-row.tsx (the claim check, the
      module-scope recentNaming record), src/modules/merchants/ui/
      merchant-review.tsx (the label prop is maskCardNumbers(group.label)),
      src/modules/merchants/application/assign-merchant.ts (the stored name
      is input.merchantName.trim(), then findMerchantByName reuses an
      EXISTING merchant record and its stored name).
    open-question: >-
      whether findMerchantByName matches case-insensitively, which would
      make the reused merchant name differ from the typed string by more
      than trimming and slip past both branches of the claim check.
  - id: B
    direction: >-
      The prediction surviving a FAILED action anywhere: after revert,
      after navigation, after a second submit on the same row, after two
      rapid submits on different rows.
    status: not-yet-probed
  - id: C
    direction: >-
      The typed failure Result swallowing a real error class. NEXT_REDIRECT
      is rethrown; what else reaches that catch.
    status: not-yet-probed
  - id: D
    direction: >-
      The toast under DR-0026: dismiss-only, no timer, role alert versus
      status, per-row or global collisions.
    status: not-yet-probed
  - id: E
    direction: >-
      aria-describedby dangling when there is no failure.
    status: not-yet-probed
  - id: F
    direction: >-
      The client leaf's import closure widening the client boundary.
    status: not-yet-probed
  - id: G
    direction: >-
      Success-path revalidate and redirect still byte-identical, so the
      merged M3-P12 banner and the review screen's server truth stay
      authoritative.
    status: not-yet-probed
  - id: H
    direction: >-
      The new e2e spec going vacuous through project membership,
      testIgnore or prod-only projects (the M3-P9 precedent).
    status: not-yet-probed
findings:
  - id: HZ-M3P11-01
    severity: medium
    title: >-
      Two notices occupy the identical fixed position, so one hides the
      other, which is the exact harm the no-timer decision was taken to
      prevent.
    status: draft
  - id: HZ-M3P11-02
    severity: medium
    title: >-
      The module-scope naming record is a SINGLE SLOT claimed by the first
      matching row, not by the row that was named: a second submit erases
      the first row's pending difference claim, and a merchant that has
      both an income and a spend group can have the notice land on the row
      the reader did not touch.
    status: draft
  - id: HZ-M3P11-03
    severity: low
    title: >-
      The claim check compares the reader's typed string against the MASKED
      server label, so a stored name the masking rewrites is swapped in
      silently.
    status: draft
