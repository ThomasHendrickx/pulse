# Clean-room review, HAZARD contract, phase M3-P2 (pdf-belfius, severity
# critical). DEPLOY-VERIFY DEFECT-ROUND VERDICT (expedited, owner blocked).
#
# Head now reviewed: f0d6fb8 (executable head 073f824 plus a docs commit) on
# the REBUILT claude/m3-p2-pdf-belfius, merged into this clone at fc4fc95
# with every conflict resolved to the f0d6fb8 side; verified after resolving:
# git diff f0d6fb8 HEAD over src, prisma, test, messages and the five config
# files is empty, so the tree probed here is the tree under review. Prior
# verdicts in this file's git history: round 0 FIX-ROUND-NEEDED at 9b40b14
# (commit 91e895a), round 1 APPROVE at 22f717b (commit 90b4f02).
#
# HEADER DISCLOSURES.
# DR-0003: Claude-family verdict, contract hazard, framing destructive-paths,
# under the same-family varied-lenses allowance; pairs with the criteria
# review for distinct-contract and distinct-framing.
# ESCALATION CARRIED FORWARD (unchanged): the plan parks cross-format dedup
# unification while naming the double-store (delivery/plan/pulse-v02.yaml:1246
# and :1300); a mixed-format household silently stores every row twice.
# Standing owner risk acknowledgement (finding HZ-004), not a fix-round
# demand.
#
# DEFECT-ROUND VERDICT: APPROVE.
# The deploy fix does what it claims, verified by execution in both a working
# and a deliberately broken runtime; no new medium or high hazard was found;
# what remains is low and tracked (one new low HZ-007 on an erased internal
# distinction, plus the carried lows HZ-001-R, HZ-003-N, HZ-004).
#
# GATES RE-RUN AT THE DEFECT HEAD (this clone, node v26.7.0):
#   fast gate: vitest run -> 27 files, 283 passed (283), exit 0, matching the
#     expected count. typecheck: tsc --noEmit exit 0.
#
# LANE ITEMS, EACH PROBED OR READ AS DIRECTED.
#
# (1) HZ-003 REVISION (module failure now a Result landing a FAILED import).
#   Probe-executed in TWO runtimes (esbuild bundle of the real adapter, use
#   case mapping and health route; pdfjs-dist external; run once from the
#   worktree where the module resolves, once from a directory where no
#   node_modules is reachable):
#     working runtime: corrupt magic-header bytes -> kind
#       pdf-extraction-failed, reason extraction-failed; real 39bada64
#       upload detects and parses 39 rows; probePdfExtraction reports
#       moduleLoad ok, extraction ok.
#     broken runtime: the same real 39bada64 bytes -> detect fails with
#       reason extraction-failed (loud FAILED import, zero rows, no throw
#       reaches the caller); probePdfExtraction reports moduleLoad failed;
#       stderr carries EXACTLY ONE "[pulse:pdf] pdfjs-dist failed to load"
#       log with ERR_MODULE_NOT_FOUND and a 10-frame stack (the server-side
#       diagnosability the round-1 rethrow was buying, kept).
#   DISTINGUISHABILITY, judged honestly: corrupt bytes versus missing module
#   remain distinguishable in the two channels that matter, the health
#   probe's staged booleans (moduleLoad failed versus moduleLoad ok with
#   extraction failed) and the one logged stack; the user-visible import
#   reason coincides (extraction-failed for both), which is acceptable
#   because the owner-facing remedy question ("is it my file or the
#   deployment?") is answered by /api/health/pdf without log access. One
#   internal erasure found and filed as HZ-007 (low): the adapter collapses
#   the extractor's two kinds before the port, so pdf-module-unavailable
#   never crosses it at runtime.
#   The user-flow contract is also witnessed by the shipped test at the live
#   adapter with the module import itself mocked to throw
#   (test/application/pdf-module-unavailable.test.ts:23 to 45): FAILED
#   import, extraction-failed, zero rows, no throw.
#
# (2) HEALTH ROUTE (/api/health/pdf), read and probed.
#   Response construction (src/app/api/health/pdf/route.ts:22 to 32) emits a
#   closed field set: status, pdfExtraction, moduleLoad, extraction, and at
#   most errorName plus errorCode; the probe type
#   (pdf-text-extractor.ts:147 to 152) carries nothing else, errorName is an
#   Error class name, and stringCodeOf (lines 154 to 160) forwards only
#   STRING codes. Nothing env-derived, path-derived or message-derived can
#   enter the payload by construction; the probe document is an embedded
#   base64 constant (line 144), so no filesystem path is even consulted.
#   Probed bytes, working runtime: status 200, body exactly
#   {"status":"ok","pdfExtraction":"ok","moduleLoad":"ok","extraction":"ok"}.
#   Probed bytes, broken runtime: status 500, body exactly
#   {"status":"error","pdfExtraction":"failed","moduleLoad":"failed",
#   "extraction":"failed"}; a scan of both bodies for separators,
#   node_modules, home, tmp, vercel, env-variable and secret material found
#   none. Middleware exemption verified: the matcher at src/middleware.ts:55
#   excludes api/health, covering this route like its db sibling; the
#   unauthenticated exposure is bounded to those booleans plus a constant
#   error name/code, and each GET costs one parse of a fixed 700-byte
#   embedded document (no caller-controlled input), which is the same
#   acceptance the db sibling carries.
#
# (3) outputFileTracingIncludes, read at next.config.ts:28 to 34. The route
#   key is "/**" but the VALUES are three LITERAL file paths (pdfjs-dist
#   package.json, legacy/build/pdf.mjs, legacy/build/pdf.worker.mjs), no
#   glob characters, all three verified present in node_modules (0.7KB, 1.0MB
#   and 2.4MB library files). No fixture, env file or repository path can
#   ride these includes; the only cost is bundle size in every function.
#
# (4) ENGINES PIN, read at package.json ("node": "22.x"). No .npmrc and no
#   engine-strict anywhere in the repo, so locally (Node 26) the pin is
#   advisory: this review's own gates ran green under v26.7.0 (283 tests,
#   typecheck), and fleet tooling on Node 26 (warning 2) is unaffected; npm
#   will emit EBADENGINE warnings on install under 26, which is cosmetic.
#   On Vercel the field pins the function runtime to 22.x, satisfying
#   pdfjs-dist 6.x's floor (>=22.13) as the config comment states. No
#   hazard; the deploy-verify criterion remains the place the pin is proven.
#
# (5) REBUILT-BRANCH SHAPE, verified with git. The old merged tip 22f717b is
#   subsumed by main's merge commit a577e51: git diff 22f717b a577e51
#   touches ONLY delivery documents (the two review verdicts, work history,
#   plan amendment), zero code paths, so the force-replace lost nothing.
#   git log origin/main..origin/claude/m3-p2-pdf-belfius is exactly the four
#   defect-round commits (adbdf60, 3468aa3, 073f824, f0d6fb8), and the
#   diff against main is exactly the defect-round file set plus one
#   expected artifact: the branch, cut from a577e51, does not carry main's
#   own later plan amendment 51b71d0 (five lines in
#   delivery/plan/pulse-v02.yaml), which merges cleanly and is main-side
#   bookkeeping, not lost content.
#
# (6) FACTS IMMUTABILITY AND THE DELIMITED PATH. git diff a577e51 f0d6fb8
#   over src contains zero transaction update or delete occurrences; the
#   domain layer, the UI layer and prisma/ are untouched this round (empty
#   diffs), so every round-1 template property (positional classification,
#   sequence gate, version gate) carries over unchanged, re-witnessed by the
#   real 39bada64 upload parsing 39 rows and reconciling in the working-
#   runtime probe. The delimited path is untouched (detect-profile.ts,
#   parse-statement.ts unchanged; non-PDF bytes branch at
#   statement-parser.ts:45 never touches pdfjs, so CSV imports keep working
#   even in a runtime where the module is absent).
kind: finding
verdict: APPROVE
produced-by: claude (Claude Code CLI, model claude-fable-5)
findings:
  - id: HZ-007
    severity: low
    evidence:
      - >-
        Probe-executed at f0d6fb8, broken runtime (no resolvable
        pdfjs-dist): statementParser.detect over the real 39bada64 bytes
        returns kind pdf-extraction-failed, not pdf-module-unavailable.
        Mechanism: extractPages at
        src/modules/import/adapters/statement-parser.ts:36 collapses both
        extractor error kinds to undefined, and detect and parse re-tag the
        failure as pdf-extraction-failed at
        src/modules/import/adapters/statement-parser.ts:55 and :78. The
        pdf-module-unavailable member constructed at
        src/modules/import/adapters/pdf-text-extractor.ts:77 therefore
        never crosses the parser port at runtime, and the mapping branches
        for it (src/modules/import/application/upload-statement.ts:29 and
        :40, src/modules/import/application/fix-profile.ts:151) are dead
        code on the live path: a reader branching on that kind downstream
        would never fire.
      - >-
        Judged honestly: no behavioural defect follows today, because both
        kinds deliberately map to the same extraction-failed reason and the
        diagnosis channels (health probe staged booleans, the one logged
        ERR_MODULE_NOT_FOUND stack) sit below the collapse and were both
        probe-verified distinguishable. The finding is that the type-level
        distinction the defect round introduced is erased one layer above
        where it is created, which quietly weakens the machine-readable
        error contract the same round added.
    concrete-edit: >-
      Make extractPages return the extractor's Result instead of undefined
      (or propagate the error kind alongside the pages), so detect and
      parse forward pdf-extraction-failed and pdf-module-unavailable as
      distinct kinds to the port; the reason mapping already handles both
      and keeps the user-visible reason identical.
  - id: HZ-001-R
    severity: low
    evidence:
      - >-
        Carried from round 1, unchanged: the template layer is untouched
        this round (empty git diff a577e51 f0d6fb8 over
        src/modules/import/domain). The declared residue stands as probed
        at 22f717b: a corrupted margin-level first-transaction start line
        with a zero amount drops silently
        (belfius-current-account-template.ts:206 to 214, continuity gate at
        :219 to 233 has no lower anchor). Documented in code, not
        adversarially reachable, zero monetary impact; tracked low under
        DR-0012 condition 2.
    concrete-edit: >-
      Keep tracked, or close by anchoring the sequence-continuity gate to
      an expected first sequence as recorded in the round-1 verdict (weigh
      the holder-block furniture tradeoff noted there).
  - id: HZ-003-N
    severity: low
    evidence:
      - >-
        Carried from round 1, unchanged this round: a fingerprint-matched
        document that fails page structure maps to the "unparseable" reason
        while a cleanly unrecognised PDF maps to "layout-unsupported"
        (failureReasonForParseError in
        src/modules/import/application/upload-statement.ts:34 to 44). Zero
        rows are written either way; cosmetic labelling only.
    concrete-edit: >-
      Optional: map a fingerprint-matched pdf-structure failure to
      layout-unsupported at upload, or accept as tracked.
  - id: HZ-004
    severity: low
    evidence:
      - >-
        Carried forward, decided-as-parked at plan level
        (delivery/plan/pulse-v02.yaml:1246 and :1300): the same account
        period imported once as CSV and once as PDF stores every row twice,
        probe-confirmed in round 0 (divergent key families). Standing
        escalation in this verdict's header.
    concrete-edit: >-
      Plan or backlog edit: a tripwire for a household holding both a
      delimited and a pdf-layout profile bound to the same account.
