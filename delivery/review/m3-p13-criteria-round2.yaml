# M3-P13 CRITERIA REVIEW, ROUND TWO, THE CLOSING ROUND under the two-round cap.
# Clean-room reviewer, CRITERIA contract. Worktree /home/user/wt-m3p13-crit on
# claude/m3-p13-rev-crit. Round one was written against head 936fcda and
# recorded FIX-ROUND-NEEDED with six findings; this round is against the fix
# head 2a0cc03. Whatever this round leaves open rides the implementer's brief
# as work, never a round three.
#
# BEACON LOG, appended as the work happened, kept as comments because the
# verdict schema forbids additional properties.
#
# CHECKPOINT 1. Fetched. 20 commits between 936fcda and 2a0cc03, 16 files,
# 1296 insertions, 147 deletions. verdict.schema.json was read in round one and
# this document is shaped to it from the first push. NOTHING VERIFIED YET: every
# closure line below says NOT YET VERIFIED and the criteria walks say the same.
# The claimed dispositions I must witness or refute:
#   CR-M3P13-01 fixed, and by a STRONGER remedy than asked (an opaque digest
#     rather than a mask), so the excluded set is claimed to be two channels.
#   CR-M3P13-02 fixed, and claimed better than asked.
#   CR-M3P13-03 fixed at SIX surfaces rather than the five I named, the sixth
#     being the month view's reserves label, with the residue note replaced by
#     a test-derived enumeration.
#   CR-M3P13-04 fixed, an unregistered country now failing closed through
#     ISO 7064, both separator directions pinned, the false comment corrected
#     with its old text quoted.
#   CR-M3P13-05 three comment claims corrected.
#   CR-M3P13-06 NOT acted on, by the orchestrator's instruction, with a second
#     sighting recorded.
# And three things the implementer found that neither review lane named, all of
# which this lane must judge rather than accept:
#   a React element passed as a client-component prop ships its OWN props, so
#     every raw transaction description was being serialised into the page;
#   next dev serialises every server component's props, so a page-source
#     absence assertion against next dev proves nothing;
#   answering the import format question twice with one name crashes the server
#     on an unhandled unique-constraint violation, pre-existing and out of scope.
kind: verdict
phase: M3-P13
verdict: FIX-ROUND-NEEDED
produced-by: claude
framing: criteria-contract
review-contract: criteria
findings:
  - id: CR2-M3P13-00
    severity: low
    analysis: >-
      PLACEHOLDER, checkpoint 1. This entry exists only so the document
      validates on its first push and is replaced before the verdict is set.
      Round one's six findings and their claimed dispositions are listed in the
      beacon log above, every one of them NOT YET VERIFIED.
    evidence:
      - "NOT YET VERIFIED at checkpoint 1."
    concrete-fix: >-
      Replace this entry with the round-two findings before setting the verdict.
criteria:
  - id: "13.1"
    quote: >-
      npm run test:e2e exits 0 with a spec that loads /merchants ... asserts, by
      testid, that the group holding 3 transactions of one counterparty renders
      a count of 3 and a basis line whose text is the catalogue string for the
      shared-account basis, and that a descriptor-basis group renders the
      catalogue string for the shared-description basis.
    evidence:
      - "MET AT 936fcda in round one. NOT YET RE-WALKED against the delta."
    met: false
  - id: "13.2"
    quote: >-
      THE ACCOUNT NEVER LEAVES THE DISPLAY LAYER UNMASKED AND NEVER ENTERS THE
      KEY ... the rendered HTML of /merchants contains the masked form and does
      NOT contain the unmasked account anywhere, asserted over the full page
      source; and the hidden field the naming form submits carries the unmasked
      namespaced identity key, asserted on the same page.
    evidence:
      - "NOT MET at 936fcda. NOT YET RE-WALKED at 2a0cc03."
    met: false
  - id: "13.3"
    quote: >-
      THE ROWS BEHIND A GROUP ARE VISIBLE ... 3 rows render, each with a distinct
      description, a distinct booking date and its own amount, and the three
      amounts sum to the group total, compared in integer cents.
    evidence:
      - "MET AT 936fcda in round one. NOT YET RE-WALKED against the delta."
    met: false
  - id: "13.4"
    quote: >-
      THE REACH IS STATED BEFORE THE NAMING ... in English and, with the locale
      switched, in Dutch and in French.
    evidence:
      - "MET AT 936fcda in round one. NOT YET RE-WALKED against the delta, which
        touches all three catalogues."
    met: false
  - id: "13.5"
    quote: >-
      A NAMING STILL MOVES NO TOTAL. The existing hazard H3.2 journey stays
      green.
    evidence:
      - "MET AT 936fcda in round one. NOT YET RE-WALKED against the delta."
    met: false
  - id: "13.6"
    quote: >-
      TOKENS ONLY ... and any token added is declared in styles/tokens.css in the
      same commit.
    evidence:
      - "MET AT 936fcda in round one. NOT YET RE-WALKED against the delta."
    met: false
  - id: "13.7"
    quote: >-
      THREE LANGUAGES, NO ORPHANS ... and no user-facing string is hardcoded in a
      component.
    evidence:
      - "MET AT 936fcda in round one. NOT YET RE-WALKED against the delta, which
        changes one string in each catalogue."
    met: false
  - id: "13.8"
    quote: >-
      THE STANDING GATE. npm run typecheck, npm run lint, npm test and npm run
      gate:privacy all exit 0, and npm run test:e2e exits 0.
    evidence:
      - "NOT MET at 936fcda (my run exited 1 on an intermittent). NOT YET
        RE-RUN at 2a0cc03."
    met: false
  - id: "13.9"
    quote: >-
      NOTHING OF THE OWNER'S DATA REACHES THE REPOSITORY.
    evidence:
      - "MET AT 936fcda in round one. NOT YET RE-WALKED against the delta."
    met: false
deviations-judged: []
