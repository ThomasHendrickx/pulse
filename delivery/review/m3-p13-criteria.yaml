# M3-P13 CRITERIA REVIEW, ROUND ONE. Clean-room reviewer, CRITERIA contract.
# Worktree /home/user/wt-m3p13-crit on claude/m3-p13-rev-crit at the
# implementer's head 936fcda, base main 975d0ce. Clean room: the implementation
# session was not read; delivery/work-history/m3-p13.yaml and m3-p13-notes.md
# are treated as CLAIMS to be witnessed or refuted.
#
# BEACON LOG, appended as the work happened, kept as comments because the
# verdict schema forbids additional properties.
#
# CHECKPOINT 1. Worktree added. Plan phase M3-P13 read (delivery/plan/
# pulse-v02.yaml:8547 onward), nine acceptance criteria transcribed below and
# every one marked NOT YET WALKED. verdict.schema.json read before writing so
# this document validates on its first push. Nothing witnessed yet.
#
# ORCHESTRATOR RULING CARRIED INTO THIS LANE: criterion 13.2 is
# self-contradictory as written and the implementer's escalation is not counted
# as a miss. What this lane judges instead is whether the resolution asserted is
# the STRONGEST TRUE form.
kind: verdict
phase: M3-P13
verdict: FIX-ROUND-NEEDED
produced-by: claude
framing: criteria-contract
review-contract: criteria
findings: []
criteria:
  - id: "13.1"
    quote: >-
      npm run test:e2e exits 0 with a spec that loads /merchants for a household
      imported from test/fixtures/belfius-counterparty-identity.pdf at a 390 by
      844 viewport and asserts, by testid, that the group holding 3 transactions
      of one counterparty renders a count of 3 and a basis line whose text is the
      catalogue string for the shared-account basis, and that a descriptor-basis
      group renders the catalogue string for the shared-description basis.
      Asserting that the strings exist in the catalogue does not meet this
      criterion; the assertion is on the rendered page.
    evidence:
      - "NOT YET WALKED at checkpoint 1."
    met: false
  - id: "13.2"
    quote: >-
      THE ACCOUNT NEVER LEAVES THE DISPLAY LAYER UNMASKED AND NEVER ENTERS THE
      KEY. For an account-basis group whose rows carry no counterparty name, the
      rendered HTML of /merchants contains the masked form and does NOT contain
      the unmasked account anywhere, asserted over the full page source; and the
      hidden field the naming form submits carries the unmasked namespaced
      identity key, asserted on the same page. A test asserts the same masking
      function is not called anywhere in src/modules/merchants/domain or
      src/modules/merchants/application.
    evidence:
      - "NOT YET WALKED at checkpoint 1."
    met: false
  - id: "13.3"
    quote: >-
      THE ROWS BEHIND A GROUP ARE VISIBLE. The same spec opens the group holding
      3 transactions and asserts 3 rows render, each with a distinct description,
      a distinct booking date and its own amount, and that the three amounts sum
      to the group total shown on the group row, compared in integer cents.
    evidence:
      - "NOT YET WALKED at checkpoint 1."
    met: false
  - id: "13.4"
    quote: >-
      THE REACH IS STATED BEFORE THE NAMING. The naming form for that group
      renders the catalogue string carrying the number 3, and the form for a
      group of 1 renders the same string carrying 1, asserted on the rendered
      page in English and, with the locale switched, in Dutch and in French.
    evidence:
      - "NOT YET WALKED at checkpoint 1."
    met: false
  - id: "13.5"
    quote: >-
      A NAMING STILL MOVES NO TOTAL. The existing hazard H3.2 journey stays
      green: the spec reads both direction totals before and after a naming and
      asserts they are byte-identical, so this screen change cannot have moved a
      cent between income and spend.
    evidence:
      - "NOT YET WALKED at checkpoint 1."
    met: false
  - id: "13.6"
    quote: >-
      TOKENS ONLY. grep over the files this phase touches finds no literal hex
      colour, no rgb() or hsl() literal, no px or rem font size and no px or rem
      spacing value outside styles/tokens.css, and any token added is declared in
      styles/tokens.css in the same commit.
    evidence:
      - "NOT YET WALKED at checkpoint 1."
    met: false
  - id: "13.7"
    quote: >-
      THREE LANGUAGES, NO ORPHANS. Every key added to messages/en.json exists in
      messages/nl.json and messages/fr.json, asserted by the existing copy gate,
      and no user-facing string is hardcoded in a component, asserted by grep
      over src/modules/merchants/ui/merchant-review.tsx.
    evidence:
      - "NOT YET WALKED at checkpoint 1."
    met: false
  - id: "13.8"
    quote: >-
      THE STANDING GATE. npm run typecheck, npm run lint, npm test and npm run
      gate:privacy all exit 0, and npm run test:e2e exits 0, each with its
      captured output in the phase work history.
    evidence:
      - "NOT YET WALKED at checkpoint 1."
    met: false
  - id: "13.9"
    quote: >-
      NOTHING OF THE OWNER'S DATA REACHES THE REPOSITORY. Every string,
      identifier and amount used in the new spec and in any new fixture is
      invented, listed in test/fixtures/allowed-identifiers.txt where the privacy
      gate requires it, and the phase work history quotes no descriptor,
      counterparty, place, amount or date from either real document. The branch's
      commit messages carry no data of any kind.
    evidence:
      - "NOT YET WALKED at checkpoint 1."
    met: false
deviations-judged: []
