# pulse v0.2 delivery plan, written by the M0-P2 scout (plan-writer).
# Input report: delivery/intake/pulse-v0.2-pdf-addendum.md, plus the owner's
# two first-use feedback items (fleet backlog, DR-0002 feedback log,
# 2026-08-21): navigation missing (phase-blocking) and PDF import essential
# (testing halted until it exists). Owner-directed sequencing: this work
# comes before the DR-0002 one-real-month test.
# Baseline: e4ea3ba (M1 walking skeleton complete, M1-P1..M1-P5 merged).
#
# work-log (incremental, newest last):
# - created this beacon file before verification; about to read the M1 tree
#   at e4ea3ba and extract text from the two real statement PDFs in the
#   container uploads (structure only, nothing copied out)
# - PDF structural pass done (pdfplumber, scratchpad only): both PDFs carry a
#   full text layer, no OCR needed. Belfius: 5 pages, fingerprint markers
#   present, 39 tx rows across pages 1-4, page 5 is the BIJLAGE annex,
#   opening + sum(39) == closing exact in cents, sequence numbers continuous,
#   IBAN band-between-dashes lines present, sign spacing varies as claimed.
#   KBC: fingerprint present; word spacing is extraction-tolerance-sensitive
#   (naive layout extraction glues words); 22 rows; DOMICILIERING credit is a
#   row and equals the negated previous balance; identical duplicate pair
#   confirmed (1 pair); prev balance + sum(rows) == the Afrekening line
#   amount, which uses a SPACE thousands separator; the Totaal bedrag line
#   itself carries no amount. Next: repo verification at e4ea3ba.
# - repo verification pass done at e4ea3ba: StatementParser port and the one
#   delimited adapter located (ports.ts:18, delimited-file-parser.ts:10);
#   SourceProfileSpec is a flat delimited-only record (source-profile.ts:49),
#   so the addendum's "second adapter, nothing upstream changes" claim fails
#   in the strict reading and owns a verification-first step; both section 5
#   amendments are ALREADY IMPLEMENTED (dedup.ts:80-105, classify-flow.ts:77,
#   constants.ts:16-27, corrections.ts:38); import status machine with FAILED
#   and machine-readable failureReason confirmed (import.prisma:8, ports.ts:38);
#   rawContent and rawLine confirmed (import.prisma:64, import.prisma:106);
#   re-parse path confirmed (fix-profile.ts); reconciliation panel confirmed
#   (month-view.tsx:289-319). Navigation feedback confirmed: header carries
#   brand, household, sign-out and no nav (layout.tsx:18-26); the empty state
#   names the import action with no link (month-view.tsx:387-396,
#   messages/en.json:28-29); upload accepts CSV only (upload-form.tsx:22); no
#   PWA manifest anywhere; desktop-first line at pulse-frontend SKILL.md:113.
#   Counterparty IBANs inside Belfius descriptions confirmed (STORTING VAN /
#   NAAR + IBAN shapes). Belfius natural key in the addendum (account + year
#   + sequence) DIFFERS from the implemented key shape (account + statement +
#   sequence, dedup.ts:95): recorded as a failed claim.
# - new anonymized structural facts recorded in the fleet home note
#   notes/export-format-facts.md (text layers, extraction tolerance, KBC
#   Afrekening semantics, space thousands separator, DOMICILIERING equals
#   negated previous balance, duplicate pair count).
# - wrote the phases below (M3-P1 navigation first per owner feedback, then
#   PDF foundation + Belfius, KBC template, mobile-first flow, share-sheet
#   target gated on DR-0021; DR-0020 raised for the PDF text-extraction
#   dependency, a charter stop-for). Validating with tiphys next.
# - validated: npx tiphys validate --type plan --context . exits 0 (reports
#   dispatchable: false, by construction: M3-P2 and M3-P5 carry unfilled
#   fill-ins gated on DR-0020 and DR-0021; valid for review).
# - privacy scrub run in-container with a probe list built from the real
#   statements (IBANs compact and spaced, thousands-form amounts, holder
#   name variants, customer and statement references): zero hits for any
#   identifier. Two thousands-form AMOUNTS collide with invented amounts in
#   M1-era synthetic fixtures that already exist at baseline e4ea3ba
#   (coincidental low-entropy overlap, pre-existing, not from this phase);
#   recorded so a reviewer's scrub does not false-alarm on them.
# - self-checks: no em dashes, no "works correctly", git diff e4ea3ba --
#   delivery/intake/ empty, DR-0020 and DR-0021 each referenced by their
#   owning phase.
# - fix round 1 (R-061): read the adversarial review
#   delivery/review/pulse-v02-plan-adversarial.yaml (verdict revise,
#   findings PR2-001..PR2-008); applied all eight, disputed none.
#   PR2-001: criterion 2.4 gains the specEquals recognition round-trip
#   against the detect-emitted spec. PR2-002 (reviewer-contributed
#   structural fact: the bare BIJLAGE token occurs inside a real
#   transaction description): fixture reproduces the in-description marker
#   and 2.1 asserts that row parses while the annex page stays empty.
#   PR2-003: second overlapping Belfius fixture; 2.3 fails a
#   statement-scoped key. PR2-004: month-straddling KBC row; 3.1 asserts
#   bookingDate is the transaction date. PR2-005: D-10 decides the
#   no-preservation contract for unauthenticated shares, new criterion
#   5.4 witnesses it, H5.2 re-pointed at 5.4. PR2-006: 1.1 pins the nav
#   testid to exactly one file, the shell layout. PR2-007: format-naming
#   catalog copy updated in M3-P2 step 8, witnessed in 2.5. PR2-008: 2.6
#   and 3.5 rebuilt as two-half gates (whole-tree identifier grep plus
#   all-five-category grep over phase-touched files) with the
#   not-witnessed honesty clause.
#   Owner decisions applied: DR-0020 decided (pdfjs-dist, server side,
#   behind the adapter) and DR-0021 decided (Android, share target
#   built), both 2026-08-21 in the fleet decisions directory; M3-P2 and
#   M3-P5 groundings updated and fill-ins filled, open-questions now
#   carries only DR-0002. Re-validated to exit 0, dispatchable including
#   M3-P2 and M3-P5.
# - new round (owner-directed, 2026-08-21): appended phase M3-P6 after the
#   owner's first deployed Belfius PDF import. Re-measured both reported
#   defects in-container through the SHIPPED pipeline (pdfjs extractor,
#   reconstructed lines, Belfius template, merchants normaliser), counts
#   only: 39 rows, 15 card rows, ZERO with counterpartyName, 15 distinct
#   keys after normalisation, 15 of 15 keys still carrying a 16-digit card
#   number; the literal bracketed country marker is real input on 8 lines,
#   7 of them card rows. Two reported counts did NOT reproduce and are
#   recorded as unverified with the verification-first step owning them:
#   the five-real-merchants count (a crude strip leaves six candidates,
#   residual city and country tails) and the two-distinct-card-numbers
#   count (exactly one distinct sequence across 15 occurrences). Found one
#   thing the brief did not name: the merchant-source rule is duplicated at
#   merchant-review.ts:50, so a one-place fix would leave the review screen
#   grouping on the old rule. Decided D-11 (merchant source stays
#   counterpartyName ?? description, unified into one helper; the
#   per-profile alternative is rejected with reasons under R-005, no
#   decision record raised), D-12 (masking is display-only, never the rule
#   subject) and D-13 (the CR-402 branch is selected by a measured rule
#   count, making the no-rules premise checkable). Every existing phase
#   left byte-unchanged. Privacy scrub re-run over the plan file with an
#   87-probe list built from the real statement through the shipped
#   extractor: ZERO digit-shaped hits (no account identifier, no card
#   number, no amount, no date). Three word-shaped hits, all three Dutch
#   FORMAT KEYWORDS the owner's own intake already names (the deposit,
#   annex and card-settlement tokens), recorded so a reviewer's scrub does
#   not false-alarm on them. Re-validated to exit 0.
# - fix round 2 (R-061), M3-P6 focused adversarial review
#   (delivery/review/pulse-v02-plan-adversarial.yaml at bed2038, verdict
#   revise, findings PR3-001..PR3-008): applied all eight, disputed none.
#   PR3-001 (high) confirmed by re-measurement before applying: contiguous
#   16-digit runs in the parsed descriptors number ZERO while every card
#   row carries a separator-insensitive run, so criterion 6.3 as written
#   could not fail; both halves are now the same separator-insensitive
#   13-to-19-digit test and the fixture prints the grouped form.
#   PR3-002: the fixture's one merchant now carries both card-tail shapes.
#   PR3-003: re-measured my own two entries through the shipped pipeline
#   and corrected both. The card-row predicate is now stated (descriptor
#   matching the card-number label pattern, 15 of 39 rows); the 15-of-15
#   claim held only under a separator-insensitive reading it did not
#   state, and the only-bracketed-shape sub-claim was FALSE: 47
#   bracket-delimited tokens exist, 39 of them the value-date
#   parenthetical the Belfius template depends on, so the marker strip is
#   now anchored to the angle-bracket shape. Two further facts found
#   while re-measuring and folded into the phase: 20 of 39 rows carry a
#   13-to-19-digit run, so five NON-card rows carry long structured
#   references a length-only strip would eat (fixture row and assertion
#   added), and the masked tail shape appears on 11 raw lines that reach
#   no parsed descriptor here, so PR3-002's second shape is prospective
#   robustness and is recorded as such rather than overclaimed.
#   PR3-004: new criterion 6.10 pins the merchant-source expression to
#   exactly three files (shared helper plus the two frozen recipes) and
#   H6.1 now points at it. PR3-005: criterion 6.6's migration test must
#   seed from PRE-change outputs and assert new(old(x)) equals new(raw).
#   PR3-006: criterion 6.5 gains the facts-untouched clause and H6.4 is
#   split, its rawLine clause becoming H6.7 addressed by 6.5. PR3-007:
#   criterion 6.6 re-measures the rule count at the release head.
#   PR3-008: criterion 6.7 names Playwright test.use, so files-to-touch
#   stays accurate and conflicts-with stays empty. D-11 keeps a forward
#   note: the second bank grammar is the moment to revisit it.
#   Re-validated to exit 0.
# - micro fix round (R-061), M3-P6 delta verdict at 2459464 (findings
#   PR4-001..PR4-003, all eight PR3 findings confirmed closed): applied
#   all three, disputed none. Each was re-measured through the shipped
#   pipeline before applying rather than taken on the verdict's word.
#   PR4-001 (high, a contradiction MY fix round created): criteria 6.1
#   and 6.3 were mutually unsatisfiable. Confirmed: the real structured
#   references are 13 and 15 digits (13-digit run x1, 15-digit x9) and 20
#   of 39 descriptors fall inside 6.3's 13-to-19 window while only 15 are
#   card rows, so a faithful fixture reference cannot avoid the sweep and
#   the only escape was a sub-13-digit reference that would have disarmed
#   the guard PR3-003 bought. 6.3 now sweeps for the fixture's invented
#   card number EXACTLY plus a general run-sweep with the 6.1 reference
#   row as its one named exception, and 6.1 pins that reference at 13 to
#   19 digits so the exception cannot be dodged by shrinking it.
#   PR4-002: my own corrected entry was imprecise a second time. Measured:
#   11 parsed DESCRIPTORS carry X tokens (not zero as I wrote), and ZERO
#   of them is a card descriptor, which is the fact the conclusion needs;
#   the conclusion stands, the stated reason did not. Entry restated to
#   the measured predicate, and step 3 now anchors the partially-masked
#   pattern to the card-tail grammar rather than to the presence of an
#   X token, because the only real instances sit on rows the strip must
#   not touch. PR4-003: 6.10 now fixes the LITERAL grep pattern and gains
#   a positive half, both former call sites importing the shared helper,
#   so a ternary-phrased duplicate or an inline recomputation cannot pass
#   a purely negative pin. Re-validated to exit 0.
# - final micro round (R-061), M3-P6 final verdict at 55c1dbf (findings
#   PR5-001..PR5-003; PR4-001 and PR4-002 confirmed closed): applied all
#   three, disputed none, every count executed against the tree at this
#   plan's head before being written into a criterion.
#   PR5-001 (high, blocker, a pin that could not pass): executed
#   grep -rln 'counterpartyName ??' src/ returns SIX files, because a
#   persistence mapper (import-repository.ts:271 and :360) and a preview
#   cell (profile-confirmation.tsx:100) carry unrelated null-coalescings,
#   neither of them the merchant-source rule and neither in
#   files-to-touch, so the bare literal would match FIVE after
#   unification against a criterion demanding THREE. Replaced with the
#   operand-bearing pattern, which I ran twice: as written by me, and
#   again after extracting it back out of the parsed YAML to be sure the
#   escaping survives, both returning exactly FOUR files today
#   (interpret-window.ts, merchant-review.ts, dedup.ts, corrections.ts)
#   and THREE after unification. The measured numbers are stated in the
#   criterion text itself.
#   PR5-002: 6.3(b)'s exception named the key while the prohibition
#   covers key or label, and for an unresolved group the two are the SAME
#   string (merchant-review.ts:86, month-projection.ts:83, both read in
#   the tree); the exception now covers that row's key and its label, so
#   the criterion no longer depends on the accident of that row being
#   resolved in the scenario.
#   PR5-003: the fixture's non-card control row had no stated shape, so
#   step 3's anchor had no witness. The control row now carries an
#   X-masked token plus holder-like text and NO card-number label, the
#   measured shape of 8 of the 11 real non-card X-token descriptors, and
#   criterion 6.1 asserts its key is unchanged by the strip, so a
#   masked-tail pattern keyed on holder text alone fails. Re-validated to
#   exit 0.
# - M0-P3 round (owner-directed, 2026-08-22), mobile-first rebuild amendment
#   under DR-0022. Beacon opened before verification. Baseline for this
#   round is b560e85 (the owner-approved mockup commit). Verification pass
#   in progress: measured grep -c "@media" src/app/globals.css = 0 at
#   b560e85, so the whole stylesheet has no breakpoint at any width. The
#   dispatch's stated root cause names .month-grid as
#   grid-template-columns: minmax(0, 1fr) var(--layout-rail); that is the
#   PRE-M3-P6 text. At b560e85 globals.css:704 is a wrapping flex, changed
#   by 5f7b62c. Recording as a failed claim. Next: read month-view.tsx,
#   merchant-review.tsx, the mockup frames, playwright.config.ts and the
#   e2e specs.
#   Verification pass findings so far, all at b560e85: NO viewport meta and
#   no viewport export anywhere in src (src/app/layout.tsx:1 through :19,
#   grep -rn viewport over src/app and src/platform returns nothing), so
#   whether a phone lays out at 390 or at the 980 default rests on Next's
#   implicit injection, which node_modules is absent here to confirm:
#   recorded UNVERIFIED and owned by the rebuild phase's step 1. The
#   shipped spend grouping COLLAPSES a tag to one row
#   (month-projection.ts:67 through :74) while the mockup shows merchant
#   rows under a tag subhead, so mockup fix 4 is a projection change, not
#   a layout change. ReviewGroup carries no month span
#   (merchant-review.ts:36) though the mockup prints "3 rows, June and
#   July". Every existing phone-viewport spec measures scrollWidth only
#   (navigation.spec.ts:168, merchants.spec.ts:166, import.spec.ts:184)
#   and month-view.spec.ts sets no viewport at all. Next: the tokens
#   inventory and the amendment itself.
#   Amendment written: two new phases, M3-P7 (constraint change, the five
#   missing tokens, the app shell and the month view, the phone regression
#   guard) and M3-P8 (merchant review). Eleven new report-code-disagreement
#   entries, four of them unverified and owned by the two verification-first
#   steps. Eight new decisions D-14 through D-21. One open question, DR-0023,
#   on whether the spend card gains merchant rows under tag subheads, with
#   the recommendation written first. M3-P4 amended so the skill paragraph
#   is written once: its step 1 becomes a check and criterion 4.4 is added,
#   4.1 through 4.3 byte unchanged. Seven parked items. Nothing under
#   delivery/intake/ touched. Validating with tiphys next.
# - M0-P3 FIX ROUND (R-061), adversarial plan review
#   delivery/review/pulse-v02-mobile-plan-adversarial.yaml at f1612ed,
#   verdict revise, findings MPR-001 through MPR-016. Merged origin/main
#   first (gate-privacy.sh and test/fixtures/allowed-identifiers.txt landed
#   after this branch was cut; bash scripts/gate-privacy.sh prints
#   "gate:privacy clean" at the merge). Re-verified every finding I applied
#   against the tree rather than taking the verdict's word. Confirmed
#   MPR-010's 19-line offset directly: MerchantReview.dc.html:69 is the
#   CRF label I had cited as :31 and :57 is the naming input I had cited as
#   :38; BooksDoNotClose.dc.html:86 is "Unexplained difference" cited as
#   :48. The cause was mine: I read those two frames through sed -n
#   '20,200p' with a re-labelled index and then quoted the index instead of
#   the file line. Main.dc.html was read with plain cat -n and its
#   citations are correct. README.md is 37 lines, so every ":24 through
#   :40" I wrote ran past the end of the file.
#   Applied 15 of the 16 findings, disputed none outright; MPR-009's
#   allow-list half was already closed on main and is recorded as closed
#   rather than re-done, which is the only half of any finding not acted on.
#   MPR-001 (critical) is closed by NEW criterion 7.14, which measures the
#   row itself in three ways and states the arithmetic that makes the tree
#   as it stands fail all three: .month-columns and .month-row are both
#   three-track grids, the name gets about 118 of the row's 310 content
#   pixels at 390, and the delta is baseline-aligned beside the name rather
#   than beneath it. New hazard H7.9 points at it, because H7.1 could not
#   reach a row that neither overflows nor clips. Also new: 7.15 and 8.10
#   (privacy, MPR-009), 5.5 (MPR-007). Rewritten: 7.3, 7.4, 7.6, 7.7, 7.8,
#   7.10, 7.11, 7.13, 8.2, 8.3, 8.4, 8.7, 8.9, and M3-P4's 4.1. Every
#   MerchantReview and BooksDoNotClose citation re-derived (+19) and every
#   README and canvas citation corrected; M3-P7 step 1 gains (e), a
#   re-check of every cited mockup line. M3-P1, M3-P2, M3-P3 and M3-P6
#   parse byte-identical to b4e6704; M3-P4 changes criterion 4.1 only,
#   M3-P5 adds 5.5 only. Re-validated to exit 0, dispatchable true, and
#   bash scripts/gate-privacy.sh prints "gate:privacy clean".
# - M0-P3 SECOND FIX ROUND (R-061), adversarial delta review at b4b012b,
#   verdict "revise, narrowly": thirteen of sixteen round-1 findings closed
#   cleanly and BOTH deviations from the reviewer's proposed edits were
#   judged correct and better than what it proposed (the overflow-y scoping
#   on 7.7(b) and 8.4(b), and the 7.13 fixture loosening); neither is
#   revisited. Seven new findings, MPR-017 through MPR-023, all applied.
#   MPR-020 was already closed by the merge at 25feca9 before this round
#   started: git merge-base --is-ancestor now reports 88c861e, 512b461 and
#   fd1d51d all ancestors of HEAD, the branch carries the corrected mockup
#   frame, and delivery/design/mobile-v02/README.md is 46 lines here. Its
#   criterion half is applied below. Re-verified the box chain myself before
#   rewriting 7.14: .app-main padding var(--space-12) var(--space-9)
#   (globals.css:181 through :187) is 48 horizontal, .month-card carries a
#   hairline each side (:727 through :731), .month-row takes 32 of padding
#   (:786). 390 less 48 less 2 is a 340 BORDER box and less 32 more is a 308
#   content box, which is why the denominator moved: the row's padding is
#   inside the old denominator and the same change sets it.
#   Applied all seven. 7.14(b) now takes its ratio against the BORDER box
#   and carries absolute floors of 180 at 390 and 160 at 360, and (b) and
#   (c) reach income and reserve rows as well as spend rows. Checked the
#   reviewer's padding lever against the new text before writing it: hold
#   the border box at 340 and raise the row's padding to 60 a side, and the
#   content box falls to 220, a two-track grid leaves the name 124 and the
#   distance 140; 140 of 340 is about 41 percent, under 55, and 140 is under
#   the 180 floor, so both halves fail it. The one-level-up variant, raising
#   .app-main's padding so the border box itself shrinks, passes the ratio
#   and fails the floor, which is why both are there. 7.6's floor list gains
#   the four cause blocks, the pill, spend-delta, group-label and the
#   conditional testids, and its self-referential clause is replaced by a
#   base-versus-head testid grep. 7.7 and 8.4 gain (d), the exact-label
#   check on the dense fixture, closing the truncate-before-render route.
#   7.4(d)'s pattern is pinned to a className value. 7.15(a) and 8.10(a) name
#   which gate by origin/main sha. Every README line citation is replaced by
#   the document's own fix numbers except one in a citations list, re-derived
#   to :34 against the merged tree. DenseAlternate is stated to carry no
#   requirement. Re-validated to exit 0.
kind: plan
status: draft
baseline-commit: e4ea3ba
binding-rule: >-
  If it is not written here, it is not being made. Unanswered questions go to
  the orchestrator.
process-summary: >-
  Orchestrated delivery on @tiphys/kernel 0.1.0 under the full mode: verified
  plan, adversarial plan review, one phase per branch, fast gate (npm run
  typecheck, npm run lint, npm test) every iteration, Playwright golden
  journey before a phase closes, dual clean-room review before merge under
  DR-0012's delegated-merge conditions and DR-0003's same-family override.
  This plan is the v0.2 iteration: one milestone, M3, dispatched BEFORE the
  DR-0002 one-real-month checkpoint by owner direction (fleet backlog,
  DR-0002 feedback log, 2026-08-21). M2 (intake slices 5 and 6) stays
  reserved and gated on DR-0002 exactly as the v1 plan left it.
standing-context: |
  What previous runs bought: the whole M1 walking skeleton, merged and
  deployed, release verification per DR-0001 green at the release head.
  Verified present at e4ea3ba and load-bearing for this plan: the
  StatementParser port with detect and parse (ports.ts:18 in
  src/modules/import/application/, implemented by the one delimited adapter,
  src/modules/import/adapters/delimited-file-parser.ts:10); deterministic
  profile detection (src/modules/import/domain/detect-profile.ts:1); the
  upload pipeline identify, declare, ingest, interpret
  (src/modules/import/application/upload-statement.ts:38 through :92) with
  the ask-once declaration branch (upload-statement.ts:57) and spec-equality
  profile recognition (upload-statement.ts:101,
  src/modules/import/domain/source-profile.ts:217); the import status
  machine with FAILED and machine-readable failureReason
  (prisma/schema/import.prisma:8, src/modules/import/application/ports.ts:38);
  immutable facts with rawContent and rawLine kept verbatim
  (prisma/schema/import.prisma:64, prisma/schema/import.prisma:106); the
  dedup recipe with the occurrence ordinal, which IS the addendum's section
  5 first amendment, already implemented and tested
  (src/modules/import/domain/dedup.ts:80, dedup.ts:95,
  prisma/schema/import.prisma:112); the card settlement matching step, which
  IS the addendum's section 5 second amendment, already implemented between
  the declared-set checks and the sign fallback
  (src/modules/ledger/domain/classify-flow.ts:77,
  src/modules/ledger/domain/constants.ts:16,
  src/modules/ledger/domain/corrections.ts:38) with the settlement total
  DERIVED from the card import's stored rows, never persisted
  (src/modules/ledger/domain/interpret.ts:67); the profile-fix re-parse path
  (src/modules/import/application/fix-profile.ts:1); and the reconciliation
  panel in the month view (src/modules/overview/ui/month-view.tsx:289).

  Deploy state: the M1 product is live on Vercel against the dedicated
  Supabase project; release verification is DR-0001 (fast gate plus golden
  journey at the release head).

  The input evidence: the owner's v0.2 addendum
  (delivery/intake/pulse-v0.2-pdf-addendum.md) and two REAL statement PDFs
  in this container's uploads directory, read by the scout for structure
  only. Every structural claim the addendum makes was checked against them
  and against the tree; the outcomes are in report-code-disagreement below.
  New anonymized structural facts are recorded in the fleet home note
  notes/export-format-facts.md (2026-08-21 section), the most consequential
  being: both real PDFs carry full text layers (no OCR anywhere in v0.2);
  KBC text extraction is word-gap tolerance-sensitive, so the extractor must
  reconstruct lines from positioned text items deliberately; and the KBC
  closing figure sits on the Afrekening line with a space thousands
  separator. The real PDFs are NEVER committed; committed fixtures are
  synthetic derivatives with invented identifiers, and every phase that
  touches fixtures carries that as a hazard class.

  Decision records touching this plan: DR-0002 stays OPEN and its clock
  starts only when the owner can feed Pulse from their phone (owner,
  2026-08-21). The two decision records this plan raised are now DECIDED by
  the owner (2026-08-21, fleet decisions/DR-0020.yaml and DR-0021.yaml):
  DR-0020, the PDF text-extraction runtime dependency, a charter stop-for
  ("any new runtime dependency beyond the declared stack", charter.yaml:92),
  is decided as pdfjs-dist, server side only, behind the parser adapter;
  DR-0021, the mobile share-sheet mechanism, is decided as Android, the
  share target IS built. M3-P2's and M3-P5's fill-ins are filled
  accordingly and both phases are dispatchable. M3-P1 (navigation) depends
  on neither and ships first.

  Revision note, fix round 1 under R-061: the adversarial plan review
  (delivery/review/pulse-v02-plan-adversarial.yaml on branch
  claude/m0-p2-plan-review, verdict revise, findings PR2-001 to PR2-008) is
  applied in full in this revision, no finding disputed. The one new
  structural fact it contributed: the bare BIJLAGE token also occurs INSIDE
  a transaction description block on a real Belfius transaction page, so
  annex skipping must be body-starts-with, never marker-anywhere, and the
  fixture now encodes that trap (PR2-002). The other findings tightened
  criteria from provable-in-prose to failable-by-test: recognition
  round-trip for pre-widening stored profiles (PR2-001), a cross-file
  overlap witness for the year-scoped dedup key (PR2-003), a
  month-straddling booking-date witness (PR2-004), an explicit and
  witnessed unauthenticated-share contract (PR2-005, decision D-10), a
  grep pin of the nav to the shell (PR2-006), format-naming copy updated
  with the capability it announces (PR2-007), and privacy gates covering
  all five promised categories on phase-touched files with a not-witnessed
  honesty clause (PR2-008).
  V0.2 MOBILE ROUND (added by the M0-P3 scout, 2026-08-22), phases M3-P7 and
  M3-P8. On first real use of the shipped month view the owner, on a phone,
  graded the product 2 out of 10 for quality and 1 out of 10 for usability
  and named the cause as the total lack of mobile first. DR-0022 (owner,
  2026-08-22) decides mobile first and supersedes the desktop-first paragraph
  at .claude/skills/pulse-frontend/SKILL.md:113. The owner approved a mockup,
  committed at delivery/design/mobile-v02/, whose README lists as fixes 1
  through 6 the six things it fixes and whose canvas.json fixes the frame at 390
  by 844.

  These two phases are grounded at b560e85, NOT at this plan's
  baseline-commit e4ea3ba. That field is deliberately left where it is: every
  citation in M3-P1 through M3-P6 resolves against e4ea3ba, M3-P1, M3-P2 and
  M3-P6 have merged since, and re-anchoring the field would silently invalidate
  the line numbers in six merged phases. Every file and line reference in
  M3-P7, in M3-P8 and in their report-code-disagreement entries was read at
  b560e85 and says so.

  The bar these phases are held to is the point of them. After the first
  mobile report the standing acceptance criterion was set as "phone viewport:
  controls visible and clickable, no horizontal scrolling", and the month view
  passes that bar today. The fleet backlog carries that as the orchestrator's
  own recorded error. CORRECTED in the M0-P3 fix round (finding MPR-016),
  because the first draft of this paragraph said the suite measures overflow
  "and nothing else", which is checkable and was false. The existing phone bar
  is visibility plus overflow and nothing more.
  test/e2e/navigation.spec.ts:161 through :192 asserts at 390 that main-nav,
  the three nav links, household-context and Sign out are visible and that
  Sign out is clickable; test/e2e/merchants.spec.ts:126 through :262 asserts
  masked card labels at 390; and all four phone assertions end at
  document.documentElement.scrollWidth (navigation.spec.ts:168,
  merchants.spec.ts:165, :214 and :259, import.spec.ts:184). Nothing measures
  a rendered height, a density, or whether content survived the narrowing.
  That is the orchestrator's recorded criterion word for word, controls
  visible and clickable with no horizontal scrolling, and the month view
  passes it today. test/e2e/month-view.spec.ts sets no viewport at all, so all
  six of its tests run at the desktop default (playwright.config.ts:53).
  Criteria 7.5 through 7.9, 7.14 and 8.2 through 8.6 measure rendered
  tap-target height, the same testid set at both widths, no element clipped
  on either axis anywhere in main, the reconciliation answer inside the first
  700 pixels, one column in income, spend, reserves order, and the two-line
  row itself. A screen that is unusable but does not scroll sideways fails
  every one of them. Criterion 7.14 is the one the M0-P3 adversarial review
  had to add (finding MPR-001, critical): the first draft measured everything
  around the row and never the row, so the three-track grid that spends 160
  of 390 pixels on two amount columns could have survived the whole rebuild.

  DR-0023 is raised by this round and stays OPEN. The question: does the spend
  card keep one row per tag, which is what the shipped projection emits
  (src/modules/overview/domain/month-projection.ts:67 through :74), or do
  merchant rows appear nested under tag subheads, which is what the approved
  mockup draws (delivery/design/mobile-v02/Main.dc.html:124 through :176) and
  what the mockup's README names as FIX 4? This plan's
  recommendation, written before the question was raised and stated so the
  owner can simply agree with it: keep one row per tag for the rebuild. It is
  a projection change rather than a layout change; it lengthens the spend card
  on the screen that was just graded 1 out of 10 for usability, by an amount
  this plan has NOT measured, which the M0-P3 fix round removed a made-up
  figure for (finding MPR-014) rather than leave a number nothing stands
  behind; and with zero merchant rules in the owner's data it renders nothing
  either way today. Neither M3-P7 nor M3-P8 waits on the answer and no
  criterion in either phase changes with it; the affected slot is the spend
  card's internal grouping and nothing else.

  WHAT AGREEING OR DISAGREEING COSTS, added in the M0-P3 fix round because the
  question as first framed did not say. This recommendation FORECLOSES mockup
  fix 4 for v0.2: criteria 7.10 and 8.7 pin the overview projection to an
  empty diff and no phase in this plan carries the change, so a yes answer
  lands as a NEW phase after M3-P8 rather than inside either rebuild. The
  question the owner is answering is therefore whether tag subheads ship in
  v0.2 or later, not whether they ship at all.

report-code-disagreement:
  - phase: M3-P1
    claim: >-
      Owner feedback item 1 (fleet backlog, DR-0002 feedback log): the app
      has no general navigation; the month empty state names the import
      action with no link; once data exists there is no way to reach /import
      or /merchants from the header. Checked: the authenticated shell header
      renders brand, household name and sign-out only, no nav links
      (src/app/(app)/layout.tsx:18 through :26); the empty state renders
      title and body naming the import action with no Link element
      (src/modules/overview/ui/month-view.tsx:387 through :396,
      messages/en.json:28); the routes exist at src/app/(app)/import/page.tsx
      and src/app/(app)/merchants/page.tsx, reachable today only by typed
      URL.
    verified: true
  - phase: M3-P2
    claim: >-
      Addendum section 1 (pulse-v0.2-pdf-addendum.md:18): "PDF is a second
      adapter behind the existing StatementParser port. Nothing upstream
      changes." The port exists and its shape holds
      (src/modules/import/application/ports.ts:18: detect bytes to spec,
      parse bytes plus spec to statement), and the use-case pipeline is
      untouched in the addendum's sense. But the claim FAILS in the strict
      reading: both port methods are typed to SourceProfileSpec, which is a
      flat delimited-only record (src/modules/import/domain/
      source-profile.ts:49: delimiter, encoding, headerRowIndex, dateFormat,
      decimalStyle, amountRepresentation, columns), parseStatementRow
      assumes delimited lines (src/modules/import/domain/
      parse-statement.ts:60), and hasNaturalKey and specEquals read
      delimited fields (source-profile.ts:62, source-profile.ts:217). A PDF
      adapter cannot slot in until SourceProfileSpec becomes a discriminated
      union, and the failure-reason union must grow
      (src/modules/import/application/ports.ts:38 carries exactly
      mixed-accounts, undetectable, unparseable). Owned by M3-P2's
      verification-first step; the widening is decision D-2.
    verified: false
  - phase: M3-P2
    claim: >-
      Addendum section 1 (pulse-v0.2-pdf-addendum.md:19): identify, declare,
      ingest, interpret are untouched, and the ask-once account-declaration
      step stays exactly as is with only the format question disappearing
      for recognised layouts. Checked: the pipeline lives in
      src/modules/import/application/upload-statement.ts:38 through :92
      (detect, parse, mixed-account guard, declaration branch at :57, ingest
      at :74, interpret at :92); profile recognition is spec equality
      (upload-statement.ts:101), which a code-owned template spec satisfies
      by template id; account identity rides the file's own-account IBAN or
      the profile binding (upload-statement.ts:113,
      prisma/schema/import.prisma:43). The flow accommodates PDF without
      structural change once the spec type is widened.
    verified: true
  - phase: M3-P2
    claim: >-
      Addendum section 2 (pulse-v0.2-pdf-addendum.md:35): both statement
      types carry an opening balance, the transactions, and a closing
      balance or settlement total, and opening plus the sum of parsed
      transactions equals closing in integer cents. Checked arithmetically
      against both real PDFs in-container: Belfius opening SALDO OP plus the
      sum of all 39 transaction amounts equals the closing SALDO OP exactly;
      KBC previous balance plus the sum of all 22 rows including the
      settlement credit equals the Afrekening amount exactly. The identity
      is real and usable as a hard gate.
    verified: true
  - phase: M3-P2
    claim: >-
      Addendum section 3 structural claims for the Belfius layout
      (pulse-v0.2-pdf-addendum.md:50 through :62): fingerprint text plus
      SALDO OP lines, IBAN in a band line between dashes, a repeated
      per-page header block, annex pages starting with the BIJLAGE marker
      that contain amount-like non-transactions, opening and closing as
      first and last SALDO OP lines, and the transaction start pattern
      (4-digit sequence, DD-MM-YYYY booking date, VAL. value date, sign and
      amount at line end). Checked against the real statement's text layer:
      all present; fingerprint text on all 5 pages; 4 band lines; 39
      transaction starts on pages 1 through 4; page 5 is the annex and
      contributes zero transaction starts; exactly 2 SALDO OP lines and the
      balance identity closes. Counterparty IBANs inside descriptions
      confirmed (STORTING VAN and NAAR followed by IBAN shapes).
    verified: true
  - phase: M3-P2
    claim: >-
      Addendum section 3 amount traps (pulse-v0.2-pdf-addendum.md:63):
      decimal comma, thousands dot only from 1.000,00 up, and the space
      between sign and amount disappears on large amounts. Checked: decimal
      comma everywhere and thousands dot only on large amounts hold; the
      sign-space correlation is DIRECTIONAL, not strict (spaced signs were
      observed only without a thousands dot, but tight signs occur both with
      and without one). The parser requirement the addendum draws, accept
      both forms, is the right one and is what M3-P2 builds; a parser
      encoding the strict correlation would reject real lines.
    verified: true
  - phase: M3-P2
    claim: >-
      Addendum section 3 dedup key (pulse-v0.2-pdf-addendum.md:68): natural
      key is account plus year of booking date plus sequence number, the
      sequence being per account and continuous across statements within a
      year. Two failures of verification: (a) the implemented natural key at
      e4ea3ba is a DIFFERENT shape, account plus statementNumber plus
      sequenceNumber (src/modules/import/domain/dedup.ts:95), so the
      addendum's key is new wiring, not existing behaviour; (b) continuity
      of the sequence ACROSS statements cannot be verified from the single
      real statement available (continuity within it holds, 39 consecutive).
      Owned by M3-P2's verification-first step; the key choice is decision
      D-4.
    verified: false
  - phase: M3-P2
    claim: >-
      Addendum section 5 first amendment (pulse-v0.2-pdf-addendum.md:91):
      "the current hash key collides and would silently drop one" of two
      identical rows, so the key must gain an occurrence index. STALE
      relative to e4ea3ba: the occurrence ordinal is implemented exactly as
      the addendum specifies, including the cross-import
      highest-occurrence-wins semantics
      (src/modules/import/domain/dedup.ts:80 through :105,
      prisma/schema/import.prisma:112), with the identical-pair fixture and
      tests from M1-P2 (test/domain/dedup.test.ts). The v1 plan absorbed
      this amendment in its fix rounds. No new build is owed; M3-P3 only
      re-witnesses it through the PDF path.
    verified: true
  - phase: M3-P2
    claim: >-
      Addendum section 6 (pulse-v0.2-pdf-addendum.md:111): fixtures may be
      synthetic PDFs "or their extracted text, if the parser consumes a text
      layer". Checked: BOTH real statements carry full machine-readable text
      layers (extraction in-container produced complete text for all pages),
      so the v0.2 parser consumes the text layer and no OCR path exists in
      this plan. Fixture format is decision D-3.
    verified: true
  - phase: M3-P3
    claim: >-
      Addendum section 4 structural claims for the KBC layout
      (pulse-v0.2-pdf-addendum.md:73 through :88): fingerprint text, masked
      card number, statement number and period line, row shape (transaction
      date, settlement date, description, amount), FX continuation lines
      belonging to the preceding row, the previous-balance and total lines
      being balances not transactions, and the settlement credit line being
      a real transaction. Checked against the real statement's text layer:
      all present (22 rows; 4 FX transactions each with two continuation
      lines). Three nuances the addendum does not state, recorded in
      notes/export-format-facts.md: the closing figure sits on the
      Afrekening line, not the Totaal bedrag line, and uses a SPACE
      thousands separator; text extraction is word-gap tolerance-sensitive
      (default assembly glues words), so the extractor reconstructs lines
      from positioned items and template matching must not depend on
      interior label spacing; and the DOMICILIERING credit equals the
      negated previous balance exactly.
    verified: true
  - phase: M3-P3
    claim: >-
      Addendum section 4 (pulse-v0.2-pdf-addendum.md:86): no sequence
      numbers exist in the card format, so the hash dedup key applies with
      the occurrence amendment, and a real identical duplicate row pair
      exists. Checked: the real statement carries no per-row sequence
      column, and exactly one identical pair (same transaction date,
      description, amount) occurs; the implemented hash path with occurrence
      ordinal (src/modules/import/domain/dedup.ts:80) covers it.
    verified: true
  - phase: M3-P3
    claim: >-
      Addendum section 5 second amendment (pulse-v0.2-pdf-addendum.md:100):
      the settlement debit carries no IBAN, so classification gains a step
      between the declared-set checks and the sign fallback, matching by
      amount against an imported card statement's settlement total within a
      date window, with the unimported-card SPEND fallback. STALE relative
      to e4ea3ba: implemented as decision D-11 of the v1 plan
      (src/modules/ledger/domain/classify-flow.ts:77,
      src/modules/ledger/domain/corrections.ts:38,
      src/modules/ledger/domain/constants.ts:16), and the credit-side
      pattern already matches the DOMICILIERING line
      (src/modules/ledger/domain/constants.ts:26). The settlement total is
      derived from stored rows (src/modules/ledger/domain/interpret.ts:67),
      so KBC PDF imports feed it with no schema change. No new build is
      owed; M3-P3 witnesses the pairing end to end through two PDF imports.
    verified: true
  - phase: M3-P4
    claim: >-
      Addendum section 7 (pulse-v0.2-pdf-addendum.md:123): the v1 desktop
      stance was downstream of CSV, PDF exports come from the phone, the
      natural v0.2 import path is the phone share sheet, and the frontend
      skill's desktop-first line gets revisited in the v0.2 plan. Checked:
      the line exists verbatim (.claude/skills/pulse-frontend/SKILL.md:113,
      "Desktop first. The import is a desk activity."); the upload input
      accepts CSV only (src/modules/import/ui/upload-form.tsx:22,
      accept=".csv,text/csv"); no PWA manifest and no share-target handler
      exist anywhere in the tree (no public/ directory, no manifest file
      under src/app). The share-sheet MECHANISM is platform-dependent and
      unverifiable from the repository: Web Share Target requires an
      installed PWA and exists on Android/Chromium only, with no iOS Safari
      support. That platform question is DR-0021; the mobile-first upload
      flow itself (M3-P4) is platform-neutral and not gated on it.
    verified: true
  - phase: M3-P6
    claim: >-
      Owner defect 1, deployed Belfius PDF import (2026-08-21): merchant
      grouping is dead for card rows because
      src/modules/ledger/application/interpret-window.ts:87 sets the
      merchant text to counterpartyName ?? description and card payments
      carry no counterparty account, so the whole descriptor becomes the
      key. Re-measured by the scout in-container through the SHIPPED
      pipeline (pdfjs extractor, reconstructed lines, Belfius template,
      shipped merchants normaliser; counts only, no content copied): 39
      parsed rows, 15 card-payment rows, ZERO of them carrying
      counterpartyName, 15 distinct raw sources and 15 distinct keys after
      the shipped normaliser, so the month view degenerates to one group
      per transaction exactly as reported. One detail the report does not
      state and this phase must carry: the same merchant-source rule is
      DUPLICATED at src/modules/merchants/domain/merchant-review.ts:50, so
      a fix applied in one place leaves the review screen grouping on the
      old rule.
    verified: true
  - phase: M3-P6
    claim: >-
      The report's count of five real merchants behind the fifteen card
      rows. NOT independently confirmed: the scout's crude strip of the
      rail prefix, the embedded date, the amount and the card tail leaves
      SIX distinct candidates, the residual variance being city and
      country tails that a token-list strip would still have to absorb.
      The direction is confirmed (fifteen rows collapse to single digits);
      the exact number is not, and no criterion in this phase may hardcode
      it, because every criterion runs against synthetic fixtures rather
      than the real statement. Owned by the verification-first step.
    verified: false
  - phase: M3-P6
    claim: >-
      Owner defect 2: the statement prints the full unmasked card number
      and it reaches the rendered descriptor. CORRECTED in fix round 2
      after finding PR3-003, with the predicate this plan previously left
      unstated: a CARD ROW is a parsed row whose normalised descriptor
      matches the card-number label pattern, and by that predicate 15 of
      39 rows are card rows. Every one of the 15 carries a card-number run
      in its normalised descriptor, which is the string both label
      surfaces render (src/modules/overview/ui/month-view.tsx:78 and
      src/modules/merchants/ui/merchant-review.tsx:29), but the run is
      GROUPED four by four with single spaces: contiguous 16-digit runs in
      the parsed descriptors number ZERO, which is why criterion 6.3 is
      now separator-insensitive (finding PR3-001). The earlier wording,
      "15 of 15 card rows carry a 16-digit run", was true only under a
      separator-insensitive reading it did not state. The reported count
      of two distinct sequences still does not reproduce: exactly ONE
      distinct number in 15 occurrences. Two further measured facts the
      strip rule must respect: 20 of the 39 rows carry a 13-to-19-digit
      run once separators are removed, so five NON-card rows carry long
      structured references that a length-only rule would eat; and at
      descriptor level exactly one card-tail shape occurs in this
      statement, while X-masked tokens appear in 11 parsed DESCRIPTORS, in
      two shapes, and NONE of those 11 is a card descriptor. Corrected
      again in the micro round after finding PR4-002: the earlier wording
      said those shapes reach no parsed descriptor, which an independent
      measurement contradicts; the conclusion it supported survives
      unchanged, because what the conclusion needs is that no CARD
      descriptor carries the masked shape. This statement therefore
      carries no measured key-level split from a second card-tail shape,
      and the second shape required in the fixture (finding PR3-002) is
      prospective robustness rather than a reproduction of a measured
      split.
    verified: true
  - phase: M3-P6
    claim: >-
      The angle-bracketed country marker in Belfius card descriptors is
      faithful input rather than an extraction artifact. CORRECTED in fix
      round 2 after finding PR3-003, because one sub-claim of the earlier
      wording was false and it was the load-bearing one. What reproduces
      on re-measurement: the angle token occurs on exactly 8 reconstructed
      lines, and 7 of the 15 card rows carry it inside their descriptor.
      What does NOT: it is the only ANGLE-bracketed shape, not the only
      bracketed shape. The document carries 47 bracket-delimited tokens,
      39 of them the parenthesised value-date token on every transaction
      start line, which the Belfius template depends on. A strip rule may
      treat the angle marker as stable grammar ONLY when anchored to the
      angle-bracket shape; a bracket-general strip would eat the value
      date and break the template.
    verified: true
  - phase: M3-P6
    claim: >-
      The dedup key's normaliser is a separate frozen copy that this phase
      must not touch. Verified: src/modules/import/domain/dedup.ts:30
      defines normalizeCounterpartyForKey with the frozen stability
      contract at dedup.ts:18, and the merchants normaliser carries its
      own contract naming the dedup one as its sibling
      (src/modules/merchants/domain/normalise-counterparty.ts:16). The two
      are independent by design and the ledger's refund correction keys on
      a third function (src/modules/ledger/domain/corrections.ts:169),
      which this phase also leaves alone.
    verified: true
  - phase: M3-P6
    claim: >-
      The merchants normaliser carries a stability contract (M1-P4 finding
      CR-402) requiring any recipe change to ship EITHER a one-off
      re-normalisation of stored MerchantRule patterns OR a recorded
      versioning decision. Verified verbatim at
      src/modules/merchants/domain/normalise-counterparty.ts:16 through
      :34, with the pinned regression table it names present at
      test/domain/normalise-counterparty.test.ts:119, and the EXACT rule
      subject written through the same pipeline at
      src/modules/merchants/application/assign-merchant.ts:54.
    verified: true
  - phase: M3-P6
    claim: >-
      Production currently holds no MerchantRule rows, so the
      recipe-change window is open. NOT verifiable at planning time: a
      plan-writer runs no database command, and the repository cannot
      answer what the deployed database holds. The claim is made
      CHECKABLE rather than asserted: the phase's verification-first step
      measures the count with a pinned read-only connection and records
      the number and the branch it selects before the recipe changes, and
      criterion 6.6 fails if that record is missing.
    verified: false
  - phase: M3-P7
    claim: >-
      The M0-P3 dispatch's stated root cause names the layout rule as
      ".month-grid is declared as grid-template-columns: minmax(0, 1fr)
      var(--layout-rail), a fixed two-column desktop grid". That is the
      text at e4ea3ba, not at b560e85. Checked at b560e85: the rule is a
      WRAPPING FLEX, src/app/globals.css:704 through :709 declaring display
      flex with flex-wrap wrap, with src/app/globals.css:711 through :714
      giving spend flex 1 1 var(--layout-rail) and src/app/globals.css:716
      through :719 giving the rail min-width min(100%, var(--layout-rail));
      commit 5f7b62c (M3-P6) made that change and its own comment
      (src/app/globals.css:696 through :703) records why. The top-level
      grid therefore DOES collapse to one column on a phone already, and
      the plan must not spend a step fixing it. What survives of the claim
      is the measured half: grep -c "@media" src/app/globals.css returns 0,
      so the stylesheet carries no breakpoint at any width, and
      src/app/globals.css:564 still reads "Desktop first". The real defect
      is one level down: src/app/globals.css:756 through :760 and
      src/app/globals.css:781 through :786 both declare
      grid-template-columns minmax(0, 1fr) auto auto, a three-column row
      that does not wrap, with src/app/globals.css:767 and :772 pinning two
      of those columns to min-width var(--space-14), which is 80px each
      (styles/tokens.css:121).
    verified: false
  - phase: M3-P7
    claim: >-
      A phone lays the product out at the phone's width. CONFIRMED in the
      M0-P3 fix round (finding MPR-015), and recorded here as settled rather
      than left as this amendment's largest open item, which is what it was
      when first written. src/app/layout.tsx:1 through :19 exports metadata
      only and declares no viewport object, and grep -rn "viewport" over
      src/app and src/platform returns nothing at b560e85, so this codebase
      writes no viewport tag of its own. Next's App Router injects
      width=device-width, initial-scale=1 when no route exports a viewport
      object, and that exact string was read off the deployed application's
      rendered head on 2026-08-22 (fleet backlog, M0-P3 corrections). The
      consequence for the plan: criterion 7.2 pins a FRAMEWORK DEFAULT this
      project does not control, which is why it is kept rather than dropped,
      and no phase needs to edit src/app/layout.tsx. It stays worth pinning
      because no existing spec can see the tag either way: Playwright
      chromium without isMobile ignores it entirely and sizes the layout
      viewport directly, so test/e2e/navigation.spec.ts:168,
      test/e2e/merchants.spec.ts:165 and test/e2e/import.spec.ts:184 pass
      whether the tag is there or not, which is decision D-21's whole
      reason.
    verified: true
  - phase: M3-P7
    claim: >-
      Mockup fix 4 ("Tag names as subheads inside the spend card rather than
      a separate level of nesting", delivery/design/mobile-v02/README fix 4)
      is a layout change. It is NOT: it is a projection change. The mockup
      draws a tag subhead with its merchants listed beneath it
      (delivery/design/mobile-v02/Main.dc.html:124 through :176: the
      "Boodschappen" subhead followed by two merchant rows). The shipped
      projection cannot emit that shape: src/modules/overview/domain/
      month-projection.ts:67 through :74 folds every tagged merchant row
      into ONE group keyed "tag:<name>" whose label is the tag, so the
      merchant's own total never reaches the screen at all, and
      src/modules/overview/application/month-overview.ts:101 turns that
      folding on for spend. Producing the mockup's shape means changing
      OverviewGroup (month-projection.ts:26 through :40) and the delta
      attachment keyed on group key (month-projection.ts:118 through :132).
      Raised to the owner as DR-0023; M3-P7 is planned around the slot and
      changes no projection file (criterion 7.10).
    verified: false
  - phase: M3-P7
    claim: >-
      The mockup implies design values that styles/tokens.css does not carry
      at b560e85, and CLAUDE.md non-negotiable 4 requires the token to exist
      before a rule uses it. Checked against styles/tokens.css:82 through
      :132. FIVE are genuinely missing. (i) A 44px minimum tap target
      (delivery/design/mobile-v02/Main.dc.html:26, :29, :40;
      delivery/design/mobile-v02/MerchantReview.dc.html:57, :58): the space
      scale runs 2, 4, 6, 8, 10, 12, 16, 20, 24, 28, 32, 40 and 80px
      (styles/tokens.css:109 through :121) and carries no 44. (ii) A 56px
      list-row minimum height (Main.dc.html:85): no 56 in that scale.
      (iii) A 40px phone headline figure size (Main.dc.html:60): the type
      scale (styles/tokens.css:82 through :91) has --text-3xl at 32px and
      --text-4xl at 44px and nothing at 40. (iv) A -0.02em tracking for that
      figure (Main.dc.html:60): styles/tokens.css:97 and :98 carry -0.03em
      and -0.01em and nothing between. (v) A breakpoint value of any kind:
      styles/tokens.css:127 and :128 carry --layout-max and --layout-rail
      and no breakpoint, and grep -c "@media" src/app/globals.css returns 0.
      FOUR further mockup values are implied and deliberately NOT added,
      because an existing token sits within 2px and growing the scale for
      that is how a scale stops meaning anything: 14px card padding
      (Main.dc.html:66, use --space-7), 18px button padding
      (MerchantReview.dc.html:58, use --space-8), a 3px gap
      (BooksDoNotClose.dc.html:81, use --space-2) and a 1.45 line-height
      (BooksDoNotClose.dc.html:86, use --leading-normal). One existing token
      is dead and stays dead: --text-4xl at 44px (styles/tokens.css:91) is
      referenced by nothing but the theme re-export (grep for text-4xl over
      src/ and styles/ returns only styles/pulse-theme.css:43).
    verified: true
  - phase: M3-P7
    claim: >-
      The mockup can be copied as drawn. It cannot, in two places. It draws
      five inline SVG icons (chevrons at Main.dc.html:41 and :44, a check at
      Main.dc.html:68, an alert circle at BooksDoNotClose.dc.html:62, an
      alert at Main.dc.html:52) where the shipped screen uses text glyphs
      (src/modules/overview/ui/month-view.tsx:447 and :456), and grep -rln
      "<svg" src/ returns nothing at b560e85, so the product carries no icon
      and no icon library today. Two of those SVGs hardcode an oklch stroke
      colour (Main.dc.html:68, BooksDoNotClose.dc.html:62), which
      CLAUDE.md non-negotiable 4 forbids in a component. Resolved: icons are
      hand-written inline SVG with stroke="currentColor" and no new runtime
      dependency, that being a charter stop-for (charter.yaml:92), and the
      colour-literal grep still exits 1 over src/ (criterion 7.4).
    verified: true
  - phase: M3-P7
    claim: >-
      The mockup's own tap targets are all 44px. They are not, and the M0-P3
      review found it (finding MPR-012) where this scout's own pass did not.
      delivery/design/mobile-v02/README states FIX 2 as "Tap targets of
      at least 44px on every row that navigates or opens", and
      Main.dc.html:26, :29 through :31, :40 and :43 honour it, but the
      unnamed-counterparties pill at Main.dc.html:51 carries min-height 32px
      while being an anchor that navigates to /merchants. The shipped control
      is the same thing: src/app/globals.css:651 styled as a Next Link at
      src/modules/overview/ui/month-view.tsx:484 through :495. Resolved in
      favour of the CRITERION and against the mockup: the pill is rebuilt at
      the 44px token like every other navigating control, because README fix 2
      is the rule the mockup states and the pill is the mockup breaking it,
      and because a control that opens the merchant review is exactly the row
      the owner taps on a phone. Criterion 7.5 names it.
    verified: true
  - phase: M3-P7
    claim: >-
      The mockup truncates and the product must not. Every long name in the
      mockup carries overflow hidden with text-overflow ellipsis and
      white-space nowrap (Main.dc.html:87, :131, :158, :168). The shipped
      screen wraps instead: src/app/globals.css:803 through :808 sets
      text-wrap pretty and overflow-wrap anywhere on .month-group-label, and
      .claude/skills/pulse-frontend/SKILL.md:122 forbids "no truncation of
      anything the user needs" in as many words. Resolved in favour of the
      shipped screen: names wrap, never ellipsize, and criterion 7.7
      fails on any clipped element. The one deliberate exception in the tree
      stays exactly as merged: src/app/globals.css:160 through :166 lets the
      household identity ellipsize, which M3-P1 criterion 1.5 decided.
    verified: true
  - phase: M3-P7
    claim: >-
      The mockup's copy is copy. It is not: the mockup's README says in its
      own words, in the paragraph introducing the frames, that they are "not
      a source of truth for copy" and the shipped
      catalogue already carries every string the rebuild needs. The mockup
      shortens the reconciliation part labels to In, Out, Reserves and Pot
      (Main.dc.html:72 through :75), adds "compared with June 2026"
      (Main.dc.html:63), "Unexplained difference" (BooksDoNotClose.dc.html:67)
      and "What is missing" (BooksDoNotClose.dc.html:79). messages/en.json
      already carries reconciles ("Books close"), reconBroken ("Books do not
      close"), potChange ("Change in the pot"), income, spend, reserves,
      difference and vsMonth, 111 keys in total. Resolved in favour of the
      shipped catalogue: the rebuild adds and changes no message key, in any
      of the three languages, and criterion 7.10 pins messages/ to an empty
      diff.
    verified: true
  - phase: M3-P7
    claim: >-
      The phone viewport is already covered by tests. It is covered by
      VISIBILITY PLUS OVERFLOW and by nothing more. CORRECTED in the M0-P3 fix
      round (finding MPR-016): the first draft of this entry said "one
      measurement, horizontal overflow, and nothing else", which understated
      what the suite does and was checkably false.
      test/e2e/navigation.spec.ts:161 through :175 asserts at 390 that
      main-nav, the three nav links, household-context and the Sign out button
      are visible on every authenticated route, and :186 through :190 clicks
      Sign out at that width and asserts the sign-in heading;
      test/e2e/merchants.spec.ts:163 through :164 asserts masked card labels at
      390. Every one of those describes then ends at
      document.documentElement.scrollWidth
      (test/e2e/navigation.spec.ts:168 through :172,
      test/e2e/merchants.spec.ts:165, :214 and :259, and
      test/e2e/import.spec.ts:184 through :187), compared to the viewport
      width. Nothing measures a rendered height, a density, or whether content
      survived the narrowing. The accurate version is the stronger argument,
      because "visible and clickable with no horizontal scrolling" is the
      orchestrator's recorded wording that this round exists to replace.
      test/e2e/month-view.spec.ts sets no viewport anywhere in the
      file, so all six of its tests run at the chromium project's Desktop
      Chrome default (playwright.config.ts:53 through :58). The month view at
      b560e85 therefore passes the entire phone-viewport suite while being
      the screen the owner graded 1/10 for usability. This is the
      orchestrator's named error, on the record in the fleet backlog, and
      criteria 7.5 through 7.9 exist so it cannot repeat.
    verified: true
  - phase: M3-P8
    claim: >-
      The mockup's merchant review can be built from the shipped projection.
      Three of its elements have no data and no capability behind them.
      (i) A month span per group, "3 rows and June and July"
      (MerchantReview.dc.html:53, :68, :83): ReviewGroup carries key, label,
      merchantId, counterpartyText, totalCents and count and no date
      (src/modules/merchants/domain/merchant-review.ts:36 through :47), and
      CountedRow carries no booking date either (merchant-review.ts:22
      through :29), so nothing in the review projection can produce it.
      (ii) A "Recently named" list with a Rename link
      (MerchantReview.dc.html:93 through :110): no rename capability exists,
      src/modules/merchants/ui/actions.ts and
      src/modules/merchants/application/assign-merchant.ts expose assignment
      only, and messages/en.json carries no rename key. (iii) A named count,
      "3 left and 17 named" (MerchantReview.dc.html:43 through :45): the
      projection exposes unresolvedCount only (merchant-review.ts:54) and
      the screen renders it at
      src/modules/merchants/ui/merchant-review.tsx:114 through :117. All
      three are resolved in favour of the shipped screen and parked; none is
      built by M3-P8, and criterion 8.7 pins the domain, the application
      layer and messages/ to an empty diff.
    verified: false
  - phase: M3-P8
    claim: >-
      The mockup's structure can replace the shipped structure. It cannot
      without losing a guarantee a merged phase bought. The mockup drops the
      two direction sections and their totals entirely
      (MerchantReview.dc.html:48 through :111 shows no income or spend
      total). Those totals are load-bearing:
      src/modules/merchants/ui/merchant-review.tsx:14 through :16 records
      that they render beside each section precisely so that naming can be
      SEEN to leave them unchanged, which is hazard H3.2 from M1-P4, and
      test/e2e/merchants.spec.ts:18 asserts exactly that across a naming.
      The mockup also orders groups by resolution state where the shipped
      screen orders by magnitude in the DOMAIN
      (src/modules/merchants/domain/merchant-review.ts:57 through :67).
      Resolved in favour of the shipped screen on both counts: the direction
      sections and their totals stay (criterion 8.5), and the ordering is
      not touched, because changing it is a domain edit and M3-P8 is a
      layout phase. The unresolved rows already carry their own flag
      treatment at any width (src/app/globals.css:498 through :502).
    verified: true
  - phase: M3-P8
    claim: >-
      The merchant review's controls are usable on a phone. They are not, by
      the 44px bar the mockup sets. The naming input carries padding
      var(--space-3) var(--space-5) on font-size var(--text-base)
      (src/app/globals.css:522 through :529, which is 6px of vertical
      padding on 13px text plus a 1px border) and the submit button carries
      padding var(--space-3) var(--space-6) on the same font size
      (src/app/globals.css:536 through :544), so both render around 30px
      tall, not 44. The row itself wraps at phone width already
      (src/app/globals.css:487 through :496, added by M3-P6 for the overflow
      measurement) so the form drops onto its own line and nothing scrolls
      sideways, which is exactly why the shipped bar passed it. The mockup
      sets both controls to min-height 44px
      (MerchantReview.dc.html:57 and :58). Criterion 8.2 measures the
      rendered height instead of the overflow.
    verified: true
phases:
  - id: M3-P1
    branch: claude/m3-p1-navigation
    intent: >-
      General navigation, the owner's phase-blocking feedback item. Header
      navigation in the authenticated shell (overview, import, merchants)
      with a visible current-route state, and an empty state that links to
      the import screen instead of only naming it. Small and shippable
      first, before any PDF work.
    grounding: >-
      Fleet backlog, DR-0002 feedback log item 1 (owner, 2026-08-21,
      phase-blocking): "no general navigation so later imports past empty
      screen also won't work". Verified at e4ea3ba: the shell header carries
      no nav (src/app/(app)/layout.tsx:18 through :26); the empty state
      names the import action with no link
      (src/modules/overview/ui/month-view.tsx:387,
      messages/en.json:28); the target routes exist
      (src/app/(app)/import/page.tsx, src/app/(app)/merchants/page.tsx).
    severity: high
    verified-root-cause: >-
      Not a defect in any module's logic: M1 phases each built their screen
      and no phase owned the shell's navigation, so none exists. The fix is
      additive UI in the authenticated shell and the empty state.
    steps:
      - text: >-
          Add a nav element to the authenticated shell header
          (src/app/(app)/layout.tsx:18): three links, overview at /, import
          at /import, merchants at /merchants, each with a stable
          data-testid and an aria-current or data-active marker on the
          current route. Labels come from the message catalogs (new keys in
          messages/en.json, messages/nl.json, messages/fr.json, EN as
          source), never hardcoded copy. Styling with existing design
          tokens only; a missing token is added to styles/tokens.css first
          (CLAUDE.md non-negotiable 4).
      - text: >-
          Give the month view's empty state
          (src/modules/overview/ui/month-view.tsx:387) a Link to /import,
          with its label from the catalogs, so the named action is
          reachable where it is named.
      - text: >-
          Add a fast-gate catalog parity test (new file under test/) that
          reads messages/en.json, messages/nl.json and messages/fr.json and
          asserts the three key sets are identical, so this and every later
          phase cannot ship an untranslated key. No such test exists at
          e4ea3ba (checked under test/).
      - text: >-
          Add test/e2e/navigation.spec.ts: on an empty household the empty
          state's link navigates to /import; seeded with data, the header
          links navigate between /, /import and /merchants and the active
          marker follows the route; the nav is present on all three routes.
    files-to-touch:
      - src/app/(app)/layout.tsx
      - src/modules/overview/ui/month-view.tsx
      - messages/en.json
      - messages/nl.json
      - messages/fr.json
      - styles/tokens.css
      - test/e2e/navigation.spec.ts
      - test/app/ (the catalog parity test)
    extras:
      - src/platform/ui/nav-link.tsx
      - src/app/globals.css
    acceptance:
      - id: "1.1"
        criterion: >-
          npm run test:e2e exits 0 with test/e2e/navigation.spec.ts passing:
          it asserts the three header nav links are present (by testid) on
          /, /import and /merchants, that clicking each navigates to its
          route, that the active marker matches the current route, and that
          the empty state for a household with no imports contains a link
          whose click lands on /import. And the nav is rendered by the
          shell, not by pages: grep -rl for the nav's data-testid over src/
          matches exactly one component file, src/app/(app)/layout.tsx
          (finding PR2-006: per-page copies on the three current routes
          would pass the route assertions and ship the next route bare).
      - id: "1.2"
        criterion: >-
          npm test exits 0 including the new catalog parity test, which
          fails red when any key exists in one of messages/en.json,
          messages/nl.json, messages/fr.json and not the others (witnessed
          red once in the work history by removing a key locally, then
          green).
      - id: "1.3"
        criterion: >-
          npm run typecheck && npm run lint && npm test exit 0 at the phase
          head, and grep -rE "oklch\(|#[0-9a-fA-F]{3,8}" over src/ excluding
          the token and theme files exits 1.
      - id: "1.4"
        criterion: >-
          The pre-existing test/e2e/golden-journey.spec.ts passes unmodified
          except for selectors the new header markup forces; git diff
          --name-only against the phase base shows no deleted test files.
      - id: "1.5"
        criterion: >-
          Owner-feedback amendment (2026-08-21, orchestrator): at a phone
          viewport (390x844) on every authenticated route, the navigation
          links, the account identity and the sign-out control are all
          visible and clickable without horizontal scrolling
          (document.documentElement.scrollWidth less than or equal to the
          viewport width), asserted in test/e2e/navigation.spec.ts with an
          explicit viewport; the same spec keeps a desktop-viewport
          assertion so neither presentation regresses the other.
    hazard-classes:
      - id: H1.1
        statement: >-
          Navigation bolted onto individual pages instead of the shell, so a
          route added later ships without it and the owner's exact complaint
          recurs.
        addressed-by: criterion 1.1
      - id: H1.2
        statement: >-
          Hardcoded nav copy bypassing the catalogs, breaking the EN, NL, FR
          contract for the most visible chrome in the product.
        addressed-by: criterion 1.2
      - id: H1.3
        statement: >-
          Literal colours or spacing creeping in with the new chrome instead
          of tokens.
        addressed-by: criterion 1.3
    migrations: none, UI only.
    conflicts-with: []
    parallelizable: false
    citations:
      - src/app/(app)/layout.tsx:18
      - src/modules/overview/ui/month-view.tsx:387
      - messages/en.json:28
    fill-in:
      filled: true
      root-cause: >-
        No M1 phase owned shell navigation, so none exists; verified against
        src/app/(app)/layout.tsx:18 through :26 at e4ea3ba.
      fix-shape: >-
        Header nav in the authenticated shell, empty-state link, catalog
        keys in three languages, catalog parity test, navigation e2e spec.
      files:
        - src/app/(app)/layout.tsx
        - src/modules/overview/ui/month-view.tsx
        - test/e2e/navigation.spec.ts
  - id: M3-P2
    branch: claude/m3-p2-pdf-belfius
    intent: >-
      The PDF import path, foundation plus the first layout: a PDF
      text-extraction adapter behind the existing StatementParser port, the
      SourceProfileSpec widened to a discriminated union, code-owned layout
      templates selected by fingerprint, the balance contract as a hard
      per-file gate shared by every template, the Belfius current-account
      template end to end (upload through interpreted month), loud FAILED
      imports for unrecognised layouts and non-reconciling files, and
      synthetic fixtures.
    grounding: >-
      pulse-v0.2-pdf-addendum.md:18 (second adapter behind the port),
      :23 (code-owned layout templates selected by fingerprint, never by
      asking the user), :27 (unrecognised layout fails loudly), :35 (the
      balance contract), :50 through :69 (template A), :111 (fixture
      requirements). Owner feedback item 2 makes this the essential path.
      The one new runtime dependency this phase needs was a charter
      stop-for (charter.yaml:92), raised as DR-0020 and now DECIDED by the
      owner (2026-08-21, fleet decisions/DR-0020.yaml): pdfjs-dist,
      server side only, behind the parser adapter, exactly the scout's
      recorded recommendation (positioned text items support the
      deliberate line reconstruction the real KBC statement requires, no
      wrapper dependency). The dispatch gate is satisfied and the fill-in
      below is filled; the criteria were and are library-independent.
    severity: critical
    verified-root-cause: >-
      Not a defect phase. The verification-first step exists because two
      addendum claims failed verification: "second adapter, nothing
      upstream changes" fails against the delimited-only SourceProfileSpec
      (source-profile.ts:49) that types both port methods (ports.ts:18);
      and the addendum's Belfius natural key (account plus year plus
      sequence, pulse-v0.2-pdf-addendum.md:68) differs from the implemented
      key shape (dedup.ts:95) and its cross-statement continuity premise is
      unverifiable from the single real statement available.
    steps:
      - kind: verification-first
        text: >-
          Before building, confirm and write into the work history: (a) the
          spec seam, that SourceProfileSpec (source-profile.ts:49) types
          detect, parse, specEquals, hasNaturalKey, parseSourceProfileSpec
          and the fix-profile re-parse, and that the chosen widening (D-2, a
          kind discriminant with the existing delimited shape unchanged)
          leaves every stored profile Json readable, proven by a test that
          parses a spec captured in the e4ea3ba shape; (b) the natural-key
          wiring (D-4): the addendum's year-scoped key against the
          implemented statement-scoped mechanism (dedup.ts:95), recording
          how the Belfius template emits key components so re-importing an
          adjacent or re-exported statement cannot duplicate rows, and
          noting that cross-statement sequence continuity remains an
          assumption until a second real statement is observed.
      - text: >-
          Widen SourceProfileSpec to a discriminated union: kind
          "delimited" with the existing fields byte-for-byte, and kind
          "pdf-layout" carrying templateId and templateVersion.
          parseSourceProfileSpec, specEquals and hasNaturalKey branch on
          kind; every existing delimited test stays green unmodified.
          Recognised template profiles keep the ask-once account
          declaration exactly as is (upload-statement.ts:57); only the
          format question disappears (pulse-v0.2-pdf-addendum.md:27).
      - text: >-
          PDF text extraction behind a new adapter-internal port using the
          DR-0020 dependency, server side only: PDF magic-bytes sniffing in
          detect, then deliberate line reconstruction from positioned text
          items (group by y within tolerance, sort by x, explicit word-gap
          threshold), because the real KBC statement showed default library
          text assembly glues words (notes/export-format-facts.md). Same
          bytes always yield the same lines; no randomness, no environment
          dependence.
      - text: >-
          Template registry keyed by fingerprint: the Belfius template
          matches its header text plus a SALDO OP line
          (pulse-v0.2-pdf-addendum.md:50). A PDF matching no template fails
          the import loudly: FAILED import row, new machine reason
          "layout-unsupported" added to the ImportFailureReason union
          (ports.ts:38) and translated in all three catalogs, zero rows
          written. Non-PDF bytes flow down the delimited path untouched.
      - text: >-
          The Belfius template per addendum section 3 and the verified
          structure: skip the repeated per-page header block, skip annex
          pages whose body starts with the BIJLAGE marker, take opening and
          closing from the first and last SALDO OP lines, start a
          transaction at the 4-digit sequence plus DD-MM-YYYY plus VAL.
          pattern, join following indented lines into the description and
          keep the whole block verbatim as rawLine, accept every sign
          spacing and thousands-dot combination (the strict correlation the
          addendum implies does not hold on the real file), extract a
          counterparty IBAN from the description where present, and emit
          the account identity from the band-line IBAN. Emit natural-key
          components per the verification-first outcome (D-4).
      - text: >-
          The balance contract in the shared PDF parse path, not in the
          template: opening plus the sum of parsed transactions must equal
          closing in integer cents, or the import is FAILED with new reason
          "balance-mismatch" and nothing is written
          (pulse-v0.2-pdf-addendum.md:35). Every future template inherits
          the gate by construction.
      - text: >-
          Synthetic fixtures (D-3): a committed deterministic generator
          script produces a Belfius-style fixture PDF with invented IBAN,
          names and amounts reproducing variable sign spacing with and
          without thousands dot, multi-line descriptions, an annex page, a
          page break mid-transaction-list, one transaction on a
          TRANSACTION page whose description block contains the annex
          marker phrase as a description line (finding PR2-002: the real
          statement carries the bare marker token inside a description, so
          body-starts-with skipping must be distinguishable from
          marker-anywhere skipping), and reconciling balances; plus a
          second Belfius-style statement file with a different statement
          identity whose rows re-carry some of the first fixture's
          year-and-sequence pairs alongside genuinely new rows (a
          re-exported overlap, finding PR2-003); plus a deliberately
          non-reconciling variant; plus a valid PDF matching no template.
          Fixture bytes are stable across runs (fixed timestamps and
          metadata). No string from the real statements appears anywhere.
      - text: >-
          Wire the upload UI: the file input accepts PDF next to CSV
          (upload-form.tsx:22), the two new failure reasons render
          translated, and every catalog string that names accepted formats
          (the empty-state body, messages/en.json:29, and the import
          screen's field and hint copy) names PDF alongside CSV in all
          three languages (finding PR2-007). Extend the re-parse test:
          fix-profile over an ingested PDF import re-parses from
          rawContent with an unchanged spec as a strict no-op (zero key
          changes, zero row changes).
    files-to-touch:
      - package.json
      - package-lock.json
      - src/modules/import/domain/source-profile.ts
      - src/modules/import/domain/parse-statement.ts
      - src/modules/import/domain/dedup.ts
      - src/modules/import/domain/ (new pdf extraction, template registry and belfius template files)
      - src/modules/import/adapters/
      - src/modules/import/application/ports.ts
      - src/modules/import/ui/upload-form.tsx
      - src/modules/import/ui/status-keys.ts
      - messages/en.json
      - messages/nl.json
      - messages/fr.json
      - test/fixtures/ (generator script and generated synthetic PDFs)
      - test/domain/
      - test/application/
      - test/e2e/import.spec.ts
    extras:
      - next.config.ts
      - src/app/(app)/import/[id]/page.tsx
      - src/modules/import/application/
      - src/modules/import/ui/profile-confirmation.tsx
      - src/modules/import/ui/import-result.tsx
      - src/modules/import/domain/detect-profile.ts
      - playwright.config.ts
      - src/app/globals.css
      - src/app/api/health/pdf/route.ts
      - test/e2e/prod-smoke.spec.ts
      - test/application/pdf-module-unavailable.test.ts
      - .gitignore
      - eslint.config.mjs
      - tsconfig.json
    acceptance:
      - id: "2.1"
        criterion: >-
          npm test exits 0 including new tests that, against the synthetic
          Belfius fixture PDF, assert: the expected row count is parsed;
          the sum of parsed amountCents equals the fixture's known total;
          the annex page contributes zero rows WHILE the transaction whose
          description block contains the annex marker phrase parses as a
          row with the phrase verbatim in its rawLine (finding PR2-002:
          marker-anywhere page skipping fails this pair of assertions,
          body-starts-with passes); a multi-line description is joined and
          its rawLine holds the block verbatim; and each sign-spacing and
          thousands-dot combination parses to its expected integer cents.
      - id: "2.2"
        criterion: >-
          npm test exits 0 including a test that uploads the
          non-reconciling fixture through uploadStatement and asserts
          exactly one FAILED import with failureReason "balance-mismatch"
          and zero transaction rows written, and another that uploads the
          no-template PDF and asserts FAILED with "layout-unsupported" and
          zero rows.
      - id: "2.3"
        criterion: >-
          npm test exits 0 including an idempotency test: uploading the
          same Belfius fixture twice reports all rows added on the first
          import and zero added, all known on the second; AND a
          cross-file witness of the year-scoped natural key (finding
          PR2-003, decision D-4): importing the second overlapping
          Belfius-style fixture (different statement identity, shared
          year-and-sequence pairs) after the first reports the shared
          rows as known and adds only the genuinely new rows, so a
          template emitting statement-scoped key components fails this
          test; and a re-parse test extending
          test/application/reparse.test.ts asserting an unchanged-spec
          re-parse of the ingested PDF import changes zero dedup keys and
          zero rows.
      - id: "2.4"
        criterion: >-
          npm test exits 0 including a spec-compatibility test that feeds
          parseSourceProfileSpec a JSON object in the exact e4ea3ba
          delimited shape (no kind discriminant, or the chosen migration
          form documented in the test) and asserts it parses to the
          delimited variant, AND the same test round-trips recognition:
          that parsed spec compares specEquals-true against the spec the
          delimited parser's detect emits for a matching CSV file, so a
          profile stored before the widening is recognised by
          findProfileBySpec (upload-statement.ts:101) on the next upload
          without re-asking the declaration (finding PR2-001: a
          parse-only assertion lets canonical-JSON specEquals compare
          differing key sets unequal while every parse test passes); and
          git diff --name-only against the phase base lists no deleted
          files under test/.
      - id: "2.5"
        criterion: >-
          npm run test:e2e exits 0 with test/e2e/import.spec.ts extended: a
          journey uploads the synthetic Belfius fixture PDF through the
          import screen, confirms the ask-once account declaration if the
          account is unseen, reaches an import detail state reporting rows
          added, and the month view for the fixture's month renders its
          reconciliation panel with data-state "ok"; and the rendered copy
          of the import screen and of the empty state names PDF, asserted
          in the journey (finding PR2-007: the e4ea3ba copy tells the
          owner the product reads CSV exports).
      - id: "2.6"
        criterion: >-
          Privacy gate, both halves executed with a probe list read from
          the real PDFs in the container uploads at review time and never
          committed (finding PR2-008): (a) git grep across the whole
          worktree for the real Belfius statement's IBAN, account-holder
          name, statement number and each merchant string from it exits 1;
          (b) over the files this phase adds or modifies (git diff
          --name-only against the phase base), the same grep extended to
          ALL FIVE promised categories (identifiers, names, merchant
          strings, thousands-form amounts and dates from the real
          statement) exits 1, which is D-3's whole promise made testable
          without false-alarming on the pre-existing M1 amount collisions
          the work-log records. If the real PDFs are absent from the
          executing container, this criterion is recorded as
          not-witnessed in the work history and the review escalates for
          a container that has them, never marked green by vacuity.
      - id: "2.7"
        criterion: >-
          npm run typecheck && npm run lint && npm test exit 0 and npm run
          test:e2e exits 0 at the phase head.
      - id: "2.8"
        criterion: >-
          After merge, the DR-0001 release verification extended by one
          production check passes: the deployed import screen accepts the
          synthetic Belfius fixture PDF and reaches the same
          rows-added state as criterion 2.5, observed against the deployed
          URL, proving the DR-0020 dependency runs in the Vercel Node
          runtime.
    hazard-classes:
      - id: H2.1
        statement: >-
          Real statement content leaking into the repository through
          fixtures, tests, comments or the fixture generator.
        addressed-by: criterion 2.6
      - id: H2.2
        statement: >-
          A dropped or duplicated line passing silently into the facts
          layer, the exact failure the balance contract exists to stop.
        addressed-by: criterion 2.2
      - id: H2.3
        statement: >-
          Extraction nondeterminism or word-gap variance producing
          different rows for the same bytes across runs or environments,
          which would corrupt dedup keys and break the re-parse no-op
          contract.
        addressed-by: criterion 2.3
      - id: H2.4
        statement: >-
          The spec widening silently invalidating stored delimited
          profiles, turning every existing source unrecognised on the next
          upload.
        addressed-by: criterion 2.4
      - id: H2.5
        statement: >-
          The extraction dependency working locally and failing in the
          deployed serverless runtime (native bindings, workers, or
          filesystem expectations).
        addressed-by: criterion 2.8
    migrations: >-
      none. The widened spec lives in the existing SourceProfile.spec Json
      column (prisma/schema/import.prisma:39); balances are verified at
      parse time and not persisted (decision D-6); no new columns or tables.
    conflicts-with: []
    parallelizable: false
    citations:
      - delivery/intake/pulse-v0.2-pdf-addendum.md:18
      - delivery/intake/pulse-v0.2-pdf-addendum.md:35
      - delivery/intake/pulse-v0.2-pdf-addendum.md:68
      - src/modules/import/application/ports.ts:18
      - src/modules/import/domain/source-profile.ts:49
      - src/modules/import/domain/dedup.ts:95
      - charter.yaml:92
    fill-in:
      filled: true
      root-cause: >-
        Not a defect; the essential PDF import path on a verified seam.
        The dispatch gate was DR-0020 (charter stop-for on a new runtime
        dependency), decided by the owner 2026-08-21 (fleet
        decisions/DR-0020.yaml): pdfjs-dist, server side only, behind the
        parser adapter.
      fix-shape: >-
        Spec union widening, pdfjs-dist extraction adapter with deliberate
        line reconstruction, fingerprint template registry, the balance
        contract gate, the Belfius template end to end, two new translated
        failure reasons, synthetic fixture generator with the
        in-description annex marker and overlap fixtures.
      files:
        - package.json
        - src/modules/import/domain/
        - src/modules/import/adapters/
        - test/fixtures/
  - id: M3-P3
    branch: claude/m3-p3-pdf-kbc
    intent: >-
      The KBC Mastercard uitgavenstaat template on the M3-P2 foundation:
      fingerprint, the two-date row shape, FX continuation lines folded
      into their rows, the previous-balance and Afrekening lines as the
      balance identity, the settlement credit as a real transaction, hash
      dedup with the occurrence ordinal witnessed through PDF, and the
      account-side settlement pairing witnessed end to end across two PDF
      imports.
    grounding: >-
      pulse-v0.2-pdf-addendum.md:73 through :88 (template B), :91 and :100
      (the section 5 amendments this phase re-witnesses through PDF; both
      verified already implemented at e4ea3ba, see report-code-disagreement).
      Verified nuances from the real statement (notes/export-format-facts.md,
      2026-08-21 section): the closing figure sits on the Afrekening line
      with a space thousands separator, the Totaal bedrag line carries no
      amount, extraction word spacing is tolerance-sensitive, and the
      DOMICILIERING credit equals the negated previous balance.
    severity: high
    verified-root-cause: >-
      Not a defect phase. No verification-first step: every claim this
      phase builds on verified true against the real statement and the tree
      (the two false claims of this plan are owned by M3-P2). The balance
      gate, spec union, extraction and registry arrive from M3-P2.
    steps:
      - text: >-
          The KBC template per addendum section 4 and the verified
          structure: fingerprint on the KBC-Mastercard plus Uitgavenstaat
          header text, tolerant of interior label spacing; account identity
          from the masked card number through the profile's account binding
          (card files carry no IBAN, upload-statement.ts:113); booking date
          is the transaction date, settlement date kept in the raw
          description block; FX continuation lines (original amount and
          exchange rate) folded into the preceding row's rawLine, never
          rows; the DOMICILIERING credit parsed as a transaction; the
          previous-balance and Totaal bedrag and Afrekening lines parsed as
          balances, with the closing figure read from the Afrekening line
          including its space thousands separator.
      - text: >-
          Balance identity for this template: previous balance plus the sum
          of all rows including the settlement credit must equal the
          Afrekening amount in integer cents, else FAILED
          "balance-mismatch" with zero rows, through the shared M3-P2 gate.
      - text: >-
          Synthetic KBC fixture via the D-3 generator: invented card
          number, merchants and amounts reproducing FX continuation lines,
          the settlement credit equal to the negated previous balance, the
          identical duplicate row pair, at least one row whose transaction
          date and settlement date fall in DIFFERENT months (finding
          PR2-004), and reconciling balances; plus a companion
          Belfius-side fixture carrying the settlement debit whose amount
          equals this card fixture's Afrekening total, so the pairing is
          witnessable.
      - text: >-
          End-to-end witness of the already-implemented interpretation
          machinery through PDF: with the card account declared, the
          duplicate pair both survive import and re-import (addendum
          section 5's named test, through the PDF path), the DOMICILIERING
          credit classifies INTERNAL as the mirror leg
          (classify-flow.ts:90), and the account-side settlement debit
          pairs INTERNAL to the card import
          (classify-flow.ts:87) with the SPEND fallback intact when no
          card import matches.
    files-to-touch:
      - src/modules/import/domain/ (kbc template file, registry entry)
      - test/fixtures/ (generator additions and generated synthetic PDFs)
      - test/domain/
      - test/application/
      - test/e2e/import.spec.ts
    extras: []
    acceptance:
      - id: "3.1"
        criterion: >-
          npm test exits 0 including new tests asserting, against the
          synthetic KBC fixture: the expected row count with FX
          continuation lines contributing zero rows and appearing verbatim
          in their row's rawLine; the DOMICILIERING credit present as a row
          with positive cents; the space-thousands Afrekening amount parsed
          to its expected integer cents; the month-straddling row's stored
          bookingDate equal to its TRANSACTION date, not its settlement
          date, so spend lands in the month it happened
          (pulse-v0.2-pdf-addendum.md:76, finding PR2-004); and the
          balance identity gate passing this fixture and failing a
          non-reconciling KBC variant with "balance-mismatch" and zero
          rows written.
      - id: "3.2"
        criterion: >-
          npm test exits 0 including the duplicate-pair test through the
          PDF path: both identical rows survive the first import, and
          re-importing the same fixture adds zero and reports all known,
          with both rows still present.
      - id: "3.3"
        criterion: >-
          npm test exits 0 including an application-level test importing
          the KBC fixture and the companion Belfius fixture into one
          household with declared accounts, asserting the account-side
          settlement debit is INTERNAL and linked to the KBC import, the
          DOMICILIERING credit is INTERNAL, and removing the KBC import
          from the scenario leaves the settlement debit SPEND against the
          card issuer.
      - id: "3.4"
        criterion: >-
          npm run test:e2e exits 0 with the import journey extended:
          uploading both fixture PDFs, the month view for the fixture month
          renders the reconciliation panel with data-state "ok".
      - id: "3.5"
        criterion: >-
          Privacy gate, both halves executed with a probe list read from
          the real PDFs in the container uploads at review time and never
          committed (finding PR2-008): (a) git grep across the whole
          worktree for the real KBC statement's card number fragments,
          customer reference, statement number and each merchant string
          from it exits 1; (b) over the files this phase adds or modifies
          (git diff --name-only against the phase base), the same grep
          extended to all five promised categories (identifiers, names,
          merchant strings, thousands-form amounts and dates from the
          real statement) exits 1. If the real PDFs are absent from the
          executing container, this criterion is recorded as
          not-witnessed in the work history and the review escalates for
          a container that has them, never marked green by vacuity.
      - id: "3.6"
        criterion: >-
          npm run typecheck && npm run lint && npm test exit 0 and npm run
          test:e2e exits 0 at the phase head.
    hazard-classes:
      - id: H3.1
        statement: >-
          Real statement content leaking into the repository through the
          KBC fixtures or tests.
        addressed-by: criterion 3.5
      - id: H3.2
        statement: >-
          FX continuation lines parsed as transactions, double-counting
          foreign spend; or the settlement credit skipped as a balance
          line, breaking both the balance identity and the settlement
          pairing.
        addressed-by: criterion 3.1
      - id: H3.3
        statement: >-
          The dedup key collapsing the legitimate identical pair, silently
          dropping a real payment while the books still close per file.
        addressed-by: criterion 3.2
      - id: H3.4
        statement: >-
          The settlement debit counted as SPEND while its card rows are
          also imported, double-counting an entire card statement in the
          month view.
        addressed-by: criterion 3.3
    migrations: none, template and fixtures only.
    conflicts-with: []
    parallelizable: false
    citations:
      - delivery/intake/pulse-v0.2-pdf-addendum.md:73
      - delivery/intake/pulse-v0.2-pdf-addendum.md:83
      - delivery/intake/pulse-v0.2-pdf-addendum.md:91
      - src/modules/ledger/domain/classify-flow.ts:77
      - src/modules/ledger/domain/constants.ts:26
      - src/modules/import/application/upload-statement.ts:113
    fill-in:
      filled: true
      root-cause: >-
        Not a defect; second layout template on the M3-P2 foundation, every
        structural claim verified against the real statement in-container.
      fix-shape: >-
        KBC template file plus registry entry, synthetic KBC and companion
        Belfius fixtures, duplicate-pair and settlement-pairing witnesses.
      files:
        - src/modules/import/domain/
        - test/fixtures/
        - test/application/
  - id: M3-P4
    branch: claude/m3-p4-mobile-import
    intent: >-
      The phone-first import flow: the import and confirmation screens
      redesigned mobile-first per addendum section 7, the whole journey
      exercised at a phone viewport, and the frontend skill's desktop-first
      line revised as the owner's addendum directs. Platform-neutral: this
      works in any phone browser via the file picker regardless of how
      DR-0021 resolves.
    grounding: >-
      pulse-v0.2-pdf-addendum.md:123 through :127 (PDF exports come from
      the phone, the upload flow gets designed mobile-first, the frontend
      skill's desktop-first line gets revisited in the v0.2 plan). Verified:
      the line is .claude/skills/pulse-frontend/SKILL.md:113; the owner's
      halted-testing feedback makes the phone the primary import surface.
      DR-0002's clock starts only when the owner can feed Pulse from their
      phone (fleet backlog, 2026-08-21).
    severity: high
    verified-root-cause: >-
      Not a defect phase. The v1 desktop-first stance was a deliberate
      intake decision now superseded by owner-authored intake for v0.2; no
      verification-first step because every claim this phase builds on
      verified true.
    steps:
      - text: >-
          AMENDED by the M0-P3 mobile round (decision D-15, DR-0022): do
          NOT rewrite the skill's constraint here. M3-P7 replaces the
          desktop-first paragraph once, under DR-0022, and DR-0022 goes
          further than this step was written to go: mobile first is every
          screen, not the import screens alone, so the "the month view
          remains desktop-competent" wording this step carried is
          superseded. This step is now a CHECK: confirm that the
          mobile-first constraint M3-P7 wrote is present and unweakened
          before building on it, and add nothing to it. Criterion 4.4
          carries the check; criteria 4.1, 4.2 and 4.3 are unchanged and
          all three remain satisfiable, because the paragraph M3-P7
          replaced no longer contains "desk activity" and does state a
          mobile-first stance.
      - text: >-
          Mobile-first import flow: the upload, declaration and result
          screens laid out for a phone viewport with token-only styling
          (viewport meta verified present or added in the root layout,
          touch-target sizing via tokens, no horizontal overflow), keeping
          every existing testid and the desktop layout acceptable through
          responsive CSS rather than a second tree.
      - text: >-
          A Playwright mobile project or device-emulated spec running the
          full PDF import journey at a phone viewport (390px class) with
          the synthetic fixtures: upload, declare if unseen, result, month
          view.
    files-to-touch:
      - .claude/skills/pulse-frontend/SKILL.md
      - src/modules/import/ui/
      - src/app/layout.tsx
      - styles/tokens.css
      - messages/en.json
      - messages/nl.json
      - messages/fr.json
      - playwright.config.ts
      - test/e2e/ (mobile import spec)
    extras: []
    acceptance:
      - id: "4.1"
        criterion: >-
          STRENGTHENED in the M0-P3 fix round (finding MPR-007). As written
          this was the overflow-only bar, in the phase that ships three new
          mobile screens, and DR-0022's decided consequence is that
          phone-width evidence measuring usability becomes a standing
          criterion on EVERY UI phase and not only on the two being rebuilt.
          Nothing is removed: the journey and the overflow assertion stay.
          npm run test:e2e exits 0 including the synthetic-PDF import journey
          run under the phone project M3-P7 added, asserting as before that
          document.documentElement.scrollWidth does not exceed the viewport
          width on the import, declaration, result and month screens; and at
          390 by 844 those same screens satisfy the three standing phone
          measurements M3-P7 introduced: every element matching "a, button,
          input:not([type=hidden]), select, [role=button]" inside main with a
          non-zero bounding box has a bounding height of at least 44 CSS
          pixels; the sorted collection of every data-testid paired with its
          trimmed textContent is deeply equal at 1280 by 720 and at 390 by
          844, with every collected element non-zero in size at the phone
          width; and the clipping sweep of criterion 7.7 returns an empty
          offender list on both axes. FALSIFIED BY one offender in any of the
          three, or by the journey failing under the phone project.
      - id: "4.2"
        criterion: >-
          grep -n "desk activity" .claude/skills/pulse-frontend/SKILL.md
          exits 1 and the file contains a mobile-first import stance line
          citing the v0.2 addendum.
      - id: "4.3"
        criterion: >-
          npm run typecheck && npm run lint && npm test exit 0, npm run
          test:e2e exits 0 including the unmodified desktop golden journey,
          and grep -rE "oklch\(|#[0-9a-fA-F]{3,8}" over src/ excluding the
          token and theme files exits 1.
      - id: "4.4"
        criterion: >-
          ADDED by the M0-P3 mobile round (decision D-15). At this phase's
          base, grep -n "Desktop first" .claude/skills/pulse-frontend/SKILL.md
          exits 1, grep -n "desk activity" over the same file exits 1, and
          the file contains the literal string DR-0022; and git diff
          --name-only against this phase's base over
          .claude/skills/pulse-frontend/SKILL.md prints nothing, so this
          phase consumes the constraint M3-P7 wrote and does not rewrite it a
          second time. FALSIFIED BY either grep exiting 0, by DR-0022 being
          absent from the file, or by the diff printing the skill path.
    hazard-classes:
      - id: H4.1
        statement: >-
          The mobile redesign regressing the desktop month view or import
          flow, trading one blocked surface for another.
        addressed-by: criterion 4.3
      - id: H4.2
        statement: >-
          Literal breakpoint, spacing or size values landing in components
          instead of tokens during layout work, the highest-pressure moment
          for that violation.
        addressed-by: criterion 4.3
      - id: H4.3
        statement: >-
          Real statement content leaking through screenshots, fixtures or
          test data added for the mobile journey; the mobile spec uses only
          the committed synthetic fixtures.
        addressed-by: criterion 4.1
    migrations: none, UI and test surface only.
    conflicts-with: []
    parallelizable: false
    citations:
      - delivery/intake/pulse-v0.2-pdf-addendum.md:123
      - .claude/skills/pulse-frontend/SKILL.md:113
      - src/modules/import/ui/upload-form.tsx:22
    fill-in:
      filled: true
      root-cause: >-
        The v1 desktop-first stance is superseded by owner-authored v0.2
        intake; the import screens were built desk-first and unverified at
        phone widths.
      fix-shape: >-
        Skill line revision, mobile-first responsive import screens,
        mobile-viewport Playwright coverage of the PDF journey.
      files:
        - .claude/skills/pulse-frontend/SKILL.md
        - src/modules/import/ui/
        - test/e2e/
  - id: M3-P5
    branch: claude/m3-p5-share-target
    intent: >-
      The share-sheet entry: the phone's share sheet hands an exported PDF
      straight to Pulse. DR-0021 is decided (Android, owner 2026-08-21),
      so this phase ships a web app manifest with a share_target and a
      receiving route that feeds the existing upload pipeline unchanged,
      with an explicit no-preservation contract for unauthenticated
      shares (decision D-10).
    grounding: >-
      pulse-v0.2-pdf-addendum.md:125 (the natural v0.2 import path is the
      phone share sheet). Web Share Target requires an installed PWA and
      exists on Android/Chromium; iOS Safari has no share-target
      mechanism, so the owner's platform decided whether this phase is
      built at all. That question was raised as DR-0021 and is now
      DECIDED by the owner (2026-08-21, fleet decisions/DR-0021.yaml):
      Android, the share target IS built. The dispatch gate is satisfied
      and the fill-in below is filled. Verified at e4ea3ba: no manifest,
      no public/ directory, no share-target surface anywhere.
    severity: medium
    verified-root-cause: >-
      Not a defect phase; dispatchable since DR-0021 was decided (Android,
      build). No verification-first step: the absence of any manifest or
      share-target surface at e4ea3ba is verified, and the platform
      question was a decision record, not a code check.
    steps:
      - text: >-
          Web app manifest with share_target (method POST, enctype
          multipart/form-data, a files member accepting application/pdf),
          served from the app with name, icons and start_url; installable
          on the owner's device. NO service worker unless installability on
          the owner's platform measurably requires one, and if one is
          required it registers with no fetch handler and touches no cache
          API, because the deliberately-absent list bans caching
          (charter.yaml:80).
      - text: >-
          The receiving route accepts the share POST, requires the
          authenticated household context exactly like the upload action,
          hands the file to uploadStatement unchanged, and redirects into
          the import flow at the declaration, result or failure state the
          upload produced. An unauthenticated share redirects to sign-in
          and the shared file is NOT preserved: no Import row, no stored
          bytes, no server-side handoff; after signing in the user shares
          again, the share sheet being one tap away (decision D-10,
          finding PR2-005).
      - text: >-
          An integration or e2e test posting multipart form data with the
          synthetic fixture PDF to the share route and following the
          redirect to the same states criterion 2.5 walks.
    files-to-touch:
      - src/app/ (manifest and share receiving route)
      - src/modules/import/ui/
      - messages/en.json
      - messages/nl.json
      - messages/fr.json
      - test/e2e/ (share-target spec)
    extras: []
    acceptance:
      - id: "5.1"
        criterion: >-
          curl -s against the deployed or dev manifest URL returns JSON
          whose share_target declares method POST, multipart/form-data and
          a files entry accepting application/pdf, and npm run test:e2e
          exits 0 including a spec that POSTs the synthetic fixture PDF as
          multipart form data to the share route and asserts the redirect
          chain ends on an import state with the fixture's rows added.
      - id: "5.2"
        criterion: >-
          grep -rE "caches\.|CacheStorage|workbox" src/ exits 1, and if a
          service worker file exists it contains no fetch event listener,
          asserted by grep for "fetch" over that file exiting 1.
      - id: "5.3"
        criterion: >-
          npm run typecheck && npm run lint && npm test exit 0 and npm run
          test:e2e exits 0 at the phase head, including the unmodified
          M3-P4 mobile journey.
      - id: "5.4"
        criterion: >-
          A test POSTs the fixture PDF as multipart form data to the share
          route with NO session and asserts: the response redirects to
          sign-in, no Import row is created, and no uploaded bytes are
          persisted anywhere (decision D-10's no-preservation contract;
          finding PR2-005).
      - id: "5.5"
        criterion: >-
          ADDED in the M0-P3 fix round (finding MPR-007): this is the phase
          that makes the phone the entry point and it carried no phone-width
          measurement of any kind, while DR-0022 makes phone-width evidence a
          standing criterion on every UI phase. The share journey is run
          under the phone project M3-P7 added, and at 390 by 844 the screen
          the share target lands the user on satisfies the same three
          standing measurements criterion 4.1 names: every element matching
          "a, button, input:not([type=hidden]), select, [role=button]" inside
          main with a non-zero bounding box has a bounding height of at least
          44 CSS pixels; the sorted testid-and-text collection is deeply
          equal at 1280 by 720 and at 390 by 844, with every collected
          element non-zero in size at the phone width; and the clipping sweep
          of criterion 7.7 returns an empty offender list on both axes.
          FALSIFIED BY one offender in any of the three.
    hazard-classes:
      - id: H5.1
        statement: >-
          A service worker creeping toward caching to satisfy an
          installability checklist, entering the deliberately-absent list
          through the side door.
        addressed-by: criterion 5.2
      - id: H5.2
        statement: >-
          The share route accepting files outside the authenticated
          household context, turning the share endpoint into an
          unauthenticated write path.
        addressed-by: criterion 5.4
      - id: H5.3
        statement: >-
          Building the share target for a platform the owner does not
          carry, an unusable phase.
        addressed-by: "state-not-entered: DR-0021 is decided (Android, owner 2026-08-21, fleet decisions/DR-0021.yaml), so the wrong-platform state cannot arise"
    migrations: none.
    conflicts-with: []
    parallelizable: false
    citations:
      - delivery/intake/pulse-v0.2-pdf-addendum.md:125
      - charter.yaml:80
      - src/modules/import/application/upload-statement.ts:23
    fill-in:
      filled: true
      root-cause: >-
        Not a defect; the share-sheet entry point. The dispatch gate was
        DR-0021 (owner platform), decided by the owner 2026-08-21 (fleet
        decisions/DR-0021.yaml): Android, the share target is built.
      fix-shape: >-
        Web app manifest with share_target, receiving route feeding
        uploadStatement, D-10 no-preservation contract for
        unauthenticated shares, no-caching discipline witnessed by grep.
      files:
        - src/app/
        - src/modules/import/ui/
        - test/e2e/
  - id: M3-P6
    branch: claude/m3-p6-card-merchant-grouping
    intent: >-
      Restore merchant grouping for card rows, whose descriptors embed the
      transaction's own date, amount, card number and holder name so every
      payment becomes its own group, and mask the card number in the
      display layer only. Rules only, no LLM: the merchants normaliser
      learns the card-descriptor grammar, the merchant-source rule stops
      being duplicated, and the stored-rule stability contract is
      discharged rather than tripped.
    grounding: >-
      Owner's first deployed use of the Belfius PDF import (2026-08-21,
      fleet backlog): the parse is correct and two defects surfaced.
      Re-measured by the scout in-container through the shipped pipeline,
      counts only: 39 rows, 15 card-payment rows, none carrying
      counterpartyName, 15 distinct grouping keys after the shipped
      normaliser (src/modules/ledger/application/interpret-window.ts:87
      picks counterpartyName ?? description; none of the normaliser's
      terminal-noise, masked-PAN, city or date patterns matches this
      descriptor grammar), and 15 of 15 normalised descriptors still
      carrying the full 16-digit card number that both label surfaces
      render. The same merchant-source rule is duplicated at
      src/modules/merchants/domain/merchant-review.ts:50. Two stability
      contracts bound this phase: the merchants recipe contract
      (normalise-counterparty.ts:16, finding CR-402) and the frozen dedup
      recipe one module over (dedup.ts:18), which this phase must not
      touch.
    severity: high
    verified-root-cause: >-
      Measured, not inferred. Card payments have no counterparty account,
      so the merchant key is the entire descriptor, and the descriptor
      embeds per-transaction values (date, amount, card number, holder),
      which makes every payment unique. The shipped normaliser reduces to
      uppercase plus whitespace collapse on this shape, so it removes none
      of that variance. The verification-first step exists because three
      claims did not survive verification as stated: the count of real
      merchants behind the card rows, the multiplicity of card numbers in
      the file, and the premise that production holds no MerchantRule rows
      (unanswerable from the repository, and it selects which branch of
      the CR-402 contract this phase must ship).
    steps:
      - kind: verification-first
        text: >-
          Before changing the recipe, establish and write into the work
          history: (a) the number of MerchantRule rows the deployed
          database holds, measured with a read-only count over a
          connection string pinned explicitly to the pulse deployed
          project's session pooler (never the ambient DATABASE_URL, fleet
          warnings 1 and 5), and the branch it selects under the CR-402
          contract, zero rows meaning no stored patterns exist to
          re-normalise and any nonzero count meaning the one-off
          re-normalisation of stored EXACT and PREFIX patterns ships in
          this same change; (b) the pre-change grouping measurement on the
          committed synthetic card fixture (distinct keys per merchant),
          so the after-measurement in criterion 6.1 has a recorded before;
          (c) that the real statement's merchant count is not the number
          any criterion asserts, since every criterion runs on synthetic
          fixtures with invented values; (d) the PREDICATE that defines a
          card row, written down explicitly, together with the count of
          rows carrying each card-tail shape and the count of non-card
          rows carrying a long structured reference, because this plan's
          own first pass recorded counts whose predicate was implicit and
          two sub-claims did not survive an independent re-measurement
          (finding PR3-003).
      - text: >-
          Unify the merchant source: one exported helper is the single
          definition of which text a transaction resolves under, and both
          call sites use it (src/modules/ledger/application/
          interpret-window.ts:87 and src/modules/merchants/domain/
          merchant-review.ts:50). The rule itself stays counterpartyName
          ?? description, per decision D-11.
      - text: >-
          Teach the merchants normaliser the card-descriptor grammar,
          word-bounded patterns only, no LLM anywhere (Claude API is bound
          to slice 5 onward, charter.yaml:41): strip the payment-rail
          prefix, the embedded transaction date in its bare day-and-month
          shape, the trailing amount plus currency, the card-number tail
          in every observed shape (full grouped and partially masked,
          finding PR3-002) together with the holder name that follows it,
          where the partially-masked pattern is anchored to the CARD-TAIL
          GRAMMAR, the tail's position after the card-number label and the
          holder text that follows it, and never to the presence of an
          X-masked token alone, because X-masked tokens occur in 11
          non-card descriptors in the real statement and the strip must
          not touch those rows (finding PR4-002),
          and the country marker that Belfius emits as real text, whose
          pattern is anchored to the ANGLE-bracket shape specifically and
          never to bracketing in general, because the parenthesised
          value-date token on every transaction start line is bracketed
          too and the Belfius template depends on it (finding PR3-003).
          No pattern keys on digit-run length alone: five non-card rows in
          the real statement carry long structured references. Each
          added pattern is grounded in a committed synthetic fixture, and
          none may consume a merchant name from the middle of a word.
      - text: >-
          Discharge the CR-402 stability contract in the same change:
          update the pinned regression table
          (test/domain/normalise-counterparty.test.ts:119) so an
          accidental future change still reddens, and ship the branch step
          1 selected, either the one-off re-normalisation of stored EXACT
          and PREFIX MerchantRule patterns (the pipeline is idempotent
          over its own output) or the recorded decision to version the
          recipe.
      - text: >-
          Display-layer masking: one helper masks a card-number run to its
          last four digits, applied where the descriptor is RENDERED, on
          the month view group label
          (src/modules/overview/ui/month-view.tsx:78) and the merchant
          review group label (src/modules/merchants/ui/
          merchant-review.tsx:29). It is applied nowhere else: never to
          rawLine, which stays verbatim because facts are immutable
          (prisma/schema/import.prisma:106), never to the normalised key,
          and never to the hidden counterpartyText value the review form
          submits (merchant-review.tsx:42), because that value becomes the
          EXACT rule subject (assign-merchant.ts:54) and a masked subject
          would match nothing (decision D-12).
      - text: >-
          Synthetic fixtures only, invented values in the real grammar:
          several card rows of ONE merchant spread across two months with
          differing dates and amounts, carrying the card tail in BOTH
          observed shapes, full grouped on some rows and partially masked
          ending in four digits on others (finding PR3-002); two DISTINCT
          merchants sharing a chain prefix and a city, the over-stripping
          witness; a row whose descriptor carries a full card-number run
          PRINTED IN THE GROUPED FOUR-BY-FOUR FORM WITH SEPARATORS, which
          is the form the real statement prints and the form criterion
          6.3's separator-insensitive test exists for (finding PR3-001); a
          row carrying a legitimate long structured reference that is not
          a card number and must survive the strip; and a non-card control
          row that must be unaffected, carrying an X-masked token followed
          by holder-like text and NO card-number label, which is the shape
          8 of the 11 real non-card X-token descriptors have (finding
          PR5-003), so a partially-masked pattern anchored on the holder
          text alone fires on it and fails. Real statement content is
          never committed.
    files-to-touch:
      - src/modules/merchants/domain/normalise-counterparty.ts
      - src/modules/merchants/domain/merchant-review.ts
      - src/modules/merchants/ui/merchant-review.tsx
      - src/modules/ledger/application/interpret-window.ts
      - src/modules/overview/ui/month-view.tsx
      - src/platform/ui/ (the display-only masking helper)
      - test/domain/normalise-counterparty.test.ts
      - test/domain/merchant-review.test.ts
      - test/fixtures/
      - test/e2e/merchants.spec.ts
    extras:
      - src/app/globals.css
      - src/modules/import/ui/profile-confirmation.tsx
      - src/modules/overview/adapters/overview-repository.ts
    acceptance:
      - id: "6.1"
        criterion: >-
          npm test exits 0 with a grouping test over the synthetic card
          fixture asserting that the several rows of the one fixture
          merchant, differing in date and amount across two months AND
          carrying the card tail in BOTH observed shapes, a full grouped
          number on some rows and a partially masked tail ending in four
          digits on others (finding PR3-002), produce exactly ONE distinct
          normalised key and exactly one review group, where the same test
          pinned against the pre-change recipe produces one key per row;
          the work history records both numbers. A separate row in the
          same fixture carries a legitimate long structured reference that
          is NOT a card number, 13 to 19 digits long, the length the real
          statement carries (measured: 13-digit and 15-digit references,
          and 20 of 39 real descriptors fall inside that window while only
          15 are card rows), so it exercises criterion 6.3's named
          exception rather than dodging the sweep by being shorter
          (finding PR4-001); and the test asserts that reference survives
          in its key, so the strip stays anchored to the card-tail grammar
          rather than to any long digit run; and the test asserts that the
          non-card control row's key, an X-masked token followed by
          holder-like text with NO card-number label, is UNCHANGED by the
          strip, which is what witnesses step 3's anchor and fails an
          implementation that keys the masked-tail pattern on the holder
          text alone (finding PR5-003; measured, the card-number label
          occurs in 15 of 15 card descriptors and 0 of the 11 X-token
          descriptors, while holder-like text follows the token in 8 of
          those 11 as well).
      - id: "6.2"
        criterion: >-
          npm test exits 0 with an over-stripping test asserting the two
          distinct fixture merchants that share a chain prefix and a city
          normalise to two DIFFERENT keys and stay two review groups, so a
          strip that swallows the distinguishing token fails.
      - id: "6.3"
        criterion: >-
          Both halves use the SAME separator-insensitive test, where a
          card-number run is 13 to 19 consecutive digits after spaces,
          dots and dashes are REMOVED (finding PR3-001: the real statement
          carries the number grouped four by four, the extractor's own gap
          rule inserts those spaces and the normaliser collapses
          whitespace rather than removing it, so a contiguous-16-digit
          regex matches nothing and cannot fail; measured through the
          shipped pipeline, contiguous runs in the parsed descriptors: 0,
          separator-insensitive runs: every card row). npm test exits 0
          with tests asserting, under that separator-insensitive test,
          that (a) no normalised key produced from the card fixture and no
          rendered group label contains the fixture's INVENTED CARD NUMBER
          exactly, and (b) no key or label contains ANY 13-to-19-digit run
          except the key AND the rendered label of the single row
          criterion 6.1 declares as carrying a legitimate structured
          reference, which this test names explicitly as its one permitted
          exception, the key and the label being the SAME STRING for an
          unresolved group (src/modules/merchants/domain/
          merchant-review.ts:86 and src/modules/overview/domain/
          month-projection.ts:83 both set the label to the normalised text
          that is the key), so the exception must cover both or the
          criterion is satisfiable only by the accident of that row
          happening to be resolved in the scenario (finding PR5-002); and
          that the
          display helper renders such a run masked to its last four
          digits. npm run test:e2e exits 0 with the merchant review and
          month view specs asserting the same two sweeps over rendered
          group labels. The exact sweep and the named exception exist
          because the general sweep alone contradicted 6.1 (finding
          PR4-001): measured over the real statement, every structured
          reference is 13 or 15 digits and 5 non-card descriptors fall
          inside the 13-to-19 window, so a faithful fixture reference
          cannot avoid it and the only way to satisfy both criteria
          without this exception is a sub-13-digit reference, which would
          disarm the guard criterion 6.1 exists to provide.
      - id: "6.4"
        criterion: >-
          npm test exits 0 with a round-trip test: the value the review
          form submits for a card group is the UNMASKED normalised text,
          assigning a merchant from it writes an EXACT rule whose pattern
          equals that text, and re-running resolution over the fixture
          resolves every row of that merchant, including a row from the
          other month that was not the one named.
      - id: "6.5"
        criterion: >-
          git diff --name-only against the phase base contains neither
          src/modules/import/domain/dedup.ts nor
          src/modules/ledger/domain/corrections.ts, npm test exits 0 with
          test/domain/dedup.test.ts unmodified, and an import test
          re-importing the card fixture reports zero rows added and all
          rows known, so no stored dedup key moved. FACTS UNTOUCHED
          (finding PR3-006): the same test asserts that after this phase's
          changes the stored rawLine and rawContent of the card fixture's
          rows are byte-identical to what the pre-change pipeline stored
          and still carry the unmasked card-number run, so masking never
          reached the facts layer (prisma/schema/import.prisma:106,
          CLAUDE.md non-negotiable 5) and the M3-P2 re-parse contract
          still holds.
      - id: "6.6"
        criterion: >-
          The work history records the measured deployed MerchantRule
          count, the branch selected under the CR-402 contract and the
          command that produced the count; the pinned regression table in
          test/domain/normalise-counterparty.test.ts is updated in the
          same commit as the recipe change; and if the count was nonzero,
          npm test exits 0 with a migration test whose input patterns are
          produced by the PRE-change recipe, either pinned as literals
          captured before the recipe is edited or produced by running the
          previous implementation, NEVER by calling the new normaliser,
          and which asserts for each such pattern that its re-normalised
          value equals the NEW normalisation of the raw descriptor it was
          created from (finding PR3-005: the contract's idempotency,
          new(new(x)) equals new(x), makes a self-seeded test green for
          any recipe whatever; the property the migration needs is
          new(old(x)) equals new(raw), and a strip anchored to a token the
          old recipe already removed fails exactly there). RE-MEASURED AT
          THE HEAD (finding PR3-007): the MerchantRule count is measured
          again at the release head with the same pinned read-only command
          and recorded; if it is nonzero while the verification-first
          measurement was zero, the phase does not close until the
          re-normalisation branch ships.
      - id: "6.7"
        criterion: >-
          npm run test:e2e exits 0 with a phone-viewport spec at 390x844
          asserting the month view and the merchant review render their
          group labels masked and
          document.documentElement.scrollWidth does not exceed 390 on both
          screens. The spec sets its own viewport in the spec file
          (Playwright test.use, the shape test/e2e/import.spec.ts:160 and
          test/e2e/navigation.spec.ts:178 already use), so no change to
          playwright.config.ts is needed and this phase's files-to-touch
          stays accurate (finding PR3-008); if M3-P4's mobile project has
          already merged, the spec may run under it instead, which is
          additive either way and is why conflicts-with stays empty.
      - id: "6.8"
        criterion: >-
          Privacy gate, both halves executed with a probe list read from
          the real PDFs in the container uploads at review time and never
          committed: (a) git grep across the whole worktree for the real
          statement's identifiers, holder name, card number in every
          grouping shape and each merchant string from it exits 1; (b)
          over the files this phase adds or modifies (git diff
          --name-only against the phase base), the same grep extended to
          all five promised categories (identifiers, names, merchant
          strings, thousands-form amounts and dates) exits 1. If the real
          PDFs are absent from the executing container, this criterion is
          recorded as not-witnessed in the work history and the review
          escalates for a container that has them, never marked green by
          vacuity.
      - id: "6.9"
        criterion: >-
          npm run typecheck && npm run lint && npm test exit 0 and npm run
          test:e2e exits 0 at the phase head, including the unmodified
          golden journey.
      - id: "6.10"
        criterion: >-
          grep for the merchant-source expression over src/, where the
          grep is grep -rlE 'counterpartyName \?\? [A-Za-z_]*\.?description',
          a literal operand-bearing pattern rather than a prose
          description of one (findings PR4-003 and PR5-001), matches
          exactly THREE files: the module exporting the shared
          helper, src/modules/import/domain/dedup.ts and
          src/modules/ledger/domain/corrections.ts, the last two being the
          frozen sibling recipes that keep their own copies by design; and
          grep for the shared helper's module path over those two files
          exits 1, so neither frozen recipe imports it. This is what
          witnesses decision D-11's operative content, the single
          definition, which no other criterion can see (finding PR3-004);
          the pin allows three files rather than one precisely because
          criterion 6.5 exists to keep the two frozen copies where they
          are. THE OPERAND IS PART OF THE PATTERN, not decoration
          (finding PR5-001, executed by the scout against the tree at this
          plan's head rather than asserted): the bare literal
          "counterpartyName ??" matches SIX files today, because
          src/modules/import/adapters/import-repository.ts:271 and :360
          write a persistence mapper's null-coalescing and
          src/modules/import/ui/profile-confirmation.tsx:100 renders a
          preview cell, neither of them the merchant-source rule and
          neither in this phase's files-to-touch, so a bare-literal pin
          would match FIVE after unification and could be reached only by
          editing unrelated code. The operand-bearing pattern above
          matches exactly FOUR files today (interpret-window.ts,
          merchant-review.ts, dedup.ts, corrections.ts) and exactly THREE
          after the unification, which is the number this criterion
          demands. POSITIVE HALF (finding PR4-003): grep -l for the shared
          helper's import specifier over src/ matches BOTH former call
          sites, src/modules/ledger/application/interpret-window.ts and
          src/modules/merchants/domain/merchant-review.ts, so the
          unification is witnessed by what those call sites import and not
          only by what no longer appears; a review reading of the diff
          confirms no fourth definition was written in an equivalent form
          the literal pattern cannot see, such as a ternary on undefined.
    hazard-classes:
      - id: H6.1
        statement: >-
          The dedup normaliser or the ledger's refund key edited along
          with the merchants one, rewriting stored dedup keys and turning
          the next re-upload into a full duplicate import; or the reverse
          error, a unification sweep that pulls the two frozen copies into
          the shared helper.
        addressed-by: criterion 6.10
      - id: H6.2
        statement: >-
          Over-stripping: a strip broad enough to collapse the card noise
          also collapses genuinely distinct merchants that share a chain
          prefix and a city into one group, which is silent and looks like
          success.
        addressed-by: criterion 6.2
      - id: H6.3
        statement: >-
          The recipe change silently detaching stored MerchantRule
          patterns (finding CR-402), so the owner's namings quietly stop
          applying while totals stay right.
        addressed-by: criterion 6.6
      - id: H6.4
        statement: >-
          Masking applied to the grouping key or to the value the review
          form submits, producing an EXACT rule that can never match.
        addressed-by: criterion 6.4
      - id: H6.7
        statement: >-
          Masking applied to rawLine or rawContent, editing an immutable
          fact and destroying the M3-P2 re-parse contract, which is the
          plausible reading of "mask where the descriptor is rendered" for
          an implementer who thinks of the label as starting at the fact
          (finding PR3-006).
        addressed-by: criterion 6.5
      - id: H6.5
        statement: >-
          Real statement content reaching the repository through the card
          fixtures written to reproduce the descriptor grammar.
        addressed-by: criterion 6.8
      - id: H6.6
        statement: >-
          The full card number surviving into a rendered label or a
          screenshot after the phase, the exposure the owner reported.
        addressed-by: criterion 6.3
    migrations: >-
      No schema migration. One CONDITIONAL data migration, selected by the
      verification-first step's measured count: a one-off re-normalisation
      of stored MerchantRule EXACT and PREFIX patterns under the CR-402
      contract, shipped in this change if the deployed database holds any
      rule rows, and recorded as not needed with the measured count if it
      holds none.
    conflicts-with: []
    parallelizable: false
    citations:
      - src/modules/ledger/application/interpret-window.ts:87
      - src/modules/merchants/domain/merchant-review.ts:50
      - src/modules/merchants/domain/normalise-counterparty.ts:16
      - src/modules/merchants/application/assign-merchant.ts:54
      - src/modules/import/domain/dedup.ts:18
      - prisma/schema/import.prisma:106
      - charter.yaml:41
    fill-in:
      filled: true
      root-cause: >-
        Measured through the shipped pipeline: card rows carry no
        counterparty account, so the merchant key is the whole descriptor,
        which embeds the transaction's own date, amount, card number and
        holder; the shipped normaliser matches none of that grammar, so 15
        card rows yield 15 keys and every key carries the card number the
        labels render.
      fix-shape: >-
        One merchant-source helper used by both call sites, card-grammar
        strip patterns in the merchants normaliser with the pinned table
        updated and the CR-402 branch discharged, a display-only masking
        helper on both label surfaces, and synthetic fixtures witnessing
        collapse, non-collapse, masking and the unmasked rule subject.
      files:
        - src/modules/merchants/domain/normalise-counterparty.ts
        - src/modules/ledger/application/interpret-window.ts
        - src/platform/ui/
        - test/fixtures/
  - id: M3-P7
    branch: claude/m3-p7-mobile-month-view
    intent: >-
      Mobile first becomes the project's constraint and the month view is
      rebuilt against it: the pulse-frontend skill's desktop-first paragraph
      is replaced under DR-0022, the five design tokens the approved mockup
      needs are added to styles/tokens.css first, the app shell and the month
      view are rebuilt one-column at phone width with the reconciliation
      answer above the fold, and the phone-width regression guard stops
      measuring horizontal overflow and starts measuring whether the screen
      can be used.
    grounding: >-
      DR-0022 (owner, 2026-08-22): mobile first supersedes the desktop-first
      charter line, decided against an approved mockup after the owner graded
      the shipped month view 2/10 for quality and 1/10 for usability on a
      phone. The layout reference is delivery/design/mobile-v02/, whose
      README states as fixes 1 through 6 the six things it fixes and whose
      Main.dc.html:21 fixes the frame at 390 by 844 through its min-height.
      CORRECTED in the M0-P3 fix round (finding MPR-004): the first draft
      cited canvas.json:3 for that figure, and canvas.json:3 and :4 actually
      declare artboard heights of 1180 and 1010, so the approved design
      itself expects the page to scroll below the fold and 844 is the device
      height rather than the fold. Criterion 7.8 is measured against 700 for
      that reason. The frames are
      Main.dc.html (books close), BooksDoNotClose.dc.html (a reconciliation
      gap). The fourth frame, DenseAlternate.dc.html, CARRIES NO REQUIREMENT
      and is cited by no line anywhere in this plan, which is deliberate and
      is stated here because the second M0-P3 review could not spot-check it
      the way it checked the other three: the mockup's README table records
      it as a denser row variant considered and NOT chosen, nothing in either
      rebuild phase is built from it, and the parked list carries the reason
      it was rejected. This phase is grounded at b560e85, not at
      this plan's baseline-commit e4ea3ba: M3-P1, M3-P2 and M3-P6 have merged
      since, and every file and line reference in this phase and in its
      report-code-disagreement entries was read at b560e85. Measured there:
      grep -c "@media" src/app/globals.css returns 0; src/app/globals.css:564
      reads "Desktop first"; .claude/skills/pulse-frontend/SKILL.md:113 is the
      paragraph DR-0022 supersedes, and SKILL.md:109 carries the rail
      sentence that has to move with it. DR-0023 is raised to the owner by
      this phase and does not gate it: it asks whether the spend card keeps
      one row per tag or gains merchant rows nested under tag subheads, and
      every criterion below holds whichever way it resolves.
    severity: critical
    verified-root-cause: >-
      Not a defect round on one commit; the whole screen was built to a
      charter line that has now been superseded. The measured cause is that
      src/app/globals.css carries no media query at any width (grep -c
      "@media" returns 0 at b560e85) and no layout rule below the top-level
      grid gives way: src/app/globals.css:756 and :781 both declare
      grid-template-columns minmax(0, 1fr) auto auto with two of those tracks
      pinned to min-width var(--space-14), 80px each (styles/tokens.css:121),
      so a spend row spends 160px of a 390px screen on two figures and gives
      the merchant name what is left. The dispatch's own statement of the
      cause named a rule that M3-P6 had already replaced, which is why step 1
      is verification-first: see the first report-code-disagreement entry for
      this phase. Whether a phone even lays the document out at its own width
      is UNCONFIRMED and is the first thing step 1 must settle.
    steps:
      - kind: verification-first
        text: >-
          Before editing any component, settle the four unverified claims
          this plan records against this phase and write each answer into the
          phase work history with the command and its captured output beside
          it. (a) Read the viewport meta tag off a rendered page of the
          running app and record the exact string. The M0-P3 fix round
          settled what it is (width=device-width, initial-scale=1, injected
          by the framework, observed on the deployment on 2026-08-22), so
          this is a re-observation on the phase's own base and not an open
          question; the contingency this step used to carry is gone. (b) Re-read
          the .month-grid rule and record its current text, since the
          dispatch's description of it is stale. (c) Record grep -c "@media"
          src/app/globals.css and grep -n "Desktop first" over both
          src/app/globals.css and .claude/skills/pulse-frontend/SKILL.md.
          (d) Confirm that the spend projection still collapses a tag to one
          group (src/modules/overview/domain/month-projection.ts:67) so that
          DR-0023 is asked about the real code and not about a memory of it.
          (e) ADDED in the M0-P3 fix round (finding MPR-010): re-open each
          cited line of every mockup frame and confirm it carries the content
          the citing entry describes. (f) ADDED in the second fix round
          (finding MPR-020): confirm the branch contains the mockup's
          descriptor replacement (commit 512b461) and the current privacy
          gate before any work starts, and record the origin/main sha the
          phase is dispatched against. The frames are a committed reference
          in a PUBLIC repository (DR-0024), so working from a branch that is
          behind on that correction republishes it. The M0-P3 review found a uniform
          19-line offset in two of the four frames, caused by the scout
          quoting a re-indexed reading rather than the file line; the numbers
          in this plan are corrected, and this step is what stops the next
          reader inheriting the same class of error.
      - text: >-
          Replace the desktop-first paragraph. .claude/skills/pulse-frontend/
          SKILL.md:113 currently reads "Desktop first. The import is a desk
          activity. Mobile is not a v1 target and no effort is spent on it."
          It is replaced by a mobile-first constraint written in that
          document's own voice and at that document's own length: every
          screen is designed at 390 by 844 first and widened from there,
          phone-width evidence is a standing acceptance criterion on every UI
          phase, and the evidence measures reachable targets, readable
          density and content that survives the narrowing, never the absence
          of horizontal scrolling. Cite DR-0022 by id in the text, AND cite
          delivery/intake/pulse-v0.2-pdf-addendum.md by name, because M3-P4
          criterion 4.2 requires the stance line to cite the v0.2 addendum
          and M3-P4 criterion 4.4 forbids that phase from editing this file
          (finding MPR-011). The rail
          sentence two paragraphs above it (SKILL.md:109) moves with it: the
          rail at wide widths is a widening of the one-column layout, not the
          layout the phone degrades from, which is what
          delivery/design/mobile-v02/README fix 6 records. The stale comment
          at src/app/globals.css:564 goes in the same change. Touch nothing
          under delivery/intake/: the intake documents are the record of what
          was asked for and are not edited to match what was later decided.
      - text: >-
          Add the five missing tokens to styles/tokens.css BEFORE any rule
          uses them, in the file's existing two-layer shape and with the same
          kind of one-line justification the file already carries: a 44px
          minimum tap target, a 56px list-row minimum height, a phone
          headline figure size at 40px, a -0.02em tracking for that figure,
          and a single breakpoint value. The breakpoint token is
          documentation only, because a custom property cannot appear in a
          media-query condition, so the same literal is repeated in the one
          @media condition the stylesheet is allowed to grow, with a comment
          saying that. Use the nearest existing token for the four values the
          mockup implies that are within 2px of one (14px, 18px, 3px and
          1.45); see this phase's token entry in report-code-disagreement.
      - text: >-
          Rebuild the app shell at phone width: the header's brand,
          navigation, household identity and sign-out laid out per
          delivery/design/mobile-v02/Main.dc.html:23 through :33, with the
          three nav links as an equal-width row whose every target clears
          44px. Keep every testid the shell already publishes (main-nav,
          nav-overview, nav-import, nav-merchants, household-context) and
          keep the deliberate identity ellipsis M3-P1 criterion 1.5 decided
          (src/app/globals.css:160 through :166); the identity is the one
          element in the product allowed to give way.
      - text: >-
          Rebuild the month view one column at phone width, in DOM order
          income, spend, reserves, with the reconciliation answer above the
          fold. The three-column rows (src/app/globals.css:756 and :781)
          become the mockup's two-line row: name and amount on line one,
          row count and month-over-month delta on line two, amounts
          right-aligned in the mono face through the existing .pulse-amount
          class (styles/tokens.css:163 through :168). Every figure the
          desktop screen shows still renders: the in-progress badge, the
          days-elapsed meta, the row count, the comparison head, every group
          total, every delta, the unresolved pill, the pot figure, the
          reconciliation parts and every cause block with its gap rows. The
          reconciliation panel moves ahead of the three cards in DOM order.
          At the one breakpoint and above, the rail returns as a widening:
          spend wide with income and reserves beside it, produced by grid
          placement and never by the CSS order property, so the DOM order
          assistive technology reads is the order the phone shows.
      - text: >-
          Rebuild the phone-width regression guard so that a screen which is
          unusable but does not scroll sideways FAILS. Add a Playwright
          project to playwright.config.ts that spreads a chromium mobile
          device descriptor with isMobile true at a 390 by 844 viewport,
          alongside the existing chromium and chromium-prod projects, and run
          test/e2e/month-view.spec.ts and test/e2e/navigation.spec.ts under
          both the desktop project and the new phone project. isMobile is the
          point and not a detail: without it Playwright sizes the layout
          viewport directly and the meta viewport tag is never exercised,
          which is why the existing phone specs could not see its absence.
          Add the four measurements the old bar did not make: rendered tap
          target height, the same testid set at both widths, no clipped
          element anywhere in main, and the reconciliation verdict inside the
          first 844 pixels. Keep every existing assertion; the overflow check
          stays, it is simply no longer the only one.
      - text: >-
          Raise DR-0023 to the owner with this plan's recommendation stated
          first: keep the shipped one-row-per-tag spend grouping for the
          rebuild, because nesting merchants under tag subheads is a change
          to the projection and to the delta keying rather than to the
          layout, because it makes the spend card roughly three times taller
          on the screen the owner just graded 1/10 for usability, and because
          with zero merchant rules in the owner's data it renders nothing
          today either way. Build nothing on the answer in this phase.
    files-to-touch:
      - .claude/skills/pulse-frontend/SKILL.md
      - styles/tokens.css
      - src/app/globals.css
      - src/app/(app)/layout.tsx
      - src/platform/ui/nav-link.tsx
      - src/modules/overview/ui/month-view.tsx
      - playwright.config.ts
      - test/e2e/month-view.spec.ts
      - test/e2e/navigation.spec.ts
      - test/fixtures/ (one new hand-written dense month fixture, criterion 7.13)
    extras: []
    acceptance:
      - id: "7.1"
        criterion: >-
          The phase work history records an answer to each of the four
          verification-first questions with the command and its captured
          output beside it: the exact content string of the rendered
          viewport meta tag or a record that no such tag exists, the current
          text of the .month-grid rule, the output of grep -c "@media"
          src/app/globals.css, and the output of grep -n
          "options.useTags" src/modules/overview/domain/month-projection.ts.
          FALSIFIED BY any of the four missing, or by a recorded answer that
          carries a conclusion with no captured output beside it.
      - id: "7.2"
        criterion: >-
          A spec running in the new phone project asserts that
          document.querySelector('meta[name="viewport"]').getAttribute("content")
          is a string containing "width=device-width". FALSIFIED BY the
          selector returning null, or by a content string without that
          substring.
      - id: "7.3"
        criterion: >-
          REWRITTEN in the M0-P3 fix round (finding MPR-005): the count was
          scoped to one stylesheet, so a second stylesheet was a free escape.
          grep -rc "@media" over every file matching *.css under src/ and
          styles/ sums to at least 1; the distinct media conditions across
          those same files, taken as grep -rhoE "@media[^{]+" followed by
          whitespace normalisation, sort -u and wc -l, number exactly 1; git
          ls-files "*.css" returns exactly the four files present at this
          phase's base, being src/app/globals.css, styles/tokens.css,
          styles/pulse-theme.css and styles/pulse-tokens.css, so no
          stylesheet is added to escape the count; and styles/tokens.css
          declares a breakpoint token whose value is the same literal that
          one condition uses. FALSIFIED BY a second distinct media condition
          in any stylesheet, by a fifth stylesheet, or by a breakpoint
          literal with no token declaring the same value.
      - id: "7.4"
        criterion: >-
          REWRITTEN in the M0-P3 fix round (finding MPR-005): the literal
          gate was scoped to one stylesheet and one file extension, and
          Tailwind is live in this tree (src/app/globals.css:1 imports it and
          styles/pulse-theme.css:11 through :52 re-exports the tokens into an
          @theme block), so a responsive utility prefix or an arbitrary value
          in a className carried a breakpoint or a literal straight past it.
          styles/tokens.css declares all five tokens named in this phase's
          token entry in report-code-disagreement (a 44px tap-target minimum,
          a 56px row minimum height, a 40px phone headline figure size, a
          -0.02em tracking, and the breakpoint), and all four checks below
          hold. (a) grep -rEn "oklch\(|#[0-9a-fA-F]{3,8}|[0-9]+px" over src/
          restricted to *.tsx exits 1. (b) grep -nE "^[^/*]*[0-9]+(px|rem)"
          run over every file matching *.css under src/ and styles/ EXCEPT
          styles/tokens.css returns exactly four lines: the three inside the
          .visually-hidden clip pattern (src/app/globals.css:550, :551 and
          :553 at b560e85, measured as the only three that command returns
          across those files today) and the single @media condition. (c) npm
          run gate:tokens exits 0. (d) grep -rEn over *.tsx under src/ for a
          Tailwind breakpoint prefix and for a Tailwind arbitrary value exits
          1. THE PATTERN IS PINNED, in the second M0-P3 fix round (finding
          MPR-023), because the naive form exits 0 at this phase's base for a
          reason unrelated to the work: a comment at
          src/modules/import/ui/profile-confirmation.tsx:47 cites an intake
          document by filename and section, and the two characters before
          that colon happen to be one of the tokens. The breakpoint half
          matches only INSIDE a className attribute value, that is the form
          className="..." or className={"..."} containing a
          whitespace-delimited word beginning sm:, md:, lg:, xl: or 2xl:; the
          arbitrary-value half matches an opening square bracket immediately
          following a utility prefix such as min-h-, max-h-, h-, w-, p-, gap-
          or text-. FALSIFIED BY any one of the four checks failing.
      - id: "7.5"
        criterion: >-
          At 390 by 844 and again at 360 by 740, on the month view seeded
          with the reconciling dataset and on the empty state, every element
          matching "a, button, input:not([type=hidden]), select,
          [role=button]" inside the shell header and inside main that has a
          non-zero bounding box has a bounding box height of at least 44 CSS
          pixels, and the spec names every offender by testid or text in its
          failure message. FALSIFIED BY one offender. The elements that fail
          this today and must therefore change are .app-nav-link
          (src/app/globals.css:129, 4px of vertical padding on 12px text),
          .app-signout (src/app/globals.css:168, no padding), .month-nav
          (src/app/globals.css:602 through :609, width and height
          var(--space-9), 24px), .month-unresolved-pill
          (src/app/globals.css:651) and .empty-state-cta
          (src/app/globals.css:210). ADDED in the M0-P3 fix round (finding
          MPR-012): the unresolved pill is inside this selector and the
          mockup's own 32px treatment of it (Main.dc.html:51) does NOT apply,
          for the reason recorded in this phase's report-code-disagreement
          entry on the mockup's own tap targets.
      - id: "7.6"
        criterion: >-
          REWRITTEN in the M0-P3 fix round (finding MPR-002), which found two
          escape hatches: .visually-hidden is width 1px by height 1px
          (src/app/globals.css:548 through :556), so its bounding rect is
          already non-zero and the exemption protected nothing while
          licensing the class as a way to hide a figure; and deleting a
          data-testid removed an element from both collections at once.
          A spec loads the SAME seeded month twice, at 1280 by 720 and at 390
          by 844, and at each width collects every element carrying a
          data-testid as the pair of its testid and its trimmed textContent,
          sorted; the two collections are deeply equal. At 390 by 844 EVERY
          collected element, with no exemption for any class, has a bounding
          rect whose width and height are both greater than zero. THE FLOOR LIST WAS
          EXTENDED in the second M0-P3 fix round (finding MPR-019): the first
          version named ten testids, omitted the four reconciliation cause
          blocks that hazard H7.2 names in its own text, and ended in a
          self-referential clause that a both-widths deletion satisfied
          vacuously. The collected testid set at 390 by 844 contains at least
          month-title, in-progress-badge, month-meta, compare-head,
          compare-na or the comparison figures the dataset implies,
          pot-change, recon-panel, recon-verdict, spend-delta, group-label,
          income-total, spend-total, reserves-net; recon-difference,
          unresolved-pill, month-no-rows and no-reserves wherever the dataset
          renders them; the four cause blocks recon-cause-uninterpreted,
          recon-cause-unmatched, recon-cause-in-transit and
          recon-cause-unresolved wherever the dataset renders them; and every
          group-total and group-delta. The general rule that list stands in
          for, which is what makes the self-referential clause unnecessary:
          grep for data-testid over src/modules/overview/ui/ at this phase's
          BASE and at its head returns the same set of testid values, so no
          testid that existed before the rebuild is absent after it. No
          element inside main has computed display none or visibility hidden,
          or carries the visually-hidden class, at 390 by 844 unless it also
          does at 1280 by 720. FALSIFIED BY a testid present at one width and
          absent at the other, by any differing text, by any zero-sized
          element, by a missing testid from that list, by a testid present at
          the base and absent at the head, or by any element hidden at one
          width and not the other.
      - id: "7.7"
        criterion: >-
          REWRITTEN in the M0-P3 fix round (finding MPR-003), which found the
          sweep both unsatisfiable and blind. Unsatisfiable: .visually-hidden
          combines width 1px with white-space nowrap and overflow hidden
          (src/app/globals.css:548 through :556), so every such element is an
          offender before anything is built, and this phase's icon-only month
          navigation adds more of them inside main. Blind: it measured width
          only, so a name clamped downward with -webkit-line-clamp is clipped
          with textContent intact and passes both this and 7.6. Three checks,
          at 390 by 844 and at 360 by 740, in each of en, nl and fr, on the
          reconciling and the non-reconciling seeded datasets and on the
          dense dataset criterion 7.13 adds. (a) Over every element inside
          main that does NOT match .visually-hidden, element.scrollWidth
          greater than element.clientWidth plus 1 returns an empty offender
          list. (b) Over every element inside main whose computed overflow-y
          is one of hidden, clip, scroll or auto, element.scrollHeight
          greater than element.clientHeight plus 1 returns an empty offender
          list; the vertical half is scoped to those elements deliberately,
          because an element with visible overflow reports content height it
          is not clipping. (c) grep -nE "line-clamp|-webkit-box-orient|
          text-overflow" over every file matching *.css under src/ and
          styles/ returns exactly one line, the text-overflow inside
          .app-household that M3-P1 criterion 1.5 decided
          (src/app/globals.css:164 at b560e85). (d) ADDED in the second
          M0-P3 fix round (finding MPR-022), which found the one truncation
          route left after (a), (b) and (c): a label shortened in the
          COMPONENT, by a slice or an ellipsis helper, clips nothing and
          changes textContent identically at both widths, so it passes 7.6
          and both axes here. On the dense dataset criterion 7.13 adds, whose
          labels are known because the fixture defines them, every collected
          group-label's trimmed textContent is character for character the
          label that fixture defines for that group, and contains no
          horizontal-ellipsis character and no trailing run of full stops.
          The failure message names each offender by its testid or its first
          40 characters of text and says which axis overflowed. FALSIFIED BY
          a non-empty list on either axis, by any line-clamp or box-orient
          declaration, by a second text-overflow, or by a rendered label
          shorter than the fixture's, which is truncation performed before
          render and is what decision D-17 forbids.
      - id: "7.8"
        criterion: >-
          REWRITTEN in the M0-P3 fix round (finding MPR-004): 844 is the
          device height of the mockup frame, not the fold. Playwright renders
          no browser chrome, so a verdict placed at a top of about 790 landed
          inside 844 and below the fold on the owner's actual phone, and the
          criterion would have certified the thing it exists to prevent. The
          month view publishes testid recon-verdict on the element carrying
          the books-close or books-do-not-close answer, so this criterion's
          subject is named rather than chosen after the fact. At 390 by 844
          with window.scrollY at 0, in each of en, nl and fr, on the
          reconciling seeded dataset AND on the non-reconciling one, the
          element with testid recon-verdict has a bounding rect with top at
          least 0 and bottom at most 700, that being 844 less a deliberately
          generous allowance of about 144 CSS pixels for the URL bar and
          bottom bar a real phone browser spends and Playwright does not
          render; and on the non-reconciling dataset the element with testid
          recon-difference also has bottom at most 700. FALSIFIED BY a bottom
          greater than 700 in any of the six cases.
      - id: "7.9"
        criterion: >-
          The month view publishes testids income-card, spend-card and
          reserves-card on its three sections. At 390 by 844 the three have
          equal x within 1px and equal width within 1px, and
          income-card.y is less than spend-card.y which is less than
          reserves-card.y. Their DOM order, read by a single
          document.querySelectorAll over the three testids, is income-card,
          spend-card, reserves-card. grep -nE "^[[:space:]]*order:"
          src/app/globals.css returns nothing, so the visual order is never
          faked. At 1280 by 720 the same three satisfy spend-card.x less than
          income-card.x, and income-card.x equal to reserves-card.x within
          1px, so the rail survives as a widening. FALSIFIED BY any one of
          these five.
      - id: "7.10"
        criterion: >-
          npm run typecheck && npm run lint && npm test exit 0; npm run
          test:e2e exits 0 with every pre-existing spec passing under the
          desktop project, test/e2e/golden-journey.spec.ts and the desktop
          describe at test/e2e/navigation.spec.ts:192 among them; grep -n
          "maskCardNumbers" src/modules/overview/ui/month-view.tsx returns at
          least two call sites, so the M3-P6 display-only masking survives
          the rebuild (decision D-12); grep -n "maskCardNumbers"
          src/platform/ui/mask-card-number.ts shows the function body
          unchanged from this phase's base, so the guard covers the function
          and not only its call sites; and git diff --name-only against this
          phase's base over src/modules/overview/application/,
          src/modules/overview/domain/, src/modules/merchants/,
          src/modules/import/, src/modules/ledger/, src/platform/ui/amount.tsx,
          src/platform/ui/mask-card-number.ts, src/platform/auth/, prisma/
          and messages/ prints NOTHING. The three src/platform/ entries were
          ADDED in the M0-P3 fix round (finding MPR-013): amount.tsx is the
          one mandatory way this product renders money, and a phone-friendly
          format that dropped the cents would change textContent at BOTH
          widths and leave criterion 7.6 green. src/platform/ui/nav-link.tsx
          is deliberately NOT pinned, because step 4 edits it and it is
          declared in files-to-touch. FALSIFIED BY any printed path or a
          changed masking function: this is a layout phase and a printed path
          means it stopped being one.
      - id: "7.11"
        criterion: >-
          grep -n "desk activity" .claude/skills/pulse-frontend/SKILL.md
          exits 1; grep -n "Desktop first" .claude/skills/pulse-frontend/SKILL.md
          exits 1; grep -n "Desktop first" src/app/globals.css exits 1; and
          the skill file contains the literal string DR-0022, the literal
          string 390, the literal string pulse-v0.2-pdf-addendum.md, and a
          sentence placing the wide-width rail as a widening of the
          one-column layout. The addendum string was ADDED in the M0-P3 fix
          round (finding MPR-011): M3-P4 criterion 4.2 requires the stance
          line to cite the v0.2 addendum and M3-P4 criterion 4.4 forbids that
          phase from editing this file, so if M3-P7 does not write the
          citation, M3-P4 can never close. FALSIFIED BY any of the three
          greps exiting 0 or by any of the four strings being absent.
      - id: "7.12"
        criterion: >-
          playwright.config.ts declares a third project alongside chromium
          and chromium-prod whose use spreads a chromium mobile device
          descriptor with isMobile true and a 390 by 844 viewport, and npx
          playwright test --list prints test/e2e/month-view.spec.ts and
          test/e2e/navigation.spec.ts under BOTH the desktop project and that
          phone project. FALSIFIED BY either spec appearing under one project
          only, or by isMobile not being true in the phone project's use.
      - id: "7.13"
        criterion: >-
          REWRITTEN in the M0-P3 fix round (finding MPR-008). The old text
          banned every new fixture, which banned the only fixture that would
          make the rest of this phase mean anything: the committed month-view
          datasets are three to five rows with short invented labels
          (test/e2e/month-view.spec.ts:8 through :40), while the owner's real
          month is 39 transactions with ZERO merchant rules, so every spend
          row folds under a raw bank descriptor
          (src/modules/overview/domain/month-projection.ts:67 through :84)
          and the screen that was graded 1 out of 10 is the dense one. The
          pin exists to stop REAL data being committed, not to stop fixtures
          existing. This phase adds exactly ONE new fixture, hand-written and
          wholly invented, carrying no value taken from any statement, that
          produces at least 20 spend groups in one month with labels of at
          least 28 characters in the shape of a bank descriptor and no
          identifier shape that is not already on
          test/fixtures/allowed-identifiers.txt; criteria 7.5, 7.7, 7.8, 7.9
          and 7.14 are each evaluated against that dataset as well as against
          the existing reconciling and non-reconciling ones; git diff
          --name-only against this phase's base over test/fixtures/ prints
          only that one file; no screenshot, trace or attachment is committed
          anywhere under test/; and npm run gate:privacy exits 0. FALSIFIED
          BY the fixture being absent, by fewer than 20 spend groups, by any
          of those five criteria being evaluated only on the small datasets,
          by a second printed path, by any committed binary under test/, or
          by the privacy gate failing.
      - id: "7.14"
        criterion: >-
          ADDED in the M0-P3 fix round to close finding MPR-001, which was
          critical and correct: the two-line row is the one structural change
          this phase exists to make, step 5 is the only place it appeared,
          and nothing measured it. An implementer could pad five controls,
          move the panel up, stack the cards, add the tokens, the one media
          condition and the phone project, never touch .month-columns or
          .month-row, and turn every other criterion green with the row
          unchanged. THIS CRITERION MEASURES THE ROW, at 390 by 844 and again
          at 360 by 740, on the reconciling and non-reconciling datasets and
          on the dense dataset criterion 7.13 adds. (a) TRACK COUNT. For
          every .month-row and every .month-columns element inside main,
          getComputedStyle(element).gridTemplateColumns is either the string
          "none" or resolves to at most TWO tracks. (b) NAME WIDTH. REWRITTEN in the second
          M0-P3 fix round (finding MPR-017): the first version took the ratio
          against the row's CONTENT box, which is a box the same change sets,
          so raising the row's own horizontal padding shrank the denominator
          and turned the criterion green while making the name area smaller.
          For every element with data-testid spend-group, income-group or
          reserve-group, the horizontal distance from the left edge of its
          group-label to the left edge of its group-total is at least 55
          percent of the row element's BORDER-box width, AND is at least 180
          CSS pixels at 390 by 844 and at least 160 CSS pixels at 360 by 740.
          The border box is the denominator because the row's own padding
          sits inside the content box and outside the border box, so padding
          the row out no longer moves the number the ratio is taken against;
          and the absolute floors are there because a percentage of anything
          is satisfiable by making everything narrower one level up, which is
          what raising .app-main's padding would do to the border box itself.
          The ratio and the floors catch different cheats and neither is
          redundant. (c) TWO LINES. For every spend-group, income-group or
          reserve-group row: the group-total's bounding top is within 6 CSS
          pixels of the group-label's bounding top, so name and amount share
          line one; the row-count element's bounding top is at least the
          group-label's bounding bottom minus 2, and where a group-delta
          renders, its bounding top is likewise at least the group-label's
          bounding bottom minus 2, so count and delta are on line two. The
          spec names every offending row by its group-label text. WHAT WOULD FAIL IT: any row that keeps a
          three-track grid; any row that leaves the name less than 55 percent
          of the row's border box, or less than 180 pixels at 390 or 160 at
          360, to sit in, however that narrowing is achieved; and any row
          that keeps the row count or the month-over-month delta beside the
          name instead of beneath it. MEASURED AT THE PHASE BASE AND STATED
          HERE SO THIS CRITERION CANNOT BE SATISFIED BY THE TREE AS IT
          STANDS. The box chain at 390: .app-main takes 48 of horizontal
          padding (src/app/globals.css:181 through :187, var(--space-12)
          var(--space-9)), .month-card a hairline each side
          (src/app/globals.css:727 through :731), and .month-row 32 of its
          own padding (src/app/globals.css:786), so the row's BORDER box is
          about 340 and its content box about 308. (a) fails because
          .month-columns (src/app/globals.css:756) and .month-row
          (src/app/globals.css:781) are both grid-template-columns minmax(0,
          1fr) auto auto, three tracks. (b) fails because two gaps of
          var(--space-7) take 32 and the two amount tracks take
          var(--space-14) each, 160 in total (src/app/globals.css:767 and
          :772, styles/tokens.css:121), leaving the name about 116 pixels and
          the label-to-total distance about 132: that is about 39 percent of
          the 340 border box and below the 180 floor, and at 360 the same
          chain gives a 310 border box, a name of about 86 and a distance of
          about 102, about 33 percent and below the 160 floor. (c) fails
          because .month-row is align-items baseline with the delta in its
          third track, so the delta's top equals the label's top. THE INCOME
          AND RESERVES ROWS ARE INSIDE (b) AND (c) DELIBERATELY, added in the
          second fix round (finding MPR-018): they already satisfy (a) at the
          base, because .month-row-rail is two tracks
          (src/app/globals.css:790 through :792) and .month-rail-amount
          stacks the total over the delta (src/app/globals.css:822 through
          :827), so a criterion bound to spend rows alone would have proved
          nothing for two of the three cards while the mockup asks for the
          two-line row on the whole screen (README fix 1, and the income list
          item at delivery/design/mobile-v02/Main.dc.html:85 through :93).
          They fail (c) at the base for the same reason spend rows do: the
          row count sits inside the same wrapping flex as the label
          (.month-row-main, src/app/globals.css:794 through :801) and renders
          beside it. FALSIFIED BY one offending row in any of (a), (b) or
          (c).
      - id: "7.15"
        criterion: >-
          ADDED in the M0-P3 fix round to close finding MPR-009: this phase
          and M3-P8 were the only two in the plan with no privacy criterion,
          and they are the two whose criteria 7.1 and 8.1 require captured
          rendered output of screens whose stated first use is the owner's
          real month. Privacy gate, three halves, in the shape of criterion
          2.6. (a) npm run gate:privacy exits 0, and the gate is the version at
          origin/main HEAD when the phase is dispatched, recorded by sha in
          the phase work history. NAMING WHICH GATE was added in the second
          M0-P3 fix round (finding MPR-020): the script changed twice during
          this round, so a criterion that says the gate exits 0 has to say
          which gate or it names a moving target. (b) A whole-worktree grep for the real
          statements' identifier, holder-name, merchant-string,
          thousands-form amount and date probes, built in the container at
          review time from the real uploads and never committed, exits 1; and
          the same grep over the files this phase adds or modifies exits 1.
          (c) Every rendered measurement quoted in the phase work history,
          criterion 7.1's viewport string included, is captured from a
          committed synthetic fixture and not from the deployed household,
          and the command that produced it is written beside it. If the real
          statements are absent from the executing container, half (b) is
          recorded as NOT WITNESSED rather than green. FALSIFIED BY the gate
          exiting non-zero, by any probe hit, or by a captured measurement
          whose stated source is the deployed database.
    hazard-classes:
      - id: H7.1
        statement: >-
          The phase repeats the orchestrator's recorded error and sets a bar
          the currently broken screen already passes, by measuring horizontal
          overflow instead of whether the screen can be used.
        addressed-by: criterion 7.5
      - id: H7.2
        statement: >-
          The mockup is treated as the specification and the phone view is
          tidy because it says less: a badge, a meta line, a comparison head
          or a reconciliation cause quietly stops rendering below the
          breakpoint.
        addressed-by: criterion 7.6
      - id: H7.3
        statement: >-
          A literal colour, size, spacing or breakpoint value lands in a
          component or a rule, because the mockup is written entirely in
          literals and copying it is the path of least resistance. This is
          the highest-pressure moment in the project for that violation.
        addressed-by: criterion 7.4
      - id: H7.4
        statement: >-
          The layout rebuild quietly becomes a projection change, most likely
          by starting on DR-0023 before the owner has answered it.
        addressed-by: criterion 7.10
      - id: H7.5
        statement: >-
          The phone is fixed and the desk screen is broken in the same
          change, trading one unusable surface for another.
        addressed-by: criterion 7.9
      - id: H7.6
        statement: >-
          Content from the owner's real statements reaches a new fixture, a
          committed screenshot, a trace, or the phase work history, which is
          the surface criterion 7.13's git diff cannot see and which is where
          the fleet has already lost a round.
        addressed-by: criterion 7.15
      - id: H7.7
        statement: >-
          RESTATED in the M0-P3 fix round (finding MPR-015). The tag is
          confirmed present, so this is no longer an open question but a
          regression: a later change adds a viewport export that drops
          width=device-width, and the product silently starts rendering at
          the 980px fallback on a real phone while every Playwright
          measurement stays green.
        addressed-by: criterion 7.2
      - id: H7.8
        statement: >-
          The rebuilt rows drop the M3-P6 card-number masking, putting a card
          number back on screen, because the masking lives in the rendering
          the rebuild replaces.
        addressed-by: criterion 7.10
      - id: H7.9
        statement: >-
          ADDED in the M0-P3 fix round (finding MPR-001). The phase satisfies
          every other criterion and never touches the row: the three-track
          grid survives, the name keeps about a third of the width, and the
          screen is the one the owner graded 1 out of 10 with a tidier
          header. H7.1 does not reach this, because a three-track row neither
          overflows nor clips at 390 and its label wraps rather than
          truncating.
        addressed-by: criterion 7.14
    migrations: >-
      None. This phase touches no Prisma schema, no query and no projection;
      criterion 7.10 pins prisma/ and both overview module layers to an empty
      diff.
    conflicts-with:
      - >-
        M3-P4 and M3-P8 (same stylesheet and, for M3-P4, the same skill
        paragraph): run this phase first and those after it, never beside it.
    parallelizable: false
    citations:
      - DR-0022
      - delivery/design/mobile-v02/README.md:34
      - .claude/skills/pulse-frontend/SKILL.md:113
      - src/app/globals.css:564
      - src/app/globals.css:756
      - src/modules/overview/ui/month-view.tsx:509
      - test/e2e/month-view.spec.ts:300
      - playwright.config.ts:53
    fill-in:
      filled: true
      root-cause: >-
        The month view was built to a charter line that DR-0022 has now
        superseded, so it has no phone layout to degrade from: grep -c
        "@media" src/app/globals.css returns 0 at b560e85 and the row grids
        at src/app/globals.css:756 and :781 pin two tracks to 80px each
        (styles/tokens.css:121) inside a 390px screen. Whether a phone even
        lays the document out at its own width is unconfirmed and is step 1.
      fix-shape: >-
        Replace the constraint in the skill, add the five missing tokens to
        styles/tokens.css first, rebuild the shell and the month view one
        column at 390 by 844 with the reconciliation answer above the fold,
        and replace the overflow-only phone bar with four measurements that a
        broken screen fails.
      files:
        - .claude/skills/pulse-frontend/SKILL.md
        - styles/tokens.css
        - src/app/globals.css
        - src/modules/overview/ui/month-view.tsx
        - playwright.config.ts
        - test/e2e/
  - id: M3-P8
    branch: claude/m3-p8-mobile-merchant-review
    intent: >-
      The merchant review screen rebuilt at phone width on the primitives
      M3-P7 defines: the group row becomes two lines, the naming form's input
      and button clear 44px, and the direction totals that let a naming be
      SEEN to change no total stay exactly where they are.
    grounding: >-
      DR-0022 and the same approved mockup as M3-P7, frame
      delivery/design/mobile-v02/MerchantReview.dc.html at 390 by 844
      (MerchantReview.dc.html:21; the artboard itself is 1010 tall,
      canvas.json:5, so the design expects this screen to scroll too). Read
      at b560e85: the screen is
      src/modules/merchants/ui/merchant-review.tsx, its rules are
      src/app/globals.css:419 through :546, and its row already wraps at
      phone width because M3-P6 made it wrap to clear an overflow
      measurement (src/app/globals.css:479 through :496), which is precisely
      why the shipped bar passed a screen whose controls are 30px tall. This
      phase consumes M3-P7's tokens and adds none of its own. Three elements
      the mockup draws have no data or no capability behind them and are
      resolved in favour of the shipped screen; see this phase's entries in
      report-code-disagreement.
    severity: high
    verified-root-cause: >-
      Same superseded constraint as M3-P7. Measured here: the naming input
      (src/app/globals.css:522 through :529) and its submit button
      (src/app/globals.css:536 through :544) both carry 6px of vertical
      padding on 13px text and render around 30px tall, against the mockup's
      44px minimum (MerchantReview.dc.html:57 and :58), and the row wrapping
      added by M3-P6 means nothing scrolls sideways while that is true.
    steps:
      - kind: verification-first
        text: >-
          Before editing, record with captured output: the rendered heights
          of the naming input and its submit button at 390 by 844 on the
          shipped screen, the current ReviewGroup shape (grep -n over
          src/modules/merchants/domain/merchant-review.ts:36 through :47),
          and the result of grep -rn "rename" src/modules/merchants/ and over
          messages/. The three mockup elements this plan says have no data
          behind them are asserted from a reading at b560e85 and must be
          confirmed at the phase base before the phase declines to build
          them, because declining to build something the code can already do
          is the same failure as building something it cannot.
      - text: >-
          Rebuild the group row two-line at phone width per
          MerchantReview.dc.html:48 through :61: label and total on line one,
          row count on line two, the total right-aligned in the mono face
          through the existing .pulse-amount class. Names wrap and never
          ellipsize (decision D-17). Keep every testid the screen publishes:
          unresolved-group, merchant-group, group-label, group-total,
          unresolved-count, income-total, spend-total.
      - text: >-
          Give the naming form phone-sized controls using M3-P7's tap-target
          token: the input and the submit button both clear 44px, the input
          stays wide enough to type a merchant name into, and the hidden
          counterpartyText field keeps submitting the UNMASKED value, which
          decision D-12 requires because that value becomes the exact
          MerchantRule subject (src/modules/merchants/application/
          assign-merchant.ts:54).
      - text: >-
          Run test/e2e/merchants.spec.ts under both the desktop project and
          the phone project M3-P7 adds, so the naming journey and the
          card-label masking assertions are both proved at both widths, and
          add the tap-target, same-testids-at-both-widths and no-clipped-
          element measurements to it. Do not delete the existing overflow
          assertions; they stay, they are simply no longer the only ones.
    files-to-touch:
      - src/modules/merchants/ui/merchant-review.tsx
      - src/app/globals.css
      - test/e2e/merchants.spec.ts
      - playwright.config.ts
    extras: []
    acceptance:
      - id: "8.1"
        criterion: >-
          The phase work history records, with captured output beside each,
          the rendered bounding-box heights of the naming input and its
          submit button at 390 by 844 on the screen as shipped at the phase
          base, the ReviewGroup field list, and the output of grep -rn
          "rename" over src/modules/merchants/ and messages/. FALSIFIED BY a
          missing record or by a stated height with no captured measurement
          beside it.
      - id: "8.2"
        criterion: >-
          At 390 by 844 and again at 360 by 740, every element matching "a,
          button, input:not([type=hidden]), select, [role=button]" inside
          main that has a non-zero bounding box has a bounding box height of
          at least 44 CSS pixels. THE SEED WAS STRENGTHENED in the M0-P3 fix
          round (finding MPR-008): "at least one resolved and one unresolved
          group in each direction" is not the state the owner has, so this
          and criterion 8.3 are evaluated on the DENSE dataset M3-P7's
          criterion 7.13 introduces, at least 20 unresolved spend groups with
          descriptor-shaped labels of at least 28 characters, as well as on
          the small mixed seed. FALSIFIED BY one offender on either seed. The
          naming input (src/app/globals.css:522) and its submit button
          (src/app/globals.css:536) are the two that fail this at the phase
          base, both about 30 pixels tall.
      - id: "8.3"
        criterion: >-
          REWRITTEN in the M0-P3 fix round (finding MPR-002), for the reason
          recorded on criterion 7.6: the visually-hidden exemption protected
          nothing, because that class is 1px by 1px and already non-zero, and
          deleting a testid removed an element from both collections at once.
          /merchants is loaded twice with the same seed, at 1280 by 720 and
          at 390 by 844, on the dense dataset and on the small mixed seed;
          the sorted collection of every data-testid paired with its trimmed
          textContent is deeply equal at the two widths; at 390 by 844 EVERY
          collected element, with no exemption for any class, has a bounding
          rect with width and height both greater than zero; the collected
          testid set at 390 by 844 contains at least unresolved-group,
          merchant-group, group-label, group-total, unresolved-count,
          income-total and spend-total; and no element inside main has
          computed display none or visibility hidden, or carries the
          visually-hidden class, at 390 by 844 unless it also does at 1280 by
          720. FALSIFIED BY a testid present at one width only, by any
          differing text, by any zero-sized element, by a missing testid from
          that list, or by any element hidden at one width and not the
          other.
      - id: "8.4"
        criterion: >-
          REWRITTEN in the M0-P3 fix round (finding MPR-003). As first
          written this criterion could not pass at the phase base:
          src/modules/merchants/ui/merchant-review.tsx:52 renders a
          .visually-hidden label inside main, and that class combines width
          1px with white-space nowrap and overflow hidden
          (src/app/globals.css:548 through :556), so its scrollWidth exceeds
          its clientWidth before M3-P8 changes anything. It was also blind to
          vertical clamping, which is the truncation mode a phone rebuild
          actually reaches for and the one decision D-17 forbids. Three
          checks, at 390 by 844 and at 360 by 740, in en, nl and fr, on the
          dense dataset and on the small mixed seed. (a) Over every element
          inside main that does NOT match .visually-hidden,
          element.scrollWidth greater than element.clientWidth plus 1 returns
          an empty offender list. (b) Over every element inside main whose
          computed overflow-y is one of hidden, clip, scroll or auto,
          element.scrollHeight greater than element.clientHeight plus 1
          returns an empty offender list. (c) grep -nE "line-clamp|
          -webkit-box-orient|text-overflow" over every file matching *.css
          under src/ and styles/ returns exactly one line, the text-overflow
          inside .app-household that M3-P1 criterion 1.5 decided
          (src/app/globals.css:164 at b560e85). (d) ADDED in the second
          M0-P3 fix round (finding MPR-022), for the reason recorded on
          criterion 7.7: on the dense dataset, every collected group-label's
          trimmed textContent is character for character the label the
          fixture defines for that group, and contains no
          horizontal-ellipsis character and no trailing run of full stops, so
          a label shortened in the component rather than by CSS is caught.
          The failure message names each offender and the axis that
          overflowed. FALSIFIED BY a non-empty list on either axis, by any
          line-clamp or box-orient declaration, by a second text-overflow, or
          by a rendered label shorter than the fixture's.
      - id: "8.5"
        criterion: >-
          The testids income-total and spend-total are present and non-zero
          in size at 390 by 844, and the existing journey at
          test/e2e/merchants.spec.ts:18, which asserts that naming an
          unresolved counterparty regroups it and changes no total, passes
          UNCHANGED in its assertions under BOTH the desktop project and the
          phone project. FALSIFIED BY either testid missing at phone width,
          by that spec failing at either width, or by any weakening of its
          assertions.
      - id: "8.6"
        criterion: >-
          At 390 by 844 the naming input and its submit button both have a
          bounding rect whose right edge is at most 390, and the input's
          bounding width is at least 200 CSS pixels. FALSIFIED BY a control
          extending past 390 or by an input narrower than 200.
      - id: "8.7"
        criterion: >-
          npm run typecheck && npm run lint && npm test exit 0; npm run
          test:e2e exits 0 under every project; and git diff --name-only
          against this phase's base over
          src/modules/merchants/application/,
          src/modules/merchants/domain/, src/modules/overview/,
          src/modules/ledger/, src/platform/, prisma/, messages/ and
          styles/tokens.css prints NOTHING. src/platform/ was ADDED in the
          M0-P3 fix round (finding MPR-013): amount.tsx is the one mandatory
          way this product renders money and mask-card-number.ts is the M3-P6
          masking, and a change to either is invisible to every other
          criterion here. FALSIFIED BY any printed path: this phase consumes
          M3-P7's tokens and changes no projection, no sort order, no copy,
          no money rendering and no schema.
      - id: "8.8"
        criterion: >-
          The card-label describe at test/e2e/merchants.spec.ts:126 through
          :262 passes unchanged, and grep -n "maskCardNumbers"
          src/modules/merchants/ui/merchant-review.tsx returns the rendering
          call site while grep -n "maskCardNumbers" against the hidden
          counterpartyText input returns nothing, so masking stays
          display-only per decision D-12. FALSIFIED BY that describe failing
          or by the masked value reaching the submitted field.
      - id: "8.9"
        criterion: >-
          EXTENDED in the M0-P3 fix round (finding MPR-006), which found the
          hole: this phase edits src/app/globals.css, which is neither under
          src/modules/merchants/ nor pinned by criterion 8.7, and M3-P7's own
          literal count is evaluated at M3-P7's head and never re-run, so
          writing min-height 44px straight into .merchant-row passed every
          check and spent the invariant M3-P7 bought. Three checks. (a) grep
          -rEn "oklch\(|#[0-9a-fA-F]{3,8}|[0-9]+px" over
          src/modules/merchants/ exits 1. (b) grep -nE "^[^/*]*[0-9]+(px|rem)"
          over every file matching *.css under src/ and styles/ except
          styles/tokens.css returns the SAME line count, with the same
          declarations, that it returned at this phase's base, so this phase
          adds no literal to the stylesheet M3-P7 cleaned. (c) grep -n for
          the tap-target token name and the row-height token name in
          src/app/globals.css each return at least one use, so the rules this
          phase adds reference M3-P7's tokens rather than re-deriving their
          values. FALSIFIED BY any hit, by a changed line count or changed
          declaration, or by either token going unreferenced.
      - id: "8.10"
        criterion: >-
          ADDED in the M0-P3 fix round to close finding MPR-009, the same
          omission criterion 7.15 closes: this phase requires captured
          rendered measurements of a screen whose stated first use is the
          owner's real month, and carried no privacy criterion while M3-P2
          and M3-P3 carry 2.6 and 3.5. Privacy gate, three halves. (a) npm
          run gate:privacy exits 0, and the gate is the version at
          origin/main HEAD when the phase is dispatched, recorded by sha in
          the phase work history, for the reason given on criterion 7.15
          (finding MPR-020: the script changed twice during the M0-P3 round). (b) A
          whole-worktree grep for the real statements' identifier,
          holder-name, merchant-string, thousands-form amount and date
          probes, built in the container at review time and never committed,
          exits 1, and the same grep over the files this phase adds or
          modifies exits 1. (c) Every rendered measurement quoted in the
          phase work history, criterion 8.1's control heights included, is
          captured from a committed synthetic fixture and not from the
          deployed household, with the command that produced it written
          beside it. If the real statements are absent from the executing
          container, half (b) is recorded as NOT WITNESSED rather than green.
          FALSIFIED BY the gate exiting non-zero, by any probe hit, or by a
          captured measurement whose stated source is the deployed database.
    hazard-classes:
      - id: H8.1
        statement: >-
          The mockup is treated as the specification and the screen grows a
          capability it does not have (a rename link, a month span, a named
          count) or loses one it does.
        addressed-by: criterion 8.3
      - id: H8.2
        statement: >-
          The direction totals are dropped to match the mockup, and with them
          the M1-P4 guarantee that naming a counterparty can be SEEN to move
          no money between income and spend.
        addressed-by: criterion 8.5
      - id: H8.3
        statement: >-
          The overflow-only bar is repeated: the row already wraps at phone
          width, so a screen with 30px controls passes every measurement the
          suite makes today.
        addressed-by: criterion 8.2
      - id: H8.4
        statement: >-
          A presentation problem is solved in the domain, most likely by
          reordering groups so unresolved ones come first, which edits the
          sort at src/modules/merchants/domain/merchant-review.ts:57 to serve
          a layout.
        addressed-by: criterion 8.7
      - id: H8.5
        statement: >-
          The rebuilt row drops the M3-P6 display-only masking, or masks the
          hidden counterpartyText value, which would make the stored
          MerchantRule subject match nothing ever again.
        addressed-by: criterion 8.8
      - id: H8.6
        statement: >-
          The phase starts before M3-P7 has landed its tokens and invents its
          own tap-target and row-height values, or writes them as literals
          into src/app/globals.css, which criterion 8.7 does not pin because
          this phase edits that file, so the two screens drift and the token
          invariant M3-P7 bought is silently spent.
        addressed-by: criterion 8.9
      - id: H8.7
        statement: >-
          ADDED in the M0-P3 fix round (finding MPR-009). A rendered
          measurement of the merchant review is captured against the owner's
          deployed household and pasted into the phase work history, carrying
          real counterparty text with it. That is the failure the fleet has
          already lost a round to, and no git diff over fixtures can see it.
        addressed-by: criterion 8.10
    migrations: >-
      None. This phase touches no Prisma schema and no query; criterion 8.7
      pins prisma/ and both merchants module layers to an empty diff.
    conflicts-with:
      - >-
        M3-P7 (same stylesheet, and this phase consumes its tokens): runs
        strictly after it, never beside it.
    parallelizable: false
    citations:
      - DR-0022
      - delivery/design/mobile-v02/MerchantReview.dc.html:48
      - src/modules/merchants/ui/merchant-review.tsx:14
      - src/app/globals.css:522
      - src/modules/merchants/domain/merchant-review.ts:36
      - test/e2e/merchants.spec.ts:18
    fill-in:
      filled: true
      root-cause: >-
        The naming controls render around 30px tall at phone width
        (src/app/globals.css:522 and :536, 6px of vertical padding on 13px
        text) and the row wraps rather than overflowing
        (src/app/globals.css:487), so the screen is hard to use and passes
        every phone measurement the suite makes today.
      fix-shape: >-
        Two-line group rows and 44px controls on M3-P7's tokens, the
        direction sections and totals untouched, the phone measurements added
        to test/e2e/merchants.spec.ts and that file run under both projects.
      files:
        - src/modules/merchants/ui/merchant-review.tsx
        - src/app/globals.css
        - test/e2e/merchants.spec.ts
decisions:
  - id: D-1
    statement: >-
      The v0.2 work is milestone M3 (phases M3-P1 through M3-P5). M2 stays
      reserved for intake slices 5 and 6 exactly as the v1 plan left them,
      gated on DR-0002; the owner directed v0.2 ahead of the DR-0002 month
      (fleet backlog, 2026-08-21), and reusing M2 for different content
      would silently rewrite a merged plan's meaning.
  - id: D-2
    statement: >-
      SourceProfileSpec becomes a discriminated union on a kind field:
      "delimited" carrying the existing shape unchanged
      (source-profile.ts:49) and "pdf-layout" carrying templateId and
      templateVersion. The StatementParser port shape (ports.ts:18) and the
      spec-equality recognition path (upload-statement.ts:101) stand
      unchanged; layout templates are code-owned and selected by
      fingerprint, never by asking the user
      (pulse-v0.2-pdf-addendum.md:23). Chosen over a parallel PDF-only port
      because the upload pipeline, profile recognition, account binding and
      re-parse machinery all key on the one spec type, and a second port
      would duplicate every one of those seams.
  - id: D-3
    statement: >-
      Committed fixtures are synthetic PDFs produced by a committed
      deterministic generator script (stable bytes, fixed metadata), plus
      extracted-text unit fixtures where a template test wants them. Both
      real statements carry full text layers, so the parser consumes the
      text layer and no OCR path exists in v0.2; generating real PDF bytes
      keeps the extraction adapter inside the tested path instead of only
      the template logic. No identifier, name, amount, date or merchant
      string from the real statements appears in any fixture.
  - id: D-4
    statement: >-
      The Belfius PDF dedup key follows the addendum (account plus year of
      booking date plus sequence number, pulse-v0.2-pdf-addendum.md:68),
      emitted through the existing natural-key mechanism (dedup.ts:95) with
      the year as the statement-scope component; the exact wiring is
      settled by M3-P2's verification-first step because the implemented
      key shape is statement-scoped and the addendum's cross-statement
      continuity premise rests on the owner's format knowledge plus one
      observed statement. The year-scoped key is strictly safer against
      re-exported or overlapping statements. Unifying dedup keys across the
      CSV and PDF paths for the same account is parked, not decided.
  - id: D-5
    statement: >-
      The import failure vocabulary grows exactly two machine reasons,
      "layout-unsupported" and "balance-mismatch", extending the existing
      union (ports.ts:38) and translated in the three catalogs like the
      existing reasons, so an unrecognised layout is a loud translated
      failure and never a user-facing parser question
      (pulse-v0.2-pdf-addendum.md:27).
  - id: D-6
    statement: >-
      Opening and closing balances are verified at parse time and NOT
      persisted: no schema change anywhere in v0.2. The settlement matching
      machinery derives card totals from stored rows
      (src/modules/ledger/domain/interpret.ts:67) and needs no stored
      balance; persisting balances for audit is parked until something
      reads them.
  - id: D-7
    statement: >-
      Navigation is server-rendered links in the authenticated shell
      (layout.tsx) with the active route derived from the request path: no
      client state library (the deliberately-absent list,
      charter.yaml:80), no route registry abstraction for three links.
  - id: D-8
    statement: >-
      The KBC balance identity is: previous balance plus the sum of all
      transaction rows including the DOMICILIERING settlement credit equals
      the Afrekening amount, the closing figure sitting on the Afrekening
      line (space thousands separator), because that is the identity that
      closes exactly in integer cents on the real statement; the addendum's
      looser "closing balance or settlement total" wording is pinned to
      this verified form.
  - id: D-9
    statement: >-
      LLM-assisted extraction for unknown layouts stays out of v0.2
      entirely. The addendum permits it in principle for v0.3+ behind the
      balance gate (pulse-v0.2-pdf-addendum.md:45); nothing in this plan
      builds toward it beyond the gate itself, which M3-P2 ships for its
      own reasons.
  - id: D-10
    statement: >-
      An unauthenticated share is NOT preserved through sign-in: the share
      route redirects to sign-in, stores no Import row, no bytes and no
      server-side handoff, and the user shares again after signing in,
      the share sheet being one tap away (finding PR2-005). Chosen over a
      bounded server-side handoff because the handoff stores
      unauthenticated bytes before any household context exists, which is
      hazard H5.2's exact shape, and its whole benefit is saving one tap
      on a rare cold-session path; criterion 5.4 witnesses the
      no-preservation contract.
  - id: D-11
    statement: >-
      The merchant source stays counterpartyName ?? description and does
      NOT become a per-SourceProfile or per-template concern; what changes
      is that the rule gets ONE definition, an exported helper both call
      sites use (src/modules/ledger/application/interpret-window.ts:87 and
      src/modules/merchants/domain/merchant-review.ts:50 currently define
      it twice). Considered and rejected under R-005 as a question with a
      defensible answer rather than a genuine fork: a template-declared
      merchant extractor would put an INTERPRETATION concern inside the
      code-owned facts-parsing layer, which the facts-versus-interpretation
      split forbids; the CSV path carries the same descriptor shapes and
      would gain nothing from template ownership; the merchants normaliser
      is the one place with a stability contract and a pinned regression
      table, so grouping logic belongs there; and the choice is cheap to
      revisit, since adding a template hook later is additive and touches
      no stored data. No decision record is raised. FORWARD NOTE, recorded
      from the fix-round-2 adversarial review, which judged this decision
      correct and named one cost to keep in view rather than a finding:
      every future bank grammar added to the shared normaliser is a CR-402
      recipe change that re-normalises every household's stored rules, so
      the SECOND bank grammar is the moment to revisit the
      template-extractor option rather than to add a third set of patterns
      here by reflex.
  - id: D-12
    statement: >-
      Card-number masking is DISPLAY ONLY. The masking helper is applied
      to rendered labels and to nothing else: not to rawLine, which stays
      verbatim because facts are immutable
      (prisma/schema/import.prisma:106) and the statement genuinely prints
      the unmasked number; not to the normalised grouping key; and not to
      the hidden counterpartyText value the review form submits
      (src/modules/merchants/ui/merchant-review.tsx:42), because that
      value becomes the EXACT MerchantRule subject
      (src/modules/merchants/application/assign-merchant.ts:54) and a
      masked subject would match no future transaction. Criterion 6.4
      witnesses the unmasked subject, criterion 6.3 the masked label.
  - id: D-13
    statement: >-
      Which branch of the CR-402 stability contract M3-P6 ships is
      selected by MEASUREMENT, not by assumption: the verification-first
      step counts stored MerchantRule rows in the deployed database over
      an explicitly pinned read-only connection and records the number,
      zero selecting the no-migration branch and any nonzero count
      selecting the one-off re-normalisation of stored EXACT and PREFIX
      patterns in the same change. The claim that production holds no
      rules is thereby made checkable rather than asserted, and criterion
      6.6 fails if the record or the migration is missing.
  - id: D-14
    statement: >-
      The rebuild is TWO phases, not one and not three: M3-P7 carries the
      constraint change, the tokens, the app shell and the month view, and
      M3-P8 carries the merchant review. They are separable because the
      dependency runs one way only: M3-P8 consumes the tap-target token, the
      row-height token and the two-line row primitive that M3-P7 defines and
      contributes none of its own back, which criterion 8.7 pins by requiring
      styles/tokens.css to be unchanged by M3-P8. Splitting further would be
      dishonest, and so would merging them: the shell, the tokens and the
      month view cannot be split from each other because the shell is where
      the 44px rule first lands and the month view is the only screen that
      exercises the wide-width rail, while the merchant review shares nothing
      with them except the stylesheet and the primitives. The cost of the
      split is that the two screens are briefly inconsistent between the two
      merges, which is visible only to whoever is running the fleet.
  - id: D-15
    statement: >-
      The pulse-frontend skill's desktop-first paragraph is replaced ONCE, by
      M3-P7, and M3-P4 does not replace it a second time. M3-P4 was written
      before DR-0022 and its step 1 said the same paragraph would be revised
      to "import is mobile-first, the month view remains desktop-competent",
      which DR-0022 supersedes: mobile first is now every screen, not the
      import screens alone. M3-P4's step 1 is amended to a CHECK that the
      constraint M3-P7 wrote is present and unweakened, and criterion 4.4 is
      ADDED to carry that check. CORRECTED in the M0-P3 fix round (finding
      MPR-011): the original claim that criteria 4.1, 4.2 and 4.3 were all
      still satisfiable byte unchanged was right about 4.2's "desk activity"
      grep and WRONG about the rest of 4.2, which also requires the stance
      line to CITE THE V0.2 ADDENDUM. M3-P7 step 2 as first written cited only
      DR-0022, and criterion 4.4 forbids M3-P4 from editing the file, so M3-P4
      would have arrived at a paragraph it could not fix and could not close
      against. 4.2 stays satisfiable only because M3-P7 step 2 and criterion
      7.11 now require the addendum citation as well. Criteria 4.2 and 4.3
      remain byte unchanged; criterion 4.1 was STRENGTHENED in the same round
      (finding MPR-007) off the overflow-only bar, because DR-0022 makes
      phone-width usability evidence standing on every UI phase. Nothing in
      M3-P4 is weakened; one duplicated edit is removed and one bar is
      raised.
  - id: D-16
    statement: >-
      The stylesheet is allowed exactly ONE breakpoint, and the value is
      declared as a token even though the token cannot be referenced from the
      media condition. The tree reached b560e85 with zero media queries on
      purpose: src/app/globals.css:97 records that a var() cannot appear in a
      media-query condition, so the alternative to a literal was intrinsic
      wrapping, and the M3-P6 comment at src/app/globals.css:696 chose
      wrapping for exactly that reason. Mobile first changes the calculation:
      the phone layout is now the default and the rail is the exception, and
      an exception stated as "wrap when it happens to fit" cannot express
      "spend wide with income and reserves beside it". So one @media
      (min-width) condition is permitted, its literal is mirrored by a
      breakpoint token in styles/tokens.css so a reader finds it where every
      other layout constant lives, a comment says why the literal is repeated,
      and criterion 7.3 fails the moment a second distinct condition appears.
      This is an engineering call with a defensible answer, not a product
      fork, so it is decided here and no decision record is raised.
  - id: D-17
    statement: >-
      Names WRAP and are never truncated, on both rebuilt screens, against
      the mockup. The mockup ellipsizes every long name
      (Main.dc.html:87, :131, :158, :168; MerchantReview.dc.html:50, :65,
      :80), the shipped screen wraps (src/app/globals.css:803 through :808),
      and .claude/skills/pulse-frontend/SKILL.md:122 forbids truncating
      anything the user needs. A Belgian bank descriptor and a Dutch or
      French label are exactly the strings that lose their meaning in their
      last twelve characters, and the mockup's own README says it is a layout
      reference and not a source of truth. The single deliberate exception in
      the tree stays: the household identity may ellipsize
      (src/app/globals.css:160 through :166), which M3-P1 criterion 1.5
      decided and criterion 8.4 pins to that one rule.
  - id: D-18
    statement: >-
      The mockup is a LAYOUT reference. Where it and the shipped product
      disagree about copy, about data, or about what the screen can do, the
      shipped product wins unless the difference is one of the six things
      delivery/design/mobile-v02/README lists as fixes 1 through 6 as the
      things the mockup exists to fix. By that rule the rebuild adds no message key in any
      language (criteria 7.10 and 8.7 pin messages/ to an empty diff), builds
      no rename capability, prints no month span it has no dates for, and
      keeps the merchant review's two direction sections and their totals.
      Four mockup elements are parked rather than dropped silently; each is
      listed under parked with the file and line that shows it has nothing
      behind it. ONE EXCEPTION THIS RULE DOES NOT SETTLE BY ITSELF, recorded
      in the M0-P3 fix round (finding MPR-014): fix 4, the tag subheads, IS
      one of the six, so this rule points at building it, and it is
      nonetheless a projection change rather than a layout one. That is
      exactly why it is raised as DR-0023 rather than decided here, and why
      the recommendation attached to it now says what a yes costs.
  - id: D-19
    statement: >-
      ORDERING. M3-P7 runs first, then M3-P8, and both run BEFORE M3-P4 and
      M3-P5. Before M3-P4 because M3-P4 rewrites the same skill paragraph
      (decision D-15) and because M3-P4 designs the import screens
      mobile-first and would otherwise invent its own tap-target and row
      tokens hours before M3-P7 adds the real ones. Before M3-P5 because the
      whole point of a share target is that the phone hands Pulse a statement
      and the user lands on the month view: shipping that entry point onto
      the screen the owner graded 1/10 delivers the defect faster rather than
      fixing it. INDEPENDENT of M3-P3, which is implemented, green and
      paused: M3-P3's files-to-touch are src/modules/import/domain/,
      test/fixtures/ and test/application/ and it changes no UI file, so its
      review can resume at any point without waiting for or blocking the
      rebuild. Severity is the tie-breaker where the ordering was otherwise
      free: the owner's own grade of the shipped product is the highest-
      severity open item in v0.2 and DR-0002's clock does not start until the
      owner can actually use Pulse from a phone. ADDED in the M0-P3 fix round
      (finding MPR-007): the standing criterion DR-0022 decided applies to
      M3-P4 and M3-P5 too, not only to the two rebuild phases, which is why
      criterion 4.1 was raised off the overflow-only bar and criterion 5.5 was
      added. Ordering and the standing criterion are the same argument: it is
      no better to build the next three mobile screens to the discredited bar
      than to ship the share target onto the screen the owner graded 1 out
      of 10.
  - id: D-20
    statement: >-
      Both rebuild phases are LAYOUT phases and the plan makes that
      falsifiable rather than aspirational: criteria 7.10 and 8.7 require git
      diff --name-only over the domain and application directories, prisma/
      and messages/ to print nothing. If the rebuild turns out to need a
      projection change, the phase stops and says so rather than growing one,
      which is the same discipline CLAUDE.md's scope-discipline section
      applies to queues, caches and state libraries. Nothing in either phase
      needs a queue, a cron, an event bus, a cache, a state management
      library, a component library, a per-transaction override, a goal or a
      budget, and the icons the mockup draws are hand-written inline SVG
      precisely so that no icon library is added.
  - id: D-21
    statement: >-
      The phone project uses a chromium device descriptor with isMobile true
      rather than a bare viewport override, and this is load-bearing rather
      than stylistic. Playwright without isMobile sizes the layout viewport
      directly and never consults the meta viewport tag, which is exactly why
      three existing phone specs (test/e2e/navigation.spec.ts:168,
      test/e2e/merchants.spec.ts:166, test/e2e/import.spec.ts:184) pass
      whether that tag exists or not, and whether it exists is unconfirmed at
      b560e85. The specs that already set their own viewport with test.use
      keep doing so and keep working; the project adds the device profile
      that makes criterion 7.2 mean something.
open-questions:
  - DR-0002
  - DR-0023
parked:
  - item: >-
      Cross-format dedup unification (the same period imported once as CSV
      and once as PDF stores every row twice, because the hash tuple and
      the natural-key components differ across formats).
    reason: >-
      The owner's stated input is PDF from the phone going forward, CSV
      requiring PC banking; the balance gate keeps each file internally
      consistent; and unifying keys would rewrite stored keys, which the
      dedup stability contract (dedup.ts:18) marks as the costly direction.
      Revisit only if a mixed-format household actually appears; recorded
      here so the risk is visible rather than discovered.
  - item: Persisting opening and closing balances on the Import row.
    reason: >-
      Decision D-6: nothing reads them yet; the gate verifies in memory and
      the settlement machinery derives totals from rows. Add columns when a
      feature reads them, additively.
  - item: Additional bank layout templates beyond Belfius and KBC.
    reason: >-
      The addendum's own routing (pulse-v0.2-pdf-addendum.md:27): an
      unrecognised layout fails loudly and becomes a backlog item, not a
      user problem; templates land one phase each as real statements
      appear.
  - item: OCR for image-only statement PDFs.
    reason: >-
      Both real statements carry full text layers; an image-only export
      fails as layout-unsupported until one is actually observed.
  - item: LLM-assisted extraction for unknown layouts behind the balance gate.
    reason: >-
      Decision D-9: v0.3+ in principle per the addendum, out of v0.2, and
      Claude API use is bound to slice 5 onward by the charter
      (charter.yaml:41).
  - item: >-
      Queue, cron, event bus, caching layer, state management library,
      component library.
    reason: >-
      The deliberately-absent list is binding (charter.yaml:80); nothing in
      the PDF path needs any of them, and the share-target phase carries an
      explicit no-caching criterion so PWA work cannot smuggle one in.
  - item: >-
      Merchant rows nested under tag subheads in the spend card, as
      delivery/design/mobile-v02/Main.dc.html:124 through :176 draws them.
    reason: >-
      Raised to the owner as DR-0023 and not decided here. It is a projection
      change, not a layout change: src/modules/overview/domain/
      month-projection.ts:67 through :74 folds every tagged merchant row into
      one group labelled with the tag, so producing the mockup's shape means
      changing OverviewGroup and the delta keying. This plan's recommendation
      is stated in M3-P7 step 7 and in the open question below; whichever way
      it resolves, no criterion in M3-P7 or M3-P8 changes. With zero merchant
      rules in the owner's data it renders nothing today either way. ADDED in
      the M0-P3 fix round (finding MPR-014): the recommendation FORECLOSES
      this for v0.2, because criteria 7.10 and 8.7 pin the overview projection
      to an empty diff and no phase in this plan carries the change, so a yes
      answer lands as a new phase after M3-P8. The owner is answering whether
      tag subheads ship in v0.2 or later, not whether they ship.
  - item: >-
      A month span per group on the merchant review ("3 rows and June and
      July", delivery/design/mobile-v02/MerchantReview.dc.html:53).
    reason: >-
      No data behind it: ReviewGroup carries no date
      (src/modules/merchants/domain/merchant-review.ts:36 through :47) and
      neither does CountedRow (merchant-review.ts:22 through :29). Adding it
      means widening the review projection and its query, which is not a
      layout change. Revisit if the owner asks for it after using the
      rebuilt screen.
  - item: >-
      A "Recently named" list with a Rename affordance on the merchant review
      (delivery/design/mobile-v02/MerchantReview.dc.html:93 through :110).
    reason: >-
      No capability behind it: src/modules/merchants/ui/actions.ts and
      src/modules/merchants/application/assign-merchant.ts expose assignment
      only, and messages/ carries no rename key. Renaming a merchant that
      already has stored MerchantRule rows is a rule-rewrite question with a
      stability contract attached, not a button; it is its own phase if the
      owner wants it.
  - item: >-
      A named count beside the unresolved count on the merchant review ("3
      left and 17 named", MerchantReview.dc.html:43 through :45).
    reason: >-
      Cheap to derive but it is new copy in three languages for a number the
      owner did not ask for, and the screen already shows the count that
      matters (src/modules/merchants/ui/merchant-review.tsx:114 through :117,
      testid unresolved-count). Decision D-18: the mockup is not a source of
      copy.
  - item: >-
      Ordering the merchant review's groups unresolved first, as
      delivery/design/mobile-v02/MerchantReview.dc.html shows them.
    reason: >-
      The order is a DOMAIN sort by magnitude
      (src/modules/merchants/domain/merchant-review.ts:57 through :67) and
      M3-P8 is a layout phase that changes no domain file (criterion 8.7).
      The unresolved rows already carry their own flag treatment at every
      width (src/app/globals.css:498 through :502), so the work queue is
      visible without a resort.
  - item: >-
      A 48px compact-row height token, implied by the mockup's "recently
      named" rows (MerchantReview.dc.html:96 and :103).
    reason: >-
      Only that parked section needs it. Adding a token nothing references is
      how a token scale stops being a scale; add it with the section, if the
      section is ever built.
  - item: >-
      The invented IBAN-shaped token the approved mockup carries in four
      committed files (delivery/design/mobile-v02/Main.dc.html,
      DenseAlternate.dc.html, MerchantReview.dc.html and the README's own
      note that it is deliberately not a valid IBAN).
    reason: >-
      Raised by the M0-P3 review (finding MPR-009) as an item this plan did
      not record. Recorded here as CLOSED rather than parked open: the value
      was neutralised before it was committed and it is on
      test/fixtures/allowed-identifiers.txt:49 with its provenance, and bash
      scripts/gate-privacy.sh prints "gate:privacy clean" on this branch after
      the merge of origin/main that brought the gate in. Kept in this list so
      the next reader who greps the design directory for an identifier shape
      finds the answer instead of re-opening it.
  - item: >-
      A dense single-ledger month view, the DenseAlternate frame.
    reason: >-
      delivery/design/mobile-v02/README's file table records it as considered
      and NOT chosen by the owner, and canvas.json's second annotation states its
      cost: the comparison with the previous month moves behind a tap, and
      that comparison is part of the primary view by
      .claude/skills/pulse-frontend/SKILL.md:111. Recorded so a later reader
      does not rediscover it as a new idea.
